name: Full stack build

on:
  push:
    branches:
      - main
    paths:
      - 'utk_curio/**'
      - '.github/workflows/docker-compose.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'utk_curio/**'
      - '.github/workflows/docker-compose.yml'
  workflow_dispatch:  # Run manually from Actions tab or: gh workflow run docker-compose.yml

jobs:
  test-compose:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: 🔨 Build all service images in parallel
        run: docker compose -p curio build --parallel

      - name: ▶️ Start containers
        run: docker compose -p curio up -d

      - name: 🧪 Wait for services to become healthy
        run: |
          # Same endpoints and timeouts as Dockerfile HEALTHCHECK (5002/health, 2000/health, 8080/)
          echo "Waiting for backend (5002), sandbox (2000), frontend (8080)..."
          timeout 180 bash -c '
            until curl -sf --connect-timeout 5 --max-time 10 http://localhost:5002/health >/dev/null && \
                  curl -sf --connect-timeout 5 --max-time 10 http://localhost:2000/health >/dev/null && \
                  curl -sf --connect-timeout 5 --max-time 10 http://localhost:8080/ >/dev/null; do
              echo "Still waiting..."
              sleep 3
            done
          ' || { echo "Services did not become ready within 180s."; docker compose -p curio ps -a; exit 1; }
          echo "Services are ready."

      - name: 🧪 Run backend unit tests
        run: |
          docker compose -p curio exec curio \
            python -m unittest discover -s utk_curio/backend/tests -t . -p "test_*.py" -v

      - name: 🧪 Run sandbox unit tests
        run: |
          docker compose -p curio exec curio \
            python -m unittest discover -s utk_curio/sandbox/tests -t . -p "test_*.py" -v

      - name: 🌐 Check frontend availability
        run: |
          curl --fail http://localhost:8080 || (echo "Frontend not reachable" && exit 1)

      - name: 🐍 Set up Python (for e2e)
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: 🎭 Install Playwright and run e2e tests
        run: |
          pip install -r requirements.txt
          python -m playwright install chromium
          cd utk_curio/backend
          CURIO_E2E_USE_EXISTING=1 PYTHONPATH="$GITHUB_WORKSPACE" python -m pytest tests/test_frontend/test_alive.py -v

      - name: 🧹 Tear down containers
        run: docker compose -p curio down
