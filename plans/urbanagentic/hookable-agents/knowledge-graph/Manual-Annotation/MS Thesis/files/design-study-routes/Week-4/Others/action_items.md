1. Generate the dataset to train the model
2. Use different methods to train the model (Entropy Weighted Method, training it as an agent deciding on the weights (punishment/reward), or others)
3. Use the training results as the weights for the cosos del grafo