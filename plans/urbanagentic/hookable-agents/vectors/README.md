# Early hand-drawn vector sketches (non-canonical)

These three SVGs predate the deterministic concept renderer
(`../sources/render_hookable_agents_png_concepts.py`) and are kept only as
historical exploration. They are **not implementation references**:

- They show pre-`DEC-042` chrome (no header split — see `../dev/21`): the current
  design puts the agent identity, cycling arrows, session details, and Close in the
  opened agent view's single top header (no Pin), and reserves the static
  `Agents Catalog` header (Pin only) for the Agents Roster drawer.
- They may also predate `DEC-041` (`../dev/18` — the node Explanation tab is retained)
  and the memo-12 lifecycle.

For current visuals use `../png-concepts/` / `../svg-concepts/` (regenerated
2026-07-21) and the authoritative text specs under `../docs/`.
