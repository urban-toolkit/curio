# Build Log — P5: First-Release Agents & Orchestration

Child log for Phase 5 (see `../3.1-Agents-Catalog-Build-Log.md`). Entries follow the
Build Entry Template and are append-only.

---

## BL-P5-20260719-01: Built-in agent definitions (13 prompt-agent migrations) + Global Catalog source

- Date / author: 2026-07-19 / Karla
- Status: verified
- Requirements: `REQ-PROMPT-001` (each prompt behavior → a manifest-defined agent), `REQ-CAT-001` (browsable Global Catalog with real content), `REQ-CAP-001`
- Design decisions/artifacts: `DEC-008` (prompts→hookable agents), `SRC-MEMO-PROMPT-006` (`dev/06` canonical migration map), `DEC-040` (FS-backed)
- Tasks: `TASK-P5-builtin-roster`
- Risks/questions: `RISK-PROMPT-001` (prompt asset resolution), `RISK-PROMPT-003` (the blocked evaluator must not be fabricated)
- Design-to-code decision or deviation: author the 13 releasable built-ins as a **data-driven roster** (`app/agents/builtin.py`) generated from the `dev/06` map + the existing `utk_curio/llm-prompts/*.txt`, rather than 26 hand-written committed files — one source of truth, each entry validated through `parse_agent_manifest`. The Global Catalog scope lists these built-ins; Import/Install resolve a coord from the user store **or** the built-in roster. `agent.generated-content-evaluator` is intentionally excluded (no prompt file — blocked by `OQ-007`). Prompt-byte materialization into a user store (copying from `llm-prompts/`) is a follow-up; catalog cards + import/install coords don't need it.
- Files/modules changed:
  - `utk_curio/backend/app/agents/builtin.py` (new — 13-agent roster + manifest generation + coord resolution; `PROMPT_SOURCE_DIR` → `utk_curio/llm-prompts/`)
  - `utk_curio/backend/app/agents/services.py` (`list_global_catalog`; `_resolve_definition` = store ∪ built-in roster)
  - `utk_curio/backend/app/agents/routes.py` (`GET /api/agents/catalog`)
  - `utk_curio/backend/tests/test_agents/test_builtin.py` (new), `test_routes.py` (catalog + built-in import/install)
  - `docs/AGENTS.md` (§6 Global Catalog = 13 built-ins)
- Tests added/updated: `TEST-P5-builtin` = `tests/test_agents/test_builtin.py` (roster ↔ dev/06 map, prompt files exist, 13 manifests validate); catalog route tests in `test_routes.py`
- Verification evidence: `pytest test_agents/ test_llm_default_provider.py` → 79 passed. `GET /api/agents/catalog` returns 13 built-in cards; a built-in imports/installs without a prior store copy; evaluator absent.
- Commit/PR: `COMMIT-6412bff`
- Issues/regressions discovered: initial `PROMPT_SOURCE_DIR` used `parents[2]` (backend/) — fixed to `parents[3]` (utk_curio/); caught by `test_every_prompt_file_exists`.
- Resolution: path corrected; all prompt files resolve.
- Follow-up work: materialize built-in prompt bytes into the user store on install; orchestration/composite agents (P5 remainder, v2); imported-only Publish.
- Remaining risks/questions: built-in prompt path is nominal until materialization; categories/hooks assigned per a small mapping (refine against final UX).

**Amendment (2026-07-27, memo `dev/38`):** the roster manifests were missing the `dev/05` System-file column and `inputs` — every built-in now declares `prompts.system` (`default_preamble.txt`; the syntax agent its own preamble) and grounded `inputs.reads` (from each legacy call site's actual context); install materializes both assets; and the run path restores the legacy `preamble + prompt` composition (`dev/06` parity — an edited intent replaces the instruction portion only). Tests: `TestPreambleAndInputs`, `TestMaterializePreamble`, updated run-path assertions; 245 agents tests green.

---

## BL-P5-20260804-02: Node Builder — the first P5 composite (node.build + the delegation seam) (memo dev/48)

- Date / author: 2026-08-04 / Karla
- Status: verified
- Requirements: `REQ-ORCH-001` (missing specialist → reviewed Install-in-project, no auto-chaining), `REQ-REVIEW-001`/`DEC-006` (review-before-apply structural for the two new mutations), `REQ-PERM-001`/`DEC-017` (server-allowlisted contracts), `REQ-CAP-002` (granted ids pinned), owner node-creation policy (reuse-first; justified factory-backed creation fallback)
- Design decisions/artifacts: **`DEC-046`** (delegation as direct provider-port code, depth-1 structural; LangChain revisit narrowed to Dataflow Builder), `DEC-034` (partial: node-builder slice), memo `dev/48` (+ dev/49 consolidation: orchestration deltas DR-1…DR-5 stay with the future Dataflow Builder memo); recorded dev/15 deviations (canvas-only target, package-recommendation delegate deferred, no profile families)
- Tasks: `TASK-P5-node-builder`
- Risks/questions: the §1.3 apply/canvas clobber defect (shipped dev/41 behavior) found in the memo audit — fixed here for both mutations via the bridge; child-run quota abuse bounded by the child's own policy admission + the shared round budget
- Design-to-code decision or deviation: **(1) Roster** — `BuiltinAgentSpec` gains `delegates_to`/`review_policy` (defaults keep the thirteen migrated manifest dicts byte-identical, regression-tested); `agent.node-builder` (net-new `node_build_instruction.txt` beside the migration assets — same resolution/materialization). **(2) Reuse-first `node.create`** — the packages domain owns ALL template knowledge: `packages.services.available_templates` (seeded `curio.builtin@<highest-major>` + the project package lockfile, authorability flags); the run's tail lists available templates fresh per run; mint validates via the helper (unknown/non-authorable refused) and pins `{nodeType}` only (id server-minted at apply — no digest, drift target doesn't exist); apply re-validates (package uninstalled → 409 + `stale`), inserts right of the node extent under the spec write path, returns `createdNode`. **(3) `node.template.create`** — the justified creation fallback: mandatory justification (the review card is the adequacy gate), reuse-territory collisions refused at mint; apply drives the EXISTING factory (`build_packageage_archive` + `install_packageage_from_archive` + `install_to_project`) minting a namespaced `curio.agent.<slug>@1` single-template package, then inserts the first node — transactional, factory errors verbatim as 409 + `stale`. **(4) Delegation (DEC-046)** — `delegateRequest` part (exclusive, bounded, both-requests-in-one-tail invalid); `delegation.py` resolution (current-project lockfile ∩ `delegatesTo`, order = preference) + `run_delegate` (child's own prompts, NO tail instruction, reply never parsed — depth-1 structural; own pins/ledger pair under the child's policy with parent-attachment attribution; `parentExecutionId`); missing specialist → reviewed `project.install` proposal whose apply reuses `install_in_project` (idempotent re-apply); parent records gain `delegations`; SSE `delegate_requested/started/result`. **(5) Frontend bridge (§1.3 fix)** — `agentCanvasEvents` + `useAgentCanvasMutations` (mounted in `AgentDockOverlay`): apply responses carry the mutation to the LIVE canvas — `createdNode` → `useCode.createCodeNode` with the SERVER id (new template first lands in `projectPackagesStore` + `refreshPackageRegistry`, the Save-as pulse); `appliedContent` → live `data.code`/`defaultCode` update; idempotent per node id. Review card renders the new kinds (justification-first template card, per-kind effect lines); delegate transient activity lines.
- Files/modules changed: backend `app/agents/{builtin.py,tools.py,content.py,services.py,delegation.py (new)}`, `app/packages/services.py` (`available_templates`, `create_template_package`, `template_slug`); `utk_curio/llm-prompts/node_build_instruction.txt` (new); frontend `utils/agentCanvasEvents.ts` (new), `components/agents/attach/{useAgentCanvasMutations.ts (new),AgentAttachmentsProvider.tsx,AgentDockOverlay.tsx}`, `components/agents/content/{AgentReviewCard.tsx,AgentReviewCard.module.css}`, `api/agentsApi.ts`
- Tests added/updated: backend `test_builtin.py` (`TestNodeBuilderComposite` + byte-parity), `test_content.py` (`TestDelegateRequestPart`, `TestDelegationInstruction`, creation-pins), `test_delegation.py` (new, 17: resolution determinism/current-project-only/missing/unresolvable; child prompts + depth-1 structural; records + ledger pairs + attribution; child failure recovery; missing-specialist proposal + apply idempotence; shared round budget; SSE ordering), `test_routes.py` (`TestNodeCreate` 6 incl. injection resistance + id-spoof + template-gone 409; `TestNodeTemplateCreate` 5 incl. transactional factory failure + round-trip), `test_tools.py`; `test_packages/test_available_templates.py` (new, 5); frontend `useAgentCanvasMutations.test.tsx` (new, 5 incl. store→registry→insert ordering + double-event no-op), `AgentAttachmentsProvider.test.tsx` (+4: bridge dispatch per response kind, delegate lines transient), `AgentReviewCard.test.tsx` (+4 kinds)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 951 passed (8 skipped); frontend `npx jest` full → 630 passed (57 suites); injection-resistance and rule-9 share suites included in both
- Commit/PR: `COMMIT-ed671bc1` (roster), `COMMIT-8b025fd2` (node.create), `COMMIT-a7c73372` (delegation), `COMMIT-bec6afdb` (node.template.create), `COMMIT-7a0b193d` (frontend)
- Issues/regressions discovered: the §1.3 clobber defect in shipped dev/41 behavior (applied content silently lost on the next canvas save) — fixed + regression-tested via the bridge.
- Follow-up work: Dataset Finder next (after T4 provider profiles), then Dataflow Builder (the DEC-046 LangChain revisit + dev/49 DR-1…DR-5); `connection` target + `agent.package-recommendation` delegate when their prerequisites exist; node-explainer `current_input`/`current_output` (dev/44 note) unchanged.

**Amendment (2026-08-04, memo `dev/51` — post-implementation testing feedback, `COMMIT-e48603dc`):** the dev/48 apply→canvas bridge shipped with three defects, found by a NEW integration regression driving the REAL `FlowProvider`/`reactflow`/`useCode` (the dev/48 unit suite had mocked exactly those seams): (1) created nodes landed off-viewport (backend placement = right of the whole graph extent) with no pan — invisible until the load-time fitView, read as "not created"; fixed by centering the viewport on the inserted node (`setCenter`, current zoom). (2) The bridge's `useReactFlow().setNodes` writes were clobbered by the controlled-mode props re-sync — `data.code` never landed (save would re-clobber content; `node.content.write` applies never reached the editor); fixed by seeding `data.code` in `generateCodeNode` when `code` is passed (palette drops byte-identical) and a new `FlowProvider.applyNodeContent(nodeId, content)` provider-state helper (sets `defaultCode` + `code`, merge-not-replace) used by the content branch. (3) A store-sync lag could double-insert on re-fired events; fixed with an event-level processed-ids ref beside the live-graph check. Chat feedback, apply flow, and all existing `updateDefaultCode` consumers unchanged. Tests: the integration suite (3, kept as the regression net) + updated bridge unit suite (7); frontend 635 passed (58 suites).

---

## BL-P5-20260804-03: Dataset Finder — the second P5 composite (two-lane discovery + reviewed dataset install) (memo dev/50)

- Date / author: 2026-08-04 / Karla
- Status: verified
- Requirements: docs/06 (two lanes, candidate row contract, prompt-mediated confirmation, no bespoke row buttons, review-before-apply), `REQ-REVIEW-001`/`DEC-006` (the dataset install is a reviewed proposal; apply-endpoint-only), `REQ-PERM-001`/`DEC-017`, `REQ-SEC-002` (bounded + scheme-allowlisted candidate metadata, sanitizer-rendered), `REQ-ORCH-001` (missing Node Builder → reviewed install proposal)
- Design decisions/artifacts: **`DEC-047`** (user-mediated external handoff — a depth-1 child is structurally proposal-less under DEC-046, so the fetch-node proposal comes from the user's own Node Builder attachment; the delegation seam is reused for resolution + install proposals only), `DEC-034` (partial: dataset-finder slice), memo `dev/50`; recorded deviations: canvas target added (mission-first discovery), foreground-only (DEC-021 later), geography/lineage via tool results, no auto-install (conservative — every install reviewed), T4 sequencing note (no external HTTP/credentials in this slice)
- Tasks: `TASK-P5-dataset-finder`
- Risks/questions: candidate metadata is untrusted model output — bounded + scheme-allowlisted at parse (http/https only), sanitizer at render; the catalog lane is tool-grounded so install proposals can never reference hallucinated ids (mint AND apply re-validate against `list_catalog`)
- Design-to-code decision or deviation: **(1) Roster** — `agent.dataset-finder` (net-new `discovery_instruction.txt`); `compatibleTargets[].requires` gets runtime meaning at attach: a node target's canonical type must match a declared suffix (`data-loading`; tolerant of versioned/legacy spellings), empty requires = any node (all fifteen prior agents behavior-identical). **(2) `catalog.search` (read)** — thin wrapper over `DatasetCatalogService.list_catalog(dataflow_id, q, fmt, origin)` (the acting user rides the request context; the datasets service is user-object keyed): ≤40 bounded rows {id, name, format, origin, installed, description≤200}. **(3) `dataset.install` (mutate)** — mint validates the id via `get_dataset` (unknown → refusal; already-installed → honest-chat refusal, no dead proposal); apply re-validates (gone → 409 + `stale`) then the EXISTING dataset-only flow (`install_dataset` — duplicate collapse, OSM groups); result card + `installedDataset`; apply consumes no quota. **(4) `datasetCandidates` part + card** — two labeled lanes, ≤8 rows each per the docs/06 row contract; catalog rows require `datasetId` (tool-grounded); coexists with `suggestedPrompts` in one tail; frontend card renders keyboard-operable multi-select checkbox rows (no per-row buttons) and composes the lane-correct confirmation prompt into the chat input through the prefill-never-overwrites-a-draft rule. **(5) DEC-047 handoff** — external confirmation = card + Node Builder-addressed suggested prompt; missing Node Builder → the dev/48 `project.install` proposal (tested by name); the finder's tail offers `dataset.fetch.author — handled by Node Builder`.
- Files/modules changed: backend `app/agents/{builtin.py,tools.py,content.py,services.py}`; `utk_curio/llm-prompts/discovery_instruction.txt` (new); frontend `api/agentsApi.ts`, `components/agents/content/{AgentDatasetCandidatesCard.tsx (new),AgentDatasetCandidatesCard.module.css (new),AgentReviewCard.tsx}`, `components/agents/attach/AgentChatPanel.tsx`
- Tests added/updated: backend `test_builtin.py` (roster 15 + `TestDatasetFinderComposite` + requires-parity), `test_routes.py` (`TestAttachRequiresGating` 5; `TestDatasetFinderTools` 6 incl. grounding, q-filter, unknown-id refusal, apply + already-installed refusal, gone→409+stale, injection resistance), `test_content.py` (`TestDatasetCandidatesPart` 5 incl. scheme allowlist + bounds), `test_delegation.py` (`TestDec047DatasetFinderHandoff` 2), `test_tools.py`; frontend `AgentDatasetCandidatesCard.test.tsx` (5 incl. hostile-metadata inertness + lane-correct composition), `AgentReviewCard.test.tsx` (+1)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 971 passed (8 skipped); frontend `npx jest` full → 641 passed (59 suites)
- Commit/PR: `COMMIT-dc044c24` (roster + gating), `COMMIT-1f45fdcf` (tools), `COMMIT-06f4bcac` (part + card), docs commit (this entry + DEC-047 ledgers + AGENTS.md)
- Issues/regressions discovered: none.
- Follow-up work: `web.search` + credential-aware external discovery (T4 / dev/49 DR-6); batch handoffs and capability-first resolution at Dataflow Builder (DR-4, DEC-046 revisit); authorized auto-install policy if demand appears.

---

## BL-P5-20260805-04: Dataflow Builder — the third P5 composite (Plan → Revise → Solve → Run) (memo dev/52)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: dev/49 DR-1 (graph-level proposals + typed plan domain), DR-2 (persisted orchestration session), DR-3 (planning templates), DR-4 (bounded Solve + the capability-first resolution widening, dev/03:366), DR-5 (phase-aware builder panel); `REQ-REVIEW-001`/`DEC-006` (structural review for the two new mutation surfaces), `REQ-ORCH-001`, rule 9 (the builder session rides the share-stripped attachment record)
- Design decisions/artifacts: **`DEC-048`** (direct-code orchestration; `DEC-007` retired with its re-open condition recorded — `DEC-021` background execution through the `delegation.py` seam; the Solve authorization model: one authenticated action per batch), `DEC-034` (now complete across all three composites), memo `dev/52`; recorded deviations: additive-only plans, foreground-only synchronous Solve, static templates, package-recommendation delegate deferred, per-child reservations (DEC-044 derives aggregates)
- Tasks: `TASK-P5-dataflow-builder`
- Risks/questions: plan bounds are abuse backstops (200 nodes/600 edges; plan-specific 256 KiB tail budget — the classic 4 KiB cap stays byte-identical for every other tail; large designs chain across successive additive plans); a hard-crashed Solve leaves the transient `solving` phase — a 15-minute staleness override prevents wedging
- Design-to-code decision or deviation: **(1) Roster + resolution** — `agent.dataflow-builder` (net-new `orchestration_instruction.txt`, nine-delegate list); `delegation.resolve` widened to capability-first (delegatesTo preference → any current-project template, sorted-coord tie-break → visible-roster missing-specialist fallback per dev/03:366, superseding dev/48's delegatesTo-scoped proposal targeting; regression tests updated). **(2) `dataflowPlan` part + mint** — typed additive plan (plan-local refs, edge integrity, fail-open); the runtime mints the `dataflow.plan.write` proposal from a validated part on the final reply (model never requests it); mint validates every nodeType via `available_templates` (grant-time roster rides plan grants too) and pins the whole-graph shape digest (node-id + edge-id sets — content edits deliberately don't invalidate additive plans); ungranted parts pass through informationally; mint refusals surface as an error card. **(3) Apply + session** — atomic insertion (server ids, ref map, topological columns right of the existing extent, `goal` = "title — intent", pending content empty), drift → 409 + `stale`; `builderSession {phase, appliedPlanId, nodeRuns}` on the attachment record (server-owned transitions incl. dismiss; exposed on the attachment card). **(4) Solve (DEC-048)** — `POST …/solve {nodeIds?}`: the explicit user action IS the review; capability-first resolution of `node.content.generate`; `ThreadPoolExecutor(3)` over `delegation.run_delegate` children (own ledger pairs, `parentExecutionId` = the solve execution id); per-node digest guard (still-empty content only — user edits `skipped`); one batched spec write under re-read; failed subset Retry; missing specialist → ONE reviewed `project.install` proposal; endpoint quota-free; in-flight 409 guard with staleness override; result-card turn carrying the solve execution record + `delegations`. **(5) Frontend** — `graph-created` bridge (bulk `createCodeNode` + edge inserts via `onEdgesChange` + `fitViewWithMenuOffset`; idempotent per plan id), plan review card (summary-first counts + scrollable node list + dynamic effect line), `AgentBuilderStrip` (phase chips, six template seeds through the prefill rule, Solve/Retry with per-node aria-live progress, Run via the existing `playAllNodes`; rendered only for dataflow-builder attachments — every other chat pixel-identical).
- Files/modules changed: backend `app/agents/{builtin.py,delegation.py,content.py,tools.py,services.py,routes.py}`; `utk_curio/llm-prompts/orchestration_instruction.txt` (new); frontend `api/agentsApi.ts`, `utils/agentCanvasEvents.ts`, `components/agents/attach/{useAgentCanvasMutations.ts,AgentAttachmentsProvider.tsx,AgentBuilderStrip.tsx (new),AgentBuilderStrip.module.css (new),builderTemplates.ts (new),AgentChatPanel.tsx,AgentDockOverlay.tsx}`, `components/agents/content/AgentReviewCard.tsx`
- Tests added/updated: backend `test_builtin.py` (roster 16 + `TestDataflowBuilderComposite` + parity re-pin), `test_delegation.py` (`TestCapabilityFirstResolution` 4 + widened-semantics updates), `test_content.py` (`TestDataflowPlanPart` 6 incl. the sized-for-real-graphs 150-node budget regression + key-smuggling guard), `test_routes.py` (`TestDataflowPlanMint` 3, `TestDataflowPlanApply` 5 incl. additive/drift/content-edit-survival/ready/dismiss, `TestSolve` 6 incl. skip-user-edits, failure isolation + subset retry, single install proposal, quota attribution, injection resistance), `test_tools.py`; frontend `AgentBuilderStrip.test.tsx` (6), `useAgentCanvasMutations.test.tsx` (+3 graph-created), `AgentReviewCard.test.tsx` (+1), `AgentChatPanel.test.tsx` (provider mock for the strip's import chain)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 998 passed (8 skipped); frontend `npx jest` full → 651 passed (60 suites)
- Commit/PR: `COMMIT-d4afe271` (roster + resolution), `COMMIT-6156a1b5` (part + mint), `COMMIT-3e48fb58` (apply + session), `COMMIT-67a3bef3` (Solve), `COMMIT-5471a5bd` (frontend), docs commit (this entry + DEC-048 ledgers + AGENTS.md)
- Issues/regressions discovered: none new; two dev/48-era resolution tests encoded the delegatesTo narrowing and were updated to the dev/03:366 semantics.
- Follow-up work: destructive replan reconciliation (its own memo); streamed solve progress + cancellation with `DEC-021`; server-owned planning templates; `agent.package-recommendation` wiring (dev/16); the P5 composite roster is complete — remaining program tracks are T4 (provider profiles + secret store) and the dev/49 DR-6 research-node package.

**Amendment (2026-08-05, memo `dev/53` — post-implementation testing feedback on dev/52, `COMMIT-9cee1343`):** "the apply plan button is not appearing after showing the planned nodes and connections" — the streamed backend path was verified correct end-to-end; two client-side defects hid the control: (1) the pre-delta stream fallback (`useAgentAttachments.run` → provider) returned/appended TEXT ONLY, silently discarding every content part — the plan proposal (and equally node/dataset proposals, prompts, candidates) never rendered a card whenever the fallback ran; fixed with full payload parity (reply + execution + content, proposal-aware reload). (2) The builder strip named the pending review ("Apply or dismiss the plan review first") but carried no control; it now surfaces **Apply plan / Dismiss** during `plan_review`, targeting the `activeProposal` mirror (the dev/41 fast-lookup home — works even when a transcript part is missing); the transcript review card is unchanged. Tests: fallback-parity provider regression, strip review-control suite (+3), run-return shape update; frontend 655 passed (60 suites).

**Amendment (2026-08-05, memo `dev/54` — second fix round on dev/52, `COMMIT-d1ecbb36`):** the true root cause of "the apply plan button is not appearing": the mint required a byte-perfect `dataflowPlan` JSON tail with exact canonical template ids in ONE shot — any grammar slip (a 301-char intent, one malformed character) failed the whole block open, streaming the raw plan JSON as chat text with no proposal minted anywhere (no card, no strip button — both derive from the proposal), and a parseable plan with a wrong template id died post-loop in a one-shot error card. Local llama4/gemma-class providers rarely deliver that first-try. The PRIMARY path is now self-correcting (no fallback paths): the plan parser reports precise field-level errors (`nodes[2].intent is 301 chars (max 300)`, `edges[0].to references unknown ref 'ghost'`, JSON breakage included) via `plan_tail_diagnosis`; the loop feeds them back as corrective rounds sharing the existing `MAX_TOOL_ROUNDS` budget (mint refusals correct too — moved in-loop, replacing the post-loop `_maybe_mint_plan`); invalid attempts never leak — the stream holds plan-ish invalid tails and corrective prose is not folded; at the cap the failure is LOUD: the held tail releases (fail-open transparency) plus the "Plan not proposable" card carrying the errors. SSE `plan_revision` events render "revising the plan …" transient lines. Non-granted agents keep byte-identical fail-open behavior (regression-pinned). Tests: `TestPlanTailDiagnosis` (4), `TestPlanCorrectionRounds` (6: JSON-then-corrected, wrong-id-then-corrected, loud cap, shared budget, ungranted regression, stream hold + event ordering); backend 1008 passed, frontend 655 passed.

**Amendment (2026-08-05, memo `dev/55` — third fix round on dev/52, `COMMIT-9202bd85`):** the model's transcript ("I cannot directly apply a plan… click the Apply button on the dataflowPlan block") exposed a SELF-CONTRADICTING system prompt: the dev/41 grants paragraph teaches the generic `{"toolRequest": {"tool": "<id>", "params": {}}}` syntax for every granted tool — `dataflow.plan.write` included — while the plan flow only accepted the `dataflowPlan` block form. A model following the grants paragraph hit "no proposal flow exists for tool 'dataflow.plan.write'" (small plans) or the 1,024-byte params cap failing the whole block open as raw text (real plans), concluded it couldn't apply plans, and invented UI instructions. The toolRequest form is now a first-class equivalent: `_mint_plan_from_params` (payload = `params.dataflowPlan` ?? `params`) validates with the same verbose parser and routes into the same mint — errors return as the tool refusal that the existing round already feeds back (self-correction on the shared budget); per-tool params budgets give the plan form `PLAN_TAIL_MAX_BYTES` while every other tool keeps 1,024 bytes regression-pinned; the enlarged tail budget + the dev/54 stream hold recognize the form. Tests: `TestPlanToolRequestForm` (5, incl. the user-scenario correction, the 40-node large form, and the params-cap regression); backend 1013 passed.

**Amendment (2026-08-05, memo `dev/56` — fourth fix round on dev/52, `COMMIT-a59447a3`):** the remaining blind spot behind "the apply button still not appearing": dev/54/55 only recognized plans inside a terminal ` ```curio.v1 ` fence (or its toolRequest form) — real models overwhelmingly emit ` ```json `/bare fences, mid-reply, with prose after; a PERFECTLY VALID plan in that shape produced no part, no diagnosis, no correction, no card (reproduced). `content.extract_plan_attempt` now scans every fenced block of a plan-granted reply for the plan payload (`{"dataflowPlan": …}`, the toolRequest form, or the bare `goal`+`nodes` object; broken-JSON plan-marked fences return their body so the JSON error still feeds back); `_handle_plan_reply` routes a scanner hit into the SAME mint (block stripped from the persisted text — the review card is the plan's home) or the SAME corrective rounds with added fence guidance ("use a ```curio.v1 fence as the very last thing in your reply"). Non-plan agents and non-plan fences byte-identical (regression-pinned). Tests: `TestExtractPlanAttempt` (5), `TestFenceAgnosticPlanRecognition` (4 incl. the reported scenario run+stream); backend 1022 passed.

**Amendment (2026-08-05, memo `dev/57` — Solve-flow feedback, node-content hygiene):** generated node content arrived wrapped in response formatting (```python fences, language ids, JSON wrappers, "Here is the code:" prose) because model replies became content VERBATIM at every write point: Solve's child replies, the `node.create`/`node.template.create`/`node.content.write` mints, plan-carried content, and the legacy Get Code path (whose crude `replaceAll("```python"…)` missed other ids, kept prose, and corrupted legitimate backticks). One deterministic extractor now guards every model-output→node-content boundary: `content.extract_node_content` (backend) + `utils/extractNodeContent.ts` (frontend mirror, replacing the legacy replaceAll) — JSON wrappers with a single plausible string field unwrap (bounded recursion, wrapper-around-fence handled), the LARGEST fenced block's body is the content when fences exist, and unfenced text passes through byte-identical (conservative: preserving legitimate content outranks cosmetic cleanup; the `not controllable` sentinel survives). The user's LLM-verification-agent suggestion is recorded against the Validation-agent track (OQ-011) as a later semantic layer — deterministic extraction is the primary mechanism. Tests: `TestExtractNodeContent` (8), `TestGeneratedContentExtraction` (2: Solve writes clean content; wrapped node.create params preview+apply clean), frontend util suite (5); backend 1032 passed, frontend 660 passed.

**Amendment (2026-08-05, memo `dev/58` — plan-apply edge crash):** clicking Apply raised `TypeError: Cannot read properties of undefined (reading 'keywordHighlighted') at UniDirectionalEdge` (user screenshot) and the applied graph only appeared after a refresh — the dev/52 bridge inserted edges WITHOUT a `data` object while both edge components read `data.keywordHighlighted` unguarded, so the first render of a bridge-inserted edge crashed the canvas update (the saved spec was already correct; `loadTrill` always sets `data = {}`, which is why the refresh worked). Fixed in both layers: the bridge's edge items now carry `data: {}` (loadTrill parity, regression-pinned in the bridge unit test) and both edge components guard with `data?.keywordHighlighted` so a data-less edge from any path can never take down the render. Frontend 660 passed.

---

## BL-P5-20260805-05: Destructive replan reconciliation (memo dev/59, DEC-049)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: dev/49 DR-2 remainder (the session's preservation contract: unchanged nodes keep ids/positions/content; removals explicit; conflicts explained before applying), `REQ-REVIEW-001`/`DEC-006` (extended to deletions), rule 9
- Design decisions/artifacts: **`DEC-049`** (per-victim content-digest pins; removals reviewed by name; removals only on explicit user request — superseding dev/52's "removals are theirs to make" at its recorded revisit point), memo `dev/59`; recorded deferrals: modify-existing-node ops (own memo on demand), deterministic canvas-diff engine (DR-2's outer edge), removal undo (provenance track)
- Tasks: `TASK-P5-destructive-replan`
- Risks/questions: deletions are irreversible — mitigated three ways: per-victim digests (work protection), by-name review with content flags (informed consent), instruction posture (never uninvited)
- Design-to-code decision or deviation: **(1) Grammar** — `removeNodes`/`removeEdges` (bounded, deduplicated; keys emitted only when used → additive plans byte-identical, regression-pinned); edge endpoints may name EXISTING nodes (grammar checks shape + not-a-victim; existence is the mint's spec check) — lifting dev/52's self-contained-plan restriction; remove-only plans valid; all errors ride the dev/54 correction rounds and the dev/56 fence-agnostic scanner unchanged. **(2) Mint** — spec-aware validation (unknown victims/endpoints → corrective errors), the edge cascade computed for display, `removeContentSha256` pins per victim (part AND proposal mirror), the card's `removals` display block (label · type · contentChars) + cascade count, summary/preview extended. **(3) Apply** — victim digest checks after the shape digest (drift → 409 + `stale` naming the node); removals in place (listed edges + recomputed cascade, then nodes) BEFORE inserts; `attachments.prune_orphaned_attachments` (dev/32) for attachments on victims; `builderSession.nodeRuns` MERGE semantics (victims dropped, survivors kept, new pending joined — replacing dev/52's reset); edge insertion resolves endpoints via ref map ∪ existing ids; `appliedGraph` gains `removedNodeIds`/`removedEdgeIds`. **(4) Frontend** — bridge removals-before-inserts through the canvas's own machinery (`applyRemoveChanges` + edge remove changes; already-absent victims no-op); the card's destructive Removes section (by-name rows, content flags, cascade, bidirectional effect line); additive rendering pixel-identical. **(5) Instruction** — replan procedure: real ids, wire-to-existing freely, remove only on request, restate removals in prose.
- Files/modules changed: backend `app/agents/{content.py,services.py}`; `utk_curio/llm-prompts/orchestration_instruction.txt`; frontend `api/agentsApi.ts`, `utils/agentCanvasEvents.ts`, `components/agents/attach/{useAgentCanvasMutations.ts,AgentAttachmentsProvider.tsx}`, `components/agents/content/{AgentReviewCard.tsx,AgentReviewCard.module.css}`
- Tests added/updated: backend `test_content.py` (`TestDataflowPlanRevisionGrammar` 6 + two dev/52-era tests updated to the widened endpoint semantics), `test_routes.py` (`TestDestructiveReplan` 6: replace-flow end-to-end incl. preserved-node byte-identity + existing-id wiring; victim-edit → 409 by name; attachment pruning + nodeRuns merge; remove-only; unknown-target correction; additive no-pins regression), `test_builtin.py` (instruction posture); frontend `useAgentCanvasMutations.test.tsx` (+3: removals-before-inserts ordering, absent-victim no-op, additive regression), `AgentReviewCard.test.tsx` (+2: by-name section + additive regression)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1044 passed (8 skipped); frontend `npx jest` full → 665 passed (61 suites)
- Commit/PR: `COMMIT-d0bd2d14` (grammar), `COMMIT-12e5771d` (mint + apply), `COMMIT-97fa6c41` (frontend), docs commit (this entry + DEC-049 ledgers + AGENTS.md + instruction)
- Issues/regressions discovered: none new; two dev/52-era grammar tests encoded the island restriction and were updated deliberately.
- Follow-up work: modify-existing-node plan ops (if demand appears); the deterministic canvas-diff engine; removal undo via the provenance track; streamed solve progress (`DEC-021`).

**Amendment (2026-08-05, memo `dev/60` — built-in prompt propagation, `COMMIT-c2cac301`):** "clear the canvas" was refused with the superseded dev/52 posture verbatim ("removals must be performed by you manually…") because the model still RAN it: `_resolve_prompt_text` was store-first for prompt bytes (an install made before a roster prompt update runs the stale instruction forever) and the dev/44 heal checked asset-set completeness, never byte freshness — every past and future built-in prompt improvement silently failed to reach earlier installs. Fixed in both layers: built-in-trust definitions now resolve prompt BYTES roster-first (the exact rule `_resolve_definition` already applied to metadata; the store copy is a materialization cache, not an authority), and the heal rewrites drifted bytes on re-install. Owned-import shadows of a built-in coordinate keep their own bytes (regression-pinned). Effective immediately for existing installs — no migration. Tests: `TestBuiltinPromptPropagation` (3: stale-copy run composes roster bytes; reinstall heals disk; shadow untouched); backend 1047 passed.

**Amendment (2026-08-05, memo `dev/61` — remove-only plan recognition, `COMMIT-478b1179`):** with dev/60 delivering the current instruction, "clear the canvas" produced a correct remove-only plan — which the runtime then silently ignored: the dev/56 fence-agnostic scanner predates dev/59 and recognized a bare plan block only by `"goal"`+`"nodes"`, but a remove-only plan legitimately carries NO `nodes` key. The block leaked raw into the transcript, nothing minted (no Apply button, pills stuck on the prior READY state), and the model hallucinated that a proposal existed — the exact fail-open dev/54 closed for additive plans, reopened by the dev/59 grammar. Fix: every recognition predicate (`extract_plan_attempt` markers + bare-payload claim, `plan_tail_diagnosis` markers) now treats `"removeNodes"`/`"removeEdges"` as plan markers; a remove-only payload missing `goal` is still claimed so the field error feeds the dev/54 corrective rounds instead of leaking. Mint/apply/card were already remove-only-correct (dev/59) — recognition was the whole defect. Tests: +5 (scanner recognizes the user's exact payload incl. `removeEdges: []`, goal-less claim, broken-JSON diagnosis, route-level mint → `plan_review`); backend 1052 passed. Lesson recorded: a grammar extension must sweep every RECOGNITION predicate that fronts it — the dev/59 checklist covered parse/mint/apply/UI but not the dev/56 scanner sitting upstream.

**Amendment (2026-08-05, memo `dev/62` — reviewed removals vs. the manual-delete guard, `COMMIT-281c5b59`):** with recognition fixed (dev/61), APPLYING the remove-only plan updated the spec correctly but broke live: the dev/59 bridge routed node victims through `applyRemoveChanges` — the canvas's manual-delete guardrail, which refuses any node still touching an edge with a per-victim toast ("Connected boxes cannot be removed. Remove the edges first…") — while the victims' edges were still live. Reordering could not fix it (the guard reads `reactFlow.getEdges()`, blind to same-tick removals), and the guard is semantically wrong for this caller: the DEC-049 review already authorized every victim by name, and the backend delivers the full edge cascade in the same event. Fix: a dedicated `applyReviewedRemovals(nodeIds, edgeIds)` in `useWorkflowOperations` — the `cleanCanvas` idiom (onEdgesDelete bookkeeping → edge filter → onNodesDelete → onNodesChange), no guard, absent elements no-op — exposed via FlowProvider; the bridge replaces its two-step with the one call, still before inserts. The manual path keeps its guard byte-identical (regression-pinned). Tests: +4 hook tests (no-toast bulk removal, no-op absent ids, unlisted survivors, manual-guard pin) + bridge tests reworked; frontend 669 passed / 62 suites. Lesson recorded: routing agent mutations "through the canvas's own machinery" must distinguish machinery that MUTATES from machinery that also GATES — a guardrail built for unreviewed manual clicks is not parity, it is a veto the review already overruled.

---

## BL-P5-20260805-06: Streamed Solve progress with cancellation (memo dev/63, DEC-050 — the DEC-021 user slice)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: dev/52's recorded deviation ("streaming joins later with DEC-021"); the fix-primary-paths rule (streaming IS the solve path, no dual mode); DEC-021's no-replay posture for in-flight provider work
- Design decisions/artifacts: **`DEC-050`** (memo `dev/63`); scope boundary recorded: this is the USER slice of `DEC-021` — leases/heartbeats, `interrupted` executions, background execution, and the LangChain re-open condition remain open; the 15-minute stale guard remains the crash story
- Tasks: `TASK-P5-streamed-solve`
- Risks/questions: a dropped stream must never wedge the session or lose completed work — mitigated structurally: the ONE re-guarded finally-write runs on normal completion, cancellation, client disconnect (`GeneratorExit`), and worker error alike; streamed events are transport, never truth
- Design-to-code decision or deviation: **(1) Backend** — `solve_attachment_stream` (eager validation → generator: `solve_started` → `node_started`/`node_result` per target → `done` with the blocking payload + `cancelled`/`notAttempted`); `pool.map` → `submit` + a thread-safe outcome queue the generator drains (workers never touch the response; a worker exception enqueues a `failed` outcome so the drain can never deadlock); the blocking `solve_attachment` DRAINS the generator (one implementation, byte-compatible response); cancellation = one `_should_stop()` predicate, two signals (in-process event registry keyed by the persisted `solveExecutionId`; durable `cancelRequested` flag set by `POST …/solve/cancel`), checked at task entry — undispatched targets revert to `pending`, in-flight children finish and persist. **(2) Frontend** — `postSseStream` extracted as the shared dev/22 reader (chat + solve); `solveAttachmentStream` resolves on `done`; the provider overlays transient per-node statuses (`solveProgress`), applies each solved node's content through the dev/51 bridge AS it lands, and clears the overlay on the terminal refetch; the strip renders overlay-over-nodeRuns pills, a Cancel button while solving, and the cancelled/not-attempted notice. **Deviation from the memo's §3**: user Cancel posts the endpoint but does NOT abort the fetch — the stream ends naturally with `done.cancelled`, so in-flight pills still resolve and no AbortError needs special-casing; the abort is the fallback when the cancel endpoint is unreachable (and the disconnect path covers tab close).
- Files/modules changed: backend `app/agents/{services.py,routes.py}`; frontend `api/agentsApi.ts`, `components/agents/attach/{AgentAttachmentsProvider.tsx,AgentChatPanel.tsx,AgentDockOverlay.tsx,AgentBuilderStrip.tsx}`
- Tests added/updated: backend `test_routes.py` `TestStreamedSolve` (6: SSE order + per-node content + one-write persistence; pre-flight 409s stay JSON; cancel mid-batch → 3 solved / 2 reverted-pending / phase `applied` / flag cleared; disconnect persists partials and exits `solving`; cancel-without-solve 409; durable-flag persistence + idempotence) — existing `TestSolve` (6) green through the drain wrapper unchanged; frontend `agentsApi.test.ts` (+3: lifecycle dispatch + done payload, retry subset + mid-stream error, cancel endpoint), `AgentAttachmentsProvider.test.tsx` (+2: overlay + per-node bridge apply + clear-on-done with no double-apply; cancel posts), `AgentBuilderStrip.test.tsx` (+3: overlay wins per node, Cancel appears/disables, cancelled notice)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1058 passed (8 skipped); frontend `npx jest` full → 677 passed (62 suites); `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-8618fa7b` (backend), `COMMIT-bc3c0e28` (frontend), docs commit (this entry + DEC-050 ledgers + dev/52 closure pointers)
- Issues/regressions discovered: none; the blocking endpoint's behavior is regression-pinned by the pre-existing suite running through the drain wrapper.
- Follow-up work: DEC-021 proper (leases/heartbeats, `interrupted`, background execution — the LangChain re-open condition); streamed Solve for multi-worker deployments beyond the durable-flag boundary checks; provider-call abort if a client ever supports it.

---

## BL-P5-20260805-07: Full dataflow awareness + per-node runtime journal (memo dev/67-2, DEC-052 — Foundation A of the dev/67 program)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: 67-0 "Full dataflow awareness" (agents must never say "I don't have the full dataflow edges") + "Runtime messages and validation state" (persist per-node execution messages; never rely on transient UI console output)
- Design decisions/artifacts: **`DEC-052`** (memo `dev/67-2`; program index `dev/67-1`); recorded limitations: browser-executed node types (merge/vis/pool) leave no journal record (they pass data through without a `/process*Code` call), unsaved projects (no dataflowId) skip the write
- Tasks: `TASK-P5-dataflow-awareness`
- Risks/questions: the journal must never make execution slower or flakier — mitigated structurally: best-effort writes (never raise, never block the response), fail-open reads, observational-only posture (the saved spec remains the single structural truth)
- Design-to-code decision or deviation: **(1) Journal** — `app/execution/runtime_journal.py`: latest-per-node JSON under `<project dir>/runtime/` (DEC-040 FS), status by the canonical `output.path == ""` predicate (never stderr-nonempty — warnings land there), 4000/2000-char stderr/stdout tails, normalized (dedent+strip) content sha256 so transport indentation doesn't defeat the staleness signal, filename-sanitized node ids; seam write `_record_runtime_outcome` in BOTH `/processPythonCode` and `/processJavaScriptCode`. **(2) Tools** — `dataflow.read` v2: structure-first projection (all nodes as id/type/goal/hasContent/contentChars, ALL edges with handles when present, datasets, runtime status map; over-budget shrinks node GOALS, never edges; `params.include: ["content"]` restores the full dump); new `node.runtime.read` (attached-node default, honest "never-executed", `contentChangedSinceRun`). **(3) Grants** — dataset-finder + `dataflow.read` (it had NO graph view at all); debug/node-explainer/NCB/node-builder/dataflow-builder + `node.runtime.read`. **(4) Frontend** — `readFragment` producer cases for `graphContext` (full live Trill), `mission` (workflow+goal), `installedTemplates` (client palette registry), `targetContext` (node snapshot | whole graph by target kind), `nodeIntent`; `externalSelection`/`catalog` stay deliberately null-producing (prompt-carried / tool-served).
- Files/modules changed: backend `app/execution/{__init__,runtime_journal}.py` (new), `app/api/routes.py`, `app/agents/{tools.py,builtin.py}`; frontend `components/agents/attach/agentRunContext.ts`
- Tests added/updated: backend `tests/test_execution/test_runtime_journal.py` (new, 9: path predicate + tails + seq, bounded tails, filename safety, fail-open reads, status map, normalized sha, seam write ok/error/no-identity), `test_tools.py` (+7: projection regression — ALL edges survive a spec whose old dump truncated them; runtime block; handle passthrough; never-executed; traceback evidence + content-changed flip; attached default + missing-node errors; include=content parity), `test_builtin.py` (3 pinned grant lists updated deliberately); frontend `agentRunContext.test.ts` (+5: all three composites compose non-null with edges, node-target intent, mission-only, empty-canvas null regression)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1075 passed (8 skipped); frontend `npx jest` full → 702 passed; `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-3e0add6d` (journal + seam), `COMMIT-cf538d60` (tools + grants), `COMMIT-49407f43` (frontend producers), docs commit (this entry + DEC-052 ledgers)
- Issues/regressions discovered: none new; the edges-truncate-first failure mode was reproduced in a regression test before the fix (40 nodes × 2000-char contents → old dump loses the edge list, projection keeps all 39 edges).
- Follow-up work: the rest of the dev/67 program (67-3 topology next per the index); outputSchema capture when the sandbox reports column metadata; journal records for browser-executed node types if 67-7 needs them.

---

## BL-P5-20260805-08: Topology model + Merge-node resolution (memo dev/67-3, DEC-051 — Foundation B of the dev/67 program)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: 67-0 "Invalid multiple-input topology" (inspect the target's input structure before creating edges; introduce the existing Merge node; never generate an invalid graph and defer discovery to execution) + "Merge-node detection"
- Design decisions/artifacts: **`DEC-051`** (memo `dev/67-3`; program index `dev/67-1`); recorded deviation from the memo's §3 sketch: `maxIncomingEdges` derives from RENDERED capacity (port count; merge=5), NOT the sum of declared maxima — the investigation showed declared `[1,n]` on e.g. computation-analysis is aspirational (the input plumbing is scalar per handle; a second edge silently overwrote `data.input`), so declared maxima are served as metadata (`inputs` rows) but never as enforceable capacity
- Tasks: `TASK-P5-topology-model`
- Risks/questions: over-enforcement could reject working graphs — mitigated: unknown/out-of-scope templates fail open; the LOAD path warns and never drops persisted edges; merge behavior is regression-pinned byte-identical
- Design-to-code decision or deviation: **(1) Parse once** — `manifest.parse_cardinality` (fail-open on unparseable; the schema owns the grammar); `available_templates` gains `inputs: [{types,min,max}]` + `maxIncomingEdges` with `_RENDERED_INPUT_CAPACITY = {merge-flow: 5}`. **(2) Mint** — `_validate_plan_fanin`: net incoming per target (plan edges + dev/59-SURVIVING existing edges, victims excluded) vs capacity; refusals ride the dev/54 corrective rounds naming the fix ("route A, B through a curio.builtin/merge-flow node"); merge-slot handle shape (`in_0..in_4`) validated; instruction step 2 teaches the Merge resolution + `toHandle` (propagates to existing installs via dev/60). **(3) Grammar** — optional bounded edge `toHandle`, key present only when used (additive plans byte-identical). **(4) Apply** — every created edge carries `sourceHandle`/`targetHandle`; merge targets get a deterministic free `in_N` (named free slot wins; full merge → 409+stale); persisted to the spec AND passed through the bridge — the "merge dead until reload" bug (hardcoded `targetHandle: "in"` naming a nonexistent merge handle) is fixed at the source. **(5) onConnect guard** — one edge per occupied non-merge input handle (toast: "route multiple flows through a Merge node"); per-HANDLE, so spatial-join's `in_points`/`in_polygons` each accept their edge; `skipValidation` (load) → console.warn only. Test fixture `_write_builtin_package` updated to real-manifest port parity (+merge-flow template).
- Files/modules changed: backend `app/packages/{manifest.py,services.py}`, `app/agents/{content.py,services.py}`, `utk_curio/llm-prompts/orchestration_instruction.txt`; frontend `providers/FlowProvider.tsx`, `utils/agentCanvasEvents.ts`, `api/agentsApi.ts`, `components/agents/attach/useAgentCanvasMutations.ts`
- Tests added/updated: backend `test_available_templates.py` (+3: cardinality table, rendered-capacity rules incl. the aspirational-[1,n] pin, declared-metadata survival), `test_content.py` (+2: toHandle parse + byte-parity, bound), `test_routes.py` `TestPlanFanInValidation` (4: refusal→merge-replan mints; existing-target surviving-edge counting; apply assigns in_0/in_3 honored+lowest-free, persisted to spec; bad slot name feeds correction); frontend `connectionFanInGuard.test.tsx` (new, 4: refusal toast, merge byte-identical, per-handle spatial-join shape, load-path warn-never-drop), `useAgentCanvasMutations.test.tsx` (+1: handle pass-through with defaults)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1084 passed (8 skipped); frontend `npx jest` full → 707 passed; `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-d746a136` (arity model), `COMMIT-33ff906e` (mint/apply/grammar/instruction), `COMMIT-e7e1ee6d` (frontend guard + bridge), docs commit (this entry + DEC-051 ledgers)
- Issues/regressions discovered: the agent-bridge merge-handle bug ("in" names a nonexistent handle on merge targets; healed only on reload) was confirmed and is now regression-pinned; the test builtin fixture declared computation-analysis with NO input ports (unrealistic) and was aligned to the real manifest.
- Follow-up work: 67-8 surfaces per-edge review over this validation; spatial-join's runtime per-handle input plumbing (its connect-layer collision is fixed; the propagation layer still collapses to one scalar — pre-existing, tracked); declared-cardinality enforcement if the input plumbing ever becomes multi-valued.

---

## BL-P5-20260805-09: Plan cards v2 — per-node review, editable goals, per-node Apply (memo dev/67-5 — Simulation Mode: create)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: 67-0 "Plan card behavior" (no embedded code, editable goal per node, per-node Apply) + "Per-node creation" (one node at a time; no placeholder whose required content is knowingly unresolved beyond the pending-for-Solve contract; bulk creation never implied by code authorization)
- Design decisions/artifacts: memo `dev/67-5` (program index `dev/67-1`); DEC-054 is minted by 67-9 — this entry is its create stage
- Tasks: `TASK-P5-plan-cards-v2`
- Risks/questions: per-node applies mutate the spec the whole-plan digest pinned — resolved: each apply-node RE-PINS `baseGraphDigest` to the spec it produced, so the plan's OWN progress is legitimate drift while foreign edits still 409 + stale
- Design-to-code decision or deviation: **(1) Content-free plans** — the mint refuses content-carrying plan nodes with corrective guidance ("plans describe intent; node content is generated and validated per node after creation"), superseding dev/52's "trivial code" allowance at its recorded revisit point (67-0: no shortcut); the shared grammar keeps `content` (other consumers), enforcement is the builder mint's. **(2) `expects`** — optional bounded per-node input/output one-liner (grammar + card row). **(3) Mint-time layout** — positions computed ONCE at mint (`positions[ref]`) so per-node and whole-plan applies land identically; whole-plan apply consumes the same map (fallback: legacy compute). **(4) editedGoals** — `PATCH …/proposals/<id>/plan-goals {ref, goal}`: an audited review-stage overlay on the proposal (pinned plan bytes immutable), applied at creation, mirrored through `activeProposal` so reloads keep the edits. **(5) apply-node** — `POST …/proposals/<id>/apply-node {ref}`: creates exactly ONE content-free node (drift-safe — no whole-graph digest check; template availability IS re-checked, registry failure/type-vanished → stale), idempotent per ref ("already-applied"), proposal STAYS pending until every ref applies or dismissal (edges are 67-8's stage), result card without flipping the part, `builderSession` gains `phase: "simulating"` + `nodeStates {ref: planned|created}` + `nodeIds` + the created node joins `nodeRuns` as pending; whole-plan apply after partials skips applied refs and wires edges to their REAL ids. **(6) Card rows** — pending plan proposals with the per-node callback render rows (title · type · expects · editable goal saving on blur · Apply → "Created ✓") INSTEAD of the text preview; dismiss-after-partial keeps created nodes (real reviewed nodes) and kills remaining refs.
- Files/modules changed: backend `app/agents/{content.py,services.py,routes.py}`; frontend `api/agentsApi.ts`, `components/agents/attach/{AgentAttachmentsProvider,AgentChatPanel,AgentDockOverlay,AgentBuilderStrip}.tsx`, `components/agents/content/AgentReviewCard.{tsx,module.css}`
- Tests added/updated: backend `TestPerNodePlanApply` (8: one-node create + pending proposal + mirror; idempotence; edited-goal overlay; goal-edit guards; sequential ≡ whole-plan nodes; whole-plan-after-partial wires real ids; dismiss-after-partial; expects on the card) + `test_plan_carried_content_is_refused_at_mint` (rewritten from the superseded trivial-code test, deliberately); frontend card (+4: rows render/expects/goals, Created ✓ + blur-save, per-ref Apply, no-callback preview regression), provider (+3: node-created bridge dispatch, already-applied dispatches nothing, savePlanGoal), api (+2 endpoints)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1092 passed (8 skipped); frontend `npx jest` full → 716 passed (66 suites); `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-47ea5648` (backend), `COMMIT-238c79c9` (frontend), docs commit (this entry + memo flip)
- Issues/regressions discovered: the whole-plan digest pin initially refused whole-plan applies after the plan's own per-node progress — fixed by re-pinning per apply-node (recorded above); the route-test builtin fixture's seeded baseline node surfaced in assertions (tests count created nodes explicitly).
- Follow-up work: 67-6 (the per-node Solve/content stage over `nodeStates`), 67-8 (the connection stage completing the proposal), 67-9 (the sequence driver + DEC-054); auto-attach of the Node Builder to created nodes deferred to 67-6 (its modify-existing posture is the consumer).

---

## BL-P5-20260805-10: NCB apply flow + Node Builder modify-existing (memo dev/67-6 — Simulation Mode: solve)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: 67-0 "Node Builder and Node Content Builder" (full dataflow context, current goal/configuration, upstream/downstream info, reviewed Apply — Get Code ergonomics on the agent architecture) + "Full dataflow awareness" for content generation
- Design decisions/artifacts: memo `dev/67-6` (program index `dev/67-1`)
- Tasks: `TASK-P5-ncb-apply-flow`
- Risks/questions: the single-activeProposal model means a multi-node propose batch supersedes all but the last proposal — recorded as by-design: the 67-9 sequence solves ONE node at a time; the docstring states it plainly
- Design-to-code decision or deviation: **(1) One composer** — `app/agents/node_context.py` `compose_node_context`: the node's intent + bounded current content, nearest-first upstream/downstream (≤8 each, BFS; goals, content sizes, runtime status from the dev/67-2 journal — `never-executed` stated honestly, nothing fabricated), graph summary, dataset refs; `outputSchema` joins when the journal captures it (67-2 follow-up). **(2) Solve children enriched** — the worker's inputs gain `nodeContext` (composed per node from the solve's spec snapshot); legacy keys kept. **(3) Delegate enrichment** — `_enriched_delegate_inputs` at BOTH loop call sites: `node.content.generate` delegates get the composed context appended server-side (model keys never overwritten; nodeId from inputs or the node target; no node → no enrichment). **(4) Propose-mode Solve** — `solve_attachment_stream(mode="propose")` (route-validated; the blocking endpoint stays write-only and byte-compatible): each solved child MINTS a reviewed `node.content.write` proposal via the existing machinery + a transcript turn carrying the part; `node_result` carries `proposalId`; nothing is written; nodeRuns stays `pending`; `nodeStates[ref] → "solving"`; the session returns to its pre-solve phase (typically `simulating`). **(5) Content-apply resolves the ledger** — `_apply_node_content_write` on a plan node advances `nodeStates[ref] → "approved"` + `nodeRuns[nodeId] → "solved"`. **(6) Modify-existing** — `agent.node-builder` gains the `node.content.write` grant + `node` targets (lifting the dev/48 canvas-only deviation), and its instruction's step 6 states the split: existing node → read first (node.read/node.runtime.read) → grounded NCB delegation → ONE reviewed content replacement, never a duplicate node; the NCB instruction teaches nodeContext consumption + the `previousAttempt` self-correction contract (67-7's hook).
- Files/modules changed: backend `app/agents/{node_context.py (new),services.py,routes.py,builtin.py}`, `utk_curio/llm-prompts/{node_build_instruction.txt,new_content_prompt.txt}`; frontend `api/agentsApi.ts` (mode param)
- Tests added/updated: backend `test_node_context.py` (new, 5: neighborhood+summary, journal status, nearest-first cap, content bound, missing-node None + dataset projection), `TestProposeModeSolve` (3: full loop — propose → pending/simulating/solving → apply → solved/approved; classic write untouched; invalid mode 400), `TestNodeContextEnrichment` (2: solve children see the neighborhood; delegate enrichment only for content generation, model keys survive), `test_builtin.py` manifest pins updated deliberately; frontend api (+1: propose mode body)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1102 passed (8 skipped); frontend `npx jest` full → 717 passed; `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-64aec69a` (backend), `COMMIT-47bbcedc` (frontend api), docs commit (this entry + memo flip)
- Issues/regressions discovered: none new; the propose-loop test initially forgot to install the NCB specialist — the missing-specialist path answered exactly as designed (all targets failed with the install-proposal reason).
- Follow-up work: 67-7 consumes `previousAttempt` + the journal for validation rounds; 67-9 drives propose-mode solve one node at a time and opens the review from the streamed proposalId; `nodeProposals: {ref: proposalId}` on the session if reload-addressability beyond the active mirror proves necessary.

---

## BL-P5-20260805-11: Execute-through-node validation + self-correction (memo dev/67-7 — Simulation Mode: validate)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: 67-0 "Every Solve must be validated" (execute the dataflow from its valid start through the node being solved; inspect runtime output/logs/errors; self-correct; present for approval; no "trivial code" shortcut) + "use the existing programmatic execution infrastructure … exercised by the Playwright-based dataflow tests"
- Design decisions/artifacts: memo `dev/67-7` (program index `dev/67-1`)
- Tasks: `TASK-P5-execute-through-validation`
- Risks/questions: validation must never corrupt state — structural mitigations: the candidate runs as an IN-MEMORY overlay (only an approved Apply writes), validation executions pass `save_dataset: false` (no computed-dataset side effects), journal records are marked `validation`, and the in-flight guard clears in a finally on every exit including client disconnect
- Design-to-code decision or deviation: **(1) Promotion** — `workflow_spec.py` + the sandbox-HTTP runner moved to `app/execution/` (single source; the e2e suite imports through byte-thin shims — `parse_workflow`, `execute_workflow_programmatically`, seed/widget/artifact helpers keep their historical names); `parse_workflow_dict` added for in-memory specs. **(2) `run_through_node`** — ancestor slice (reverse BFS, the server twin of `playNodesUpTo`), Kahn order, merge `in_N` assembly, pass-through for browser-only types, candidate overlay, per-node accumulate-and-stop (an upstream failure names the BLOCKER, the target is never blamed), cycle/25-node-bound refusals, transport failures reported as INFRASTRUCTURE never node errors, JS nodes via `/execJs`. **(3) Deterministic verdicts** — `app/agents/validation.py`: pass / fail (execution-error | upstream-blocker | precondition | type-mismatch — runtime output dataType vs the consumer's DEC-051 declared input types, fail-open on unmapped types or out-of-scope templates) / infrastructure; the goal is EVIDENCE for human judgment, never machine-judged. **(4) The loop** — `validate-node` SSE endpoint: generation (67-6 composer context) → threaded validation with live `node_executed` events (dev/63 queue pattern) → `round_verdict` → on fail re-generate with `previousAttempt` + `validationError` (the NCB instruction's contract), ≤2 corrective rounds → the FINAL candidate mints a `node.content.write` proposal carrying the `validation` block — PASS or FAIL, the user decides; `nodeStates[ref] → validated|failed`; an `infrastructure` verdict mints nothing and leaves state untouched. **(5) Frontend** — the review card renders the verdict block (PASS + output type / FAIL + traceback in a collapsible + upstream-blocker naming) and labels the button "Apply anyway" on FAIL; `agentsApi.validateNode` streams the loop. Recorded deviations: no dedicated cancel endpoint (disconnect clears the guard; the 67-9 driver owns pause/cancel UX); the runner journals with `durationMs: 0` (per-node timing joins when needed).
- Files/modules changed: backend `app/execution/{workflow_spec.py (new),runner.py (new)}`, `app/agents/{validation.py (new),services.py,routes.py}`, `tests/test_frontend/{workflow_spec.py,utils.py}` (shims); frontend `api/agentsApi.ts`, `components/agents/attach/AgentAttachmentsProvider.tsx`, `components/agents/content/AgentReviewCard.{tsx,module.css}`
- Tests added/updated: backend `test_runner.py` (new, 8: ancestor slice, overlay-no-mutation, upstream-blocker stop, infrastructure, cycle+bound refusals, merge fan-in assembly, journal validation flag, save_dataset false), `test_validation.py` (new, 5: pass evidence, execution-error, blocker naming, type-mismatch + fail-open, infrastructure), `TestValidateNode` (4: pass mints validated proposal + transcript block + untouched spec; self-correction sees previousAttempt + the real traceback; exhaustion fails loudly but still proposes; preflight guards 422/404/409); frontend card (+4: PASS, FAIL + Apply anyway, blocker naming, no-validation regression), api (+1 stream)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1119 passed (8 skipped); frontend `npx jest` full → 722 passed; shim imports verified (`parse_workflow is app.parse_workflow`)
- Commit/PR: `COMMIT-d2053cf9` (runner promotion), `COMMIT-6b0b0c71` (validation + endpoint), `COMMIT-9b51b4ae` (frontend), docs commit (this entry + memo flip)
- Issues/regressions discovered: none new; the Playwright suite consumes the promoted runner through shims (full e2e parity check requires the live-server suite, which stays out of the default run as before).
- Follow-up work: 67-9 drives create → propose → validate per node and owns pause/cancel; outputSchema evidence when the journal captures column metadata; per-node durations in validation journal records.

---

## BL-P5-20260805-12: Connection review stage (memo dev/67-8 — Simulation Mode: connect)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: 67-0 "Connection review happens after node validation" (vertical inspectable edge list, source/target adjacent, topology validation BEFORE an edge applies) + "Merge-node detection" at the connection stage
- Design decisions/artifacts: memo `dev/67-8` (program index `dev/67-1`)
- Tasks: `TASK-P5-connection-review`
- Risks/questions: per-edge application against live truth can partially succeed — embraced, not fought: refusals are PER EDGE and named, remaining rows stay applicable, and a refused edge blocks proposal completion (a replan resolves it)
- Design-to-code decision or deviation: **(1) Named rows at mint** — `part.plan.edges` gains labeled, index-stable rows (`fromLabel`/`toLabel` from plan titles or spec goals, `toHandle` when explicit). **(2) `apply-edges`** — the third staged apply over the pinned plan: default all-pending or an index subset; endpoints resolve through 67-5's `appliedNodeIds` (created refs) or the live spec (existing ids) with refusals like "create 'Load' first"; fan-in re-validates against the CURRENT spec at apply time (DEC-051 rendered capacity, Merge suggestion named); merge targets take the named free slot or the lowest free; a user-drawn duplicate applies as a no-op ("already connected", never a double edge); each apply re-pins the shape digest (67-5 semantics); `edgeStates {index: applied|refused}` rides the proposal + the builderSession + the activeProposal mirror. When every ref AND every edge is applied the proposal COMPLETES (status applied, phase applied|ready by nodeRuns, result turn logged). **(3) Live canvas** — a new quiet `edges-created` bridge mutation (handles + `data: {}` parity, idempotent per batch, NO fit/center — the nodes are already in view); the event-bus allowlist gained the kind (the silent-drop allowlist was the one integration bug this memo hit — caught by the new tests). **(4) Card** — the Connections section on pending plan proposals: monospace `from → to [handle]` rows, per-row Connect gated until both endpoints exist (reason as tooltip), Connected ✓ / Refused ✗ chips, Connect all.
- Files/modules changed: backend `app/agents/{services.py,routes.py}`; frontend `utils/agentCanvasEvents.ts`, `api/agentsApi.ts`, `components/agents/attach/{useAgentCanvasMutations.ts,AgentAttachmentsProvider.tsx,AgentChatPanel.tsx,AgentDockOverlay.tsx}`, `components/agents/content/AgentReviewCard.{tsx,module.css}`
- Tests added/updated: backend `TestPlanEdgeApply` (4: labeled rows on the part; refuse-until-created → apply-with-handles → spec persistence → completion + post-completion 409; manual-edge no-op without duplication; fan-in refusal naming the Merge resolution with a refused edge blocking completion); frontend card (+2: gated named rows + per-index Connect, state chips + Connect all), bridge (+1: quiet insert with handles, idempotent per batch, no fit/center), provider (+1: edges-created dispatch), api (+1: subset + default bodies)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1123 passed (8 skipped); frontend `npx jest` full → 727 passed; `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-4e94a0ac` (backend), `COMMIT-53bacb88` (frontend), docs commit (this entry + memo flip)
- Issues/regressions discovered: `subscribeAgentCanvasMutations` allowlists mutation kinds and silently dropped the new `edges-created` — caught immediately by the bridge + provider tests; the allowlist gained the kind. Lesson: an event-bus with a kind allowlist is a RECOGNITION predicate too (the dev/61 lesson, one layer down).
- Follow-up work: 67-9 sequences nodes-then-connections and owns the stage chip; edge rows for REVISION plans (removals) stay on the dev/59 card path.

---

## BL-P5-20260805-13: Simulation Mode orchestration + Apply Plan as automated sequence (memo dev/67-9, DEC-054 — the dev/67 assembly)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: 67-0 "Default build model: first-class Simulation Mode" + "Apply Plan remains available as an alternative" (never uncontrolled bulk generation; per-node generation/Solve/validation, error isolation, self-correction, ordering, topology checks; a failure pauses progression)
- Design decisions/artifacts: **`DEC-054`** (memo `dev/67-9`; program index `dev/67-1`)
- Tasks: `TASK-P5-simulation-mode`
- Risks/questions: the single-activeProposal model would have let the sequence's own content reviews SUPERSEDE the pending plan — resolved structurally: the plan PARKS (`record.planProposal`) when a non-plan proposal mints over it, `_pending_plan_proposal` falls back to the parked slot, a new plan replaces any parked one, and completion clears it; the listing exposes `planProposal` so the card's per-node state survives the parking
- Design-to-code decision or deviation: **(1) One transition function** — `_next_simulation_action`: per ref in topological order (mint's depth math) `planned→create`, `created|failed→validate` (a failed ref re-validates on RESUME; the pause happened when it first failed), `validated→approve`, `solving→await-review` (pause — a review outside the driver's loop), all-approved→`connect` (zero-edge plans simply complete). **(2) The driver** — `simulate` SSE endpoint (`step`|`auto`): read-fresh → act → persist → emit per action; pause reasons persisted (`pauseReason` {kind, ref, proposalId, message}) for validation-failed / infrastructure / connection-refused / content-review-pending; auto-approve annotates the content proposal `approvedBy: "simulation-auto"`; cancellation = dev/63 dual signals (`simulate/cancel` + registry) honored at action boundaries; disconnect clears the in-flight guard in a finally; a 500-action runaway backstop. **(3) Canvas over the stream** — `node_created`/`node_content_applied`/`edges_created` events ride the driver stream and the provider maps them onto the existing bridge (the frontend never re-implements insertion). **(4) UI** — "Build & validate plan" is the strip's primary plan action (auto), Step beside it, "Resume" when paused (with the reason narrated as a plain sentence), Cancel while running, live narration lines ("executing upstream 2/5…"), and the classic bulk apply demoted to the explicit "Apply all without validation"; parked plans keep driving the controls and the card's per-node state.
- Files/modules changed: backend `app/agents/{services.py,routes.py}`; frontend `api/agentsApi.ts`, `components/agents/attach/{AgentAttachmentsProvider,AgentBuilderStrip,AgentChatPanel,AgentDockOverlay}.tsx`
- Tests added/updated: backend `TestSimulationDriver` (5: auto builds/validates/approves/connects the whole plan with recorded auto-approval + canvas events on the stream; step performs exactly ONE action and N steps ≡ one auto run with post-completion 409; validation failure pauses with nothing downstream generated, then Apply-anyway + resume completes; cancel stops at the boundary keeping done work; preflight guards) — the parking mechanism is exercised by every multi-node sequence; frontend api (+2), provider (+1: stream→bridge mapping), strip (+2: default/secondary labeling + step, paused narration + Resume with a parked plan)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1128 passed (8 skipped); frontend `npx jest` full → 732 passed; `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-4e35002b` (backend driver + parking), `COMMIT-7beca0dd` (frontend), docs commit (this entry + DEC-054 ledgers + memo flip + index)
- Issues/regressions discovered: the completing step initially left later calls 404ing on the vanished plan proposal — the eager check now answers "the plan is no longer pending — nothing to simulate" (409) and the completing step reports `nextAction: null`.
- Follow-up work: 67-4 (Node Researcher — the program's remaining memo); a "connecting" phase chip if stage-level chips prove wanted; per-edge Step granularity in the connect stage; DEC-021 background execution for long sequences.

---

## BL-P5-20260805-14: Node Researcher + verified external discovery (memo dev/67-4, DEC-053 — the dev/67 program's completion)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: 67-0 "Dataset Finder hallucination" (external discovery as a verifiable research operation; never treat model-generated identifiers/URLs as valid without verification; chainable from the builder agents) + "Node Researcher" (a true research agent with web capability) + the owner's scope directive: **the Socrata fix generalizes to ANY dataset API connection**
- Design decisions/artifacts: **`DEC-053`** (memo `dev/67-4`; program index `dev/67-1`); LangChain declined for the ≤3-step research chain — "unbounded research chains" joins DEC-021 as a monitored re-open condition
- Tasks: `TASK-P5-verified-discovery`
- Risks/questions: agent-driven egress is a new attack surface — default-deny structurally: ONE chokepoint module, post-DNS private-address refusal (metadata/loopback/rebind shapes pinned in tests), per-hop redirect re-checks, byte/time caps, a 4-call per-run budget enforced in the tool loop, per-fetch audit records
- Design-to-code decision or deviation: **(1) Egress** — `app/agents/egress.py`: injectable transport/resolver (CI never touches the network), `EgressRefused` distinct from unreachable. **(2) The gate is provider-agnostic** — `verify.py`: `verify_endpoint` (status/content-type/response-shape sample) is the universal path; `_VALIDATORS` registry keyed by URL shapes dispatches refinements — `verify_socrata` (the 4x4-id metadata probe returning real dataset name + columns) is one entry; CKAN/ArcGIS/… are future entries, never new gates; no URL → loudly `unverified` ("never checked"). **(3) Tools** — `web.fetch`/`web.search` read contracts (no spec needed — handled before the spec read); search is deployment-configured (`CURIO_SEARCH_URL` template) and errors honestly when absent; the run loop enforces the 4-call budget as data. **(4) Roster** — `agent.node-researcher` (17th entry, category evaluate, `research.verify`/`research.summarize`, web tools + node.read; `research_instruction.txt`: verify-before-assert, failure-to-verify IS a finding, VERIFIED/REPORTED/UNVERIFIED vocabulary, budget-aware); chainable from Dataset Finder, Node Builder, Dataflow Builder. **Deviation of substance**: DEC-046 children are structurally TOOL-LESS, so a researcher-as-delegate cannot fetch — `research.verify` delegate inputs are enriched with RUNTIME-verified evidence (the deterministic validators run before the child; the child synthesizes over real evidence), keeping DEC-046 intact; the researcher's own attachment runs use the web tools directly. **(5) The finder gate** — `_verify_candidate_parts` at BOTH reply-extraction sites: external rows probed (first 4, the budget shape), URL-less rows loudly unverified; `verification` rides the row into the card (Verified ✓ / Unreachable ✗ / Refused ✗ / "Unverified — never checked" badges) and the handoff; the discovery instruction forbids ungrounded identifiers and forbids the model claiming verification itself.
- Files/modules changed: backend `app/agents/{egress.py (new),verify.py (new),tools.py,services.py,builtin.py}`, `utk_curio/llm-prompts/{research_instruction.txt (new),discovery_instruction.txt}`; frontend `api/agentsApi.ts`, `components/agents/content/AgentDatasetCandidatesCard.tsx`
- Tests added/updated: backend `test_egress.py` (new, 6: scheme allowlist, post-DNS SSRF shapes incl. rebind, public pass, bounded+audited fetch, redirect-to-private refusal at the hop, redirect cap, body-cap marker), `test_verify.py` (new, 7: the GENERIC gate verifying an arbitrary non-Socrata API — the generalization pin; 404/transport/SSRF/no-URL honesty; the Socrata refinement extracting name+columns; the exact hallucinated-id shape from session 89ae8123 answered "unreachable" BY id), `test_tools.py` `TestWebTools` (3), `TestVerifiedDiscovery` (3: rows carry verdicts through a real finder run incl. the loud no-URL case; research.verify delegate evidence enrichment with model keys surviving; researcher installable with pinned grants), roster/registry pins updated deliberately (16→17); frontend candidates card (+1: all three badge states)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1147 passed (8 skipped); frontend `npx jest` full → 733 passed; `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-7784921e` (egress + validators), `COMMIT-d8faf75e` (tools + roster + gate), `COMMIT-745c00f2` (frontend badges), docs commit (this entry + DEC-053 ledgers + memo flip + index completion)
- Issues/regressions discovered: none new; the candidates tail fixture initially omitted the `lanes` wrapper (caught by the gate test, not a runtime defect).
- Follow-up work: provider-native web search behind the same policy; more validator registry entries (CKAN, ArcGIS, Data.gov) as usage demands; egress audit records onto the execution record's pins.

---

## BL-P5-20260812-15: Progressive node lifecycle — per-node Connect/Solve/Run on the plan card (memo dev/71, adjusting dev/67)

- Date / author: 2026-08-12 / Karla
- Status: verified
- Requirements: the owner's dev/71 adjustment — `Plan → Apply node → Connect available dependencies → Solve → Apply content → Run through node → Validate → Continue`; connections created progressively as endpoints become available; per-node Solve gated on dependency readiness; per-node Run collecting outputs/schema/logs/errors accessible to agents; Node Builder attached to each created node
- Design decisions/artifacts: memo `dev/71` (adjusts DEC-054's sequencing; no new DEC — the decisions of record stand, re-sequenced)
- Tasks: `TASK-P5-progressive-lifecycle`
- Risks/questions: progressive edges complete the plan's STRUCTURE earlier than 67-8 assumed — resolved: completion (all refs + all edges applied) is the reviewed-structure milestone; content keeps its own lifecycle (nodeStates/nodeRuns), the parked plan is KEPT after completion so per-row Solve/Run and the 67-9 driver's validate/approve actions continue on the applied plan, and the last content approval flips the phase to ready
- Design-to-code decision or deviation: **(1) One per-edge policy** — the 67-8 core extracted into `_plan_edge_context` + `_apply_one_plan_edge` (endpoint resolution, already-connected no-op, DEC-051 fan-in, merge slots), consumed by the connect stage AND the new progressive sweep; `record_missing=False` lets the sweep skip not-yet-created endpoints silently while the explicit stage still names them. **(2) Apply = create + attach + connect** — `apply_plan_node` now auto-attaches the Node Builder (best-effort, idempotent, skipped with `attachedAgentId: null` when uninstalled — creation never fails over it) and sweeps every eligible plan edge (created refs or existing canvas nodes; topology refusals recorded per edge, never blocking); response carries `createdEdges`/`edgeResults`/`edgeStates`/`attachedAgentId` and the bridge draws the edges live. **(3) Run-node** — `run-node` SSE endpoint over the 67-7 runner WITHOUT a candidate (`as_validation=False` — journal rows are REAL runs), streaming `node_executed` progress, result card with output type/chain length or the stderr tail; agents read outcomes via the 67-2 journal (`node.runtime.read`), which was the "accessible to agents" requirement. **(4) Card rows** — dependency lines ("needs: Load CSV"), lifecycle chips (Created ✓ → Content review pending → Validated → Solved ✓ → Failed retries), per-row Solve (client-gated: incoming edges applied ∧ upstream plan refs approved, blocker named in the tooltip; calls the 67-7 validate loop) and Run (approved rows); readiness computed from the ledgers already on the mirror — no new persisted state. **(5) Driver alignment** — create actions emit the sweep's `edges_created`; the loop and step-preview read the plan in ANY status (`_plan_proposal_any`); the eager check answers "the plan is complete" only when NO action remains.
- Files/modules changed: backend `app/agents/{services.py,routes.py}`, `app/execution/runner.py` (`as_validation`); frontend `api/agentsApi.ts`, `components/agents/attach/{AgentAttachmentsProvider,AgentChatPanel,AgentDockOverlay}.tsx`, `components/agents/content/AgentReviewCard.tsx`
- Tests added/updated: backend `TestProgressiveLifecycle` (6: apply-order-free connectivity incl. B-before-A; existing-node edges wire on first apply; auto-attach + idempotence; uninstalled skip; run-node chain execution + real-run journal rows; failure report + guards); five dev/67-5/8/9 tests updated DELIBERATELY to the progressive semantics (sequential application now completes the structure; refuse-until-created → connect-on-apply; manual-noop and fan-in refusal re-staged; the step preview reads applied plans); frontend card (+4: dependency line, gated Solve with named blocker, enablement matrix, Run per ref), api (+1), provider (+1: node + edges dispatched from one apply)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1156 passed (8 skipped); frontend `npx jest` full → 776 passed; `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-4aa2f6f6` (progressive core), `COMMIT-4ae50b66` (run-node), `COMMIT-2d9b5c05` (rows), docs commit (this entry + memo flip)
- Issues/regressions discovered: the step-mode nextAction preview still required a PENDING proposal and reported completion mid-lifecycle — fixed with the any-status plan access.
- Follow-up work: run outcomes surfaced as row chips from the journal (currently the transcript card + agent tools carry them); driver auto mode interleaving Run-after-approve if demand appears; 67-1 index note.

## BL-P5-20260812-16: Delegation transparency — every delegated task lives in its agent's chat, icon-linked from the parent (memo dev/72)

- Date / author: 2026-08-12 / Karla
- Status: verified
- Requirements: the owner's dev/72 directive — the Solve lifecycle (generation, dependency checks, execution, validation, warnings/errors, self-corrections, outcome) surfaced in the node's attached Node Builder; the same transparency model generalized to EVERY delegated agent task (task/progress/diagnostics/retries/result in the responsible agent's transcript, concise); the parent references/summarizes with a direct chat link — the delegated agent's icon visually notched into the task title, click-to-open; failures attributable; uniform across Node Builder, Dataset Finder, Node Researcher, connection agents, future agents
- Design decisions/artifacts: memo `dev/72` (extends DEC-046's delegation seam without touching its invariants: depth-1, tool-less children, reply never parsed)
- Tasks: `TASK-P5-delegation-transparency`
- Risks/questions: single `activeProposal` slot contention (the recorded 67-6 limitation) — resolved for per-node content reviews by homing them on the node's own agent; the 67-9 parking mechanism stays for plans
- Design-to-code decision or deviation: **(1) Delegation homes** — `_delegation_home`: node-scoped work (nodeId present) → the node's `agent.node-builder` attachment (created via the dev/71 `_attach_node_builder` when missing); everything else → an existing attachment of the delegate's agent id (canvas preferred), else a best-effort canvas attach; a failed home NEVER fails the delegation (part carries `attachmentId: null`). **(2) One traced choke point** — `_run_delegate_traced` wraps `delegation.run_delegate` (the DEC-046 seam stays pure): framed task turn ("[Delegated by <parent>] <task>") + result turn (bounded reply, trace card with capability/verification-evidence/duration, child execution record) — exactly two turns per task, structure in the card; both loop call sites and the Solve worker use it. **(3) Typed `delegation` part** — runtime-emitted (never parseable from tails), bounded, persisted on the PARENT's turn: {capability, coord, name, category, attachmentId, status, summary}; the stream's `delegate_result` event gains attachmentId/name. **(4) Proposal home migration** — validate-node and propose-mode Solve mint the content proposal ON the node's Node Builder attachment when present (`done.proposalAttachmentId`), with a consolidated rounds-trace card + task turn in that chat and a parent summary turn carrying the delegation part; `nodeProposals[ref]` → `{proposalId, attachmentId}` (legacy strings tolerated); the driver approves through the home; `_apply_node_content_write` advances the BUILDER's ledger cross-attachment. **(5) Deviations** — Solve workers resolve homes find-only (`home_create=False`: worker threads must never write the spec); service-level home attach bypasses the route's target-compatibility checks by design (the route contract is unchanged); the stream's review_required emitter learned to skip non-proposal minted parts. **(6) Frontend** — `AgentDelegationEntry`: category-tinted icon badge notched into the entry's corner (approved concept style), click → `openChat(attachmentId)`, existence-checked against the live attachments list (stale → plain entry, never a dead link); plan-row review chips carry the same icon-link via `nodeProposals[ref].attachmentId`.
- Files/modules changed: backend `app/agents/{content.py,services.py}`; frontend `api/agentsApi.ts`, `components/agents/content/{AgentDelegationEntry.tsx+css (new),AgentReviewCard.tsx+css}`, `components/agents/attach/{AgentChatPanel,AgentDockOverlay,AgentAttachmentsProvider}.tsx`
- Tests added/updated: backend `TestDelegationTransparency` (3: home auto-created + two-turn transcript + parent part; existing node-target home reused; missing home never fails) and `TestSolveTraceHome` (2: validate homes trace + review at the node's agent with cross-attachment ledger advance on apply; driver auto approves through the home with the new nodeProposals shape); frontend `AgentDelegationEntry` suite (4), panel delegation rendering (2), review-card chip links (2)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1161 passed (8 skipped); frontend `npx jest` full → 784 passed; `tsc --noEmit` clean (two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-bdf8d52d` (backend), `COMMIT-9c9f4f96` (frontend), docs commit (this entry + memo flip)
- Issues/regressions discovered: minted delegation parts crashed the stream's review_required emitter (it assumed every minted part was a proposal) — guarded; the attach ROUTE enforces target compatibility (node-target-only agents refuse canvas attach) while the service-level home creation does not — recorded as intended layering.
- Follow-up work: running-state (in-flight) delegation entries in the transcript (today the transient stream line covers liveness); a Node Builder chat affordance to jump BACK to the parent builder; researcher evidence lines rendered as structured rows.

## BL-P5-20260812-17: Chat content updates mint the review — runtime-owned content→proposal, honest round cap, DFB update route (memo dev/73)

- Date / author: 2026-08-12 / Karla
- Status: verified
- Requirements: the owner's report — post-solve node update requests through the attached Node Builder or the Dataflow Builder were claimed "applied" with no change, or produced code (whole or fragments) in prose with no Apply button
- Design decisions/artifacts: memo `dev/73` (root-cause investigation + fix; DEC-006/DEC-046 preserved)
- Tasks: `TASK-P5-chat-content-review-mint`
- Risks/questions: three compounding root causes, all in the chat loop — RC1: `MAX_TOOL_ROUNDS=2` contradicted the Node Builder's own three-round modify flow (read → delegate generate → propose), with the final-round suffix forcing prose and a dangling write request SILENTLY dropped at the cap; RC2: chat delegations minted nothing applyable (only Solve/validate did) — applyability hung on the model's fragile second toolRequest step; RC3: the Dataflow Builder had no working update path (plan-write only, plans content-free) and its offered `node.build` delegation to a tool-less child invited the false success summary
- Design-to-code decision or deviation: **(1) One content→review sequence, three callers** — the Solve drain's mint extracted into `_mint_content_review_from_delegate`; both chat loops call it after any successful `node.content.generate` delegation whose node resolves (`inputs.nodeId` or the attachment's node target, via `_delegate_target_node_id`); generation success ⇒ the review EXISTS, runtime-owned. **(2) Homing per dev/72** — the review mints at the node's own agent when one exists (find-only — the traced delegation created it when it could, now passed `node_id` from chat too); a foreign home gets the review turn in its session and the stream's `review_required` gains `attachmentId` (the client's reload path already listens); a local home rides the parent turn's parts exactly like loop mints. **(3) The fed-back contract** — the delegate result message becomes "minted as reviewed proposal <id> … do NOT restate the code, do NOT say it was applied"; the parent's delegation entry summarizes "reviewed content change proposed — awaits Apply"; unusable content feeds back an honest refusal naming that nothing awaits review. **(4) Honest cap** — `MAX_TOOL_ROUNDS` 2→3; a MUTATE toolRequest (the `_mint_proposal` dispatch set, `MUTATE_PROPOSAL_TOOLS`) dangling at the cap appends a "Proposal step cut off" error card instead of the silent drop; read-tool dangles keep the text-kept behavior. **(5) Roster/prompt contracts** — DFB `delegates_to` gains `agent.node-content-builder` (the capability was previously reachable only through the silent capability-first fallback, never OFFERED); NB instruction step 6: the runtime mints, don't re-emit, summarize and point (also dropped the ungranted `node.read` mention); DFB instruction step 6: delegate `node.content.generate` with the node's REAL id, never `node.build` for edits, never claim applied. Prompt bytes flow to existing installs via the dev/60 roster-bytes rule.
- Files/modules changed: backend `app/agents/{services.py,builtin.py}`; prompts `utk_curio/llm-prompts/{node_build_instruction,orchestration_instruction}.txt`
- Tests added/updated: `TestChatContentReviewMint` (5: node-attached update mints locally + Apply writes the content end-to-end; canvas parent homes the review at the node's agent; DFB routes through the new roster entry; mutate dangle at the cap → visible cutoff card; empty generated content → honest refusal) + `TestChatContentReviewMintStream` (foreign-homed `review_required` carries `attachmentId`; the proposal never rides the parent turn); round-budget pins updated deliberately (`test_delegate_rounds_share_the_tool_round_budget` 2→3 delegates, `test_round_cap_bounds_the_loop` 3→4 calls, plan correction caps 3→4 calls), DFB `delegates_to` manifest pin extended
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1167 passed (8 skipped); no frontend changes required (additive event key; the cutoff card renders through the generic card shell; delegation entries already link homes)
- Commit/PR: `COMMIT-95f7fc27`, docs commit (this entry + memo flip)
- Issues/regressions discovered: the activeProposal mirror carries `nodeId` (not `pins`) — test assertion corrected during authoring; nothing else surfaced
- Follow-up work: the blocking (non-stream) client path reloads attachments only on local proposals — acceptable while the stream is the primary transport, revisit if the fallback path gains weight; consider offering `research.verify` the same runtime-mint treatment for verification-backed content edits

## BL-P5-20260813-18: Agents drawer adopts the shared catalog chrome — search/sort bar + avatar card grid (memo dev/68)

- Date / author: 2026-08-13 / Karla
- Status: verified
- Requirements: the owner's directive — match the approved concept (`png-concepts/01`/`02`): add the search bar and sort dropdown using the same components/styling/behavior as the Datasets and Node Packages drawers, left-align category avatars in each row on the shared row structure, reuse existing chrome instead of Agents-specific variants, preserve all agent-specific content/actions, keep the change scoped to the drawer UI
- Design decisions/artifacts: memo `dev/68`
- Tasks: `TASK-P5-agents-drawer-chrome`
- Risks/questions: `AgentCard` carries no creation timestamp — resolved: "Sort: New" is defined as the server roster order (documented in `agentListUtils`), "Sort: Name" shares the `sortPackages` comparator; the concept's category filter-chip row deliberately excluded (no sibling drawer has one — feature, not chrome)
- Design-to-code decision or deviation: **(1) Shared bar, not a fork** — `PackageSearchRow` gains optional `placeholder`/`sortAriaLabel` (defaults = today's strings, so Datasets/Nodes render byte-identically); Agents passes "Search agents, hooks, keywords..." / "Sort agents". **(2) View-state only** — search/sort are local `useState` + `useMemo` over the untouched `useAgentsCatalogDrawer` per-scope cache (dev/47 race guard/SWR intact); client-side like the Nodes drawer, no debounce (no server round-trip). **(3) Row grid reuse** — `AgentRow` moved onto `PackageCard.module.css` (accent | avatar | body | action), the exact pattern DatasetCard uses; category-tinted robot avatar keyed by the existing `agentCategoryKey`; light-surface tints added to the agents module (palette hues); ~100 lines of duplicated card/button/tag CSS deleted. **(4) Hierarchy** — reordered to header → subtitle → search/sort → tabs → list → footer per the concept; search-empty vs scope-empty messages distinguished.
- Files/modules changed: frontend `components/agents/catalog/{AgentsCatalogDrawer.tsx,AgentsCatalogDrawer.module.css,agentListUtils.ts (new)}`, `components/packages/publishing/PackageSearchRow.tsx`
- Tests added/updated: `agentListUtils` unit suite (8: field coverage, case/trim, empty query, name/new sort semantics, copy-not-mutate); drawer suite +6 (filter + clear, search-specific empty message, DOM-order name sort + roster-order default, query persists across scope tabs, per-row tinted avatar aria-hidden); existing 22 drawer tests pass unmodified
- Verification evidence: frontend `npx jest` full → 788 passed (5 consecutive runs); `tsc --noEmit` clean (two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-77bd7ed8`, docs commit (this entry + memo)
- Issues/regressions discovered: the Datasets drawer's sort select was found force-casting `DatasetSortMode` into the package options — fixed immediately after as dev/74 (next entry)
- Follow-up work: dataset-flavored search placeholder/aria copy inside the Data Catalog drawer (now trivial via the new props); panel width 560px vs the siblings' 520px; skeleton loaders + tab count badges for the Agents drawer

## BL-P5-20260813-19: PackageSearchRow typed sortOptions — Data Catalog sort select on the dataset contract (memo dev/74)

- Date / author: 2026-08-13 / Karla
- Status: verified
- Requirements: the owner's directive — fix the pre-existing bug surfaced during dev/68: the Data Catalog drawer's sort state (`"recent" | "name"`) was cast into `PackageSearchRow`'s hardcoded `"new"`/`"name"` options, so the default matched no option (blank select) and "Sort: New" sent `?sort=new`, which the backend handles only through its silent non-"name" fallback branch
- Design decisions/artifacts: memo `dev/74`
- Tasks: `TASK-P5-dataset-sort-contract`
- Risks/questions: rename the dataset vocabulary to "new" instead? — rejected: `"recent"` is used consistently across palette context, providers, UpMenu, DatasetsPaletteDropdown, hooks, API and backend (10+ files); the UI adapts to the domain, not vice versa
- Design-to-code decision or deviation: `PackageSearchRow` becomes generic (`<S extends string = SortMode>`) with optional `sortOptions` defaulting to the package pair — Nodes/Agents compile and render unchanged; the Data Catalog drawer passes `recent`/`name` options labeled "Sort: Recent"/"Sort: Name" (recency wording matches the browse page and the backend's updatedAt semantics) and both `as` casts are deleted, so only backend-documented wire values can reach `?sort=`
- Files/modules changed: frontend `components/packages/publishing/PackageSearchRow.tsx`, `components/datasets/catalog/DatasetCatalogDrawer.tsx`
- Tests added/updated: new `PackageSearchRow` suite (4: default options/copy regression for Nodes/Agents; custom options with a matching non-blank default selection; custom-vocabulary change round-trip; search forwarding + dev/68 copy overrides)
- Verification evidence: frontend `npx jest` full → 788 passed; `tsc --noEmit` clean (two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-400d7a09`, docs commit (this entry + memo)
- Issues/regressions discovered: none beyond the target bug
- Follow-up work: consider surfacing the backend's accepted sort values as a shared constant if a third sort consumer appears

## BL-P5-20260813-20: Chat transcripts follow-at-bottom auto-scroll + Jump to latest (memo dev/75)

- Date / author: 2026-08-13 / Karla
- Status: verified
- Requirements: the owner's directive — auto-follow streamed content only while the user is at/near the bottom of the transcript; a manual scroll-up detaches follow so streaming never moves the viewport; follow resumes on bottom-return or an explicit compact "Jump to latest" action shown only while newer content sits below; consistent across all agent chat transcripts and delegated-agent chats
- Design decisions/artifacts: memo `dev/75`
- Tasks: `TASK-P5-chat-autoscroll-follow`
- Risks/questions: telling programmatic scrolls from user scrolls (a smooth jump animating through non-bottom positions must not read as a detach and cancel itself) — resolved with a suppress-flag handshake: set only when a scroll write will actually move, cleared by the scroll landing near the bottom or by genuine user input (wheel/touchmove/pointerdown/keydown), so a user scrolling during the animation still wins
- Design-to-code decision or deviation: **(1) One hook, one pill** — `useTranscriptAutoScroll` owns pinned/follow/jump semantics (48px bottom threshold, tolerant of fractional zoom pixels); the shared `TranscriptJumpButton` overlay pill (dark `--curio-top-bar-bg` token, absolute inside a `position:relative` wrap so appearing never shifts layout) keeps both surfaces identical. **(2) Zero per-chunk re-renders** — SSE streaming replaces the last turn per chunk, so the pinned flag lives in refs and `atBottom` state flips only on pinned↔detached transitions; follow uses instant scroll in a layout effect (smooth lags streaming). **(3) Explicit re-pin moments** — attachment switch/`resetKey`, history hydration completing (`ready`), sending a message (`pinToLatest`), and the jump action; `prefers-reduced-motion` downgrades the jump to instant; a `ResizeObserver` (container + last child) re-pins on late layout growth only while pinned. **(4) Delegated chats free** — dev/72 opens delegates in the same `AgentChatPanel`, so one adoption covers both; `LLMChat` drops its `getElementById("messagesDiv")` effect for the ref-based hook
- Files/modules changed: frontend `hook/useTranscriptAutoScroll.ts` (new), `components/TranscriptJumpButton.tsx` + `.module.css` (new), `components/agents/attach/AgentChatPanel.tsx` + `.module.css`, `components/LLMChat.tsx`
- Tests added/updated: hook suite (10: pinned follow, streamed-while-detached regression, bottom-return resume, 48/49px threshold boundary, jump re-pin + follow, smooth-jump suppression handshake, wheel-wins-during-jump, pinToLatest, hydration deferral, resetKey force-pin); `AgentChatPanel` +5 (pill hidden at bottom, streamed turn replacement while scrolled up never moves the transcript, pill click jumps/resumes/hides, manual bottom-return hides, send re-pins); new `LLMChat` suite (2: pill visibility contract, jump behavior)
- Verification evidence: frontend `npx jest` full → 805 passed (75 suites); `tsc --noEmit` clean (two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-631b96eb` (hook + pill + unit tests), `COMMIT-f4d80b0c` (AgentChatPanel adoption + regression tests), `COMMIT-724ebd3f` (LLMChat adoption), docs commit (this entry + memo flip)
- Issues/regressions discovered: a pre-existing stray `console.log('checkForGoal', …)` debug line in the LLMChat working tree predates dev/75 — deliberately kept out of the commits, left unstaged for the owner to keep or drop
- Follow-up work: any future transcript surface must adopt the hook + pill rather than a bespoke scroll effect; consider an unread-count badge on the pill; fix the two tsconfig deprecation notices so typechecks aren't masked

## BL-P5-20260813-21: Agent chat multiline composer — Enter sends, Shift+Enter newline, paste keeps formatting (memo dev/77)

- Date / author: 2026-08-13 / Karla
- Status: verified
- Requirements: the owner's directive — replace the agent chat's single-line input with a multiline textarea that preserves typed and pasted line breaks, grows with content within sensible height limits, sends on Enter with Shift+Enter inserting a newline, and keeps styling/resizing consistent with Curio's existing chat UI
- Design decisions/artifacts: memo `dev/77`
- Tasks: `TASK-P5-chat-multiline-composer`
- Risks/questions: whether newlines survive the send path — resolved by inspection: `send()` only trims leading/trailing whitespace and both transcript bubbles already render `white-space: pre-wrap`, so the input element was the single point of loss; IME commit-Enter accidentally sending — resolved with an `e.nativeEvent.isComposing` guard
- Design-to-code decision or deviation: **(1) One hook, no state** — `useAutoGrowTextarea` (placed in `agents/attach/` per the dev/76 encapsulation rule) writes height straight to the DOM from `scrollHeight` in a `useLayoutEffect` keyed on the value, so prefills/composePrompt/post-send reset re-measure without keystrokes, typing costs no extra re-renders, and there is no pre-paint jump; a zero measurement (jsdom) falls back to the rows-based height. **(2) Growth cap 120px (~6 rows)** with `overflow-y` flipping to `auto` only when clamped — no transient scrollbar flash; the transcript keeps its `min-height: 0` scroll contract so the footer never pushes out of the panel. **(3) Pill preserved without a conditional** — `border-radius: 999px` → `18px` (half the one-row height) renders identically at one line and as a rounded rectangle when grown; `.footer` moves to `align-items: flex-end` so Send anchors beside the last line. **(4) The existing keydown contract was kept verbatim** (it already encoded Enter-sends/Shift+Enter) — only the element changed, plus the `isComposing` guard and an explicit `aria-label`; paste needs no handler in a textarea
- Files/modules changed: frontend `components/agents/attach/useAutoGrowTextarea.ts` (new), `components/agents/attach/AgentChatPanel.tsx` + `.module.css`
- Tests added/updated: hook suite (5: measure on value change, clamp + internal scroll at cap, programmatic prefill re-measure, post-send collapse, jsdom zero-measurement tolerance); `AgentChatPanel` +5 (Enter sends with interior newlines preserved through `onSend` — the regression for the issue, Shift+Enter never sends, IME composition Enter never sends, whitespace/newline-only never sends, user-turn newlines survive into the `.msgUser` bubble)
- Verification evidence: frontend `npx jest` full → 813 passed (75 suites); `tsc --noEmit` clean (two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-ae57cf42` (hook + unit tests), `COMMIT-5f41d2f6` (composer swap + styles), `COMMIT-2d0ca5ea` (regression tests), docs commit (this entry + memo)
- Issues/regressions discovered: none; the uncommitted dev/76 import-path edits in `AgentChatPanel.tsx` were deliberately kept out of the dev/77 commits (commit `5f41d2f6` stages HEAD's import paths so its tree builds standalone) and remain unstaged for the dev/76 docs commit — note dev/76 itself is still unlogged in this build log
- Follow-up work: `LLMChat` still has a single-line input — adopt `useAutoGrowTextarea` there; the intent editor textarea could adopt it as polish

## BL-P5-20260814-22: Transcript scroll contract encapsulated under agents/attach; LLMChat reverted (memo dev/76)

- Date / author: 2026-08-14 / Karla
- Status: verified
- Requirements: the owner's directive — move the Jump-to-latest action components into the appropriate `agents/` folder, keep all agent chat-specific behavior and UI encapsulated under `agents/`, move the related hooks/styles/helpers, update imports, reuse shared utilities only when genuinely cross-feature, organizational change only; amended mid-implementation: `LLMChat` must be untouched by the agents-related task
- Design decisions/artifacts: memo `dev/76`
- Tasks: `TASK-P5-transcript-scroll-encapsulation`
- Risks/questions: the dev/75 contract had two consumers (`AgentChatPanel` + `LLMChat`) — initial resolution had `LLMChat` import the agents-owned contract; superseded by the owner's untouched-LLMChat directive, which resolves the tension outright: the contract now has exactly one consumer and no cross-feature import
- Design-to-code decision or deviation: **(1) History-preserving moves** — `git mv` of `TranscriptJumpButton.{tsx,module.css}` and `useTranscriptAutoScroll.ts` → `components/agents/attach/` (the folder already holding the panel's feature hooks `useAgentAttachments`/`useAgentCanvasMutations`); the hook test → `tests/attach/` (the `agents/attach` test mirror). **(2) LLMChat revert** — the dev/75 adoption (`COMMIT-724ebd3f`) is undone: `LLMChat.tsx` restored byte-identical to its pre-dev/75 content (unconditional `getElementById("messagesDiv")` scroll effect back), `tests/components/LLMChat.test.tsx` deleted; the LLM Assistant deliberately returns to force-scroll-on-update behavior. **(3) Escape hatch recorded** — if a non-agent transcript later wants follow-at-bottom, adopt the hook and promote it back to `src/hook/` as its own change
- Files/modules changed: frontend `components/agents/attach/{TranscriptJumpButton.tsx,TranscriptJumpButton.module.css,useTranscriptAutoScroll.ts}` (moved in), `components/agents/attach/AgentChatPanel.tsx` (sibling imports), `components/LLMChat.tsx` (reverted), `tests/attach/useTranscriptAutoScroll.test.tsx` (moved in), `tests/components/LLMChat.test.tsx` (deleted)
- Tests added/updated: none added (organizational); the moved hook suite runs unchanged; LLMChat scroll suite deleted with the revert
- Verification evidence: frontend `npx jest` full → 813 passed (75 suites) on the committed tree, matching the dev/77 baseline exactly (that baseline already reflected this change's LLMChat-test deletion — dev/77's run happened on the post-revert working tree); `tsc --noEmit` nothing new in touched files; `git diff f4d80b0c -- LLMChat.tsx` shows only the pre-existing uncommitted debug line
- Commit/PR: `COMMIT-a99526aa` (atomic move + reimport + revert), docs commit (this entry + memo flip)
- Issues/regressions discovered: none — renames detected at 100%/98% similarity, history preserved; sequencing note: dev/77 landed between this change's implementation and its commit, so `a99526aa` was committed on top of the dev/77 composer (verified: its `AgentChatPanel` diff is the two import lines only)
- Follow-up work: none pending; the dev/75 follow-ups (unread-count badge, tsconfig deprecations) still stand

## BL-P5-20260814-23: Copy button on transcript code blocks (memo dev/78)

- Date / author: 2026-08-14 / Karla
- Status: verified
- Requirements: the owner's directive — add a Copy button to code blocks in agent chat transcripts: action directly on each block, copy only the code content (no Markdown fences or surrounding transcript text), brief visual feedback on success, compact control consistent with existing chat/code-output styling
- Design decisions/artifacts: memo `dev/78`
- Tasks: `TASK-P5-transcript-code-copy`
- Risks/questions: this is the frontend's FIRST clipboard integration (zero prior `navigator.clipboard`/`execCommand` usage) — resolved primary-path-only: `navigator.clipboard.writeText`, no `execCommand` fallback, a rejected write flips the button to a loud transient "Failed" state; jsdom has no clipboard, so tests define a local mock (not in `setupTests.ts`)
- Design-to-code decision or deviation: **(1) Hook point** — a `pre` entry in `SafeAgentContent`'s existing `components` map (the ONLY agent rich-content renderer, REQ-SEC-002), so every agent chat surface gets the button, delegated chats included, and inline code is excluded by construction (no `<pre>` parent). **(2) New presentational component** — `agents/content/AgentCodeBlock.tsx` + module CSS (TranscriptJumpButton pattern): wrapper `<div>` keeps the `<pre>` real so `.msgAgent pre` styling still matches; payload derived from rendered children via pure exported `extractNodeText` (strip exactly one fence-terminating `\n`, indentation verbatim); `idle|copied|failed` state with ref-held timeout, cleared on unmount, restarted on re-click; empty fences render no button. **(3) Reveal behavior** — subtle-always-visible (opacity 0.55 → 1 on block hover/`:focus-visible`/feedback); hover-only reveal rejected as undiscoverable and dead on touch. Wrapper resets `.msgAgent`'s `white-space: pre-wrap` so button chrome lays out normally. No deviations from the memo
- Files/modules changed: frontend `components/agents/content/AgentCodeBlock.tsx` (new), `components/agents/content/AgentCodeBlock.module.css` (new), `components/agents/content/SafeAgentContent.tsx` (three-line `pre` override)
- Tests added/updated: `tests/content/AgentCodeBlock.test.tsx` (new, 12 — exact payload incl. trailing-newline/indentation contract, copied/failed feedback lifecycle with fake timers, block independence, empty fence, unmount safety, timer-restart on rapid clicks, `extractNodeText` units); `tests/content/SafeAgentContent.test.tsx` (+3 — button presence, inline-code exclusion, hostile-fence copies literal text and still renders no `<script>`); `tests/attach/AgentChatPanel.test.tsx` (+1 wiring assertion inside `.msgAgent`)
- Verification evidence: frontend `npx jest` full → 829 passed (76 suites), up from the 813/75 dev/76 baseline by exactly the 16 new tests; `tsc --noEmit` → zero errors in source files (only the two pre-existing tsconfig TS5107/TS5101 deprecation notices)
- Commit/PR: `COMMIT-dafd61f6` (component + tests), `COMMIT-1ed4a707` (renderer wiring + policy regressions), `COMMIT-e1bbcc73` (integration assertion), docs commit (this entry + memo)
- Issues/regressions discovered: none
- Follow-up work: LLMChat/FloatingPanel/NodeExplanation use bare `<ReactMarkdown>` and carry no copy affordance — if wanted there, extract a shared `components` map rather than duplicating the button (same posture as the dev/77 LLMChat composer follow-up)

## BL-P5-20260818-24: Jump-to-latest fixed for real browsers — eager detach + jump landing guarantee (memo dev/79)

- Date / author: 2026-08-18 / Karla
- Status: verified
- Requirements: the owner's report — "the jump to the bottom btn is not working" in the real app while all jsdom tests pass; the dev/75 contract must actually hold in a browser: scrolling up during streaming detaches reliably, and clicking the pill always lands at the newest message
- Design decisions/artifacts: memo `dev/79` (renumbered from 78 — a parallel session claimed 78 for the transcript code-copy button)
- Tasks: `TASK-P5-jump-to-latest-detach-race`
- Risks/questions: was the diagnosis right without a browser repro? — the primary mechanism was reproduced as an ordering test against the unfixed hook (wheel-up → chunk re-render → late coalesced scroll event ⇒ scroll eaten, still pinned), then inverted into the regression test
- Design-to-code decision or deviation: **(1) Root cause 1 — the detach race**: browsers coalesce scroll events and dispatch them after queued tasks, so during streaming the next chunk's layout effect re-pins BEFORE the scroll event reporting the user's wheel-up; fix: user input meaning "leave the bottom" unpins synchronously in the input handler itself (wheel `deltaY<0`, `touchmove`, `ArrowUp`/`PageUp`/`Home`, `pointerdown` in the scrollbar gutter `offsetX>=clientWidth` with `target===container`, LTR-only noted); false positives self-heal via the next scroll event's position check. **(2) Root cause 2 — momentum cancels the jump**: native smooth scrolls die on any wheel input and macOS trackpad momentum emits wheel events for ~1–2s after the click; fix: a 450ms landing guarantee — user-intent handlers stand down while the flight is armed, a scroll event landing near the bottom disarms early, and the deadline snaps via direct `scrollTop` write (uncancelable) if the animation died; `prefers-reduced-motion` keeps its instant path. **(3) Deliberate test replacement**: the dev/75 test "wheel during a jump wins over the animation" encoded exactly the momentum-cancel behavior fix 2 removes — replaced by the flight-contract pair rather than kept
- Files/modules changed: frontend `components/agents/attach/useTranscriptAutoScroll.ts` only (consumers, pill UI, styling untouched)
- Tests added/updated: hook suite +7/−1 (race regression at the exact real-browser ordering, wheel-down never detaches, touchmove self-heal, up-key detach, gutter-vs-content pointerdown, momentum-ignored + deadline-snap with fake timers, post-landing wheel-up detaches again); `AgentChatPanel` +1 (the race at the panel surface: wheel-up → chunk → pill appears, position held)
- Verification evidence: frontend `npx jest` full → 836 passed (76 suites), up from the dev/78 829 baseline by exactly the 7 net new tests; `tsc --noEmit` nothing new in touched files
- Commit/PR: `COMMIT-62c36c17` (single squash per memo §9's allowance — both fixes interleave in the same functions), docs commit (this entry + memo flip)
- Issues/regressions discovered: memo/BL numbering collision with the parallel dev/78 code-copy work — resolved by renumbering this change to dev/79 before committing (hook comments and test names updated; the copy-button suite's dev/78 references untouched)
- Follow-up work: if a genuinely non-agent transcript ever adopts the hook, revisit the LTR-only scrollbar-gutter check and promote per dev/76's escape hatch

## BL-P5-20260818-25: Chat run-status feedback — live processing strip + session token counter (memo dev/80)

- Date / author: 2026-08-18 / Karla
- Status: verified (delivered as unstaged edits; commit plan in memo §9)
- Requirements: the owner's directive — the final assistant message must never appear without an execution status; while work is ongoing show a concise follow-up indicator (left: elapsed time + minimal animation + rotating "Cooking/Baking" label); when complete clearly indicate finished + any remaining follow-up; right side near the input shows cumulative session token usage, both updating continuously; consistent across agent and delegated-agent chats (Claude/Cursor-style); keep the existing send-button loading animation
- Design decisions/artifacts: memo `dev/80`
- Tasks: `TASK-P5-chat-run-status-feedback`
- Risks/questions: live-token updates without estimates (dev/37 Actuals-only) — resolved with an additive `usage` SSE event carrying the run's cumulative provider-reported sums once per tool-loop round; ordering of "final text" vs "finished status" — resolved by finalizing the provider's run status in the same synchronous block as the turn landing (one React 18 batched commit), ordered status-first as defense-in-depth so an unbatched world could only ever show "done beside partial text", never "final text beside running"
- Design-to-code decision or deviation: **(1) Provider-owned `runStatus` map** (`AgentAttachmentsProvider`): `{phase running|done|error, startedAt, durationMs?, usage?, liveUsage?}` keyed per attachment with a seq guard against a stale run's late events; survives panel close/reopen and cycling. **(2) Deviation — no `followUp` field on runStatus** (memo §3.1 had one): the "Awaiting your review" chip derives from the attachment's proposal mirrors (`activeProposal`/`planProposal` status `pending`) instead — self-clearing on apply/dismiss, no duplicated state, and the rehydrated path gets it for free. **(3) Status strip** sits between the suggested-prompts row and the footer (outside the scroller — the dev/75 hook untouched): left `AgentRunStatusLine` (pulsing tinted dot, deterministic label rotation `floor(elapsed/5s) % 6`, tabular-numeral elapsed; done "✓ Finished in 12s · 1.4k tokens"; error "Failed after 8s"; polite live region announcing phase transitions only, visible ticking spans aria-hidden), right `AgentSessionTokenCounter` (sum of persisted `execution.usage` + in-flight `liveUsage`; renders nothing when nothing reported). **(4) Rehydrated fallback** — without a live status, `deriveRunStatusDisplay` reads the LAST agent turn's persisted execution record (dev/37) so a reopened transcript never ends in a bare final message; pre-dev/37 history yields no fabricated status. **(5) Send-busy semantics** — `runStatus` prop `undefined` = unwired (panel-local `sending` governs: tests/previews); `null` = wired-and-idle (provider governs, keyed per attachment) — this fixes the pre-existing leak where cycling ‹ › mid-run carried the local `sending` flag into another agent's chat; the `…` glyph is unchanged. **(6) Backend additive envelope** — `usage` events per provider round (skipped when the round reported none), `durationMs` on the stream `done` AND the blocking `/run` response (both read straight off the persisted `_execution_record`); the SSE route's generic dict pass-through needed no change
- Files/modules changed: frontend `components/agents/attach/agentRunStatus.ts` (new), `useRunTicker.ts` (new), `AgentRunStatusLine.tsx` + `.module.css` (new), `AgentSessionTokenCounter.tsx` + `.module.css` (new), `AgentAttachmentsProvider.tsx`, `AgentChatPanel.tsx` + `.module.css`, `AgentDockOverlay.tsx`, `useAgentAttachments.ts`, `api/agentsApi.ts`; backend `app/agents/services.py`, `app/agents/routes.py` (docstring)
- Tests added/updated: `tests/attach/agentRunStatus.test.ts` (new, 15 — formatters, session totals incl. no-fabricated-zero and interim merge, display derivation matrix); `tests/attach/useRunTicker.test.tsx` (new, 6 — tick/rotation/resume/reduced-motion/no-interval/unmount, fake timers); `AgentAttachmentsProvider.test.tsx` +6 (running→done lifecycle with duration+usage, liveUsage from `usage` events dropped on finalize, error, blocking-fallback done, clearConversation prune, and the ordering regression: no render commit ever contains the execution-carrying final turn with a non-done phase); `AgentChatPanel.test.tsx` +11 (running strip, glyph+disable, cycling-leak regression, done/error strips, rehydrated derivation + counter, review chip, live counter, pristine no-strip, pre-dev/37 no-status); backend `test_routes.py` stream assertions extended (interim `usage` event with Actual sums, `done.durationMs` equal to the persisted record's, no `usage` event when the provider reports none)
- Verification evidence: frontend `npx jest` full → 873 passed (78 suites), up from the dev/79 836/76 baseline by exactly the 37 net new tests; `tsc --noEmit` clean (the two pre-existing tsconfig deprecation notices only); backend `pytest tests/test_agents/` → 634 passed
- Commit/PR: pending — working tree delivered unstaged by request; suggested breakdown in memo §9
- Issues/regressions discovered: pre-existing cycling leak of the panel-local `sending` flag (see decision 5) — fixed in-scope as memo §1 planned
- Follow-up work: `LLMChat` has no run-status feedback (same posture as the dev/77/78 follow-ups: adopt the shared pieces, don't duplicate); the Solve strip's batch progress could adopt `AgentRunStatusLine` for visual unity as polish

## BL-P5-20260818-26: Run status made per-reply; token counter per message AND accumulated (memo dev/80 amendment)

- Date / author: 2026-08-18 / Karla
- Status: verified (delivered as unstaged edits, amending BL-P5-20260818-25 in the same working tree)
- Requirements: the owner's follow-up on the -25 delivery — the finished/pending status with elapsed time must ride each agent message reply (not a global strip line); the token counter must show both per message and the total accumulated
- Design decisions/artifacts: memo `dev/80` (amendment note in the Status header)
- Tasks: `TASK-P5-chat-run-status-feedback`
- Risks/questions: where the live indicator lives before the reply's first delta (the streaming turn doesn't exist yet during tool rounds / the blocking fallback) — resolved with a standalone pending row at the transcript tail that hands off to the streaming turn's own meta line at the first delta
- Design-to-code decision or deviation: **(1) Per-reply meta line** — the agent row gains a bubble+meta column (`.agentCol`/`.turnMeta`); `AgentRunStatusLine` is reused per message: the in-flight reply shows the live indicator (dot + rotating label + elapsed), every finalized reply shows "✓ Finished in Xs · Nk tokens" from its OWN persisted execution record (hover = that run's in/out breakdown), error turns show "Failed" (elapsed-at-failure joined from the live run record for the just-failed reply, since client error turns carry no execution). **(2) `deriveRunStatusDisplay` → `turnStatusDisplay`** — the transcript-level fallback derivation is gone; per-turn derivation subsumes it, and the newest reply still falls back to the provider's run record when an old server sent no execution fields (the never-a-bare-final-message guarantee holds per message now). **(3) Review chip** marks only the newest reply's meta. **(4) The strip** by the composer now carries ONLY the accumulated session total, right-aligned (persisted Actuals + in-flight interim sums, unchanged); per-reply statuses being inside the scroller is correct here — a reply's status belongs to its message and scrolls with it, and the dev/75 hook needed no change (metas render in the same commit as their turns). **(5) Provider untouched** — the runStatus map, seq guard, usage events, and busy semantics from -25 all carry over
- Files/modules changed: frontend `components/agents/attach/agentRunStatus.ts`, `AgentRunStatusLine.tsx`, `AgentChatPanel.tsx` + `.module.css` (backend and provider untouched this round)
- Tests added/updated: `agentRunStatus.test.ts` — the derivation describe rewritten for `turnStatusDisplay` (5: finished from own record, review flag opt-in, error with/without record duration, null-usage no fabricated tokens, pre-dev/37 + user turns null); `AgentChatPanel.test.tsx` dev/80 block rewritten (14: standalone pending row before first delta, indicator rides the streaming reply with no duplicate row, per-reply duration+tokens across multiple replies, cumulative counter vs per-reply figures, glyph+disable, cycling-leak regression, old-server fallback, failed reply with elapsed-at-failure, rehydrated error, review chip on newest reply only, live counter with interim sums, pristine no-strip, pre-dev/37 no-status)
- Verification evidence: frontend `npx jest` full → 874 passed (78 suites); `tsc --noEmit` clean (the two pre-existing tsconfig deprecation notices only); backend unaffected (no backend files touched; -25's 634-pass result stands)
- Commit/PR: pending — working tree delivered unstaged by request (fold into the memo §9 breakdown's UI commit or a follow-up commit)
- Issues/regressions discovered: none
- Follow-up work: unchanged from -25 (LLMChat adoption; Solve strip visual unity)

## BL-P5-20260818-27: Dataset-ref ownership + install-replace semantics (memo dev/81, closing dev/65 Fixes 3+2)

- Date / author: 2026-08-18 / Karla
- Status: verified
- Requirements: the two remaining approved dev/65 fixes — an install fully REPLACES any existing ref for the same datasetId (no dict-merge; fat refs converge to lean, no stale `publishedToHub`/metadata survives a reinstall), and `spec.dataflow.datasets` becomes backend-owned so a canvas save can never resurrect an uninstalled ref or drop a fresh install (the last-writer-wins clobber)
- Design decisions/artifacts: memo `dev/81` (operationalizes dev/65 §Fix 2/3 against the 2026-08-18 tree; dev/65 Fix 1 was `f857e3f3`)
- Tasks: `TASK-P5-dataset-ref-ownership`
- Risks/questions: whether the client could stop serializing the section entirely (the dev/65 §Fix 2(a) design) — no: "Save a copy" (`saveAsNewProject`) and trill import (`loadParsedTrill` → `incomingDatasets`) legitimately seed a NEW project's refs through the client spec, so ownership is enforced server-side instead (memo deviation D1)
- Design-to-code decision or deviation: **(1) Fix 3 — replace, not merge**: `install_dataset` filters out the old ref and appends `_ref_from_item`'s output verbatim; `publishedToHub` is re-derived from the hub-registry row at listing time (dedup merge), an explicit unpublish `False` still suppresses it, `installedAt` is always the reinstall time. **(2) Fix 2 — update ignores, create seeds**: new `preserve_dataset_refs` (datasets application layer, mirroring `preserve_agent_state`) runs inside `update_project`'s `spec_write_lock` and overwrites the client-sent section with the on-disk one (`[]` when the on-disk dataflow has none; client section kept only when NO spec exists on disk — the seed rule); sink-ref pruning still runs after it. **(3) Dedicated section writer**: `replace_refs` no longer round-trips a full spec through `update_project` (which would now undo itself) — `projects/storage.py::replace_dataflow_datasets` swaps only the datasets section under the lock (modeled on the packages `_write_lockfile`), and the service wrapper bumps the project row's timestamp without rewriting the manifest. **(4) Dead code deleted, not re-pointed (deviations D2/D3)**: `merge_dataflow_dataset_ref` (dead, contained its own `{**existing,**ref}` merge bug) removed; the callerless client staging path (`persistInstalledDataset`, `applyInstalledDatasetToProject`, `buildInstalledDatasetRef`, `upsertDataflowDatasetRef`, `InstalledDatasetPayload`/`DataflowDatasetRef`) removed — the live execution path (`persistDataflowForInstall`) is untouched, and `syncDatasetsFromSavedSpec` re-hydrates the mirror from every save's authoritative response
- Files/modules changed: backend `datasets/application/mutations.py`, `datasets/application/ref_ownership.py` (new), `datasets/repositories/installed.py`, `projects/services.py`, `projects/storage.py`, `packages/services.py` (comment); frontend `hook/useWorkflowOperations.ts`, `providers/FlowProvider.tsx`, `services/datasetCatalog/datasetCatalogApi.ts`
- Tests added/updated: backend `test_install_replace_semantics.py` (new, 4: fat-ref convergence + installedAt, idempotent double install, badge survives reinstall via hub row, explicit unpublish stays suppressed), `test_dataset_ref_ownership.py` (new, 6: no-resurrection and no-drop regressions, outputs-only untouched, create seeds the client section, section writer touches only datasets + timestamp, 404 on missing spec), `test_computed_catalog_api.py` publish-keeps-install test re-seeded through `replace_refs` (intent preserved), `test_projects/test_storage.py` merge tests → `replace_dataflow_datasets` tests (3); frontend `useWorkflowOperations.installSync.test.ts` dead describe removed, +2 (mirror re-hydration from the update response, concurrent `persistDataflowForInstall` one-create), `datasetCatalogApi.test.ts` dead-helper describes removed
- Verification evidence: backend `pytest tests/ --ignore=tests/test_frontend` full → 1177 passed, 8 skipped (test_frontend needs live server ports — pre-existing environmental); frontend `npx jest` full → 864 passed (78 suites; the dev/80 874 baseline minus exactly the 10 net-removed dead-helper tests); `tsc --noEmit` clean (the two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-00310cb6` (Fix 3), `COMMIT-70d54177` (Fix 2 backend), `COMMIT-c5ac63eb` (Fix 2 frontend cleanup), docs commit (this entry + memo flip)
- Issues/regressions discovered: pre-existing (NOT introduced, NOT fixed): two concurrent installs of *different* datasets can lose one — `install_dataset`'s `list_refs` → `replace_refs` read-modify-write spans two lock acquisitions; the old `update_project` round-trip had the identical window. Fix would move the filter+append inside the section writer's lock (a mutate-callback variant of `replace_dataflow_datasets`)
- Follow-up work: the concurrent-installs read-modify-write above if multi-tab installs become a real pattern; dev/65 is now fully closed (Fixes 1+2+3 landed)

## BL-P5-20260818-28: Atomic dataset-ref mutations — mutate-callback section writer (memo dev/82)

- Date / author: 2026-08-18 / Karla
- Status: verified
- Requirements: the owner's directive — fix the concurrent-install race recorded as BL-P5-20260818-27 follow-up with the mutate-callback variant: no dataset-ref mutation may lose a concurrent mutation of a different dataset to a stale pre-lock read
- Design decisions/artifacts: memo `dev/82`
- Tasks: `TASK-P5-atomic-ref-mutations`
- Risks/questions: whether only install raced — no: five sites shared the `list_refs` → `replace_refs` two-lock-acquisition window (install, uninstall, publish's ref stamp, unpublish's ref revert, delete's cross-dataflow cleanup); all five converted so the invariant is uniform
- Design-to-code decision or deviation: **(1) One primitive**: `storage.mutate_dataflow_datasets(user_key, project_id, mutate)` re-reads the spec under `spec_write_lock`, hands the dict-filtered refs to the callback, persists its return — or writes nothing when it returns `None`; returns `(spec, changed)`. `replace_dataflow_datasets` (storage + service) and `replace_refs` (repository) all became constant-callback delegates, keeping one lock/read/write path. **(2) Callback contract**: pure list transform, runs while the lock is held (documented on the primitive — no re-entrant project I/O). **(3) `changed` gates side effects**: the service bumps the project-row timestamp + commits only on real change, so a failed uninstall (404) or a no-match publish stamp no longer rewrites the spec or churns "Recent" ordering. **(4) Closure capture** where the caller needs pre-lock state: uninstall captures the removed ref (its post-404 imported-store cleanup and `{"datasets": …}` response now read from the writer's returned spec), delete captures per-dataflow hits for `removedFrom`
- Files/modules changed: backend `projects/storage.py`, `projects/services.py`, `datasets/repositories/installed.py`, `datasets/application/mutations.py` (five call sites)
- Tests added/updated: `test_projects/test_storage.py` +4 (callback sees dict-filtered current refs and its result persists, `None` leaves the spec file byte-identical with `changed=False`, missing project → `None`, and the race regression: 24 barrier-synchronized threads appending distinct refs all survive — the deterministic port of the deleted `merge_dataflow_dataset_ref` concurrency test); `test_datasets/test_dataset_ref_ownership.py` +1 and extended (no-op uninstall 404s without writing the spec or bumping the row's `updated_at` — observed on the DB row directly since the project GET bumps it via `touch_last_opened`; `mutate_refs` 404s on a missing spec alongside `replace_refs`)
- Verification evidence: backend `pytest tests/ --ignore=tests/test_frontend` full → 1182 passed, 8 skipped (the dev/81 1177 baseline plus exactly the 5 new tests; test_frontend remains env-bound); frontend untouched
- Commit/PR: `COMMIT-bfca63d5`, docs commit (this entry + memo)
- Issues/regressions discovered: `uninstall_dataset`'s response still referenced the pre-conversion `next_refs` local (caught by the ownership suite's 500) — now derived from the writer's returned spec; the project GET route bumps `updated_at` (`touch_last_opened`), which invalidated the first draft of the timestamp assertion
- Follow-up work: none — the BL-27 follow-up is closed; dev/65 and its descendants are fully landed

## BL-P5-20260818-29: Chat-UX polish pair — Solve-strip status unity + unread count on the Jump pill (memo dev/83)

- Date / author: 2026-08-18 / Karla
- Status: verified
- Requirements: the two recorded chat-UX polish follow-ups — the Dataflow Builder strip's batch progress adopts `AgentRunStatusLine` for one status language with the dev/80 per-reply meta lines (dev/80 follow-up), and the Jump-to-latest pill carries a count of messages landed while the user is scrolled away (dev/75 follow-up)
- Design decisions/artifacts: memo `dev/83`
- Tasks: `TASK-P5-chat-ux-polish-pair`
- Risks/questions: whether the strip could show true batch elapsed — no: `builderSession` persists no batch start timestamp, so elapsed is strip-local observation time (the dev/80 client-measured posture; a panel reopened mid-run shows time since it observed the batch, nothing fabricated); whether the count needed turn-role awareness — no: the user's own send re-pins via `pinToLatest`, so user turns can't accumulate unseen, and the hook stays a number-in/number-out contract
- Design-to-code decision or deviation: **(1) Additive `AgentRunStatusLine` props** — `runningLabel` (fixed label; the rotating Cooking/Baking set is reply-flavored), `runningDetail` (suffix inside the aria-hidden ticking span), `srLabel` (live-region text); absent props render dev/80 output byte-identically, pinned by a regression test. **(2) Strip adoption** — one `activeBatchLabel` derivation (Solving / Building / Stepping) replaces the static `solving…` note; fraction `done/total nodes` counts terminal node states (`solved|failed|skipped`) from the existing `nodeRuns` ⊕ `solveProgress` overlay; running-variant only — phase chips, per-node rows, notice/error hints, and the simulation narration line all untouched; `.solvingNote` CSS deleted. **(3) The unread count lives in `useTranscriptAutoScroll`** — optional `itemCount` (the panel passes `turns.length`), baseline snapshotted on the pinned→detached transition inside `setPinned`, `unreadCount` as a pure render-time derivation keyed off `atBottom`: every re-pin path (bottom return, jump, send, resetKey, hydration) resets for free and the dev/75 zero-per-chunk contract holds (chunk growth replaces the last turn without changing the count). **(4) Pill label** — "Latest" ↔ "N new" (display cap 99+), accessible name carries the real number; positioning/focus-fallback behavior untouched. Delegated-agent chats inherit both for free (dev/72 — same panel); LLMChat untouched (dev/76)
- Files/modules changed: frontend `components/agents/attach/AgentRunStatusLine.tsx`, `useTranscriptAutoScroll.ts`, `TranscriptJumpButton.tsx`, `AgentBuilderStrip.tsx` + `.module.css`, `AgentChatPanel.tsx`
- Tests added/updated: `tests/attach/AgentRunStatusLine.test.tsx` (new, 6 — the dev/80 no-props regression, fixed label never rotates, detail suffix, srLabel, aria-hidden ticking span contract, done/error ignore the running props); `TranscriptJumpButton.test.tsx` (new, 6 — hidden, dev/75 no-count regression, 0 stays "Latest", singular/plural accessible names, 99+ display cap with the real number in aria); `useTranscriptAutoScroll.test.tsx` +8 (count/reset matrix incl. streamed-chunk immunity, re-detach baseline, shrink clamp, omitted-itemCount inertness); `AgentBuilderStrip.test.tsx` +4 (solving line with terminal-state fraction + note-gone regression, no line when idle, Building without fraction, Stepping); `AgentChatPanel.test.tsx` +3 ("1 new" accessible name, accumulate → jump reset → fresh-detach baseline, chunk growth stays "Latest")
- Verification evidence: frontend `npx jest` full → 891 passed (81 suites; the dev/81 864 baseline + exactly the 27 new tests); `tsc --noEmit` clean (the two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-21f54ed2` (primitives), `COMMIT-5c0c6f82` (strip adoption), `COMMIT-656c9ea7` (badge wiring), docs commit (this entry + memo flip)
- Issues/regressions discovered: none in code; one test-authoring wrinkle — the panel's jump-then-redetach flow needs the jump's landing scroll event emulated explicitly (the dev/75/79 suppress-flag handshake), same as the hook suite already did
- Follow-up work: none new; the dev/75 unread-badge and dev/80 Solve-strip follow-ups are closed. Remaining open chat-UX items are the LLMChat-parity decision (dev/76 tension) and the tsconfig deprecation fix

## BL-P5-20260818-30: agent.package-recommendation — the deferred 14th releasable built-in ships (memo dev/84, spec dev/16 / DEC-035)

- Date / author: 2026-08-18 / Karla
- Status: verified
- Requirements: dev/16's identify → suggest → reviewed-install contract — the build agents identify required node packages and surface each required-but-uninstalled one as a reviewed proposal with a why-needed rationale; installs route through the EXISTING package flow (permissions + dependencies + conflicts, per-project lockfile); never silent, never agent-authored, built-ins (`curio.builtin@*`) never proposed
- Design decisions/artifacts: memo `dev/84` (grounds dev/16 in the current machinery; deviations D1–D4), `DEC-030`/`DEC-035` context; closes the deferrals recorded at `builtin.py:162/199`
- Tasks: `TASK-P5-package-recommendation`
- Risks/questions: how the InstallPermissionsDialog requirement composes with "the apply endpoint is the only execution authority" — resolved: the dialog is the review surface (frontend opens it BEFORE posting the apply; its Install button IS the apply trigger), the endpoint re-checks conflicts server-side regardless and treats a conflicting/absent package as drift (409 + stale)
- Design-to-code decision or deviation: **(1) Roster entry** (18th built-in): capabilities `package.recommend`/`package.identify`, node+canvas targets, delegates to `agent.syntax-analysis-agent` (import extraction, never guessed), net-new `package_recommendation_instruction.txt`, review-before-apply. **(2) Domain-owned read surfaces**: `packages/services.py::agent_catalog_overview` (installed = the PROJECT lockfile; builtin flagged) + `agent_resolve_report` (drawer-shaped probe: user store ∪ requested, catalog-manifest overrides, per-package permissions/deps + conflicts) — the agents tools (`packages.catalog`, `packages.resolve`) stay thin wrappers (ADR-AG-007). **(3) `package.install` proposal lane**: mint validates against the catalog (unknown → corrective error; builtin → refused with "always present"; installed → honest state, no dead proposal; bounded `reason` becomes the card preview); `_apply_package_install` re-resolves, conflicts/absence → `_mark_stale` + 409, else `install_to_project` (the packages service's own lockfile write + dep provisioning under its spec lock) and the standard applied-turn card. **(4) Parent wiring**: node-builder + dataflow-builder append the delegate; **Connection Builder is the ONE migrated manifest gaining `delegatesTo`** (dev/16 §3.3 addendum; the dev/48 thirteen-manifest byte-parity regression amended for exactly it — deviation D4). **(5) Frontend dialog-gated apply**: `usePackageInstallReview` (agents/attach, per dev/76; imports the shared `InstallPermissionsDialog` — genuine cross-feature reuse) loads the catalog row + the drawer-shaped conflict probe, opens the dialog, and holds `beginReview`'s promise open until the flow settles — apply failure rejects into the review card's existing error line, cancel resolves with the proposal still pending; the card's `package.install` effect line states the dialog gate. Deviations D1–D3 (roster-generated manifest/foreground, tool-served installed flags, no settingsDefaults) landed as memo'd
- Files/modules changed: backend `packages/services.py` (agent read surfaces), `agents/tools.py` (3 contracts + 2 executors), `agents/services.py` (mint + apply + MUTATE_PROPOSAL_TOOLS), `agents/builtin.py` (roster entry + 3 delegatesTo), `utk_curio/llm-prompts/package_recommendation_instruction.txt` (new); frontend `components/agents/attach/usePackageInstallReview.ts` (new), `AgentChatPanel.tsx` (dialog host + apply branch), `components/agents/content/AgentReviewCard.tsx` (effect line); `docs/AGENTS.md` (roster = 18)
- Tests added/updated: backend `test_builtin.py` (roster 18, `TestPackageRecommendation` manifest surface, D4 parity amendment asserting Connection Builder's one-key change), `test_tools.py` (registry set + `TestPackageTools`: real-catalog rows with project-lockfile install state + builtin flags, q filter, resolver-grounded permissions/deps, bad-params/unknown errors), `test_routes.py` (`TestPackageRecommendationTools`, 6: catalog tool grounding, mint→apply writes the project lockfile with the rationale on the card, builtin/unknown/installed mint refusals, conflict-at-apply 409+stale, package-gone 409, injection resistance — prose never installs), `test_delegation.py` (`TestPackageRecommendationDelegation`, 3: parents declare it, both capabilities resolve current-project-only, missing template = missing-specialist); frontend `usePackageInstallReview.test.tsx` (new, 6: probe shape, 409-body conflicts, confirm-fires-apply + deferred settle, cancel-keeps-pending, apply-failure rejects into the card, gone-from-catalog rejects pre-dialog), `AgentChatPanel.test.tsx` +3 (Apply opens the dialog and only Install fires the apply, Cancel keeps the proposal actionable, non-package proposals regression: direct apply, no dialog, no catalog fetch)
- Verification evidence: backend `pytest tests/test_agents/ tests/test_packages/` → 865 passed, 4 skipped (progression 853→859→862→865 across the commits); frontend `npx jest` full → 900 passed (83 suites; the dev/83 891 baseline + exactly the 9 new tests); `tsc --noEmit` clean (the two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-577315d8` (tools + roster + prompt), `COMMIT-cff987df` (proposal lane), `COMMIT-60d8e6e4` (parent wiring + D4), `COMMIT-8b847211` (frontend dialog flow), docs commit (this entry + memo flip + AGENTS.md)
- Issues/regressions discovered: none in code; test-authoring notes — the chat panel itself is `role="dialog"`, so the install dialog is queried by its title text, and the deferred-rejection test must attach its expectation before confirm settles the promise
- Follow-up work: the dev/16 remainder stays as scoped — Validation/Optimization agents (OQ-011) and the generated-content evaluator (OQ-007) remain open product decisions; `connection` attach target still deferred; consider a Dataflow Builder plan-step surfacing of package recommendations once real usage shows the shape

## BL-P5-20260818-31: OQ-007/OQ-011 resolved (DEC-055/DEC-056, memo dev/85) — the generated-content evaluator ships (memo dev/86)

- Date / author: 2026-08-18 / Karla
- Status: verified
- Requirements: the owner's approval of decision memo dev/85 — DEC-055: `agent.generated-content-evaluator` is AUTHORED, not migrated, to an approved contract (the "supplied and reviewed" event OQ-007 demanded); DEC-056: the Validation concept card is a category view over the shipped family (no separate agent) and Optimization is descoped demand-driven (re-open: real demand AND per-node duration evidence in the runtime journal). DEC-056 requires no code
- Design decisions/artifacts: memos `dev/85` (decision) + `dev/86` (implementation); `DEC-055`, `DEC-056` minted into the dev/03 tables; closes the dev/57-parked LLM-verification-agent request
- Tasks: `TASK-P5-generated-content-evaluator`
- Risks/questions: RISK-EVAL-001 (false certification) — resolved structurally: report-only review policy, read-only evidence tools, a verdict STRICTLY derived from findings, DEC-054's simulation auto-approve stays exclusively empirical, and DEC-028's platform-quality firewall is untouched; the honesty rules ride the prompt (never-executed nodes have no execution evidence; ungroundable claims are uncertainty, never findings)
- Design-to-code decision or deviation: **(1) Roster entry** (19th built-in): capability `content.quality.evaluate`, category evaluate, node+canvas targets, reads `nodeContext`/`targetContext`, tools `node.read`/`node.runtime.read`/`dataflow.read`, report-only, delegates-free — deliberately the migrated-manifest shape, so the dev/48 byte-parity regression covers it UNAMENDED (contrast dev/84's D4). **(2) Net-new prompt** `evaluate_generated_content_prompt.txt` (the long-documented filename) encoding the dev/85 §4 contract: two modes (node vs canvas), `[blocker|warn|note] claim — quoted evidence` findings, `fits`/`fits-with-warnings`/`does-not-fit` derived verdicts, advisory-only + never-approve + empirical-primacy rules. **(3) Delegation**: node-builder + dataflow-builder append the evaluator as an optional post-generation check (Connection Builder untouched — dev/85 scoped composition to the two generators). **(4) Docs as decided**: dev/03 gains the DEC-055/056 rows, OQ-007/011 close, the `evaluation-disabled` profile row retires; `builtin.py`'s "intentionally absent" docstring flips to the DEC-055 reference; AGENTS.md roster = 19
- Files/modules changed: backend `agents/builtin.py`, `utk_curio/llm-prompts/evaluate_generated_content_prompt.txt` (new); docs `docs/AGENTS.md`, `dev/03` tables; no frontend changes (the agent rides every existing built-in surface)
- Tests added/updated: `test_builtin.py` (roster 19; `test_evaluator_excluded` inverted to `test_evaluator_authored_under_dec055`; `TestGeneratedContentEvaluator`: manifest surface incl. read-only tools + delegates-free + report-only, instruction carries the verdict vocabulary/severities/advisory/never-executed rules; composites' delegate lists), `test_routes.py` (catalog 19), `test_delegation.py` (`TestEvaluatorDelegation`, 3: generators declare it, `content.quality.evaluate` resolves current-project-only when installed, missing = not-installed/never assumed)
- Verification evidence: backend `pytest tests/test_agents/ tests/test_packages/` → 865 passed after commit 1, 868 passed after commit 2 (4 skipped); frontend untouched (dev/84's 900/83-suite jest baseline stands)
- Commit/PR: `COMMIT-52863552` (roster + prompt), `COMMIT-549b9d34` (delegation wiring), docs commit (this entry + dev/85/86 flips + dev/03 + AGENTS.md)
- Issues/regressions discovered: **correction to BL-P5-20260818-30**: its verification line claimed "865 passed (progression 853→859→862→865)" — the measured backend number at dev/84's close was 862 (commit 4 was frontend-only; no backend run followed commit 3's 862). The 865 figure was a projection, not a measurement; recorded here per the log's append-only rule. Today's 865 (dev/86 commit 1) is coincidental: 862 + the 3 new evaluator tests
- Follow-up work: none for the roster — it has no remaining blocked or unspecified entries; OQ-008 (retention policy) is now the program's only open question. Optimization's re-open conditions are on record in DEC-056 (per-node durations in the journal remain a dev/67-7 follow-up)

## BL-P5-20260819-32: OQ-008 resolved (DEC-057/DEC-058, memo dev/87) — retention declaration, ledger sweep, truthful deletion copy (memo dev/88)

- Date / author: 2026-08-19 / Karla
- Status: verified
- Requirements: the owner's approval of decision memo dev/87 — DEC-057: lifecycle-bound deletion as THE deletion model, retention durations only by operator declaration (never invented; undeclared stated as undeclared), truthful live-store deletion language with the declared backup posture surfaced verbatim, operator-executed closure procedure with unpublish-by-default, the guest-commingling reality stated where guests enter; DEC-058: the governance-artifact lifecycle contract recorded as build input for the v2 surface (no code)
- Design decisions/artifacts: memos `dev/87` (decision) + `dev/88` (implementation); `DEC-057`/`DEC-058` minted into the dev/03 tables; OQ-008 closed — the production-release block is now mechanical (a declared backup posture in `.curio/agents-retention.json`)
- Tasks: `TASK-P5-retention-dec057`
- Risks/questions: could the sweep ever violate the ledger's append-only truth — no, structurally: archival is `shutil.move` of whole day files into `ledger/archive/` (byte-identical, collision = skip + loud warning, `.lock`/non-day files untouched), and with no declaration the sweep is a no-op
- Design-to-code decision or deviation: **(1) `agents/retention.py`** (the `pricing.py` deployment-file pattern: read-per-call, missing/corrupt ≡ `{}`): schema `backups: "none"|{expiryDays}`, `ledger.archiveAfterDays`, `closure.graceDays`; invalid values read as undeclared, never guessed; unknown top-level keys warn loudly ("declared-but-unenforced must never be silent"); `run_retention_sweep()` wired at server startup beside `reconcile_guest_projects`. **(2) The declaration rides the existing bootstrap** — `GET /api/config/public` gains `retention: public_declaration()` (null = undeclared); no new endpoint. **(3) `services/retentionCopy.ts`** — module cache seeded from the UserProvider bootstrap; `permanentDeletionNotice()` = the live-store scope sentence + the posture line (declared-none → "the data is gone", declared-expiry → "up to N days", undeclared → said plainly); adopted by the ProjectsList Delete-forever confirm and the dataset-drawer account-level Delete confirm. **(4) Grounding note**: the Archive / Delete-forever split already existed in `ProjectsList.tsx` — §3.4(a)'s reachable purge needed no code, only the copy. **(5) Guest notice** under Continue-as-Guest (`GUEST_COMMINGLING_NOTICE`). **(6) `docs/RETENTION.md`** — the class/lifecycle tables, the config schema, the operator closure procedure (grace window, unpublish-by-default with recorded override, store+DB purge), the guest reality, and the production-gate checklist
- Files/modules changed: backend `agents/retention.py` (new), `server.py` (startup sweep), `users/routes.py` (config block); frontend `services/retentionCopy.ts` (new), `providers/UserProvider.tsx`, `pages/projects/ProjectsList.tsx`, `components/datasets/catalog/useDatasetCatalogDrawer.ts`, `components/AuthForm/AltAuthBox.tsx`; docs `docs/RETENTION.md` (new), `dev/03` tables
- Tests added/updated: backend `test_retention.py` (new, 10 — absent/corrupt ≡ defaults, declared values in the public shape, invalid values read undeclared, unknown-key loud warning, undeclared sweep moves nothing, declared age archives only past-age files byte-identically with `.lock`/non-day/recent files untouched, archive collision never overwrites, missing users dir no-op, config route carries the block); frontend `retentionCopy.test.ts` (new, 7 — the three posture lines, invalid-declaration degradation, live-store notice composition, re-seed replacement, guest-notice content)
- Verification evidence: backend `pytest tests/test_agents/ tests/test_users/` → 701 passed; frontend `npx jest` full → 907 passed (85 suites; the dev/86-era 900 baseline + exactly the 7 new tests); `tsc --noEmit` clean (the two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-1bda6dee` (backend), `COMMIT-436487d6` (frontend), docs commit (this entry + dev/87/88 flips + `docs/RETENTION.md` + dev/03 OQ-008 row)
- Issues/regressions discovered: none
- Follow-up work: DEC-058 rides the future v2 governance-surface build (its lifecycle contract is now a recorded input); production release checklist item = the operator fills the `backups` declaration. Remaining open questions: OQ-009 (deployment topology) and OQ-010 (remote-provider data classification) — both deployment-gated, neither owned by this feature story's remaining work

## BL-P5-20260820-33: Agent package authoring shipped (DEC-059/DEC-060, memos dev/89 + dev/90) — Package Builder + isolated build service + prompt-driven looks + the Researcher

- Date / author: 2026-08-20 / Karla
- Status: verified
- Requirements: the owner's approvals of dev/89 (2026-08-19) and dev/90 (2026-08-20, incl. the mid-build course correction that superseded dev/89 commit 9 and amendments A1–A3) — DEC-059: package authoring is a NEW specialist (`agent.package-builder`, 20th built-in) over an isolated build service with exact-digest reviewed promotion as the only install authority; DEC-060: custom looks are prompt-driven (no first-party fixtures), `agent.researcher` (21st) owns the post-it recipe + web gathering, the Dataflow Builder stays byte-pinned (Follow-up D)
- Design decisions/artifacts: memos `dev/89` (10-component build service; Follow-ups A/B/C) + `dev/90` (Researcher topology, delegate-draft mint, A1 web tools, A2 trusted provider host, A3 public keyed-GET providers; Follow-up D); `DEC-059`/`DEC-060` minted into the dev/03 tables; `docs/EXTENDING.md` §8; roster docs at 21
- Design-to-code decisions or deviations: **(1)** dev/89 commit 9 shipped a first-party post-it fixture and was superseded in part by dev/90 commit 4 (fixture surface deleted, net −676 lines; the DOD is now `test_custom_look_dod.py` with test-local simulated model output — post-it + a dissimilar badge + a refused violating scenario). **(2)** `package.draft.apply` was declared in the dev/89 commit-1 roster ahead of its ToolContract (DEC-017 declaration-not-grant) and registered in commit 8. **(3)** The delegate-draft mint reuses `_mint_package_draft_apply` verbatim (the dev/73 one-mint-policy), gated on the PARENT's grant — Node Builder's authoring delegations stay text-only. **(4)** dev/89 commit 10 (docs) consolidated into dev/90 commit 5 (this entry)
- Files/modules: backend `packages/{build_models,build_staging,build_extension,build_workspace,build_jobs,build_deps,build_compiler,build_packager,build_pipeline,build_promotion,node_appearance}.py` (new), `agents/{builtin,tools,services,content,egress}.py`, `packages/dependency_scanner.py` (+.tsx/.jsx), prompts `package_build_instruction.txt` + `researcher_notes_instruction.txt` (new); frontend `utils/nodeAppearance.ts` + `components/NodeColorControl.*` (new), `utils/agentCanvasEvents.ts`, `hook/useCode.ts`, `TrillGenerator.ts`, `api/agentsApi.ts`, `components/agents/attach/{AgentAttachmentsProvider,useAgentCanvasMutations}.ts(x)`
- Verification evidence: backend `pytest tests/test_agents/ tests/test_packages/` → 1138 passed; frontend `npx jest` full → 940 passed (84 suites); `tsc --noEmit` clean (the two pre-existing tsconfig deprecation notices only); the recorded DOD scenarios cover the Researcher-delegated post-it flow and the weather-in-Paris web-search loop
- Commits: dev/89 — `382327cd` (memo), `28b02972`, `a42735a0`, `f1ddaeb3`, `c4dabe8f`, `160ddbc0`, `d236eddf`, `8560034d`, `a6b42cbf`+`8ab0fdb6` (commit 8 backend/frontend), `034965f5` (commit 9, superseded in part); dev/90 — `7ac48b50` (memo), `672e8a07`, `6fff01f1`, `088daa9e`, `bcabdc4f`, `652dab81` (A1), `b9953183` (A2), `dd24dda0` (A3), docs (this entry)
- Issues/regressions discovered en route (each fixed with a pinning test): the dependency scanner ignored `.tsx/.jsx`; detected `react` imports were blocked as bundleable deps (externals-import note now); `package.draft.apply` params outgrew the classic 1KB tail budget (draft-class budget + smuggling re-check); the deps-test FakeFetcher served non-tgz bytes the compiler phase could not extract; local search providers were refused by the egress policy (A2)
- Follow-up work: A (Curio package backend sandbox), B (activation/restart lifecycle), C (`node.appearance.write` agent recolor), D (Dataflow Builder → Researcher delegation); operator setup for live search = `CURIO_SEARCH_URL` (public keyed-GET provider or self-hosted SearXNG); the rich diff/SBOM/preview-screenshot review card remains deferred polish (the proposal card shows summary + counts today)

## BL-P5-20260821-34: Package backend sandbox shipped (DEC-061, memo dev/91) — dev/89 Follow-up A delivered as an on-demand, process-isolated handler runtime

- Date / author: 2026-08-21 / Karla
- Status: verified
- Requirements: the owner's approval of memo dev/91 (2026-08-21, with the §0.1 Option-3 venv posture explicitly adopted) — a permissioned, process-isolated runtime for generated server-side package code that never imports into Curio's host process, with a versioned request/response contract, per-invocation resource limits and termination, declared-permission review, audit logging, and honest failure surfaces
- Design decisions/artifacts: memo `dev/91` (incl. §0/§0.1 code-audit evidence: the legacy "sandbox" is warm `exec` with full builtins + inherited env; five pip paths into ONE shared interpreter; install-side upgrade blast radius; partial uninstall ref-counting; warm-cache staleness); `DEC-061` minted into the dev/03 table; `docs/EXTENDING.md` §8 backend paragraph rewritten; `docs/RETENTION.md` gains `packageBackend.ledgerArchiveAfterDays`
- Design-to-code decisions or deviations: **(1) On-demand, nothing resident** — one `build_workspace` worker per invocation reuses the proven dev/89 isolation primitive (scrubbed from-scratch env, rlimits with `limits_applied` honesty, killpg, capped pipes, sanitized diagnostics); Follow-up B deliberately narrows to resident services/draining/version routing. **(2) ONE contract module** (`backend_contract.py`, `curio.pkgbackend.v1`) consumed by route, runtime, child harness (copied per invocation), probe phase, and both prompt contracts — the dev/90 A15 two-spellings lesson applied up front. **(3) Probes answered at load+resolution** (the harness, not the handler) so health checks catch import/dep breakage without executing generated bodies. **(4) §0.1 Option 3**: per-package venv/overlay DEFERRED on cost evidence, but `CURIO_BACKEND_SANDBOX_PYTHON` + an `overlay_dir → PYTHONPATH` slot ship as invocation parameters (a live test proves an overlay module resolves in-worker). **(5) The entry-digest pin lives in a per-user `package-backend-pins.json`** written by BOTH install authorities (promote + catalog) rather than the project lockfile (a bare dir-name list); a backend-less reinstall clears stale pins. **(6) The scan is honest about regexes**: escape-hatch families block with file:line + fix, network blocks/warns per declared permission, `open()` deliberately unblocked (the runtime boundary is the jail). **(7) A well-formed `ok:false` reply is HTTP 200** — the envelope is the diagnosis; only infrastructure failures are 4xx/5xx. **(8) Frontend dispatch is registry-driven** (`NodeDescriptor.backendHandler` → `usePackageBackendRun`), fixing the §0 literal-keyed dispatch finding; the invoke API lives in its own light module after the first cut dragged the vega adapter graph into every executing component (lesson recorded in the module docstring)
- Files/modules: backend `packages/{backend_contract,backend_harness,backend_runtime,backend_policy}.py` (new), `packages/{manifest,installer,build_models,build_jobs,build_pipeline,build_packager,build_promotion,services,routes}.py`, `agents/{services,retention}.py`, prompts `package_build_instruction.txt`, schema `docs/schemas/node-package.v4.json` (additive `backend` + `backendHandler`); frontend `api/packageBackendApi.ts` + `hook/usePackageBackendRun.ts` + `utils/packagePermissions.ts` (new), `api/{agentsApi,packagesApi}.ts`, `registry/{types,packagesClient}.ts`, `components/editing/CodeEditor.tsx`, `components/agents/content/AgentReviewCard.tsx`, `components/packages/publishing/InstallPermissionsDialog.tsx`
- Tests added/updated: backend `test_backend_contract.py` (25 — envelope matrix, manifest surface, installer bucket, schema-parser agreement), `test_backend_runtime.py` (20 REAL-subprocess — success/probe/handler-error/env-scrub/net-guard/data-dir+cap/timeout/no-reply/nonzero-exit/import-error/404s/drift/bounds/503/Option-3 seam), `test_backend_build.py` (32 — scan matrix, declaration reading, real-worker probing phase, entry pin round-trip/drift/stale-clear), `test_backend_route.py` (6 — route matrix), `TestBackendDraftEndToEnd` (the whole lane incl. card trust edge + tamper 409), `test_backend_package_dod.py` (13 — violating drafts refused with finding+fix in the model's face, prompt + buildRequestContract markers), retention (+2); frontend jest: card backend block (3), packagesClient descriptor mapping (2), `usePackageBackendRun` (4)
- Verification evidence: backend `pytest tests/test_agents/ tests/test_packages/` → 1270 passed (4 skipped); frontend `npx jest` full → 956 passed / 86 suites (including the CodeEditor suite the first API-import cut broke — fixed by the light-module split); touched files `tsc`-clean (pre-existing type debt unchanged)
- Commits: `1f4fca57` (contract+manifest+installer+schema), `2adf412a` (runtime+harness+ledger+retention), `a68ed44a` (build service: scan, probing phase, provenance, pins), `664bc8c4` (route + review surfaces), `6059c151` (frontend dispatch), docs (this entry)
- Issues/regressions discovered en route (each pinned): user keys are guest-or-numeric (test constant); `os._exit(3)` lands in worker-failed, not no-reply (both branches now pinned separately); `packagesApi` re-exports the registry bootstrap → vega ESM in every importer's graph (invoke API split into `packageBackendApi.ts`)
- Follow-up work: dev/89 Follow-up B (resident services: activation, draining, version routing — now explicitly narrowed), Follow-up C (`node.appearance.write`), dev/90 Follow-up D (Dataflow Builder → Researcher delegation); §0.1 Option-2 overlay isolation as a contained hardening (parameter values on the shipped seam); secret mediation stays deferred and stated; per-handler daily quotas ride the invocation ledger when demanded
