# Build Log — P5: First-Release Agents & Orchestration

Child log for Phase 5 (see `../3.1-Agents-Catalog-Build-Log.md`). Entries follow the
Build Entry Template and are append-only.

---

## BL-P5-20260719-01: Built-in agent definitions (13 prompt-agent migrations) + Global Catalog source

- Date / author: 2026-07-19 / Karla
- Status: verified
- Requirements: `REQ-PROMPT-001` (each prompt behavior → a manifest-defined agent), `REQ-CAT-001` (browsable Global Catalog with real content), `REQ-CAP-001`
- Design decisions/artifacts: `DEC-008` (prompts→hookable agents), `SRC-MEMO-PROMPT-006` (`dev/06` canonical migration map), `DEC-040` (FS-backed)
- Tasks: `TASK-P5-builtin-roster`
- Risks/questions: `RISK-PROMPT-001` (prompt asset resolution), `RISK-PROMPT-003` (the blocked evaluator must not be fabricated)
- Design-to-code decision or deviation: author the 13 releasable built-ins as a **data-driven roster** (`app/agents/builtin.py`) generated from the `dev/06` map + the existing `utk_curio/llm-prompts/*.txt`, rather than 26 hand-written committed files — one source of truth, each entry validated through `parse_agent_manifest`. The Global Catalog scope lists these built-ins; Import/Install resolve a coord from the user store **or** the built-in roster. `agent.generated-content-evaluator` is intentionally excluded (no prompt file — blocked by `OQ-007`). Prompt-byte materialization into a user store (copying from `llm-prompts/`) is a follow-up; catalog cards + import/install coords don't need it.
- Files/modules changed:
  - `utk_curio/backend/app/agents/builtin.py` (new — 13-agent roster + manifest generation + coord resolution; `PROMPT_SOURCE_DIR` → `utk_curio/llm-prompts/`)
  - `utk_curio/backend/app/agents/services.py` (`list_global_catalog`; `_resolve_definition` = store ∪ built-in roster)
  - `utk_curio/backend/app/agents/routes.py` (`GET /api/agents/catalog`)
  - `utk_curio/backend/tests/test_agents/test_builtin.py` (new), `test_routes.py` (catalog + built-in import/install)
  - `docs/AGENTS.md` (§6 Global Catalog = 13 built-ins)
- Tests added/updated: `TEST-P5-builtin` = `tests/test_agents/test_builtin.py` (roster ↔ dev/06 map, prompt files exist, 13 manifests validate); catalog route tests in `test_routes.py`
- Verification evidence: `pytest test_agents/ test_llm_default_provider.py` → 79 passed. `GET /api/agents/catalog` returns 13 built-in cards; a built-in imports/installs without a prior store copy; evaluator absent.
- Commit/PR: `COMMIT-6412bff`
- Issues/regressions discovered: initial `PROMPT_SOURCE_DIR` used `parents[2]` (backend/) — fixed to `parents[3]` (utk_curio/); caught by `test_every_prompt_file_exists`.
- Resolution: path corrected; all prompt files resolve.
- Follow-up work: materialize built-in prompt bytes into the user store on install; orchestration/composite agents (P5 remainder, v2); imported-only Publish.
- Remaining risks/questions: built-in prompt path is nominal until materialization; categories/hooks assigned per a small mapping (refine against final UX).

**Amendment (2026-07-27, memo `dev/38`):** the roster manifests were missing the `dev/05` System-file column and `inputs` — every built-in now declares `prompts.system` (`default_preamble.txt`; the syntax agent its own preamble) and grounded `inputs.reads` (from each legacy call site's actual context); install materializes both assets; and the run path restores the legacy `preamble + prompt` composition (`dev/06` parity — an edited intent replaces the instruction portion only). Tests: `TestPreambleAndInputs`, `TestMaterializePreamble`, updated run-path assertions; 245 agents tests green.

---

## BL-P5-20260804-02: Node Builder — the first P5 composite (node.build + the delegation seam) (memo dev/48)

- Date / author: 2026-08-04 / Karla
- Status: verified
- Requirements: `REQ-ORCH-001` (missing specialist → reviewed Install-in-project, no auto-chaining), `REQ-REVIEW-001`/`DEC-006` (review-before-apply structural for the two new mutations), `REQ-PERM-001`/`DEC-017` (server-allowlisted contracts), `REQ-CAP-002` (granted ids pinned), owner node-creation policy (reuse-first; justified factory-backed creation fallback)
- Design decisions/artifacts: **`DEC-046`** (delegation as direct provider-port code, depth-1 structural; LangChain revisit narrowed to Dataflow Builder), `DEC-034` (partial: node-builder slice), memo `dev/48` (+ dev/49 consolidation: orchestration deltas DR-1…DR-5 stay with the future Dataflow Builder memo); recorded dev/15 deviations (canvas-only target, package-recommendation delegate deferred, no profile families)
- Tasks: `TASK-P5-node-builder`
- Risks/questions: the §1.3 apply/canvas clobber defect (shipped dev/41 behavior) found in the memo audit — fixed here for both mutations via the bridge; child-run quota abuse bounded by the child's own policy admission + the shared round budget
- Design-to-code decision or deviation: **(1) Roster** — `BuiltinAgentSpec` gains `delegates_to`/`review_policy` (defaults keep the thirteen migrated manifest dicts byte-identical, regression-tested); `agent.node-builder` (net-new `node_build_instruction.txt` beside the migration assets — same resolution/materialization). **(2) Reuse-first `node.create`** — the packages domain owns ALL template knowledge: `packages.services.available_templates` (seeded `curio.builtin@<highest-major>` + the project package lockfile, authorability flags); the run's tail lists available templates fresh per run; mint validates via the helper (unknown/non-authorable refused) and pins `{nodeType}` only (id server-minted at apply — no digest, drift target doesn't exist); apply re-validates (package uninstalled → 409 + `stale`), inserts right of the node extent under the spec write path, returns `createdNode`. **(3) `node.template.create`** — the justified creation fallback: mandatory justification (the review card is the adequacy gate), reuse-territory collisions refused at mint; apply drives the EXISTING factory (`build_packageage_archive` + `install_packageage_from_archive` + `install_to_project`) minting a namespaced `curio.agent.<slug>@1` single-template package, then inserts the first node — transactional, factory errors verbatim as 409 + `stale`. **(4) Delegation (DEC-046)** — `delegateRequest` part (exclusive, bounded, both-requests-in-one-tail invalid); `delegation.py` resolution (current-project lockfile ∩ `delegatesTo`, order = preference) + `run_delegate` (child's own prompts, NO tail instruction, reply never parsed — depth-1 structural; own pins/ledger pair under the child's policy with parent-attachment attribution; `parentExecutionId`); missing specialist → reviewed `project.install` proposal whose apply reuses `install_in_project` (idempotent re-apply); parent records gain `delegations`; SSE `delegate_requested/started/result`. **(5) Frontend bridge (§1.3 fix)** — `agentCanvasEvents` + `useAgentCanvasMutations` (mounted in `AgentDockOverlay`): apply responses carry the mutation to the LIVE canvas — `createdNode` → `useCode.createCodeNode` with the SERVER id (new template first lands in `projectPackagesStore` + `refreshPackageRegistry`, the Save-as pulse); `appliedContent` → live `data.code`/`defaultCode` update; idempotent per node id. Review card renders the new kinds (justification-first template card, per-kind effect lines); delegate transient activity lines.
- Files/modules changed: backend `app/agents/{builtin.py,tools.py,content.py,services.py,delegation.py (new)}`, `app/packages/services.py` (`available_templates`, `create_template_package`, `template_slug`); `utk_curio/llm-prompts/node_build_instruction.txt` (new); frontend `utils/agentCanvasEvents.ts` (new), `components/agents/attach/{useAgentCanvasMutations.ts (new),AgentAttachmentsProvider.tsx,AgentDockOverlay.tsx}`, `components/agents/content/{AgentReviewCard.tsx,AgentReviewCard.module.css}`, `api/agentsApi.ts`
- Tests added/updated: backend `test_builtin.py` (`TestNodeBuilderComposite` + byte-parity), `test_content.py` (`TestDelegateRequestPart`, `TestDelegationInstruction`, creation-pins), `test_delegation.py` (new, 17: resolution determinism/current-project-only/missing/unresolvable; child prompts + depth-1 structural; records + ledger pairs + attribution; child failure recovery; missing-specialist proposal + apply idempotence; shared round budget; SSE ordering), `test_routes.py` (`TestNodeCreate` 6 incl. injection resistance + id-spoof + template-gone 409; `TestNodeTemplateCreate` 5 incl. transactional factory failure + round-trip), `test_tools.py`; `test_packages/test_available_templates.py` (new, 5); frontend `useAgentCanvasMutations.test.tsx` (new, 5 incl. store→registry→insert ordering + double-event no-op), `AgentAttachmentsProvider.test.tsx` (+4: bridge dispatch per response kind, delegate lines transient), `AgentReviewCard.test.tsx` (+4 kinds)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 951 passed (8 skipped); frontend `npx jest` full → 630 passed (57 suites); injection-resistance and rule-9 share suites included in both
- Commit/PR: `COMMIT-ed671bc1` (roster), `COMMIT-8b025fd2` (node.create), `COMMIT-a7c73372` (delegation), `COMMIT-bec6afdb` (node.template.create), `COMMIT-7a0b193d` (frontend)
- Issues/regressions discovered: the §1.3 clobber defect in shipped dev/41 behavior (applied content silently lost on the next canvas save) — fixed + regression-tested via the bridge.
- Follow-up work: Dataset Finder next (after T4 provider profiles), then Dataflow Builder (the DEC-046 LangChain revisit + dev/49 DR-1…DR-5); `connection` target + `agent.package-recommendation` delegate when their prerequisites exist; node-explainer `current_input`/`current_output` — **closed 2026-08-27** (`BL-P2-20260827-11`, memo `dev/111`).

**Amendment (2026-08-04, memo `dev/51` — post-implementation testing feedback, `COMMIT-e48603dc`):** the dev/48 apply→canvas bridge shipped with three defects, found by a NEW integration regression driving the REAL `FlowProvider`/`reactflow`/`useCode` (the dev/48 unit suite had mocked exactly those seams): (1) created nodes landed off-viewport (backend placement = right of the whole graph extent) with no pan — invisible until the load-time fitView, read as "not created"; fixed by centering the viewport on the inserted node (`setCenter`, current zoom). (2) The bridge's `useReactFlow().setNodes` writes were clobbered by the controlled-mode props re-sync — `data.code` never landed (save would re-clobber content; `node.content.write` applies never reached the editor); fixed by seeding `data.code` in `generateCodeNode` when `code` is passed (palette drops byte-identical) and a new `FlowProvider.applyNodeContent(nodeId, content)` provider-state helper (sets `defaultCode` + `code`, merge-not-replace) used by the content branch. (3) A store-sync lag could double-insert on re-fired events; fixed with an event-level processed-ids ref beside the live-graph check. Chat feedback, apply flow, and all existing `updateDefaultCode` consumers unchanged. Tests: the integration suite (3, kept as the regression net) + updated bridge unit suite (7); frontend 635 passed (58 suites).

---

## BL-P5-20260804-03: Dataset Finder — the second P5 composite (two-lane discovery + reviewed dataset install) (memo dev/50)

- Date / author: 2026-08-04 / Karla
- Status: verified
- Requirements: docs/06 (two lanes, candidate row contract, prompt-mediated confirmation, no bespoke row buttons, review-before-apply), `REQ-REVIEW-001`/`DEC-006` (the dataset install is a reviewed proposal; apply-endpoint-only), `REQ-PERM-001`/`DEC-017`, `REQ-SEC-002` (bounded + scheme-allowlisted candidate metadata, sanitizer-rendered), `REQ-ORCH-001` (missing Node Builder → reviewed install proposal)
- Design decisions/artifacts: **`DEC-047`** (user-mediated external handoff — a depth-1 child is structurally proposal-less under DEC-046, so the fetch-node proposal comes from the user's own Node Builder attachment; the delegation seam is reused for resolution + install proposals only), `DEC-034` (partial: dataset-finder slice), memo `dev/50`; recorded deviations: canvas target added (mission-first discovery), foreground-only (DEC-021 later), geography/lineage via tool results, no auto-install (conservative — every install reviewed), T4 sequencing note (no external HTTP/credentials in this slice)
- Tasks: `TASK-P5-dataset-finder`
- Risks/questions: candidate metadata is untrusted model output — bounded + scheme-allowlisted at parse (http/https only), sanitizer at render; the catalog lane is tool-grounded so install proposals can never reference hallucinated ids (mint AND apply re-validate against `list_catalog`)
- Design-to-code decision or deviation: **(1) Roster** — `agent.dataset-finder` (net-new `discovery_instruction.txt`); `compatibleTargets[].requires` gets runtime meaning at attach: a node target's canonical type must match a declared suffix (`data-loading`; tolerant of versioned/legacy spellings), empty requires = any node (all fifteen prior agents behavior-identical). **(2) `catalog.search` (read)** — thin wrapper over `DatasetCatalogService.list_catalog(dataflow_id, q, fmt, origin)` (the acting user rides the request context; the datasets service is user-object keyed): ≤40 bounded rows {id, name, format, origin, installed, description≤200}. **(3) `dataset.install` (mutate)** — mint validates the id via `get_dataset` (unknown → refusal; already-installed → honest-chat refusal, no dead proposal); apply re-validates (gone → 409 + `stale`) then the EXISTING dataset-only flow (`install_dataset` — duplicate collapse, OSM groups); result card + `installedDataset`; apply consumes no quota. **(4) `datasetCandidates` part + card** — two labeled lanes, ≤8 rows each per the docs/06 row contract; catalog rows require `datasetId` (tool-grounded); coexists with `suggestedPrompts` in one tail; frontend card renders keyboard-operable multi-select checkbox rows (no per-row buttons) and composes the lane-correct confirmation prompt into the chat input through the prefill-never-overwrites-a-draft rule. **(5) DEC-047 handoff** — external confirmation = card + Node Builder-addressed suggested prompt; missing Node Builder → the dev/48 `project.install` proposal (tested by name); the finder's tail offers `dataset.fetch.author — handled by Node Builder`.
- Files/modules changed: backend `app/agents/{builtin.py,tools.py,content.py,services.py}`; `utk_curio/llm-prompts/discovery_instruction.txt` (new); frontend `api/agentsApi.ts`, `components/agents/content/{AgentDatasetCandidatesCard.tsx (new),AgentDatasetCandidatesCard.module.css (new),AgentReviewCard.tsx}`, `components/agents/attach/AgentChatPanel.tsx`
- Tests added/updated: backend `test_builtin.py` (roster 15 + `TestDatasetFinderComposite` + requires-parity), `test_routes.py` (`TestAttachRequiresGating` 5; `TestDatasetFinderTools` 6 incl. grounding, q-filter, unknown-id refusal, apply + already-installed refusal, gone→409+stale, injection resistance), `test_content.py` (`TestDatasetCandidatesPart` 5 incl. scheme allowlist + bounds), `test_delegation.py` (`TestDec047DatasetFinderHandoff` 2), `test_tools.py`; frontend `AgentDatasetCandidatesCard.test.tsx` (5 incl. hostile-metadata inertness + lane-correct composition), `AgentReviewCard.test.tsx` (+1)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 971 passed (8 skipped); frontend `npx jest` full → 641 passed (59 suites)
- Commit/PR: `COMMIT-dc044c24` (roster + gating), `COMMIT-1f45fdcf` (tools), `COMMIT-06f4bcac` (part + card), docs commit (this entry + DEC-047 ledgers + AGENTS.md)
- Issues/regressions discovered: none.
- Follow-up work: `web.search` + credential-aware external discovery (T4 / dev/49 DR-6); batch handoffs and capability-first resolution at Dataflow Builder (DR-4, DEC-046 revisit); authorized auto-install policy if demand appears.

---

## BL-P5-20260805-04: Dataflow Builder — the third P5 composite (Plan → Revise → Solve → Run) (memo dev/52)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: dev/49 DR-1 (graph-level proposals + typed plan domain), DR-2 (persisted orchestration session), DR-3 (planning templates), DR-4 (bounded Solve + the capability-first resolution widening, dev/03:366), DR-5 (phase-aware builder panel); `REQ-REVIEW-001`/`DEC-006` (structural review for the two new mutation surfaces), `REQ-ORCH-001`, rule 9 (the builder session rides the share-stripped attachment record)
- Design decisions/artifacts: **`DEC-048`** (direct-code orchestration; `DEC-007` retired with its re-open condition recorded — `DEC-021` background execution through the `delegation.py` seam; the Solve authorization model: one authenticated action per batch), `DEC-034` (now complete across all three composites), memo `dev/52`; recorded deviations: additive-only plans, foreground-only synchronous Solve, static templates, package-recommendation delegate deferred, per-child reservations (DEC-044 derives aggregates)
- Tasks: `TASK-P5-dataflow-builder`
- Risks/questions: plan bounds are abuse backstops (200 nodes/600 edges; plan-specific 256 KiB tail budget — the classic 4 KiB cap stays byte-identical for every other tail; large designs chain across successive additive plans); a hard-crashed Solve leaves the transient `solving` phase — a 15-minute staleness override prevents wedging
- Design-to-code decision or deviation: **(1) Roster + resolution** — `agent.dataflow-builder` (net-new `orchestration_instruction.txt`, nine-delegate list); `delegation.resolve` widened to capability-first (delegatesTo preference → any current-project template, sorted-coord tie-break → visible-roster missing-specialist fallback per dev/03:366, superseding dev/48's delegatesTo-scoped proposal targeting; regression tests updated). **(2) `dataflowPlan` part + mint** — typed additive plan (plan-local refs, edge integrity, fail-open); the runtime mints the `dataflow.plan.write` proposal from a validated part on the final reply (model never requests it); mint validates every nodeType via `available_templates` (grant-time roster rides plan grants too) and pins the whole-graph shape digest (node-id + edge-id sets — content edits deliberately don't invalidate additive plans); ungranted parts pass through informationally; mint refusals surface as an error card. **(3) Apply + session** — atomic insertion (server ids, ref map, topological columns right of the existing extent, `goal` = "title — intent", pending content empty), drift → 409 + `stale`; `builderSession {phase, appliedPlanId, nodeRuns}` on the attachment record (server-owned transitions incl. dismiss; exposed on the attachment card). **(4) Solve (DEC-048)** — `POST …/solve {nodeIds?}`: the explicit user action IS the review; capability-first resolution of `node.content.generate`; `ThreadPoolExecutor(3)` over `delegation.run_delegate` children (own ledger pairs, `parentExecutionId` = the solve execution id); per-node digest guard (still-empty content only — user edits `skipped`); one batched spec write under re-read; failed subset Retry; missing specialist → ONE reviewed `project.install` proposal; endpoint quota-free; in-flight 409 guard with staleness override; result-card turn carrying the solve execution record + `delegations`. **(5) Frontend** — `graph-created` bridge (bulk `createCodeNode` + edge inserts via `onEdgesChange` + `fitViewWithMenuOffset`; idempotent per plan id), plan review card (summary-first counts + scrollable node list + dynamic effect line), `AgentBuilderStrip` (phase chips, six template seeds through the prefill rule, Solve/Retry with per-node aria-live progress, Run via the existing `playAllNodes`; rendered only for dataflow-builder attachments — every other chat pixel-identical).
- Files/modules changed: backend `app/agents/{builtin.py,delegation.py,content.py,tools.py,services.py,routes.py}`; `utk_curio/llm-prompts/orchestration_instruction.txt` (new); frontend `api/agentsApi.ts`, `utils/agentCanvasEvents.ts`, `components/agents/attach/{useAgentCanvasMutations.ts,AgentAttachmentsProvider.tsx,AgentBuilderStrip.tsx (new),AgentBuilderStrip.module.css (new),builderTemplates.ts (new),AgentChatPanel.tsx,AgentDockOverlay.tsx}`, `components/agents/content/AgentReviewCard.tsx`
- Tests added/updated: backend `test_builtin.py` (roster 16 + `TestDataflowBuilderComposite` + parity re-pin), `test_delegation.py` (`TestCapabilityFirstResolution` 4 + widened-semantics updates), `test_content.py` (`TestDataflowPlanPart` 6 incl. the sized-for-real-graphs 150-node budget regression + key-smuggling guard), `test_routes.py` (`TestDataflowPlanMint` 3, `TestDataflowPlanApply` 5 incl. additive/drift/content-edit-survival/ready/dismiss, `TestSolve` 6 incl. skip-user-edits, failure isolation + subset retry, single install proposal, quota attribution, injection resistance), `test_tools.py`; frontend `AgentBuilderStrip.test.tsx` (6), `useAgentCanvasMutations.test.tsx` (+3 graph-created), `AgentReviewCard.test.tsx` (+1), `AgentChatPanel.test.tsx` (provider mock for the strip's import chain)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 998 passed (8 skipped); frontend `npx jest` full → 651 passed (60 suites)
- Commit/PR: `COMMIT-d4afe271` (roster + resolution), `COMMIT-6156a1b5` (part + mint), `COMMIT-3e48fb58` (apply + session), `COMMIT-67a3bef3` (Solve), `COMMIT-5471a5bd` (frontend), docs commit (this entry + DEC-048 ledgers + AGENTS.md)
- Issues/regressions discovered: none new; two dev/48-era resolution tests encoded the delegatesTo narrowing and were updated to the dev/03:366 semantics.
- Follow-up work: destructive replan reconciliation (its own memo); streamed solve progress + cancellation with `DEC-021`; server-owned planning templates; `agent.package-recommendation` wiring (dev/16); the P5 composite roster is complete — remaining program tracks are T4 (provider profiles + secret store) and the dev/49 DR-6 research-node package.

**Amendment (2026-08-05, memo `dev/53` — post-implementation testing feedback on dev/52, `COMMIT-9cee1343`):** "the apply plan button is not appearing after showing the planned nodes and connections" — the streamed backend path was verified correct end-to-end; two client-side defects hid the control: (1) the pre-delta stream fallback (`useAgentAttachments.run` → provider) returned/appended TEXT ONLY, silently discarding every content part — the plan proposal (and equally node/dataset proposals, prompts, candidates) never rendered a card whenever the fallback ran; fixed with full payload parity (reply + execution + content, proposal-aware reload). (2) The builder strip named the pending review ("Apply or dismiss the plan review first") but carried no control; it now surfaces **Apply plan / Dismiss** during `plan_review`, targeting the `activeProposal` mirror (the dev/41 fast-lookup home — works even when a transcript part is missing); the transcript review card is unchanged. Tests: fallback-parity provider regression, strip review-control suite (+3), run-return shape update; frontend 655 passed (60 suites).

**Amendment (2026-08-05, memo `dev/54` — second fix round on dev/52, `COMMIT-d1ecbb36`):** the true root cause of "the apply plan button is not appearing": the mint required a byte-perfect `dataflowPlan` JSON tail with exact canonical template ids in ONE shot — any grammar slip (a 301-char intent, one malformed character) failed the whole block open, streaming the raw plan JSON as chat text with no proposal minted anywhere (no card, no strip button — both derive from the proposal), and a parseable plan with a wrong template id died post-loop in a one-shot error card. Local llama4/gemma-class providers rarely deliver that first-try. The PRIMARY path is now self-correcting (no fallback paths): the plan parser reports precise field-level errors (`nodes[2].intent is 301 chars (max 300)`, `edges[0].to references unknown ref 'ghost'`, JSON breakage included) via `plan_tail_diagnosis`; the loop feeds them back as corrective rounds sharing the existing `MAX_TOOL_ROUNDS` budget (mint refusals correct too — moved in-loop, replacing the post-loop `_maybe_mint_plan`); invalid attempts never leak — the stream holds plan-ish invalid tails and corrective prose is not folded; at the cap the failure is LOUD: the held tail releases (fail-open transparency) plus the "Plan not proposable" card carrying the errors. SSE `plan_revision` events render "revising the plan …" transient lines. Non-granted agents keep byte-identical fail-open behavior (regression-pinned). Tests: `TestPlanTailDiagnosis` (4), `TestPlanCorrectionRounds` (6: JSON-then-corrected, wrong-id-then-corrected, loud cap, shared budget, ungranted regression, stream hold + event ordering); backend 1008 passed, frontend 655 passed.

**Amendment (2026-08-05, memo `dev/55` — third fix round on dev/52, `COMMIT-9202bd85`):** the model's transcript ("I cannot directly apply a plan… click the Apply button on the dataflowPlan block") exposed a SELF-CONTRADICTING system prompt: the dev/41 grants paragraph teaches the generic `{"toolRequest": {"tool": "<id>", "params": {}}}` syntax for every granted tool — `dataflow.plan.write` included — while the plan flow only accepted the `dataflowPlan` block form. A model following the grants paragraph hit "no proposal flow exists for tool 'dataflow.plan.write'" (small plans) or the 1,024-byte params cap failing the whole block open as raw text (real plans), concluded it couldn't apply plans, and invented UI instructions. The toolRequest form is now a first-class equivalent: `_mint_plan_from_params` (payload = `params.dataflowPlan` ?? `params`) validates with the same verbose parser and routes into the same mint — errors return as the tool refusal that the existing round already feeds back (self-correction on the shared budget); per-tool params budgets give the plan form `PLAN_TAIL_MAX_BYTES` while every other tool keeps 1,024 bytes regression-pinned; the enlarged tail budget + the dev/54 stream hold recognize the form. Tests: `TestPlanToolRequestForm` (5, incl. the user-scenario correction, the 40-node large form, and the params-cap regression); backend 1013 passed.

**Amendment (2026-08-05, memo `dev/56` — fourth fix round on dev/52, `COMMIT-a59447a3`):** the remaining blind spot behind "the apply button still not appearing": dev/54/55 only recognized plans inside a terminal ` ```curio.v1 ` fence (or its toolRequest form) — real models overwhelmingly emit ` ```json `/bare fences, mid-reply, with prose after; a PERFECTLY VALID plan in that shape produced no part, no diagnosis, no correction, no card (reproduced). `content.extract_plan_attempt` now scans every fenced block of a plan-granted reply for the plan payload (`{"dataflowPlan": …}`, the toolRequest form, or the bare `goal`+`nodes` object; broken-JSON plan-marked fences return their body so the JSON error still feeds back); `_handle_plan_reply` routes a scanner hit into the SAME mint (block stripped from the persisted text — the review card is the plan's home) or the SAME corrective rounds with added fence guidance ("use a ```curio.v1 fence as the very last thing in your reply"). Non-plan agents and non-plan fences byte-identical (regression-pinned). Tests: `TestExtractPlanAttempt` (5), `TestFenceAgnosticPlanRecognition` (4 incl. the reported scenario run+stream); backend 1022 passed.

**Amendment (2026-08-05, memo `dev/57` — Solve-flow feedback, node-content hygiene):** generated node content arrived wrapped in response formatting (```python fences, language ids, JSON wrappers, "Here is the code:" prose) because model replies became content VERBATIM at every write point: Solve's child replies, the `node.create`/`node.template.create`/`node.content.write` mints, plan-carried content, and the legacy Get Code path (whose crude `replaceAll("```python"…)` missed other ids, kept prose, and corrupted legitimate backticks). One deterministic extractor now guards every model-output→node-content boundary: `content.extract_node_content` (backend) + `utils/extractNodeContent.ts` (frontend mirror, replacing the legacy replaceAll) — JSON wrappers with a single plausible string field unwrap (bounded recursion, wrapper-around-fence handled), the LARGEST fenced block's body is the content when fences exist, and unfenced text passes through byte-identical (conservative: preserving legitimate content outranks cosmetic cleanup; the `not controllable` sentinel survives). The user's LLM-verification-agent suggestion is recorded against the Validation-agent track (OQ-011) as a later semantic layer — deterministic extraction is the primary mechanism. Tests: `TestExtractNodeContent` (8), `TestGeneratedContentExtraction` (2: Solve writes clean content; wrapped node.create params preview+apply clean), frontend util suite (5); backend 1032 passed, frontend 660 passed.

**Amendment (2026-08-05, memo `dev/58` — plan-apply edge crash):** clicking Apply raised `TypeError: Cannot read properties of undefined (reading 'keywordHighlighted') at UniDirectionalEdge` (user screenshot) and the applied graph only appeared after a refresh — the dev/52 bridge inserted edges WITHOUT a `data` object while both edge components read `data.keywordHighlighted` unguarded, so the first render of a bridge-inserted edge crashed the canvas update (the saved spec was already correct; `loadTrill` always sets `data = {}`, which is why the refresh worked). Fixed in both layers: the bridge's edge items now carry `data: {}` (loadTrill parity, regression-pinned in the bridge unit test) and both edge components guard with `data?.keywordHighlighted` so a data-less edge from any path can never take down the render. Frontend 660 passed.

---

## BL-P5-20260805-05: Destructive replan reconciliation (memo dev/59, DEC-049)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: dev/49 DR-2 remainder (the session's preservation contract: unchanged nodes keep ids/positions/content; removals explicit; conflicts explained before applying), `REQ-REVIEW-001`/`DEC-006` (extended to deletions), rule 9
- Design decisions/artifacts: **`DEC-049`** (per-victim content-digest pins; removals reviewed by name; removals only on explicit user request — superseding dev/52's "removals are theirs to make" at its recorded revisit point), memo `dev/59`; recorded deferrals: modify-existing-node ops (own memo on demand), deterministic canvas-diff engine (DR-2's outer edge), removal undo (provenance track)
- Tasks: `TASK-P5-destructive-replan`
- Risks/questions: deletions are irreversible — mitigated three ways: per-victim digests (work protection), by-name review with content flags (informed consent), instruction posture (never uninvited)
- Design-to-code decision or deviation: **(1) Grammar** — `removeNodes`/`removeEdges` (bounded, deduplicated; keys emitted only when used → additive plans byte-identical, regression-pinned); edge endpoints may name EXISTING nodes (grammar checks shape + not-a-victim; existence is the mint's spec check) — lifting dev/52's self-contained-plan restriction; remove-only plans valid; all errors ride the dev/54 correction rounds and the dev/56 fence-agnostic scanner unchanged. **(2) Mint** — spec-aware validation (unknown victims/endpoints → corrective errors), the edge cascade computed for display, `removeContentSha256` pins per victim (part AND proposal mirror), the card's `removals` display block (label · type · contentChars) + cascade count, summary/preview extended. **(3) Apply** — victim digest checks after the shape digest (drift → 409 + `stale` naming the node); removals in place (listed edges + recomputed cascade, then nodes) BEFORE inserts; `attachments.prune_orphaned_attachments` (dev/32) for attachments on victims; `builderSession.nodeRuns` MERGE semantics (victims dropped, survivors kept, new pending joined — replacing dev/52's reset); edge insertion resolves endpoints via ref map ∪ existing ids; `appliedGraph` gains `removedNodeIds`/`removedEdgeIds`. **(4) Frontend** — bridge removals-before-inserts through the canvas's own machinery (`applyRemoveChanges` + edge remove changes; already-absent victims no-op); the card's destructive Removes section (by-name rows, content flags, cascade, bidirectional effect line); additive rendering pixel-identical. **(5) Instruction** — replan procedure: real ids, wire-to-existing freely, remove only on request, restate removals in prose.
- Files/modules changed: backend `app/agents/{content.py,services.py}`; `utk_curio/llm-prompts/orchestration_instruction.txt`; frontend `api/agentsApi.ts`, `utils/agentCanvasEvents.ts`, `components/agents/attach/{useAgentCanvasMutations.ts,AgentAttachmentsProvider.tsx}`, `components/agents/content/{AgentReviewCard.tsx,AgentReviewCard.module.css}`
- Tests added/updated: backend `test_content.py` (`TestDataflowPlanRevisionGrammar` 6 + two dev/52-era tests updated to the widened endpoint semantics), `test_routes.py` (`TestDestructiveReplan` 6: replace-flow end-to-end incl. preserved-node byte-identity + existing-id wiring; victim-edit → 409 by name; attachment pruning + nodeRuns merge; remove-only; unknown-target correction; additive no-pins regression), `test_builtin.py` (instruction posture); frontend `useAgentCanvasMutations.test.tsx` (+3: removals-before-inserts ordering, absent-victim no-op, additive regression), `AgentReviewCard.test.tsx` (+2: by-name section + additive regression)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1044 passed (8 skipped); frontend `npx jest` full → 665 passed (61 suites)
- Commit/PR: `COMMIT-d0bd2d14` (grammar), `COMMIT-12e5771d` (mint + apply), `COMMIT-97fa6c41` (frontend), docs commit (this entry + DEC-049 ledgers + AGENTS.md + instruction)
- Issues/regressions discovered: none new; two dev/52-era grammar tests encoded the island restriction and were updated deliberately.
- Follow-up work: modify-existing-node plan ops (if demand appears); the deterministic canvas-diff engine; removal undo via the provenance track; streamed solve progress (`DEC-021`).

**Amendment (2026-08-05, memo `dev/60` — built-in prompt propagation, `COMMIT-c2cac301`):** "clear the canvas" was refused with the superseded dev/52 posture verbatim ("removals must be performed by you manually…") because the model still RAN it: `_resolve_prompt_text` was store-first for prompt bytes (an install made before a roster prompt update runs the stale instruction forever) and the dev/44 heal checked asset-set completeness, never byte freshness — every past and future built-in prompt improvement silently failed to reach earlier installs. Fixed in both layers: built-in-trust definitions now resolve prompt BYTES roster-first (the exact rule `_resolve_definition` already applied to metadata; the store copy is a materialization cache, not an authority), and the heal rewrites drifted bytes on re-install. Owned-import shadows of a built-in coordinate keep their own bytes (regression-pinned). Effective immediately for existing installs — no migration. Tests: `TestBuiltinPromptPropagation` (3: stale-copy run composes roster bytes; reinstall heals disk; shadow untouched); backend 1047 passed.

**Amendment (2026-08-05, memo `dev/61` — remove-only plan recognition, `COMMIT-478b1179`):** with dev/60 delivering the current instruction, "clear the canvas" produced a correct remove-only plan — which the runtime then silently ignored: the dev/56 fence-agnostic scanner predates dev/59 and recognized a bare plan block only by `"goal"`+`"nodes"`, but a remove-only plan legitimately carries NO `nodes` key. The block leaked raw into the transcript, nothing minted (no Apply button, pills stuck on the prior READY state), and the model hallucinated that a proposal existed — the exact fail-open dev/54 closed for additive plans, reopened by the dev/59 grammar. Fix: every recognition predicate (`extract_plan_attempt` markers + bare-payload claim, `plan_tail_diagnosis` markers) now treats `"removeNodes"`/`"removeEdges"` as plan markers; a remove-only payload missing `goal` is still claimed so the field error feeds the dev/54 corrective rounds instead of leaking. Mint/apply/card were already remove-only-correct (dev/59) — recognition was the whole defect. Tests: +5 (scanner recognizes the user's exact payload incl. `removeEdges: []`, goal-less claim, broken-JSON diagnosis, route-level mint → `plan_review`); backend 1052 passed. Lesson recorded: a grammar extension must sweep every RECOGNITION predicate that fronts it — the dev/59 checklist covered parse/mint/apply/UI but not the dev/56 scanner sitting upstream.

**Amendment (2026-08-05, memo `dev/62` — reviewed removals vs. the manual-delete guard, `COMMIT-281c5b59`):** with recognition fixed (dev/61), APPLYING the remove-only plan updated the spec correctly but broke live: the dev/59 bridge routed node victims through `applyRemoveChanges` — the canvas's manual-delete guardrail, which refuses any node still touching an edge with a per-victim toast ("Connected boxes cannot be removed. Remove the edges first…") — while the victims' edges were still live. Reordering could not fix it (the guard reads `reactFlow.getEdges()`, blind to same-tick removals), and the guard is semantically wrong for this caller: the DEC-049 review already authorized every victim by name, and the backend delivers the full edge cascade in the same event. Fix: a dedicated `applyReviewedRemovals(nodeIds, edgeIds)` in `useWorkflowOperations` — the `cleanCanvas` idiom (onEdgesDelete bookkeeping → edge filter → onNodesDelete → onNodesChange), no guard, absent elements no-op — exposed via FlowProvider; the bridge replaces its two-step with the one call, still before inserts. The manual path keeps its guard byte-identical (regression-pinned). Tests: +4 hook tests (no-toast bulk removal, no-op absent ids, unlisted survivors, manual-guard pin) + bridge tests reworked; frontend 669 passed / 62 suites. Lesson recorded: routing agent mutations "through the canvas's own machinery" must distinguish machinery that MUTATES from machinery that also GATES — a guardrail built for unreviewed manual clicks is not parity, it is a veto the review already overruled.

---

## BL-P5-20260805-06: Streamed Solve progress with cancellation (memo dev/63, DEC-050 — the DEC-021 user slice)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: dev/52's recorded deviation ("streaming joins later with DEC-021"); the fix-primary-paths rule (streaming IS the solve path, no dual mode); DEC-021's no-replay posture for in-flight provider work
- Design decisions/artifacts: **`DEC-050`** (memo `dev/63`); scope boundary recorded: this is the USER slice of `DEC-021` — leases/heartbeats, `interrupted` executions, background execution, and the LangChain re-open condition remain open; the 15-minute stale guard remains the crash story
- Tasks: `TASK-P5-streamed-solve`
- Risks/questions: a dropped stream must never wedge the session or lose completed work — mitigated structurally: the ONE re-guarded finally-write runs on normal completion, cancellation, client disconnect (`GeneratorExit`), and worker error alike; streamed events are transport, never truth
- Design-to-code decision or deviation: **(1) Backend** — `solve_attachment_stream` (eager validation → generator: `solve_started` → `node_started`/`node_result` per target → `done` with the blocking payload + `cancelled`/`notAttempted`); `pool.map` → `submit` + a thread-safe outcome queue the generator drains (workers never touch the response; a worker exception enqueues a `failed` outcome so the drain can never deadlock); the blocking `solve_attachment` DRAINS the generator (one implementation, byte-compatible response); cancellation = one `_should_stop()` predicate, two signals (in-process event registry keyed by the persisted `solveExecutionId`; durable `cancelRequested` flag set by `POST …/solve/cancel`), checked at task entry — undispatched targets revert to `pending`, in-flight children finish and persist. **(2) Frontend** — `postSseStream` extracted as the shared dev/22 reader (chat + solve); `solveAttachmentStream` resolves on `done`; the provider overlays transient per-node statuses (`solveProgress`), applies each solved node's content through the dev/51 bridge AS it lands, and clears the overlay on the terminal refetch; the strip renders overlay-over-nodeRuns pills, a Cancel button while solving, and the cancelled/not-attempted notice. **Deviation from the memo's §3**: user Cancel posts the endpoint but does NOT abort the fetch — the stream ends naturally with `done.cancelled`, so in-flight pills still resolve and no AbortError needs special-casing; the abort is the fallback when the cancel endpoint is unreachable (and the disconnect path covers tab close).
- Files/modules changed: backend `app/agents/{services.py,routes.py}`; frontend `api/agentsApi.ts`, `components/agents/attach/{AgentAttachmentsProvider.tsx,AgentChatPanel.tsx,AgentDockOverlay.tsx,AgentBuilderStrip.tsx}`
- Tests added/updated: backend `test_routes.py` `TestStreamedSolve` (6: SSE order + per-node content + one-write persistence; pre-flight 409s stay JSON; cancel mid-batch → 3 solved / 2 reverted-pending / phase `applied` / flag cleared; disconnect persists partials and exits `solving`; cancel-without-solve 409; durable-flag persistence + idempotence) — existing `TestSolve` (6) green through the drain wrapper unchanged; frontend `agentsApi.test.ts` (+3: lifecycle dispatch + done payload, retry subset + mid-stream error, cancel endpoint), `AgentAttachmentsProvider.test.tsx` (+2: overlay + per-node bridge apply + clear-on-done with no double-apply; cancel posts), `AgentBuilderStrip.test.tsx` (+3: overlay wins per node, Cancel appears/disables, cancelled notice)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1058 passed (8 skipped); frontend `npx jest` full → 677 passed (62 suites); `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-8618fa7b` (backend), `COMMIT-bc3c0e28` (frontend), docs commit (this entry + DEC-050 ledgers + dev/52 closure pointers)
- Issues/regressions discovered: none; the blocking endpoint's behavior is regression-pinned by the pre-existing suite running through the drain wrapper.
- Follow-up work: DEC-021 proper (leases/heartbeats, `interrupted`, background execution — the LangChain re-open condition); streamed Solve for multi-worker deployments beyond the durable-flag boundary checks; provider-call abort if a client ever supports it.

---

## BL-P5-20260805-07: Full dataflow awareness + per-node runtime journal (memo dev/67-2, DEC-052 — Foundation A of the dev/67 program)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: 67-0 "Full dataflow awareness" (agents must never say "I don't have the full dataflow edges") + "Runtime messages and validation state" (persist per-node execution messages; never rely on transient UI console output)
- Design decisions/artifacts: **`DEC-052`** (memo `dev/67-2`; program index `dev/67-1`); recorded limitations: browser-executed node types (merge/vis/pool) leave no journal record (they pass data through without a `/process*Code` call), unsaved projects (no dataflowId) skip the write
- Tasks: `TASK-P5-dataflow-awareness`
- Risks/questions: the journal must never make execution slower or flakier — mitigated structurally: best-effort writes (never raise, never block the response), fail-open reads, observational-only posture (the saved spec remains the single structural truth)
- Design-to-code decision or deviation: **(1) Journal** — `app/execution/runtime_journal.py`: latest-per-node JSON under `<project dir>/runtime/` (DEC-040 FS), status by the canonical `output.path == ""` predicate (never stderr-nonempty — warnings land there), 4000/2000-char stderr/stdout tails, normalized (dedent+strip) content sha256 so transport indentation doesn't defeat the staleness signal, filename-sanitized node ids; seam write `_record_runtime_outcome` in BOTH `/processPythonCode` and `/processJavaScriptCode`. **(2) Tools** — `dataflow.read` v2: structure-first projection (all nodes as id/type/goal/hasContent/contentChars, ALL edges with handles when present, datasets, runtime status map; over-budget shrinks node GOALS, never edges; `params.include: ["content"]` restores the full dump); new `node.runtime.read` (attached-node default, honest "never-executed", `contentChangedSinceRun`). **(3) Grants** — dataset-finder + `dataflow.read` (it had NO graph view at all); debug/node-explainer/NCB/node-builder/dataflow-builder + `node.runtime.read`. **(4) Frontend** — `readFragment` producer cases for `graphContext` (full live Trill), `mission` (workflow+goal), `installedTemplates` (client palette registry), `targetContext` (node snapshot | whole graph by target kind), `nodeIntent`; `externalSelection`/`catalog` stay deliberately null-producing (prompt-carried / tool-served).
- Files/modules changed: backend `app/execution/{__init__,runtime_journal}.py` (new), `app/api/routes.py`, `app/agents/{tools.py,builtin.py}`; frontend `components/agents/attach/agentRunContext.ts`
- Tests added/updated: backend `tests/test_execution/test_runtime_journal.py` (new, 9: path predicate + tails + seq, bounded tails, filename safety, fail-open reads, status map, normalized sha, seam write ok/error/no-identity), `test_tools.py` (+7: projection regression — ALL edges survive a spec whose old dump truncated them; runtime block; handle passthrough; never-executed; traceback evidence + content-changed flip; attached default + missing-node errors; include=content parity), `test_builtin.py` (3 pinned grant lists updated deliberately); frontend `agentRunContext.test.ts` (+5: all three composites compose non-null with edges, node-target intent, mission-only, empty-canvas null regression)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1075 passed (8 skipped); frontend `npx jest` full → 702 passed; `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-3e0add6d` (journal + seam), `COMMIT-cf538d60` (tools + grants), `COMMIT-49407f43` (frontend producers), docs commit (this entry + DEC-052 ledgers)
- Issues/regressions discovered: none new; the edges-truncate-first failure mode was reproduced in a regression test before the fix (40 nodes × 2000-char contents → old dump loses the edge list, projection keeps all 39 edges).
- Follow-up work: the rest of the dev/67 program (67-3 topology next per the index); outputSchema capture when the sandbox reports column metadata; journal records for browser-executed node types if 67-7 needs them.

---

## BL-P5-20260805-08: Topology model + Merge-node resolution (memo dev/67-3, DEC-051 — Foundation B of the dev/67 program)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: 67-0 "Invalid multiple-input topology" (inspect the target's input structure before creating edges; introduce the existing Merge node; never generate an invalid graph and defer discovery to execution) + "Merge-node detection"
- Design decisions/artifacts: **`DEC-051`** (memo `dev/67-3`; program index `dev/67-1`); recorded deviation from the memo's §3 sketch: `maxIncomingEdges` derives from RENDERED capacity (port count; merge=5), NOT the sum of declared maxima — the investigation showed declared `[1,n]` on e.g. computation-analysis is aspirational (the input plumbing is scalar per handle; a second edge silently overwrote `data.input`), so declared maxima are served as metadata (`inputs` rows) but never as enforceable capacity
- Tasks: `TASK-P5-topology-model`
- Risks/questions: over-enforcement could reject working graphs — mitigated: unknown/out-of-scope templates fail open; the LOAD path warns and never drops persisted edges; merge behavior is regression-pinned byte-identical
- Design-to-code decision or deviation: **(1) Parse once** — `manifest.parse_cardinality` (fail-open on unparseable; the schema owns the grammar); `available_templates` gains `inputs: [{types,min,max}]` + `maxIncomingEdges` with `_RENDERED_INPUT_CAPACITY = {merge-flow: 5}`. **(2) Mint** — `_validate_plan_fanin`: net incoming per target (plan edges + dev/59-SURVIVING existing edges, victims excluded) vs capacity; refusals ride the dev/54 corrective rounds naming the fix ("route A, B through a curio.builtin/merge-flow node"); merge-slot handle shape (`in_0..in_4`) validated; instruction step 2 teaches the Merge resolution + `toHandle` (propagates to existing installs via dev/60). **(3) Grammar** — optional bounded edge `toHandle`, key present only when used (additive plans byte-identical). **(4) Apply** — every created edge carries `sourceHandle`/`targetHandle`; merge targets get a deterministic free `in_N` (named free slot wins; full merge → 409+stale); persisted to the spec AND passed through the bridge — the "merge dead until reload" bug (hardcoded `targetHandle: "in"` naming a nonexistent merge handle) is fixed at the source. **(5) onConnect guard** — one edge per occupied non-merge input handle (toast: "route multiple flows through a Merge node"); per-HANDLE, so spatial-join's `in_points`/`in_polygons` each accept their edge; `skipValidation` (load) → console.warn only. Test fixture `_write_builtin_package` updated to real-manifest port parity (+merge-flow template).
- Files/modules changed: backend `app/packages/{manifest.py,services.py}`, `app/agents/{content.py,services.py}`, `utk_curio/llm-prompts/orchestration_instruction.txt`; frontend `providers/FlowProvider.tsx`, `utils/agentCanvasEvents.ts`, `api/agentsApi.ts`, `components/agents/attach/useAgentCanvasMutations.ts`
- Tests added/updated: backend `test_available_templates.py` (+3: cardinality table, rendered-capacity rules incl. the aspirational-[1,n] pin, declared-metadata survival), `test_content.py` (+2: toHandle parse + byte-parity, bound), `test_routes.py` `TestPlanFanInValidation` (4: refusal→merge-replan mints; existing-target surviving-edge counting; apply assigns in_0/in_3 honored+lowest-free, persisted to spec; bad slot name feeds correction); frontend `connectionFanInGuard.test.tsx` (new, 4: refusal toast, merge byte-identical, per-handle spatial-join shape, load-path warn-never-drop), `useAgentCanvasMutations.test.tsx` (+1: handle pass-through with defaults)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1084 passed (8 skipped); frontend `npx jest` full → 707 passed; `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-d746a136` (arity model), `COMMIT-33ff906e` (mint/apply/grammar/instruction), `COMMIT-e7e1ee6d` (frontend guard + bridge), docs commit (this entry + DEC-051 ledgers)
- Issues/regressions discovered: the agent-bridge merge-handle bug ("in" names a nonexistent handle on merge targets; healed only on reload) was confirmed and is now regression-pinned; the test builtin fixture declared computation-analysis with NO input ports (unrealistic) and was aligned to the real manifest.
- Follow-up work: 67-8 surfaces per-edge review over this validation; spatial-join's runtime per-handle input plumbing (its connect-layer collision is fixed; the propagation layer still collapses to one scalar — pre-existing, tracked); declared-cardinality enforcement if the input plumbing ever becomes multi-valued.

---

## BL-P5-20260805-09: Plan cards v2 — per-node review, editable goals, per-node Apply (memo dev/67-5 — Simulation Mode: create)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: 67-0 "Plan card behavior" (no embedded code, editable goal per node, per-node Apply) + "Per-node creation" (one node at a time; no placeholder whose required content is knowingly unresolved beyond the pending-for-Solve contract; bulk creation never implied by code authorization)
- Design decisions/artifacts: memo `dev/67-5` (program index `dev/67-1`); DEC-054 is minted by 67-9 — this entry is its create stage
- Tasks: `TASK-P5-plan-cards-v2`
- Risks/questions: per-node applies mutate the spec the whole-plan digest pinned — resolved: each apply-node RE-PINS `baseGraphDigest` to the spec it produced, so the plan's OWN progress is legitimate drift while foreign edits still 409 + stale
- Design-to-code decision or deviation: **(1) Content-free plans** — the mint refuses content-carrying plan nodes with corrective guidance ("plans describe intent; node content is generated and validated per node after creation"), superseding dev/52's "trivial code" allowance at its recorded revisit point (67-0: no shortcut); the shared grammar keeps `content` (other consumers), enforcement is the builder mint's. **(2) `expects`** — optional bounded per-node input/output one-liner (grammar + card row). **(3) Mint-time layout** — positions computed ONCE at mint (`positions[ref]`) so per-node and whole-plan applies land identically; whole-plan apply consumes the same map (fallback: legacy compute). **(4) editedGoals** — `PATCH …/proposals/<id>/plan-goals {ref, goal}`: an audited review-stage overlay on the proposal (pinned plan bytes immutable), applied at creation, mirrored through `activeProposal` so reloads keep the edits. **(5) apply-node** — `POST …/proposals/<id>/apply-node {ref}`: creates exactly ONE content-free node (drift-safe — no whole-graph digest check; template availability IS re-checked, registry failure/type-vanished → stale), idempotent per ref ("already-applied"), proposal STAYS pending until every ref applies or dismissal (edges are 67-8's stage), result card without flipping the part, `builderSession` gains `phase: "simulating"` + `nodeStates {ref: planned|created}` + `nodeIds` + the created node joins `nodeRuns` as pending; whole-plan apply after partials skips applied refs and wires edges to their REAL ids. **(6) Card rows** — pending plan proposals with the per-node callback render rows (title · type · expects · editable goal saving on blur · Apply → "Created ✓") INSTEAD of the text preview; dismiss-after-partial keeps created nodes (real reviewed nodes) and kills remaining refs.
- Files/modules changed: backend `app/agents/{content.py,services.py,routes.py}`; frontend `api/agentsApi.ts`, `components/agents/attach/{AgentAttachmentsProvider,AgentChatPanel,AgentDockOverlay,AgentBuilderStrip}.tsx`, `components/agents/content/AgentReviewCard.{tsx,module.css}`
- Tests added/updated: backend `TestPerNodePlanApply` (8: one-node create + pending proposal + mirror; idempotence; edited-goal overlay; goal-edit guards; sequential ≡ whole-plan nodes; whole-plan-after-partial wires real ids; dismiss-after-partial; expects on the card) + `test_plan_carried_content_is_refused_at_mint` (rewritten from the superseded trivial-code test, deliberately); frontend card (+4: rows render/expects/goals, Created ✓ + blur-save, per-ref Apply, no-callback preview regression), provider (+3: node-created bridge dispatch, already-applied dispatches nothing, savePlanGoal), api (+2 endpoints)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1092 passed (8 skipped); frontend `npx jest` full → 716 passed (66 suites); `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-47ea5648` (backend), `COMMIT-238c79c9` (frontend), docs commit (this entry + memo flip)
- Issues/regressions discovered: the whole-plan digest pin initially refused whole-plan applies after the plan's own per-node progress — fixed by re-pinning per apply-node (recorded above); the route-test builtin fixture's seeded baseline node surfaced in assertions (tests count created nodes explicitly).
- Follow-up work: 67-6 (the per-node Solve/content stage over `nodeStates`), 67-8 (the connection stage completing the proposal), 67-9 (the sequence driver + DEC-054); auto-attach of the Node Builder to created nodes deferred to 67-6 (its modify-existing posture is the consumer).

---

## BL-P5-20260805-10: NCB apply flow + Node Builder modify-existing (memo dev/67-6 — Simulation Mode: solve)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: 67-0 "Node Builder and Node Content Builder" (full dataflow context, current goal/configuration, upstream/downstream info, reviewed Apply — Get Code ergonomics on the agent architecture) + "Full dataflow awareness" for content generation
- Design decisions/artifacts: memo `dev/67-6` (program index `dev/67-1`)
- Tasks: `TASK-P5-ncb-apply-flow`
- Risks/questions: the single-activeProposal model means a multi-node propose batch supersedes all but the last proposal — recorded as by-design: the 67-9 sequence solves ONE node at a time; the docstring states it plainly
- Design-to-code decision or deviation: **(1) One composer** — `app/agents/node_context.py` `compose_node_context`: the node's intent + bounded current content, nearest-first upstream/downstream (≤8 each, BFS; goals, content sizes, runtime status from the dev/67-2 journal — `never-executed` stated honestly, nothing fabricated), graph summary, dataset refs; `outputSchema` joins when the journal captures it (67-2 follow-up). **(2) Solve children enriched** — the worker's inputs gain `nodeContext` (composed per node from the solve's spec snapshot); legacy keys kept. **(3) Delegate enrichment** — `_enriched_delegate_inputs` at BOTH loop call sites: `node.content.generate` delegates get the composed context appended server-side (model keys never overwritten; nodeId from inputs or the node target; no node → no enrichment). **(4) Propose-mode Solve** — `solve_attachment_stream(mode="propose")` (route-validated; the blocking endpoint stays write-only and byte-compatible): each solved child MINTS a reviewed `node.content.write` proposal via the existing machinery + a transcript turn carrying the part; `node_result` carries `proposalId`; nothing is written; nodeRuns stays `pending`; `nodeStates[ref] → "solving"`; the session returns to its pre-solve phase (typically `simulating`). **(5) Content-apply resolves the ledger** — `_apply_node_content_write` on a plan node advances `nodeStates[ref] → "approved"` + `nodeRuns[nodeId] → "solved"`. **(6) Modify-existing** — `agent.node-builder` gains the `node.content.write` grant + `node` targets (lifting the dev/48 canvas-only deviation), and its instruction's step 6 states the split: existing node → read first (node.read/node.runtime.read) → grounded NCB delegation → ONE reviewed content replacement, never a duplicate node; the NCB instruction teaches nodeContext consumption + the `previousAttempt` self-correction contract (67-7's hook).
- Files/modules changed: backend `app/agents/{node_context.py (new),services.py,routes.py,builtin.py}`, `utk_curio/llm-prompts/{node_build_instruction.txt,new_content_prompt.txt}`; frontend `api/agentsApi.ts` (mode param)
- Tests added/updated: backend `test_node_context.py` (new, 5: neighborhood+summary, journal status, nearest-first cap, content bound, missing-node None + dataset projection), `TestProposeModeSolve` (3: full loop — propose → pending/simulating/solving → apply → solved/approved; classic write untouched; invalid mode 400), `TestNodeContextEnrichment` (2: solve children see the neighborhood; delegate enrichment only for content generation, model keys survive), `test_builtin.py` manifest pins updated deliberately; frontend api (+1: propose mode body)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1102 passed (8 skipped); frontend `npx jest` full → 717 passed; `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-64aec69a` (backend), `COMMIT-47bbcedc` (frontend api), docs commit (this entry + memo flip)
- Issues/regressions discovered: none new; the propose-loop test initially forgot to install the NCB specialist — the missing-specialist path answered exactly as designed (all targets failed with the install-proposal reason).
- Follow-up work: 67-7 consumes `previousAttempt` + the journal for validation rounds; 67-9 drives propose-mode solve one node at a time and opens the review from the streamed proposalId; `nodeProposals: {ref: proposalId}` on the session if reload-addressability beyond the active mirror proves necessary.

---

## BL-P5-20260805-11: Execute-through-node validation + self-correction (memo dev/67-7 — Simulation Mode: validate)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: 67-0 "Every Solve must be validated" (execute the dataflow from its valid start through the node being solved; inspect runtime output/logs/errors; self-correct; present for approval; no "trivial code" shortcut) + "use the existing programmatic execution infrastructure … exercised by the Playwright-based dataflow tests"
- Design decisions/artifacts: memo `dev/67-7` (program index `dev/67-1`)
- Tasks: `TASK-P5-execute-through-validation`
- Risks/questions: validation must never corrupt state — structural mitigations: the candidate runs as an IN-MEMORY overlay (only an approved Apply writes), validation executions pass `save_dataset: false` (no computed-dataset side effects), journal records are marked `validation`, and the in-flight guard clears in a finally on every exit including client disconnect
- Design-to-code decision or deviation: **(1) Promotion** — `workflow_spec.py` + the sandbox-HTTP runner moved to `app/execution/` (single source; the e2e suite imports through byte-thin shims — `parse_workflow`, `execute_workflow_programmatically`, seed/widget/artifact helpers keep their historical names); `parse_workflow_dict` added for in-memory specs. **(2) `run_through_node`** — ancestor slice (reverse BFS, the server twin of `playNodesUpTo`), Kahn order, merge `in_N` assembly, pass-through for browser-only types, candidate overlay, per-node accumulate-and-stop (an upstream failure names the BLOCKER, the target is never blamed), cycle/25-node-bound refusals, transport failures reported as INFRASTRUCTURE never node errors, JS nodes via `/execJs`. **(3) Deterministic verdicts** — `app/agents/validation.py`: pass / fail (execution-error | upstream-blocker | precondition | type-mismatch — runtime output dataType vs the consumer's DEC-051 declared input types, fail-open on unmapped types or out-of-scope templates) / infrastructure; the goal is EVIDENCE for human judgment, never machine-judged. **(4) The loop** — `validate-node` SSE endpoint: generation (67-6 composer context) → threaded validation with live `node_executed` events (dev/63 queue pattern) → `round_verdict` → on fail re-generate with `previousAttempt` + `validationError` (the NCB instruction's contract), ≤2 corrective rounds → the FINAL candidate mints a `node.content.write` proposal carrying the `validation` block — PASS or FAIL, the user decides; `nodeStates[ref] → validated|failed`; an `infrastructure` verdict mints nothing and leaves state untouched. **(5) Frontend** — the review card renders the verdict block (PASS + output type / FAIL + traceback in a collapsible + upstream-blocker naming) and labels the button "Apply anyway" on FAIL; `agentsApi.validateNode` streams the loop. Recorded deviations: no dedicated cancel endpoint (disconnect clears the guard; the 67-9 driver owns pause/cancel UX); the runner journals with `durationMs: 0` (per-node timing joins when needed).
- Files/modules changed: backend `app/execution/{workflow_spec.py (new),runner.py (new)}`, `app/agents/{validation.py (new),services.py,routes.py}`, `tests/test_frontend/{workflow_spec.py,utils.py}` (shims); frontend `api/agentsApi.ts`, `components/agents/attach/AgentAttachmentsProvider.tsx`, `components/agents/content/AgentReviewCard.{tsx,module.css}`
- Tests added/updated: backend `test_runner.py` (new, 8: ancestor slice, overlay-no-mutation, upstream-blocker stop, infrastructure, cycle+bound refusals, merge fan-in assembly, journal validation flag, save_dataset false), `test_validation.py` (new, 5: pass evidence, execution-error, blocker naming, type-mismatch + fail-open, infrastructure), `TestValidateNode` (4: pass mints validated proposal + transcript block + untouched spec; self-correction sees previousAttempt + the real traceback; exhaustion fails loudly but still proposes; preflight guards 422/404/409); frontend card (+4: PASS, FAIL + Apply anyway, blocker naming, no-validation regression), api (+1 stream)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1119 passed (8 skipped); frontend `npx jest` full → 722 passed; shim imports verified (`parse_workflow is app.parse_workflow`)
- Commit/PR: `COMMIT-d2053cf9` (runner promotion), `COMMIT-6b0b0c71` (validation + endpoint), `COMMIT-9b51b4ae` (frontend), docs commit (this entry + memo flip)
- Issues/regressions discovered: none new; the Playwright suite consumes the promoted runner through shims (full e2e parity check requires the live-server suite, which stays out of the default run as before).
- Follow-up work: 67-9 drives create → propose → validate per node and owns pause/cancel; outputSchema evidence when the journal captures column metadata; per-node durations in validation journal records.

---

## BL-P5-20260805-12: Connection review stage (memo dev/67-8 — Simulation Mode: connect)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: 67-0 "Connection review happens after node validation" (vertical inspectable edge list, source/target adjacent, topology validation BEFORE an edge applies) + "Merge-node detection" at the connection stage
- Design decisions/artifacts: memo `dev/67-8` (program index `dev/67-1`)
- Tasks: `TASK-P5-connection-review`
- Risks/questions: per-edge application against live truth can partially succeed — embraced, not fought: refusals are PER EDGE and named, remaining rows stay applicable, and a refused edge blocks proposal completion (a replan resolves it)
- Design-to-code decision or deviation: **(1) Named rows at mint** — `part.plan.edges` gains labeled, index-stable rows (`fromLabel`/`toLabel` from plan titles or spec goals, `toHandle` when explicit). **(2) `apply-edges`** — the third staged apply over the pinned plan: default all-pending or an index subset; endpoints resolve through 67-5's `appliedNodeIds` (created refs) or the live spec (existing ids) with refusals like "create 'Load' first"; fan-in re-validates against the CURRENT spec at apply time (DEC-051 rendered capacity, Merge suggestion named); merge targets take the named free slot or the lowest free; a user-drawn duplicate applies as a no-op ("already connected", never a double edge); each apply re-pins the shape digest (67-5 semantics); `edgeStates {index: applied|refused}` rides the proposal + the builderSession + the activeProposal mirror. When every ref AND every edge is applied the proposal COMPLETES (status applied, phase applied|ready by nodeRuns, result turn logged). **(3) Live canvas** — a new quiet `edges-created` bridge mutation (handles + `data: {}` parity, idempotent per batch, NO fit/center — the nodes are already in view); the event-bus allowlist gained the kind (the silent-drop allowlist was the one integration bug this memo hit — caught by the new tests). **(4) Card** — the Connections section on pending plan proposals: monospace `from → to [handle]` rows, per-row Connect gated until both endpoints exist (reason as tooltip), Connected ✓ / Refused ✗ chips, Connect all.
- Files/modules changed: backend `app/agents/{services.py,routes.py}`; frontend `utils/agentCanvasEvents.ts`, `api/agentsApi.ts`, `components/agents/attach/{useAgentCanvasMutations.ts,AgentAttachmentsProvider.tsx,AgentChatPanel.tsx,AgentDockOverlay.tsx}`, `components/agents/content/AgentReviewCard.{tsx,module.css}`
- Tests added/updated: backend `TestPlanEdgeApply` (4: labeled rows on the part; refuse-until-created → apply-with-handles → spec persistence → completion + post-completion 409; manual-edge no-op without duplication; fan-in refusal naming the Merge resolution with a refused edge blocking completion); frontend card (+2: gated named rows + per-index Connect, state chips + Connect all), bridge (+1: quiet insert with handles, idempotent per batch, no fit/center), provider (+1: edges-created dispatch), api (+1: subset + default bodies)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1123 passed (8 skipped); frontend `npx jest` full → 727 passed; `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-4e94a0ac` (backend), `COMMIT-53bacb88` (frontend), docs commit (this entry + memo flip)
- Issues/regressions discovered: `subscribeAgentCanvasMutations` allowlists mutation kinds and silently dropped the new `edges-created` — caught immediately by the bridge + provider tests; the allowlist gained the kind. Lesson: an event-bus with a kind allowlist is a RECOGNITION predicate too (the dev/61 lesson, one layer down).
- Follow-up work: 67-9 sequences nodes-then-connections and owns the stage chip; edge rows for REVISION plans (removals) stay on the dev/59 card path.

---

## BL-P5-20260805-13: Simulation Mode orchestration + Apply Plan as automated sequence (memo dev/67-9, DEC-054 — the dev/67 assembly)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: 67-0 "Default build model: first-class Simulation Mode" + "Apply Plan remains available as an alternative" (never uncontrolled bulk generation; per-node generation/Solve/validation, error isolation, self-correction, ordering, topology checks; a failure pauses progression)
- Design decisions/artifacts: **`DEC-054`** (memo `dev/67-9`; program index `dev/67-1`)
- Tasks: `TASK-P5-simulation-mode`
- Risks/questions: the single-activeProposal model would have let the sequence's own content reviews SUPERSEDE the pending plan — resolved structurally: the plan PARKS (`record.planProposal`) when a non-plan proposal mints over it, `_pending_plan_proposal` falls back to the parked slot, a new plan replaces any parked one, and completion clears it; the listing exposes `planProposal` so the card's per-node state survives the parking
- Design-to-code decision or deviation: **(1) One transition function** — `_next_simulation_action`: per ref in topological order (mint's depth math) `planned→create`, `created|failed→validate` (a failed ref re-validates on RESUME; the pause happened when it first failed), `validated→approve`, `solving→await-review` (pause — a review outside the driver's loop), all-approved→`connect` (zero-edge plans simply complete). **(2) The driver** — `simulate` SSE endpoint (`step`|`auto`): read-fresh → act → persist → emit per action; pause reasons persisted (`pauseReason` {kind, ref, proposalId, message}) for validation-failed / infrastructure / connection-refused / content-review-pending; auto-approve annotates the content proposal `approvedBy: "simulation-auto"`; cancellation = dev/63 dual signals (`simulate/cancel` + registry) honored at action boundaries; disconnect clears the in-flight guard in a finally; a 500-action runaway backstop. **(3) Canvas over the stream** — `node_created`/`node_content_applied`/`edges_created` events ride the driver stream and the provider maps them onto the existing bridge (the frontend never re-implements insertion). **(4) UI** — "Build & validate plan" is the strip's primary plan action (auto), Step beside it, "Resume" when paused (with the reason narrated as a plain sentence), Cancel while running, live narration lines ("executing upstream 2/5…"), and the classic bulk apply demoted to the explicit "Apply all without validation"; parked plans keep driving the controls and the card's per-node state.
- Files/modules changed: backend `app/agents/{services.py,routes.py}`; frontend `api/agentsApi.ts`, `components/agents/attach/{AgentAttachmentsProvider,AgentBuilderStrip,AgentChatPanel,AgentDockOverlay}.tsx`
- Tests added/updated: backend `TestSimulationDriver` (5: auto builds/validates/approves/connects the whole plan with recorded auto-approval + canvas events on the stream; step performs exactly ONE action and N steps ≡ one auto run with post-completion 409; validation failure pauses with nothing downstream generated, then Apply-anyway + resume completes; cancel stops at the boundary keeping done work; preflight guards) — the parking mechanism is exercised by every multi-node sequence; frontend api (+2), provider (+1: stream→bridge mapping), strip (+2: default/secondary labeling + step, paused narration + Resume with a parked plan)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1128 passed (8 skipped); frontend `npx jest` full → 732 passed; `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-4e35002b` (backend driver + parking), `COMMIT-7beca0dd` (frontend), docs commit (this entry + DEC-054 ledgers + memo flip + index)
- Issues/regressions discovered: the completing step initially left later calls 404ing on the vanished plan proposal — the eager check now answers "the plan is no longer pending — nothing to simulate" (409) and the completing step reports `nextAction: null`.
- Follow-up work: 67-4 (Node Researcher — the program's remaining memo); a "connecting" phase chip if stage-level chips prove wanted; per-edge Step granularity in the connect stage; DEC-021 background execution for long sequences.

---

## BL-P5-20260805-14: Node Researcher + verified external discovery (memo dev/67-4, DEC-053 — the dev/67 program's completion)

- Date / author: 2026-08-05 / Karla
- Status: verified
- Requirements: 67-0 "Dataset Finder hallucination" (external discovery as a verifiable research operation; never treat model-generated identifiers/URLs as valid without verification; chainable from the builder agents) + "Node Researcher" (a true research agent with web capability) + the owner's scope directive: **the Socrata fix generalizes to ANY dataset API connection**
- Design decisions/artifacts: **`DEC-053`** (memo `dev/67-4`; program index `dev/67-1`); LangChain declined for the ≤3-step research chain — "unbounded research chains" joins DEC-021 as a monitored re-open condition
- Tasks: `TASK-P5-verified-discovery`
- Risks/questions: agent-driven egress is a new attack surface — default-deny structurally: ONE chokepoint module, post-DNS private-address refusal (metadata/loopback/rebind shapes pinned in tests), per-hop redirect re-checks, byte/time caps, a 4-call per-run budget enforced in the tool loop, per-fetch audit records
- Design-to-code decision or deviation: **(1) Egress** — `app/agents/egress.py`: injectable transport/resolver (CI never touches the network), `EgressRefused` distinct from unreachable. **(2) The gate is provider-agnostic** — `verify.py`: `verify_endpoint` (status/content-type/response-shape sample) is the universal path; `_VALIDATORS` registry keyed by URL shapes dispatches refinements — `verify_socrata` (the 4x4-id metadata probe returning real dataset name + columns) is one entry; CKAN/ArcGIS/… are future entries, never new gates; no URL → loudly `unverified` ("never checked"). **(3) Tools** — `web.fetch`/`web.search` read contracts (no spec needed — handled before the spec read); search is deployment-configured (`CURIO_SEARCH_URL` template) and errors honestly when absent; the run loop enforces the 4-call budget as data. **(4) Roster** — `agent.node-researcher` (17th entry, category evaluate, `research.verify`/`research.summarize`, web tools + node.read; `research_instruction.txt`: verify-before-assert, failure-to-verify IS a finding, VERIFIED/REPORTED/UNVERIFIED vocabulary, budget-aware); chainable from Dataset Finder, Node Builder, Dataflow Builder. **Deviation of substance**: DEC-046 children are structurally TOOL-LESS, so a researcher-as-delegate cannot fetch — `research.verify` delegate inputs are enriched with RUNTIME-verified evidence (the deterministic validators run before the child; the child synthesizes over real evidence), keeping DEC-046 intact; the researcher's own attachment runs use the web tools directly. **(5) The finder gate** — `_verify_candidate_parts` at BOTH reply-extraction sites: external rows probed (first 4, the budget shape), URL-less rows loudly unverified; `verification` rides the row into the card (Verified ✓ / Unreachable ✗ / Refused ✗ / "Unverified — never checked" badges) and the handoff; the discovery instruction forbids ungrounded identifiers and forbids the model claiming verification itself.
- Files/modules changed: backend `app/agents/{egress.py (new),verify.py (new),tools.py,services.py,builtin.py}`, `utk_curio/llm-prompts/{research_instruction.txt (new),discovery_instruction.txt}`; frontend `api/agentsApi.ts`, `components/agents/content/AgentDatasetCandidatesCard.tsx`
- Tests added/updated: backend `test_egress.py` (new, 6: scheme allowlist, post-DNS SSRF shapes incl. rebind, public pass, bounded+audited fetch, redirect-to-private refusal at the hop, redirect cap, body-cap marker), `test_verify.py` (new, 7: the GENERIC gate verifying an arbitrary non-Socrata API — the generalization pin; 404/transport/SSRF/no-URL honesty; the Socrata refinement extracting name+columns; the exact hallucinated-id shape from session 89ae8123 answered "unreachable" BY id), `test_tools.py` `TestWebTools` (3), `TestVerifiedDiscovery` (3: rows carry verdicts through a real finder run incl. the loud no-URL case; research.verify delegate evidence enrichment with model keys surviving; researcher installable with pinned grants), roster/registry pins updated deliberately (16→17); frontend candidates card (+1: all three badge states)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1147 passed (8 skipped); frontend `npx jest` full → 733 passed; `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-7784921e` (egress + validators), `COMMIT-d8faf75e` (tools + roster + gate), `COMMIT-745c00f2` (frontend badges), docs commit (this entry + DEC-053 ledgers + memo flip + index completion)
- Issues/regressions discovered: none new; the candidates tail fixture initially omitted the `lanes` wrapper (caught by the gate test, not a runtime defect).
- Follow-up work: provider-native web search behind the same policy; more validator registry entries (CKAN, ArcGIS, Data.gov) as usage demands; egress audit records onto the execution record's pins.

---

## BL-P5-20260812-15: Progressive node lifecycle — per-node Connect/Solve/Run on the plan card (memo dev/71, adjusting dev/67)

- Date / author: 2026-08-12 / Karla
- Status: verified
- Requirements: the owner's dev/71 adjustment — `Plan → Apply node → Connect available dependencies → Solve → Apply content → Run through node → Validate → Continue`; connections created progressively as endpoints become available; per-node Solve gated on dependency readiness; per-node Run collecting outputs/schema/logs/errors accessible to agents; Node Builder attached to each created node
- Design decisions/artifacts: memo `dev/71` (adjusts DEC-054's sequencing; no new DEC — the decisions of record stand, re-sequenced)
- Tasks: `TASK-P5-progressive-lifecycle`
- Risks/questions: progressive edges complete the plan's STRUCTURE earlier than 67-8 assumed — resolved: completion (all refs + all edges applied) is the reviewed-structure milestone; content keeps its own lifecycle (nodeStates/nodeRuns), the parked plan is KEPT after completion so per-row Solve/Run and the 67-9 driver's validate/approve actions continue on the applied plan, and the last content approval flips the phase to ready
- Design-to-code decision or deviation: **(1) One per-edge policy** — the 67-8 core extracted into `_plan_edge_context` + `_apply_one_plan_edge` (endpoint resolution, already-connected no-op, DEC-051 fan-in, merge slots), consumed by the connect stage AND the new progressive sweep; `record_missing=False` lets the sweep skip not-yet-created endpoints silently while the explicit stage still names them. **(2) Apply = create + attach + connect** — `apply_plan_node` now auto-attaches the Node Builder (best-effort, idempotent, skipped with `attachedAgentId: null` when uninstalled — creation never fails over it) and sweeps every eligible plan edge (created refs or existing canvas nodes; topology refusals recorded per edge, never blocking); response carries `createdEdges`/`edgeResults`/`edgeStates`/`attachedAgentId` and the bridge draws the edges live. **(3) Run-node** — `run-node` SSE endpoint over the 67-7 runner WITHOUT a candidate (`as_validation=False` — journal rows are REAL runs), streaming `node_executed` progress, result card with output type/chain length or the stderr tail; agents read outcomes via the 67-2 journal (`node.runtime.read`), which was the "accessible to agents" requirement. **(4) Card rows** — dependency lines ("needs: Load CSV"), lifecycle chips (Created ✓ → Content review pending → Validated → Solved ✓ → Failed retries), per-row Solve (client-gated: incoming edges applied ∧ upstream plan refs approved, blocker named in the tooltip; calls the 67-7 validate loop) and Run (approved rows); readiness computed from the ledgers already on the mirror — no new persisted state. **(5) Driver alignment** — create actions emit the sweep's `edges_created`; the loop and step-preview read the plan in ANY status (`_plan_proposal_any`); the eager check answers "the plan is complete" only when NO action remains.
- Files/modules changed: backend `app/agents/{services.py,routes.py}`, `app/execution/runner.py` (`as_validation`); frontend `api/agentsApi.ts`, `components/agents/attach/{AgentAttachmentsProvider,AgentChatPanel,AgentDockOverlay}.tsx`, `components/agents/content/AgentReviewCard.tsx`
- Tests added/updated: backend `TestProgressiveLifecycle` (6: apply-order-free connectivity incl. B-before-A; existing-node edges wire on first apply; auto-attach + idempotence; uninstalled skip; run-node chain execution + real-run journal rows; failure report + guards); five dev/67-5/8/9 tests updated DELIBERATELY to the progressive semantics (sequential application now completes the structure; refuse-until-created → connect-on-apply; manual-noop and fan-in refusal re-staged; the step preview reads applied plans); frontend card (+4: dependency line, gated Solve with named blocker, enablement matrix, Run per ref), api (+1), provider (+1: node + edges dispatched from one apply)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1156 passed (8 skipped); frontend `npx jest` full → 776 passed; `tsc --noEmit` clean for touched files
- Commit/PR: `COMMIT-4aa2f6f6` (progressive core), `COMMIT-4ae50b66` (run-node), `COMMIT-2d9b5c05` (rows), docs commit (this entry + memo flip)
- Issues/regressions discovered: the step-mode nextAction preview still required a PENDING proposal and reported completion mid-lifecycle — fixed with the any-status plan access.
- Follow-up work: run outcomes surfaced as row chips from the journal (currently the transcript card + agent tools carry them); driver auto mode interleaving Run-after-approve if demand appears; 67-1 index note.

## BL-P5-20260812-16: Delegation transparency — every delegated task lives in its agent's chat, icon-linked from the parent (memo dev/72)

- Date / author: 2026-08-12 / Karla
- Status: verified
- Requirements: the owner's dev/72 directive — the Solve lifecycle (generation, dependency checks, execution, validation, warnings/errors, self-corrections, outcome) surfaced in the node's attached Node Builder; the same transparency model generalized to EVERY delegated agent task (task/progress/diagnostics/retries/result in the responsible agent's transcript, concise); the parent references/summarizes with a direct chat link — the delegated agent's icon visually notched into the task title, click-to-open; failures attributable; uniform across Node Builder, Dataset Finder, Node Researcher, connection agents, future agents
- Design decisions/artifacts: memo `dev/72` (extends DEC-046's delegation seam without touching its invariants: depth-1, tool-less children, reply never parsed)
- Tasks: `TASK-P5-delegation-transparency`
- Risks/questions: single `activeProposal` slot contention (the recorded 67-6 limitation) — resolved for per-node content reviews by homing them on the node's own agent; the 67-9 parking mechanism stays for plans
- Design-to-code decision or deviation: **(1) Delegation homes** — `_delegation_home`: node-scoped work (nodeId present) → the node's `agent.node-builder` attachment (created via the dev/71 `_attach_node_builder` when missing); everything else → an existing attachment of the delegate's agent id (canvas preferred), else a best-effort canvas attach; a failed home NEVER fails the delegation (part carries `attachmentId: null`). **(2) One traced choke point** — `_run_delegate_traced` wraps `delegation.run_delegate` (the DEC-046 seam stays pure): framed task turn ("[Delegated by <parent>] <task>") + result turn (bounded reply, trace card with capability/verification-evidence/duration, child execution record) — exactly two turns per task, structure in the card; both loop call sites and the Solve worker use it. **(3) Typed `delegation` part** — runtime-emitted (never parseable from tails), bounded, persisted on the PARENT's turn: {capability, coord, name, category, attachmentId, status, summary}; the stream's `delegate_result` event gains attachmentId/name. **(4) Proposal home migration** — validate-node and propose-mode Solve mint the content proposal ON the node's Node Builder attachment when present (`done.proposalAttachmentId`), with a consolidated rounds-trace card + task turn in that chat and a parent summary turn carrying the delegation part; `nodeProposals[ref]` → `{proposalId, attachmentId}` (legacy strings tolerated); the driver approves through the home; `_apply_node_content_write` advances the BUILDER's ledger cross-attachment. **(5) Deviations** — Solve workers resolve homes find-only (`home_create=False`: worker threads must never write the spec); service-level home attach bypasses the route's target-compatibility checks by design (the route contract is unchanged); the stream's review_required emitter learned to skip non-proposal minted parts. **(6) Frontend** — `AgentDelegationEntry`: category-tinted icon badge notched into the entry's corner (approved concept style), click → `openChat(attachmentId)`, existence-checked against the live attachments list (stale → plain entry, never a dead link); plan-row review chips carry the same icon-link via `nodeProposals[ref].attachmentId`.
- Files/modules changed: backend `app/agents/{content.py,services.py}`; frontend `api/agentsApi.ts`, `components/agents/content/{AgentDelegationEntry.tsx+css (new),AgentReviewCard.tsx+css}`, `components/agents/attach/{AgentChatPanel,AgentDockOverlay,AgentAttachmentsProvider}.tsx`
- Tests added/updated: backend `TestDelegationTransparency` (3: home auto-created + two-turn transcript + parent part; existing node-target home reused; missing home never fails) and `TestSolveTraceHome` (2: validate homes trace + review at the node's agent with cross-attachment ledger advance on apply; driver auto approves through the home with the new nodeProposals shape); frontend `AgentDelegationEntry` suite (4), panel delegation rendering (2), review-card chip links (2)
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1161 passed (8 skipped); frontend `npx jest` full → 784 passed; `tsc --noEmit` clean (two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-bdf8d52d` (backend), `COMMIT-9c9f4f96` (frontend), docs commit (this entry + memo flip)
- Issues/regressions discovered: minted delegation parts crashed the stream's review_required emitter (it assumed every minted part was a proposal) — guarded; the attach ROUTE enforces target compatibility (node-target-only agents refuse canvas attach) while the service-level home creation does not — recorded as intended layering.
- Follow-up work: running-state (in-flight) delegation entries in the transcript (today the transient stream line covers liveness); a Node Builder chat affordance to jump BACK to the parent builder; researcher evidence lines rendered as structured rows.

## BL-P5-20260812-17: Chat content updates mint the review — runtime-owned content→proposal, honest round cap, DFB update route (memo dev/73)

- Date / author: 2026-08-12 / Karla
- Status: verified
- Requirements: the owner's report — post-solve node update requests through the attached Node Builder or the Dataflow Builder were claimed "applied" with no change, or produced code (whole or fragments) in prose with no Apply button
- Design decisions/artifacts: memo `dev/73` (root-cause investigation + fix; DEC-006/DEC-046 preserved)
- Tasks: `TASK-P5-chat-content-review-mint`
- Risks/questions: three compounding root causes, all in the chat loop — RC1: `MAX_TOOL_ROUNDS=2` contradicted the Node Builder's own three-round modify flow (read → delegate generate → propose), with the final-round suffix forcing prose and a dangling write request SILENTLY dropped at the cap; RC2: chat delegations minted nothing applyable (only Solve/validate did) — applyability hung on the model's fragile second toolRequest step; RC3: the Dataflow Builder had no working update path (plan-write only, plans content-free) and its offered `node.build` delegation to a tool-less child invited the false success summary
- Design-to-code decision or deviation: **(1) One content→review sequence, three callers** — the Solve drain's mint extracted into `_mint_content_review_from_delegate`; both chat loops call it after any successful `node.content.generate` delegation whose node resolves (`inputs.nodeId` or the attachment's node target, via `_delegate_target_node_id`); generation success ⇒ the review EXISTS, runtime-owned. **(2) Homing per dev/72** — the review mints at the node's own agent when one exists (find-only — the traced delegation created it when it could, now passed `node_id` from chat too); a foreign home gets the review turn in its session and the stream's `review_required` gains `attachmentId` (the client's reload path already listens); a local home rides the parent turn's parts exactly like loop mints. **(3) The fed-back contract** — the delegate result message becomes "minted as reviewed proposal <id> … do NOT restate the code, do NOT say it was applied"; the parent's delegation entry summarizes "reviewed content change proposed — awaits Apply"; unusable content feeds back an honest refusal naming that nothing awaits review. **(4) Honest cap** — `MAX_TOOL_ROUNDS` 2→3; a MUTATE toolRequest (the `_mint_proposal` dispatch set, `MUTATE_PROPOSAL_TOOLS`) dangling at the cap appends a "Proposal step cut off" error card instead of the silent drop; read-tool dangles keep the text-kept behavior. **(5) Roster/prompt contracts** — DFB `delegates_to` gains `agent.node-content-builder` (the capability was previously reachable only through the silent capability-first fallback, never OFFERED); NB instruction step 6: the runtime mints, don't re-emit, summarize and point (also dropped the ungranted `node.read` mention); DFB instruction step 6: delegate `node.content.generate` with the node's REAL id, never `node.build` for edits, never claim applied. Prompt bytes flow to existing installs via the dev/60 roster-bytes rule.
- Files/modules changed: backend `app/agents/{services.py,builtin.py}`; prompts `utk_curio/llm-prompts/{node_build_instruction,orchestration_instruction}.txt`
- Tests added/updated: `TestChatContentReviewMint` (5: node-attached update mints locally + Apply writes the content end-to-end; canvas parent homes the review at the node's agent; DFB routes through the new roster entry; mutate dangle at the cap → visible cutoff card; empty generated content → honest refusal) + `TestChatContentReviewMintStream` (foreign-homed `review_required` carries `attachmentId`; the proposal never rides the parent turn); round-budget pins updated deliberately (`test_delegate_rounds_share_the_tool_round_budget` 2→3 delegates, `test_round_cap_bounds_the_loop` 3→4 calls, plan correction caps 3→4 calls), DFB `delegates_to` manifest pin extended
- Verification evidence: backend `pytest tests --ignore=tests/test_frontend` → 1167 passed (8 skipped); no frontend changes required (additive event key; the cutoff card renders through the generic card shell; delegation entries already link homes)
- Commit/PR: `COMMIT-95f7fc27`, docs commit (this entry + memo flip)
- Issues/regressions discovered: the activeProposal mirror carries `nodeId` (not `pins`) — test assertion corrected during authoring; nothing else surfaced
- Follow-up work: the blocking (non-stream) client path reloads attachments only on local proposals — acceptable while the stream is the primary transport, revisit if the fallback path gains weight; consider offering `research.verify` the same runtime-mint treatment for verification-backed content edits

## BL-P5-20260813-18: Agents drawer adopts the shared catalog chrome — search/sort bar + avatar card grid (memo dev/68)

- Date / author: 2026-08-13 / Karla
- Status: verified
- Requirements: the owner's directive — match the approved concept (`png-concepts/01`/`02`): add the search bar and sort dropdown using the same components/styling/behavior as the Datasets and Node Packages drawers, left-align category avatars in each row on the shared row structure, reuse existing chrome instead of Agents-specific variants, preserve all agent-specific content/actions, keep the change scoped to the drawer UI
- Design decisions/artifacts: memo `dev/68`
- Tasks: `TASK-P5-agents-drawer-chrome`
- Risks/questions: `AgentCard` carries no creation timestamp — resolved: "Sort: New" is defined as the server roster order (documented in `agentListUtils`), "Sort: Name" shares the `sortPackages` comparator; the concept's category filter-chip row deliberately excluded (no sibling drawer has one — feature, not chrome)
- Design-to-code decision or deviation: **(1) Shared bar, not a fork** — `PackageSearchRow` gains optional `placeholder`/`sortAriaLabel` (defaults = today's strings, so Datasets/Nodes render byte-identically); Agents passes "Search agents, hooks, keywords..." / "Sort agents". **(2) View-state only** — search/sort are local `useState` + `useMemo` over the untouched `useAgentsCatalogDrawer` per-scope cache (dev/47 race guard/SWR intact); client-side like the Nodes drawer, no debounce (no server round-trip). **(3) Row grid reuse** — `AgentRow` moved onto `PackageCard.module.css` (accent | avatar | body | action), the exact pattern DatasetCard uses; category-tinted robot avatar keyed by the existing `agentCategoryKey`; light-surface tints added to the agents module (palette hues); ~100 lines of duplicated card/button/tag CSS deleted. **(4) Hierarchy** — reordered to header → subtitle → search/sort → tabs → list → footer per the concept; search-empty vs scope-empty messages distinguished.
- Files/modules changed: frontend `components/agents/catalog/{AgentsCatalogDrawer.tsx,AgentsCatalogDrawer.module.css,agentListUtils.ts (new)}`, `components/packages/publishing/PackageSearchRow.tsx`
- Tests added/updated: `agentListUtils` unit suite (8: field coverage, case/trim, empty query, name/new sort semantics, copy-not-mutate); drawer suite +6 (filter + clear, search-specific empty message, DOM-order name sort + roster-order default, query persists across scope tabs, per-row tinted avatar aria-hidden); existing 22 drawer tests pass unmodified
- Verification evidence: frontend `npx jest` full → 788 passed (5 consecutive runs); `tsc --noEmit` clean (two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-77bd7ed8`, docs commit (this entry + memo)
- Issues/regressions discovered: the Datasets drawer's sort select was found force-casting `DatasetSortMode` into the package options — fixed immediately after as dev/74 (next entry)
- Follow-up work: dataset-flavored search placeholder/aria copy inside the Data Catalog drawer (now trivial via the new props); panel width 560px vs the siblings' 520px; skeleton loaders + tab count badges for the Agents drawer

## BL-P5-20260813-19: PackageSearchRow typed sortOptions — Data Catalog sort select on the dataset contract (memo dev/74)

- Date / author: 2026-08-13 / Karla
- Status: verified
- Requirements: the owner's directive — fix the pre-existing bug surfaced during dev/68: the Data Catalog drawer's sort state (`"recent" | "name"`) was cast into `PackageSearchRow`'s hardcoded `"new"`/`"name"` options, so the default matched no option (blank select) and "Sort: New" sent `?sort=new`, which the backend handles only through its silent non-"name" fallback branch
- Design decisions/artifacts: memo `dev/74`
- Tasks: `TASK-P5-dataset-sort-contract`
- Risks/questions: rename the dataset vocabulary to "new" instead? — rejected: `"recent"` is used consistently across palette context, providers, UpMenu, DatasetsPaletteDropdown, hooks, API and backend (10+ files); the UI adapts to the domain, not vice versa
- Design-to-code decision or deviation: `PackageSearchRow` becomes generic (`<S extends string = SortMode>`) with optional `sortOptions` defaulting to the package pair — Nodes/Agents compile and render unchanged; the Data Catalog drawer passes `recent`/`name` options labeled "Sort: Recent"/"Sort: Name" (recency wording matches the browse page and the backend's updatedAt semantics) and both `as` casts are deleted, so only backend-documented wire values can reach `?sort=`
- Files/modules changed: frontend `components/packages/publishing/PackageSearchRow.tsx`, `components/datasets/catalog/DatasetCatalogDrawer.tsx`
- Tests added/updated: new `PackageSearchRow` suite (4: default options/copy regression for Nodes/Agents; custom options with a matching non-blank default selection; custom-vocabulary change round-trip; search forwarding + dev/68 copy overrides)
- Verification evidence: frontend `npx jest` full → 788 passed; `tsc --noEmit` clean (two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-400d7a09`, docs commit (this entry + memo)
- Issues/regressions discovered: none beyond the target bug
- Follow-up work: consider surfacing the backend's accepted sort values as a shared constant if a third sort consumer appears

## BL-P5-20260813-20: Chat transcripts follow-at-bottom auto-scroll + Jump to latest (memo dev/75)

- Date / author: 2026-08-13 / Karla
- Status: verified
- Requirements: the owner's directive — auto-follow streamed content only while the user is at/near the bottom of the transcript; a manual scroll-up detaches follow so streaming never moves the viewport; follow resumes on bottom-return or an explicit compact "Jump to latest" action shown only while newer content sits below; consistent across all agent chat transcripts and delegated-agent chats
- Design decisions/artifacts: memo `dev/75`
- Tasks: `TASK-P5-chat-autoscroll-follow`
- Risks/questions: telling programmatic scrolls from user scrolls (a smooth jump animating through non-bottom positions must not read as a detach and cancel itself) — resolved with a suppress-flag handshake: set only when a scroll write will actually move, cleared by the scroll landing near the bottom or by genuine user input (wheel/touchmove/pointerdown/keydown), so a user scrolling during the animation still wins
- Design-to-code decision or deviation: **(1) One hook, one pill** — `useTranscriptAutoScroll` owns pinned/follow/jump semantics (48px bottom threshold, tolerant of fractional zoom pixels); the shared `TranscriptJumpButton` overlay pill (dark `--curio-top-bar-bg` token, absolute inside a `position:relative` wrap so appearing never shifts layout) keeps both surfaces identical. **(2) Zero per-chunk re-renders** — SSE streaming replaces the last turn per chunk, so the pinned flag lives in refs and `atBottom` state flips only on pinned↔detached transitions; follow uses instant scroll in a layout effect (smooth lags streaming). **(3) Explicit re-pin moments** — attachment switch/`resetKey`, history hydration completing (`ready`), sending a message (`pinToLatest`), and the jump action; `prefers-reduced-motion` downgrades the jump to instant; a `ResizeObserver` (container + last child) re-pins on late layout growth only while pinned. **(4) Delegated chats free** — dev/72 opens delegates in the same `AgentChatPanel`, so one adoption covers both; `LLMChat` drops its `getElementById("messagesDiv")` effect for the ref-based hook
- Files/modules changed: frontend `hook/useTranscriptAutoScroll.ts` (new), `components/TranscriptJumpButton.tsx` + `.module.css` (new), `components/agents/attach/AgentChatPanel.tsx` + `.module.css`, `components/LLMChat.tsx`
- Tests added/updated: hook suite (10: pinned follow, streamed-while-detached regression, bottom-return resume, 48/49px threshold boundary, jump re-pin + follow, smooth-jump suppression handshake, wheel-wins-during-jump, pinToLatest, hydration deferral, resetKey force-pin); `AgentChatPanel` +5 (pill hidden at bottom, streamed turn replacement while scrolled up never moves the transcript, pill click jumps/resumes/hides, manual bottom-return hides, send re-pins); new `LLMChat` suite (2: pill visibility contract, jump behavior)
- Verification evidence: frontend `npx jest` full → 805 passed (75 suites); `tsc --noEmit` clean (two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-631b96eb` (hook + pill + unit tests), `COMMIT-f4d80b0c` (AgentChatPanel adoption + regression tests), `COMMIT-724ebd3f` (LLMChat adoption), docs commit (this entry + memo flip)
- Issues/regressions discovered: a pre-existing stray `console.log('checkForGoal', …)` debug line in the LLMChat working tree predates dev/75 — deliberately kept out of the commits, left unstaged for the owner to keep or drop
- Follow-up work: any future transcript surface must adopt the hook + pill rather than a bespoke scroll effect; consider an unread-count badge on the pill; fix the two tsconfig deprecation notices so typechecks aren't masked

## BL-P5-20260813-21: Agent chat multiline composer — Enter sends, Shift+Enter newline, paste keeps formatting (memo dev/77)

- Date / author: 2026-08-13 / Karla
- Status: verified
- Requirements: the owner's directive — replace the agent chat's single-line input with a multiline textarea that preserves typed and pasted line breaks, grows with content within sensible height limits, sends on Enter with Shift+Enter inserting a newline, and keeps styling/resizing consistent with Curio's existing chat UI
- Design decisions/artifacts: memo `dev/77`
- Tasks: `TASK-P5-chat-multiline-composer`
- Risks/questions: whether newlines survive the send path — resolved by inspection: `send()` only trims leading/trailing whitespace and both transcript bubbles already render `white-space: pre-wrap`, so the input element was the single point of loss; IME commit-Enter accidentally sending — resolved with an `e.nativeEvent.isComposing` guard
- Design-to-code decision or deviation: **(1) One hook, no state** — `useAutoGrowTextarea` (placed in `agents/attach/` per the dev/76 encapsulation rule) writes height straight to the DOM from `scrollHeight` in a `useLayoutEffect` keyed on the value, so prefills/composePrompt/post-send reset re-measure without keystrokes, typing costs no extra re-renders, and there is no pre-paint jump; a zero measurement (jsdom) falls back to the rows-based height. **(2) Growth cap 120px (~6 rows)** with `overflow-y` flipping to `auto` only when clamped — no transient scrollbar flash; the transcript keeps its `min-height: 0` scroll contract so the footer never pushes out of the panel. **(3) Pill preserved without a conditional** — `border-radius: 999px` → `18px` (half the one-row height) renders identically at one line and as a rounded rectangle when grown; `.footer` moves to `align-items: flex-end` so Send anchors beside the last line. **(4) The existing keydown contract was kept verbatim** (it already encoded Enter-sends/Shift+Enter) — only the element changed, plus the `isComposing` guard and an explicit `aria-label`; paste needs no handler in a textarea
- Files/modules changed: frontend `components/agents/attach/useAutoGrowTextarea.ts` (new), `components/agents/attach/AgentChatPanel.tsx` + `.module.css`
- Tests added/updated: hook suite (5: measure on value change, clamp + internal scroll at cap, programmatic prefill re-measure, post-send collapse, jsdom zero-measurement tolerance); `AgentChatPanel` +5 (Enter sends with interior newlines preserved through `onSend` — the regression for the issue, Shift+Enter never sends, IME composition Enter never sends, whitespace/newline-only never sends, user-turn newlines survive into the `.msgUser` bubble)
- Verification evidence: frontend `npx jest` full → 813 passed (75 suites); `tsc --noEmit` clean (two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-ae57cf42` (hook + unit tests), `COMMIT-5f41d2f6` (composer swap + styles), `COMMIT-2d0ca5ea` (regression tests), docs commit (this entry + memo)
- Issues/regressions discovered: none; the uncommitted dev/76 import-path edits in `AgentChatPanel.tsx` were deliberately kept out of the dev/77 commits (commit `5f41d2f6` stages HEAD's import paths so its tree builds standalone) and remain unstaged for the dev/76 docs commit — note dev/76 itself is still unlogged in this build log
- Follow-up work: `LLMChat` still has a single-line input — adopt `useAutoGrowTextarea` there; the intent editor textarea could adopt it as polish

## BL-P5-20260814-22: Transcript scroll contract encapsulated under agents/attach; LLMChat reverted (memo dev/76)

- Date / author: 2026-08-14 / Karla
- Status: verified
- Requirements: the owner's directive — move the Jump-to-latest action components into the appropriate `agents/` folder, keep all agent chat-specific behavior and UI encapsulated under `agents/`, move the related hooks/styles/helpers, update imports, reuse shared utilities only when genuinely cross-feature, organizational change only; amended mid-implementation: `LLMChat` must be untouched by the agents-related task
- Design decisions/artifacts: memo `dev/76`
- Tasks: `TASK-P5-transcript-scroll-encapsulation`
- Risks/questions: the dev/75 contract had two consumers (`AgentChatPanel` + `LLMChat`) — initial resolution had `LLMChat` import the agents-owned contract; superseded by the owner's untouched-LLMChat directive, which resolves the tension outright: the contract now has exactly one consumer and no cross-feature import
- Design-to-code decision or deviation: **(1) History-preserving moves** — `git mv` of `TranscriptJumpButton.{tsx,module.css}` and `useTranscriptAutoScroll.ts` → `components/agents/attach/` (the folder already holding the panel's feature hooks `useAgentAttachments`/`useAgentCanvasMutations`); the hook test → `tests/attach/` (the `agents/attach` test mirror). **(2) LLMChat revert** — the dev/75 adoption (`COMMIT-724ebd3f`) is undone: `LLMChat.tsx` restored byte-identical to its pre-dev/75 content (unconditional `getElementById("messagesDiv")` scroll effect back), `tests/components/LLMChat.test.tsx` deleted; the LLM Assistant deliberately returns to force-scroll-on-update behavior. **(3) Escape hatch recorded** — if a non-agent transcript later wants follow-at-bottom, adopt the hook and promote it back to `src/hook/` as its own change
- Files/modules changed: frontend `components/agents/attach/{TranscriptJumpButton.tsx,TranscriptJumpButton.module.css,useTranscriptAutoScroll.ts}` (moved in), `components/agents/attach/AgentChatPanel.tsx` (sibling imports), `components/LLMChat.tsx` (reverted), `tests/attach/useTranscriptAutoScroll.test.tsx` (moved in), `tests/components/LLMChat.test.tsx` (deleted)
- Tests added/updated: none added (organizational); the moved hook suite runs unchanged; LLMChat scroll suite deleted with the revert
- Verification evidence: frontend `npx jest` full → 813 passed (75 suites) on the committed tree, matching the dev/77 baseline exactly (that baseline already reflected this change's LLMChat-test deletion — dev/77's run happened on the post-revert working tree); `tsc --noEmit` nothing new in touched files; `git diff f4d80b0c -- LLMChat.tsx` shows only the pre-existing uncommitted debug line
- Commit/PR: `COMMIT-a99526aa` (atomic move + reimport + revert), docs commit (this entry + memo flip)
- Issues/regressions discovered: none — renames detected at 100%/98% similarity, history preserved; sequencing note: dev/77 landed between this change's implementation and its commit, so `a99526aa` was committed on top of the dev/77 composer (verified: its `AgentChatPanel` diff is the two import lines only)
- Follow-up work: none pending; the dev/75 follow-ups (unread-count badge, tsconfig deprecations) still stand

## BL-P5-20260814-23: Copy button on transcript code blocks (memo dev/78)

- Date / author: 2026-08-14 / Karla
- Status: verified
- Requirements: the owner's directive — add a Copy button to code blocks in agent chat transcripts: action directly on each block, copy only the code content (no Markdown fences or surrounding transcript text), brief visual feedback on success, compact control consistent with existing chat/code-output styling
- Design decisions/artifacts: memo `dev/78`
- Tasks: `TASK-P5-transcript-code-copy`
- Risks/questions: this is the frontend's FIRST clipboard integration (zero prior `navigator.clipboard`/`execCommand` usage) — resolved primary-path-only: `navigator.clipboard.writeText`, no `execCommand` fallback, a rejected write flips the button to a loud transient "Failed" state; jsdom has no clipboard, so tests define a local mock (not in `setupTests.ts`)
- Design-to-code decision or deviation: **(1) Hook point** — a `pre` entry in `SafeAgentContent`'s existing `components` map (the ONLY agent rich-content renderer, REQ-SEC-002), so every agent chat surface gets the button, delegated chats included, and inline code is excluded by construction (no `<pre>` parent). **(2) New presentational component** — `agents/content/AgentCodeBlock.tsx` + module CSS (TranscriptJumpButton pattern): wrapper `<div>` keeps the `<pre>` real so `.msgAgent pre` styling still matches; payload derived from rendered children via pure exported `extractNodeText` (strip exactly one fence-terminating `\n`, indentation verbatim); `idle|copied|failed` state with ref-held timeout, cleared on unmount, restarted on re-click; empty fences render no button. **(3) Reveal behavior** — subtle-always-visible (opacity 0.55 → 1 on block hover/`:focus-visible`/feedback); hover-only reveal rejected as undiscoverable and dead on touch. Wrapper resets `.msgAgent`'s `white-space: pre-wrap` so button chrome lays out normally. No deviations from the memo
- Files/modules changed: frontend `components/agents/content/AgentCodeBlock.tsx` (new), `components/agents/content/AgentCodeBlock.module.css` (new), `components/agents/content/SafeAgentContent.tsx` (three-line `pre` override)
- Tests added/updated: `tests/content/AgentCodeBlock.test.tsx` (new, 12 — exact payload incl. trailing-newline/indentation contract, copied/failed feedback lifecycle with fake timers, block independence, empty fence, unmount safety, timer-restart on rapid clicks, `extractNodeText` units); `tests/content/SafeAgentContent.test.tsx` (+3 — button presence, inline-code exclusion, hostile-fence copies literal text and still renders no `<script>`); `tests/attach/AgentChatPanel.test.tsx` (+1 wiring assertion inside `.msgAgent`)
- Verification evidence: frontend `npx jest` full → 829 passed (76 suites), up from the 813/75 dev/76 baseline by exactly the 16 new tests; `tsc --noEmit` → zero errors in source files (only the two pre-existing tsconfig TS5107/TS5101 deprecation notices)
- Commit/PR: `COMMIT-dafd61f6` (component + tests), `COMMIT-1ed4a707` (renderer wiring + policy regressions), `COMMIT-e1bbcc73` (integration assertion), docs commit (this entry + memo)
- Issues/regressions discovered: none
- Follow-up work: LLMChat/FloatingPanel/NodeExplanation use bare `<ReactMarkdown>` and carry no copy affordance — if wanted there, extract a shared `components` map rather than duplicating the button (same posture as the dev/77 LLMChat composer follow-up)

## BL-P5-20260818-24: Jump-to-latest fixed for real browsers — eager detach + jump landing guarantee (memo dev/79)

- Date / author: 2026-08-18 / Karla
- Status: verified
- Requirements: the owner's report — "the jump to the bottom btn is not working" in the real app while all jsdom tests pass; the dev/75 contract must actually hold in a browser: scrolling up during streaming detaches reliably, and clicking the pill always lands at the newest message
- Design decisions/artifacts: memo `dev/79` (renumbered from 78 — a parallel session claimed 78 for the transcript code-copy button)
- Tasks: `TASK-P5-jump-to-latest-detach-race`
- Risks/questions: was the diagnosis right without a browser repro? — the primary mechanism was reproduced as an ordering test against the unfixed hook (wheel-up → chunk re-render → late coalesced scroll event ⇒ scroll eaten, still pinned), then inverted into the regression test
- Design-to-code decision or deviation: **(1) Root cause 1 — the detach race**: browsers coalesce scroll events and dispatch them after queued tasks, so during streaming the next chunk's layout effect re-pins BEFORE the scroll event reporting the user's wheel-up; fix: user input meaning "leave the bottom" unpins synchronously in the input handler itself (wheel `deltaY<0`, `touchmove`, `ArrowUp`/`PageUp`/`Home`, `pointerdown` in the scrollbar gutter `offsetX>=clientWidth` with `target===container`, LTR-only noted); false positives self-heal via the next scroll event's position check. **(2) Root cause 2 — momentum cancels the jump**: native smooth scrolls die on any wheel input and macOS trackpad momentum emits wheel events for ~1–2s after the click; fix: a 450ms landing guarantee — user-intent handlers stand down while the flight is armed, a scroll event landing near the bottom disarms early, and the deadline snaps via direct `scrollTop` write (uncancelable) if the animation died; `prefers-reduced-motion` keeps its instant path. **(3) Deliberate test replacement**: the dev/75 test "wheel during a jump wins over the animation" encoded exactly the momentum-cancel behavior fix 2 removes — replaced by the flight-contract pair rather than kept
- Files/modules changed: frontend `components/agents/attach/useTranscriptAutoScroll.ts` only (consumers, pill UI, styling untouched)
- Tests added/updated: hook suite +7/−1 (race regression at the exact real-browser ordering, wheel-down never detaches, touchmove self-heal, up-key detach, gutter-vs-content pointerdown, momentum-ignored + deadline-snap with fake timers, post-landing wheel-up detaches again); `AgentChatPanel` +1 (the race at the panel surface: wheel-up → chunk → pill appears, position held)
- Verification evidence: frontend `npx jest` full → 836 passed (76 suites), up from the dev/78 829 baseline by exactly the 7 net new tests; `tsc --noEmit` nothing new in touched files
- Commit/PR: `COMMIT-62c36c17` (single squash per memo §9's allowance — both fixes interleave in the same functions), docs commit (this entry + memo flip)
- Issues/regressions discovered: memo/BL numbering collision with the parallel dev/78 code-copy work — resolved by renumbering this change to dev/79 before committing (hook comments and test names updated; the copy-button suite's dev/78 references untouched)
- Follow-up work: if a genuinely non-agent transcript ever adopts the hook, revisit the LTR-only scrollbar-gutter check and promote per dev/76's escape hatch

## BL-P5-20260818-25: Chat run-status feedback — live processing strip + session token counter (memo dev/80)

- Date / author: 2026-08-18 / Karla
- Status: verified (delivered as unstaged edits; commit plan in memo §9)
- Requirements: the owner's directive — the final assistant message must never appear without an execution status; while work is ongoing show a concise follow-up indicator (left: elapsed time + minimal animation + rotating "Cooking/Baking" label); when complete clearly indicate finished + any remaining follow-up; right side near the input shows cumulative session token usage, both updating continuously; consistent across agent and delegated-agent chats (Claude/Cursor-style); keep the existing send-button loading animation
- Design decisions/artifacts: memo `dev/80`
- Tasks: `TASK-P5-chat-run-status-feedback`
- Risks/questions: live-token updates without estimates (dev/37 Actuals-only) — resolved with an additive `usage` SSE event carrying the run's cumulative provider-reported sums once per tool-loop round; ordering of "final text" vs "finished status" — resolved by finalizing the provider's run status in the same synchronous block as the turn landing (one React 18 batched commit), ordered status-first as defense-in-depth so an unbatched world could only ever show "done beside partial text", never "final text beside running"
- Design-to-code decision or deviation: **(1) Provider-owned `runStatus` map** (`AgentAttachmentsProvider`): `{phase running|done|error, startedAt, durationMs?, usage?, liveUsage?}` keyed per attachment with a seq guard against a stale run's late events; survives panel close/reopen and cycling. **(2) Deviation — no `followUp` field on runStatus** (memo §3.1 had one): the "Awaiting your review" chip derives from the attachment's proposal mirrors (`activeProposal`/`planProposal` status `pending`) instead — self-clearing on apply/dismiss, no duplicated state, and the rehydrated path gets it for free. **(3) Status strip** sits between the suggested-prompts row and the footer (outside the scroller — the dev/75 hook untouched): left `AgentRunStatusLine` (pulsing tinted dot, deterministic label rotation `floor(elapsed/5s) % 6`, tabular-numeral elapsed; done "✓ Finished in 12s · 1.4k tokens"; error "Failed after 8s"; polite live region announcing phase transitions only, visible ticking spans aria-hidden), right `AgentSessionTokenCounter` (sum of persisted `execution.usage` + in-flight `liveUsage`; renders nothing when nothing reported). **(4) Rehydrated fallback** — without a live status, `deriveRunStatusDisplay` reads the LAST agent turn's persisted execution record (dev/37) so a reopened transcript never ends in a bare final message; pre-dev/37 history yields no fabricated status. **(5) Send-busy semantics** — `runStatus` prop `undefined` = unwired (panel-local `sending` governs: tests/previews); `null` = wired-and-idle (provider governs, keyed per attachment) — this fixes the pre-existing leak where cycling ‹ › mid-run carried the local `sending` flag into another agent's chat; the `…` glyph is unchanged. **(6) Backend additive envelope** — `usage` events per provider round (skipped when the round reported none), `durationMs` on the stream `done` AND the blocking `/run` response (both read straight off the persisted `_execution_record`); the SSE route's generic dict pass-through needed no change
- Files/modules changed: frontend `components/agents/attach/agentRunStatus.ts` (new), `useRunTicker.ts` (new), `AgentRunStatusLine.tsx` + `.module.css` (new), `AgentSessionTokenCounter.tsx` + `.module.css` (new), `AgentAttachmentsProvider.tsx`, `AgentChatPanel.tsx` + `.module.css`, `AgentDockOverlay.tsx`, `useAgentAttachments.ts`, `api/agentsApi.ts`; backend `app/agents/services.py`, `app/agents/routes.py` (docstring)
- Tests added/updated: `tests/attach/agentRunStatus.test.ts` (new, 15 — formatters, session totals incl. no-fabricated-zero and interim merge, display derivation matrix); `tests/attach/useRunTicker.test.tsx` (new, 6 — tick/rotation/resume/reduced-motion/no-interval/unmount, fake timers); `AgentAttachmentsProvider.test.tsx` +6 (running→done lifecycle with duration+usage, liveUsage from `usage` events dropped on finalize, error, blocking-fallback done, clearConversation prune, and the ordering regression: no render commit ever contains the execution-carrying final turn with a non-done phase); `AgentChatPanel.test.tsx` +11 (running strip, glyph+disable, cycling-leak regression, done/error strips, rehydrated derivation + counter, review chip, live counter, pristine no-strip, pre-dev/37 no-status); backend `test_routes.py` stream assertions extended (interim `usage` event with Actual sums, `done.durationMs` equal to the persisted record's, no `usage` event when the provider reports none)
- Verification evidence: frontend `npx jest` full → 873 passed (78 suites), up from the dev/79 836/76 baseline by exactly the 37 net new tests; `tsc --noEmit` clean (the two pre-existing tsconfig deprecation notices only); backend `pytest tests/test_agents/` → 634 passed
- Commit/PR: pending — working tree delivered unstaged by request; suggested breakdown in memo §9
- Issues/regressions discovered: pre-existing cycling leak of the panel-local `sending` flag (see decision 5) — fixed in-scope as memo §1 planned
- Follow-up work: `LLMChat` has no run-status feedback (same posture as the dev/77/78 follow-ups: adopt the shared pieces, don't duplicate); the Solve strip's batch progress could adopt `AgentRunStatusLine` for visual unity as polish

## BL-P5-20260818-26: Run status made per-reply; token counter per message AND accumulated (memo dev/80 amendment)

- Date / author: 2026-08-18 / Karla
- Status: verified (delivered as unstaged edits, amending BL-P5-20260818-25 in the same working tree)
- Requirements: the owner's follow-up on the -25 delivery — the finished/pending status with elapsed time must ride each agent message reply (not a global strip line); the token counter must show both per message and the total accumulated
- Design decisions/artifacts: memo `dev/80` (amendment note in the Status header)
- Tasks: `TASK-P5-chat-run-status-feedback`
- Risks/questions: where the live indicator lives before the reply's first delta (the streaming turn doesn't exist yet during tool rounds / the blocking fallback) — resolved with a standalone pending row at the transcript tail that hands off to the streaming turn's own meta line at the first delta
- Design-to-code decision or deviation: **(1) Per-reply meta line** — the agent row gains a bubble+meta column (`.agentCol`/`.turnMeta`); `AgentRunStatusLine` is reused per message: the in-flight reply shows the live indicator (dot + rotating label + elapsed), every finalized reply shows "✓ Finished in Xs · Nk tokens" from its OWN persisted execution record (hover = that run's in/out breakdown), error turns show "Failed" (elapsed-at-failure joined from the live run record for the just-failed reply, since client error turns carry no execution). **(2) `deriveRunStatusDisplay` → `turnStatusDisplay`** — the transcript-level fallback derivation is gone; per-turn derivation subsumes it, and the newest reply still falls back to the provider's run record when an old server sent no execution fields (the never-a-bare-final-message guarantee holds per message now). **(3) Review chip** marks only the newest reply's meta. **(4) The strip** by the composer now carries ONLY the accumulated session total, right-aligned (persisted Actuals + in-flight interim sums, unchanged); per-reply statuses being inside the scroller is correct here — a reply's status belongs to its message and scrolls with it, and the dev/75 hook needed no change (metas render in the same commit as their turns). **(5) Provider untouched** — the runStatus map, seq guard, usage events, and busy semantics from -25 all carry over
- Files/modules changed: frontend `components/agents/attach/agentRunStatus.ts`, `AgentRunStatusLine.tsx`, `AgentChatPanel.tsx` + `.module.css` (backend and provider untouched this round)
- Tests added/updated: `agentRunStatus.test.ts` — the derivation describe rewritten for `turnStatusDisplay` (5: finished from own record, review flag opt-in, error with/without record duration, null-usage no fabricated tokens, pre-dev/37 + user turns null); `AgentChatPanel.test.tsx` dev/80 block rewritten (14: standalone pending row before first delta, indicator rides the streaming reply with no duplicate row, per-reply duration+tokens across multiple replies, cumulative counter vs per-reply figures, glyph+disable, cycling-leak regression, old-server fallback, failed reply with elapsed-at-failure, rehydrated error, review chip on newest reply only, live counter with interim sums, pristine no-strip, pre-dev/37 no-status)
- Verification evidence: frontend `npx jest` full → 874 passed (78 suites); `tsc --noEmit` clean (the two pre-existing tsconfig deprecation notices only); backend unaffected (no backend files touched; -25's 634-pass result stands)
- Commit/PR: pending — working tree delivered unstaged by request (fold into the memo §9 breakdown's UI commit or a follow-up commit)
- Issues/regressions discovered: none
- Follow-up work: unchanged from -25 (LLMChat adoption; Solve strip visual unity)

## BL-P5-20260818-27: Dataset-ref ownership + install-replace semantics (memo dev/81, closing dev/65 Fixes 3+2)

- Date / author: 2026-08-18 / Karla
- Status: verified
- Requirements: the two remaining approved dev/65 fixes — an install fully REPLACES any existing ref for the same datasetId (no dict-merge; fat refs converge to lean, no stale `publishedToHub`/metadata survives a reinstall), and `spec.dataflow.datasets` becomes backend-owned so a canvas save can never resurrect an uninstalled ref or drop a fresh install (the last-writer-wins clobber)
- Design decisions/artifacts: memo `dev/81` (operationalizes dev/65 §Fix 2/3 against the 2026-08-18 tree; dev/65 Fix 1 was `f857e3f3`)
- Tasks: `TASK-P5-dataset-ref-ownership`
- Risks/questions: whether the client could stop serializing the section entirely (the dev/65 §Fix 2(a) design) — no: "Save a copy" (`saveAsNewProject`) and trill import (`loadParsedTrill` → `incomingDatasets`) legitimately seed a NEW project's refs through the client spec, so ownership is enforced server-side instead (memo deviation D1)
- Design-to-code decision or deviation: **(1) Fix 3 — replace, not merge**: `install_dataset` filters out the old ref and appends `_ref_from_item`'s output verbatim; `publishedToHub` is re-derived from the hub-registry row at listing time (dedup merge), an explicit unpublish `False` still suppresses it, `installedAt` is always the reinstall time. **(2) Fix 2 — update ignores, create seeds**: new `preserve_dataset_refs` (datasets application layer, mirroring `preserve_agent_state`) runs inside `update_project`'s `spec_write_lock` and overwrites the client-sent section with the on-disk one (`[]` when the on-disk dataflow has none; client section kept only when NO spec exists on disk — the seed rule); sink-ref pruning still runs after it. **(3) Dedicated section writer**: `replace_refs` no longer round-trips a full spec through `update_project` (which would now undo itself) — `projects/storage.py::replace_dataflow_datasets` swaps only the datasets section under the lock (modeled on the packages `_write_lockfile`), and the service wrapper bumps the project row's timestamp without rewriting the manifest. **(4) Dead code deleted, not re-pointed (deviations D2/D3)**: `merge_dataflow_dataset_ref` (dead, contained its own `{**existing,**ref}` merge bug) removed; the callerless client staging path (`persistInstalledDataset`, `applyInstalledDatasetToProject`, `buildInstalledDatasetRef`, `upsertDataflowDatasetRef`, `InstalledDatasetPayload`/`DataflowDatasetRef`) removed — the live execution path (`persistDataflowForInstall`) is untouched, and `syncDatasetsFromSavedSpec` re-hydrates the mirror from every save's authoritative response
- Files/modules changed: backend `datasets/application/mutations.py`, `datasets/application/ref_ownership.py` (new), `datasets/repositories/installed.py`, `projects/services.py`, `projects/storage.py`, `packages/services.py` (comment); frontend `hook/useWorkflowOperations.ts`, `providers/FlowProvider.tsx`, `services/datasetCatalog/datasetCatalogApi.ts`
- Tests added/updated: backend `test_install_replace_semantics.py` (new, 4: fat-ref convergence + installedAt, idempotent double install, badge survives reinstall via hub row, explicit unpublish stays suppressed), `test_dataset_ref_ownership.py` (new, 6: no-resurrection and no-drop regressions, outputs-only untouched, create seeds the client section, section writer touches only datasets + timestamp, 404 on missing spec), `test_computed_catalog_api.py` publish-keeps-install test re-seeded through `replace_refs` (intent preserved), `test_projects/test_storage.py` merge tests → `replace_dataflow_datasets` tests (3); frontend `useWorkflowOperations.installSync.test.ts` dead describe removed, +2 (mirror re-hydration from the update response, concurrent `persistDataflowForInstall` one-create), `datasetCatalogApi.test.ts` dead-helper describes removed
- Verification evidence: backend `pytest tests/ --ignore=tests/test_frontend` full → 1177 passed, 8 skipped (test_frontend needs live server ports — pre-existing environmental); frontend `npx jest` full → 864 passed (78 suites; the dev/80 874 baseline minus exactly the 10 net-removed dead-helper tests); `tsc --noEmit` clean (the two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-00310cb6` (Fix 3), `COMMIT-70d54177` (Fix 2 backend), `COMMIT-c5ac63eb` (Fix 2 frontend cleanup), docs commit (this entry + memo flip)
- Issues/regressions discovered: pre-existing (NOT introduced, NOT fixed): two concurrent installs of *different* datasets can lose one — `install_dataset`'s `list_refs` → `replace_refs` read-modify-write spans two lock acquisitions; the old `update_project` round-trip had the identical window. Fix would move the filter+append inside the section writer's lock (a mutate-callback variant of `replace_dataflow_datasets`)
- Follow-up work: the concurrent-installs read-modify-write above if multi-tab installs become a real pattern; dev/65 is now fully closed (Fixes 1+2+3 landed)

## BL-P5-20260818-28: Atomic dataset-ref mutations — mutate-callback section writer (memo dev/82)

- Date / author: 2026-08-18 / Karla
- Status: verified
- Requirements: the owner's directive — fix the concurrent-install race recorded as BL-P5-20260818-27 follow-up with the mutate-callback variant: no dataset-ref mutation may lose a concurrent mutation of a different dataset to a stale pre-lock read
- Design decisions/artifacts: memo `dev/82`
- Tasks: `TASK-P5-atomic-ref-mutations`
- Risks/questions: whether only install raced — no: five sites shared the `list_refs` → `replace_refs` two-lock-acquisition window (install, uninstall, publish's ref stamp, unpublish's ref revert, delete's cross-dataflow cleanup); all five converted so the invariant is uniform
- Design-to-code decision or deviation: **(1) One primitive**: `storage.mutate_dataflow_datasets(user_key, project_id, mutate)` re-reads the spec under `spec_write_lock`, hands the dict-filtered refs to the callback, persists its return — or writes nothing when it returns `None`; returns `(spec, changed)`. `replace_dataflow_datasets` (storage + service) and `replace_refs` (repository) all became constant-callback delegates, keeping one lock/read/write path. **(2) Callback contract**: pure list transform, runs while the lock is held (documented on the primitive — no re-entrant project I/O). **(3) `changed` gates side effects**: the service bumps the project-row timestamp + commits only on real change, so a failed uninstall (404) or a no-match publish stamp no longer rewrites the spec or churns "Recent" ordering. **(4) Closure capture** where the caller needs pre-lock state: uninstall captures the removed ref (its post-404 imported-store cleanup and `{"datasets": …}` response now read from the writer's returned spec), delete captures per-dataflow hits for `removedFrom`
- Files/modules changed: backend `projects/storage.py`, `projects/services.py`, `datasets/repositories/installed.py`, `datasets/application/mutations.py` (five call sites)
- Tests added/updated: `test_projects/test_storage.py` +4 (callback sees dict-filtered current refs and its result persists, `None` leaves the spec file byte-identical with `changed=False`, missing project → `None`, and the race regression: 24 barrier-synchronized threads appending distinct refs all survive — the deterministic port of the deleted `merge_dataflow_dataset_ref` concurrency test); `test_datasets/test_dataset_ref_ownership.py` +1 and extended (no-op uninstall 404s without writing the spec or bumping the row's `updated_at` — observed on the DB row directly since the project GET bumps it via `touch_last_opened`; `mutate_refs` 404s on a missing spec alongside `replace_refs`)
- Verification evidence: backend `pytest tests/ --ignore=tests/test_frontend` full → 1182 passed, 8 skipped (the dev/81 1177 baseline plus exactly the 5 new tests; test_frontend remains env-bound); frontend untouched
- Commit/PR: `COMMIT-bfca63d5`, docs commit (this entry + memo)
- Issues/regressions discovered: `uninstall_dataset`'s response still referenced the pre-conversion `next_refs` local (caught by the ownership suite's 500) — now derived from the writer's returned spec; the project GET route bumps `updated_at` (`touch_last_opened`), which invalidated the first draft of the timestamp assertion
- Follow-up work: none — the BL-27 follow-up is closed; dev/65 and its descendants are fully landed

## BL-P5-20260818-29: Chat-UX polish pair — Solve-strip status unity + unread count on the Jump pill (memo dev/83)

- Date / author: 2026-08-18 / Karla
- Status: verified
- Requirements: the two recorded chat-UX polish follow-ups — the Dataflow Builder strip's batch progress adopts `AgentRunStatusLine` for one status language with the dev/80 per-reply meta lines (dev/80 follow-up), and the Jump-to-latest pill carries a count of messages landed while the user is scrolled away (dev/75 follow-up)
- Design decisions/artifacts: memo `dev/83`
- Tasks: `TASK-P5-chat-ux-polish-pair`
- Risks/questions: whether the strip could show true batch elapsed — no: `builderSession` persists no batch start timestamp, so elapsed is strip-local observation time (the dev/80 client-measured posture; a panel reopened mid-run shows time since it observed the batch, nothing fabricated); whether the count needed turn-role awareness — no: the user's own send re-pins via `pinToLatest`, so user turns can't accumulate unseen, and the hook stays a number-in/number-out contract
- Design-to-code decision or deviation: **(1) Additive `AgentRunStatusLine` props** — `runningLabel` (fixed label; the rotating Cooking/Baking set is reply-flavored), `runningDetail` (suffix inside the aria-hidden ticking span), `srLabel` (live-region text); absent props render dev/80 output byte-identically, pinned by a regression test. **(2) Strip adoption** — one `activeBatchLabel` derivation (Solving / Building / Stepping) replaces the static `solving…` note; fraction `done/total nodes` counts terminal node states (`solved|failed|skipped`) from the existing `nodeRuns` ⊕ `solveProgress` overlay; running-variant only — phase chips, per-node rows, notice/error hints, and the simulation narration line all untouched; `.solvingNote` CSS deleted. **(3) The unread count lives in `useTranscriptAutoScroll`** — optional `itemCount` (the panel passes `turns.length`), baseline snapshotted on the pinned→detached transition inside `setPinned`, `unreadCount` as a pure render-time derivation keyed off `atBottom`: every re-pin path (bottom return, jump, send, resetKey, hydration) resets for free and the dev/75 zero-per-chunk contract holds (chunk growth replaces the last turn without changing the count). **(4) Pill label** — "Latest" ↔ "N new" (display cap 99+), accessible name carries the real number; positioning/focus-fallback behavior untouched. Delegated-agent chats inherit both for free (dev/72 — same panel); LLMChat untouched (dev/76)
- Files/modules changed: frontend `components/agents/attach/AgentRunStatusLine.tsx`, `useTranscriptAutoScroll.ts`, `TranscriptJumpButton.tsx`, `AgentBuilderStrip.tsx` + `.module.css`, `AgentChatPanel.tsx`
- Tests added/updated: `tests/attach/AgentRunStatusLine.test.tsx` (new, 6 — the dev/80 no-props regression, fixed label never rotates, detail suffix, srLabel, aria-hidden ticking span contract, done/error ignore the running props); `TranscriptJumpButton.test.tsx` (new, 6 — hidden, dev/75 no-count regression, 0 stays "Latest", singular/plural accessible names, 99+ display cap with the real number in aria); `useTranscriptAutoScroll.test.tsx` +8 (count/reset matrix incl. streamed-chunk immunity, re-detach baseline, shrink clamp, omitted-itemCount inertness); `AgentBuilderStrip.test.tsx` +4 (solving line with terminal-state fraction + note-gone regression, no line when idle, Building without fraction, Stepping); `AgentChatPanel.test.tsx` +3 ("1 new" accessible name, accumulate → jump reset → fresh-detach baseline, chunk growth stays "Latest")
- Verification evidence: frontend `npx jest` full → 891 passed (81 suites; the dev/81 864 baseline + exactly the 27 new tests); `tsc --noEmit` clean (the two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-21f54ed2` (primitives), `COMMIT-5c0c6f82` (strip adoption), `COMMIT-656c9ea7` (badge wiring), docs commit (this entry + memo flip)
- Issues/regressions discovered: none in code; one test-authoring wrinkle — the panel's jump-then-redetach flow needs the jump's landing scroll event emulated explicitly (the dev/75/79 suppress-flag handshake), same as the hook suite already did
- Follow-up work: none new; the dev/75 unread-badge and dev/80 Solve-strip follow-ups are closed. Remaining open chat-UX items are the LLMChat-parity decision (dev/76 tension) and the tsconfig deprecation fix

## BL-P5-20260818-30: agent.package-recommendation — the deferred 14th releasable built-in ships (memo dev/84, spec dev/16 / DEC-035)

- Date / author: 2026-08-18 / Karla
- Status: verified
- Requirements: dev/16's identify → suggest → reviewed-install contract — the build agents identify required node packages and surface each required-but-uninstalled one as a reviewed proposal with a why-needed rationale; installs route through the EXISTING package flow (permissions + dependencies + conflicts, per-project lockfile); never silent, never agent-authored, built-ins (`curio.builtin@*`) never proposed
- Design decisions/artifacts: memo `dev/84` (grounds dev/16 in the current machinery; deviations D1–D4), `DEC-030`/`DEC-035` context; closes the deferrals recorded at `builtin.py:162/199`
- Tasks: `TASK-P5-package-recommendation`
- Risks/questions: how the InstallPermissionsDialog requirement composes with "the apply endpoint is the only execution authority" — resolved: the dialog is the review surface (frontend opens it BEFORE posting the apply; its Install button IS the apply trigger), the endpoint re-checks conflicts server-side regardless and treats a conflicting/absent package as drift (409 + stale)
- Design-to-code decision or deviation: **(1) Roster entry** (18th built-in): capabilities `package.recommend`/`package.identify`, node+canvas targets, delegates to `agent.syntax-analysis-agent` (import extraction, never guessed), net-new `package_recommendation_instruction.txt`, review-before-apply. **(2) Domain-owned read surfaces**: `packages/services.py::agent_catalog_overview` (installed = the PROJECT lockfile; builtin flagged) + `agent_resolve_report` (drawer-shaped probe: user store ∪ requested, catalog-manifest overrides, per-package permissions/deps + conflicts) — the agents tools (`packages.catalog`, `packages.resolve`) stay thin wrappers (ADR-AG-007). **(3) `package.install` proposal lane**: mint validates against the catalog (unknown → corrective error; builtin → refused with "always present"; installed → honest state, no dead proposal; bounded `reason` becomes the card preview); `_apply_package_install` re-resolves, conflicts/absence → `_mark_stale` + 409, else `install_to_project` (the packages service's own lockfile write + dep provisioning under its spec lock) and the standard applied-turn card. **(4) Parent wiring**: node-builder + dataflow-builder append the delegate; **Connection Builder is the ONE migrated manifest gaining `delegatesTo`** (dev/16 §3.3 addendum; the dev/48 thirteen-manifest byte-parity regression amended for exactly it — deviation D4). **(5) Frontend dialog-gated apply**: `usePackageInstallReview` (agents/attach, per dev/76; imports the shared `InstallPermissionsDialog` — genuine cross-feature reuse) loads the catalog row + the drawer-shaped conflict probe, opens the dialog, and holds `beginReview`'s promise open until the flow settles — apply failure rejects into the review card's existing error line, cancel resolves with the proposal still pending; the card's `package.install` effect line states the dialog gate. Deviations D1–D3 (roster-generated manifest/foreground, tool-served installed flags, no settingsDefaults) landed as memo'd
- Files/modules changed: backend `packages/services.py` (agent read surfaces), `agents/tools.py` (3 contracts + 2 executors), `agents/services.py` (mint + apply + MUTATE_PROPOSAL_TOOLS), `agents/builtin.py` (roster entry + 3 delegatesTo), `utk_curio/llm-prompts/package_recommendation_instruction.txt` (new); frontend `components/agents/attach/usePackageInstallReview.ts` (new), `AgentChatPanel.tsx` (dialog host + apply branch), `components/agents/content/AgentReviewCard.tsx` (effect line); `docs/AGENTS.md` (roster = 18)
- Tests added/updated: backend `test_builtin.py` (roster 18, `TestPackageRecommendation` manifest surface, D4 parity amendment asserting Connection Builder's one-key change), `test_tools.py` (registry set + `TestPackageTools`: real-catalog rows with project-lockfile install state + builtin flags, q filter, resolver-grounded permissions/deps, bad-params/unknown errors), `test_routes.py` (`TestPackageRecommendationTools`, 6: catalog tool grounding, mint→apply writes the project lockfile with the rationale on the card, builtin/unknown/installed mint refusals, conflict-at-apply 409+stale, package-gone 409, injection resistance — prose never installs), `test_delegation.py` (`TestPackageRecommendationDelegation`, 3: parents declare it, both capabilities resolve current-project-only, missing template = missing-specialist); frontend `usePackageInstallReview.test.tsx` (new, 6: probe shape, 409-body conflicts, confirm-fires-apply + deferred settle, cancel-keeps-pending, apply-failure rejects into the card, gone-from-catalog rejects pre-dialog), `AgentChatPanel.test.tsx` +3 (Apply opens the dialog and only Install fires the apply, Cancel keeps the proposal actionable, non-package proposals regression: direct apply, no dialog, no catalog fetch)
- Verification evidence: backend `pytest tests/test_agents/ tests/test_packages/` → 865 passed, 4 skipped (progression 853→859→862→865 across the commits); frontend `npx jest` full → 900 passed (83 suites; the dev/83 891 baseline + exactly the 9 new tests); `tsc --noEmit` clean (the two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-577315d8` (tools + roster + prompt), `COMMIT-cff987df` (proposal lane), `COMMIT-60d8e6e4` (parent wiring + D4), `COMMIT-8b847211` (frontend dialog flow), docs commit (this entry + memo flip + AGENTS.md)
- Issues/regressions discovered: none in code; test-authoring notes — the chat panel itself is `role="dialog"`, so the install dialog is queried by its title text, and the deferred-rejection test must attach its expectation before confirm settles the promise
- Follow-up work: the dev/16 remainder stays as scoped — Validation/Optimization agents (OQ-011) and the generated-content evaluator (OQ-007) remain open product decisions; `connection` attach target — **DEFERRED 2026-08-27, then re-opened and CLOSED the same day by `BL-P5-20260827-47` (memo dev/113, DEC-071)** — the deferral record kept for the trail: the attachment layer already supports edge targets (`attachments.py` validate/prune + tests) but no built-in declares the kind (Node Builder canvas-only per dev/48; Connection Builder defaults to `node`), and the frontend has no edge hit-test, edge-anchored badge, `targetContext`/`connectionSide` producer for edges, or insert-between apply. Two distinct concepts hide under the word — dev/15's *existing-edge* target vs. the legacy node-widget *handle-side* suggestions (`styles.tsx` `generateConnectionSuggestions`, a raw `llmRequest` caller, left untouched) — settle that before any memo. Re-open = demand + own memo; consider a Dataflow Builder plan-step surfacing of package recommendations once real usage shows the shape

## BL-P5-20260818-31: OQ-007/OQ-011 resolved (DEC-055/DEC-056, memo dev/85) — the generated-content evaluator ships (memo dev/86)

- Date / author: 2026-08-18 / Karla
- Status: verified
- Requirements: the owner's approval of decision memo dev/85 — DEC-055: `agent.generated-content-evaluator` is AUTHORED, not migrated, to an approved contract (the "supplied and reviewed" event OQ-007 demanded); DEC-056: the Validation concept card is a category view over the shipped family (no separate agent) and Optimization is descoped demand-driven (re-open: real demand AND per-node duration evidence in the runtime journal). DEC-056 requires no code
- Design decisions/artifacts: memos `dev/85` (decision) + `dev/86` (implementation); `DEC-055`, `DEC-056` minted into the dev/03 tables; closes the dev/57-parked LLM-verification-agent request
- Tasks: `TASK-P5-generated-content-evaluator`
- Risks/questions: RISK-EVAL-001 (false certification) — resolved structurally: report-only review policy, read-only evidence tools, a verdict STRICTLY derived from findings, DEC-054's simulation auto-approve stays exclusively empirical, and DEC-028's platform-quality firewall is untouched; the honesty rules ride the prompt (never-executed nodes have no execution evidence; ungroundable claims are uncertainty, never findings)
- Design-to-code decision or deviation: **(1) Roster entry** (19th built-in): capability `content.quality.evaluate`, category evaluate, node+canvas targets, reads `nodeContext`/`targetContext`, tools `node.read`/`node.runtime.read`/`dataflow.read`, report-only, delegates-free — deliberately the migrated-manifest shape, so the dev/48 byte-parity regression covers it UNAMENDED (contrast dev/84's D4). **(2) Net-new prompt** `evaluate_generated_content_prompt.txt` (the long-documented filename) encoding the dev/85 §4 contract: two modes (node vs canvas), `[blocker|warn|note] claim — quoted evidence` findings, `fits`/`fits-with-warnings`/`does-not-fit` derived verdicts, advisory-only + never-approve + empirical-primacy rules. **(3) Delegation**: node-builder + dataflow-builder append the evaluator as an optional post-generation check (Connection Builder untouched — dev/85 scoped composition to the two generators). **(4) Docs as decided**: dev/03 gains the DEC-055/056 rows, OQ-007/011 close, the `evaluation-disabled` profile row retires; `builtin.py`'s "intentionally absent" docstring flips to the DEC-055 reference; AGENTS.md roster = 19
- Files/modules changed: backend `agents/builtin.py`, `utk_curio/llm-prompts/evaluate_generated_content_prompt.txt` (new); docs `docs/AGENTS.md`, `dev/03` tables; no frontend changes (the agent rides every existing built-in surface)
- Tests added/updated: `test_builtin.py` (roster 19; `test_evaluator_excluded` inverted to `test_evaluator_authored_under_dec055`; `TestGeneratedContentEvaluator`: manifest surface incl. read-only tools + delegates-free + report-only, instruction carries the verdict vocabulary/severities/advisory/never-executed rules; composites' delegate lists), `test_routes.py` (catalog 19), `test_delegation.py` (`TestEvaluatorDelegation`, 3: generators declare it, `content.quality.evaluate` resolves current-project-only when installed, missing = not-installed/never assumed)
- Verification evidence: backend `pytest tests/test_agents/ tests/test_packages/` → 865 passed after commit 1, 868 passed after commit 2 (4 skipped); frontend untouched (dev/84's 900/83-suite jest baseline stands)
- Commit/PR: `COMMIT-52863552` (roster + prompt), `COMMIT-549b9d34` (delegation wiring), docs commit (this entry + dev/85/86 flips + dev/03 + AGENTS.md)
- Issues/regressions discovered: **correction to BL-P5-20260818-30**: its verification line claimed "865 passed (progression 853→859→862→865)" — the measured backend number at dev/84's close was 862 (commit 4 was frontend-only; no backend run followed commit 3's 862). The 865 figure was a projection, not a measurement; recorded here per the log's append-only rule. Today's 865 (dev/86 commit 1) is coincidental: 862 + the 3 new evaluator tests
- Follow-up work: none for the roster — it has no remaining blocked or unspecified entries; OQ-008 (retention policy) is now the program's only open question. Optimization's re-open conditions are on record in DEC-056 (per-node durations in the journal remain a dev/67-7 follow-up)

## BL-P5-20260819-32: OQ-008 resolved (DEC-057/DEC-058, memo dev/87) — retention declaration, ledger sweep, truthful deletion copy (memo dev/88)

- Date / author: 2026-08-19 / Karla
- Status: verified
- Requirements: the owner's approval of decision memo dev/87 — DEC-057: lifecycle-bound deletion as THE deletion model, retention durations only by operator declaration (never invented; undeclared stated as undeclared), truthful live-store deletion language with the declared backup posture surfaced verbatim, operator-executed closure procedure with unpublish-by-default, the guest-commingling reality stated where guests enter; DEC-058: the governance-artifact lifecycle contract recorded as build input for the v2 surface (no code)
- Design decisions/artifacts: memos `dev/87` (decision) + `dev/88` (implementation); `DEC-057`/`DEC-058` minted into the dev/03 tables; OQ-008 closed — the production-release block is now mechanical (a declared backup posture in `.curio/agents-retention.json`)
- Tasks: `TASK-P5-retention-dec057`
- Risks/questions: could the sweep ever violate the ledger's append-only truth — no, structurally: archival is `shutil.move` of whole day files into `ledger/archive/` (byte-identical, collision = skip + loud warning, `.lock`/non-day files untouched), and with no declaration the sweep is a no-op
- Design-to-code decision or deviation: **(1) `agents/retention.py`** (the `pricing.py` deployment-file pattern: read-per-call, missing/corrupt ≡ `{}`): schema `backups: "none"|{expiryDays}`, `ledger.archiveAfterDays`, `closure.graceDays`; invalid values read as undeclared, never guessed; unknown top-level keys warn loudly ("declared-but-unenforced must never be silent"); `run_retention_sweep()` wired at server startup beside `reconcile_guest_projects`. **(2) The declaration rides the existing bootstrap** — `GET /api/config/public` gains `retention: public_declaration()` (null = undeclared); no new endpoint. **(3) `services/retentionCopy.ts`** — module cache seeded from the UserProvider bootstrap; `permanentDeletionNotice()` = the live-store scope sentence + the posture line (declared-none → "the data is gone", declared-expiry → "up to N days", undeclared → said plainly); adopted by the ProjectsList Delete-forever confirm and the dataset-drawer account-level Delete confirm. **(4) Grounding note**: the Archive / Delete-forever split already existed in `ProjectsList.tsx` — §3.4(a)'s reachable purge needed no code, only the copy. **(5) Guest notice** under Continue-as-Guest (`GUEST_COMMINGLING_NOTICE`). **(6) `docs/RETENTION.md`** — the class/lifecycle tables, the config schema, the operator closure procedure (grace window, unpublish-by-default with recorded override, store+DB purge), the guest reality, and the production-gate checklist
- Files/modules changed: backend `agents/retention.py` (new), `server.py` (startup sweep), `users/routes.py` (config block); frontend `services/retentionCopy.ts` (new), `providers/UserProvider.tsx`, `pages/projects/ProjectsList.tsx`, `components/datasets/catalog/useDatasetCatalogDrawer.ts`, `components/AuthForm/AltAuthBox.tsx`; docs `docs/RETENTION.md` (new), `dev/03` tables
- Tests added/updated: backend `test_retention.py` (new, 10 — absent/corrupt ≡ defaults, declared values in the public shape, invalid values read undeclared, unknown-key loud warning, undeclared sweep moves nothing, declared age archives only past-age files byte-identically with `.lock`/non-day/recent files untouched, archive collision never overwrites, missing users dir no-op, config route carries the block); frontend `retentionCopy.test.ts` (new, 7 — the three posture lines, invalid-declaration degradation, live-store notice composition, re-seed replacement, guest-notice content)
- Verification evidence: backend `pytest tests/test_agents/ tests/test_users/` → 701 passed; frontend `npx jest` full → 907 passed (85 suites; the dev/86-era 900 baseline + exactly the 7 new tests); `tsc --noEmit` clean (the two pre-existing tsconfig deprecation notices only)
- Commit/PR: `COMMIT-1bda6dee` (backend), `COMMIT-436487d6` (frontend), docs commit (this entry + dev/87/88 flips + `docs/RETENTION.md` + dev/03 OQ-008 row)
- Issues/regressions discovered: none
- Follow-up work: DEC-058 rides the future v2 governance-surface build (its lifecycle contract is now a recorded input); production release checklist item = the operator fills the `backups` declaration. Remaining open questions: OQ-009 (deployment topology) and OQ-010 (remote-provider data classification) — both deployment-gated, neither owned by this feature story's remaining work

## BL-P5-20260820-33: Agent package authoring shipped (DEC-059/DEC-060, memos dev/89 + dev/90) — Package Builder + isolated build service + prompt-driven looks + the Researcher

- Date / author: 2026-08-20 / Karla
- Status: verified
- Requirements: the owner's approvals of dev/89 (2026-08-19) and dev/90 (2026-08-20, incl. the mid-build course correction that superseded dev/89 commit 9 and amendments A1–A3) — DEC-059: package authoring is a NEW specialist (`agent.package-builder`, 20th built-in) over an isolated build service with exact-digest reviewed promotion as the only install authority; DEC-060: custom looks are prompt-driven (no first-party fixtures), `agent.researcher` (21st) owns the post-it recipe + web gathering, the Dataflow Builder stays byte-pinned (Follow-up D)
- Design decisions/artifacts: memos `dev/89` (10-component build service; Follow-ups A/B/C) + `dev/90` (Researcher topology, delegate-draft mint, A1 web tools, A2 trusted provider host, A3 public keyed-GET providers; Follow-up D); `DEC-059`/`DEC-060` minted into the dev/03 tables; `docs/EXTENDING.md` §8; roster docs at 21
- Design-to-code decisions or deviations: **(1)** dev/89 commit 9 shipped a first-party post-it fixture and was superseded in part by dev/90 commit 4 (fixture surface deleted, net −676 lines; the DOD is now `test_custom_look_dod.py` with test-local simulated model output — post-it + a dissimilar badge + a refused violating scenario). **(2)** `package.draft.apply` was declared in the dev/89 commit-1 roster ahead of its ToolContract (DEC-017 declaration-not-grant) and registered in commit 8. **(3)** The delegate-draft mint reuses `_mint_package_draft_apply` verbatim (the dev/73 one-mint-policy), gated on the PARENT's grant — Node Builder's authoring delegations stay text-only. **(4)** dev/89 commit 10 (docs) consolidated into dev/90 commit 5 (this entry)
- Files/modules: backend `packages/{build_models,build_staging,build_extension,build_workspace,build_jobs,build_deps,build_compiler,build_packager,build_pipeline,build_promotion,node_appearance}.py` (new), `agents/{builtin,tools,services,content,egress}.py`, `packages/dependency_scanner.py` (+.tsx/.jsx), prompts `package_build_instruction.txt` + `researcher_notes_instruction.txt` (new); frontend `utils/nodeAppearance.ts` + `components/NodeColorControl.*` (new), `utils/agentCanvasEvents.ts`, `hook/useCode.ts`, `TrillGenerator.ts`, `api/agentsApi.ts`, `components/agents/attach/{AgentAttachmentsProvider,useAgentCanvasMutations}.ts(x)`
- Verification evidence: backend `pytest tests/test_agents/ tests/test_packages/` → 1138 passed; frontend `npx jest` full → 940 passed (84 suites); `tsc --noEmit` clean (the two pre-existing tsconfig deprecation notices only); the recorded DOD scenarios cover the Researcher-delegated post-it flow and the weather-in-Paris web-search loop
- Commits: dev/89 — `382327cd` (memo), `28b02972`, `a42735a0`, `f1ddaeb3`, `c4dabe8f`, `160ddbc0`, `d236eddf`, `8560034d`, `a6b42cbf`+`8ab0fdb6` (commit 8 backend/frontend), `034965f5` (commit 9, superseded in part); dev/90 — `7ac48b50` (memo), `672e8a07`, `6fff01f1`, `088daa9e`, `bcabdc4f`, `652dab81` (A1), `b9953183` (A2), `dd24dda0` (A3), docs (this entry)
- Issues/regressions discovered en route (each fixed with a pinning test): the dependency scanner ignored `.tsx/.jsx`; detected `react` imports were blocked as bundleable deps (externals-import note now); `package.draft.apply` params outgrew the classic 1KB tail budget (draft-class budget + smuggling re-check); the deps-test FakeFetcher served non-tgz bytes the compiler phase could not extract; local search providers were refused by the egress policy (A2)
- Follow-up work: A (Curio package backend sandbox), B (activation/restart lifecycle), C (`node.appearance.write` agent recolor), D (Dataflow Builder → Researcher delegation); operator setup for live search = `CURIO_SEARCH_URL` (public keyed-GET provider or self-hosted SearXNG); the rich diff/SBOM/preview-screenshot review card remains deferred polish (the proposal card shows summary + counts today)

## BL-P5-20260821-34: Package backend sandbox shipped (DEC-061, memo dev/91) — dev/89 Follow-up A delivered as an on-demand, process-isolated handler runtime

- Date / author: 2026-08-21 / Karla
- Status: verified
- Requirements: the owner's approval of memo dev/91 (2026-08-21, with the §0.1 Option-3 venv posture explicitly adopted) — a permissioned, process-isolated runtime for generated server-side package code that never imports into Curio's host process, with a versioned request/response contract, per-invocation resource limits and termination, declared-permission review, audit logging, and honest failure surfaces
- Design decisions/artifacts: memo `dev/91` (incl. §0/§0.1 code-audit evidence: the legacy "sandbox" is warm `exec` with full builtins + inherited env; five pip paths into ONE shared interpreter; install-side upgrade blast radius; partial uninstall ref-counting; warm-cache staleness); `DEC-061` minted into the dev/03 table; `docs/EXTENDING.md` §8 backend paragraph rewritten; `docs/RETENTION.md` gains `packageBackend.ledgerArchiveAfterDays`
- Design-to-code decisions or deviations: **(1) On-demand, nothing resident** — one `build_workspace` worker per invocation reuses the proven dev/89 isolation primitive (scrubbed from-scratch env, rlimits with `limits_applied` honesty, killpg, capped pipes, sanitized diagnostics); Follow-up B deliberately narrows to resident services/draining/version routing. **(2) ONE contract module** (`backend_contract.py`, `curio.pkgbackend.v1`) consumed by route, runtime, child harness (copied per invocation), probe phase, and both prompt contracts — the dev/90 A15 two-spellings lesson applied up front. **(3) Probes answered at load+resolution** (the harness, not the handler) so health checks catch import/dep breakage without executing generated bodies. **(4) §0.1 Option 3**: per-package venv/overlay DEFERRED on cost evidence, but `CURIO_BACKEND_SANDBOX_PYTHON` + an `overlay_dir → PYTHONPATH` slot ship as invocation parameters (a live test proves an overlay module resolves in-worker). **(5) The entry-digest pin lives in a per-user `package-backend-pins.json`** written by BOTH install authorities (promote + catalog) rather than the project lockfile (a bare dir-name list); a backend-less reinstall clears stale pins. **(6) The scan is honest about regexes**: escape-hatch families block with file:line + fix, network blocks/warns per declared permission, `open()` deliberately unblocked (the runtime boundary is the jail). **(7) A well-formed `ok:false` reply is HTTP 200** — the envelope is the diagnosis; only infrastructure failures are 4xx/5xx. **(8) Frontend dispatch is registry-driven** (`NodeDescriptor.backendHandler` → `usePackageBackendRun`), fixing the §0 literal-keyed dispatch finding; the invoke API lives in its own light module after the first cut dragged the vega adapter graph into every executing component (lesson recorded in the module docstring)
- Files/modules: backend `packages/{backend_contract,backend_harness,backend_runtime,backend_policy}.py` (new), `packages/{manifest,installer,build_models,build_jobs,build_pipeline,build_packager,build_promotion,services,routes}.py`, `agents/{services,retention}.py`, prompts `package_build_instruction.txt`, schema `docs/schemas/node-package.v4.json` (additive `backend` + `backendHandler`); frontend `api/packageBackendApi.ts` + `hook/usePackageBackendRun.ts` + `utils/packagePermissions.ts` (new), `api/{agentsApi,packagesApi}.ts`, `registry/{types,packagesClient}.ts`, `components/editing/CodeEditor.tsx`, `components/agents/content/AgentReviewCard.tsx`, `components/packages/publishing/InstallPermissionsDialog.tsx`
- Tests added/updated: backend `test_backend_contract.py` (25 — envelope matrix, manifest surface, installer bucket, schema-parser agreement), `test_backend_runtime.py` (20 REAL-subprocess — success/probe/handler-error/env-scrub/net-guard/data-dir+cap/timeout/no-reply/nonzero-exit/import-error/404s/drift/bounds/503/Option-3 seam), `test_backend_build.py` (32 — scan matrix, declaration reading, real-worker probing phase, entry pin round-trip/drift/stale-clear), `test_backend_route.py` (6 — route matrix), `TestBackendDraftEndToEnd` (the whole lane incl. card trust edge + tamper 409), `test_backend_package_dod.py` (13 — violating drafts refused with finding+fix in the model's face, prompt + buildRequestContract markers), retention (+2); frontend jest: card backend block (3), packagesClient descriptor mapping (2), `usePackageBackendRun` (4)
- Verification evidence: backend `pytest tests/test_agents/ tests/test_packages/` → 1270 passed (4 skipped); frontend `npx jest` full → 956 passed / 86 suites (including the CodeEditor suite the first API-import cut broke — fixed by the light-module split); touched files `tsc`-clean (pre-existing type debt unchanged)
- Commits: `1f4fca57` (contract+manifest+installer+schema), `2adf412a` (runtime+harness+ledger+retention), `a68ed44a` (build service: scan, probing phase, provenance, pins), `664bc8c4` (route + review surfaces), `6059c151` (frontend dispatch), docs (this entry)
- Issues/regressions discovered en route (each pinned): user keys are guest-or-numeric (test constant); `os._exit(3)` lands in worker-failed, not no-reply (both branches now pinned separately); `packagesApi` re-exports the registry bootstrap → vega ESM in every importer's graph (invoke API split into `packageBackendApi.ts`)
- Follow-up work: dev/89 Follow-up B (resident services: activation, draining, version routing — now explicitly narrowed), Follow-up C (`node.appearance.write`), dev/90 Follow-up D (Dataflow Builder → Researcher delegation); §0.1 Option-2 overlay isolation as a contained hardening (parameter values on the shipped seam); secret mediation stays deferred and stated; per-handler daily quotas ride the invocation ledger when demanded

## BL-P5-20260821-35: Agents could not create nodes — one node-type vocabulary, a reachable reuse ladder, and a package store that stops destroying itself (DEC-062, memo dev/93)

- Date / author: 2026-08-21 / Karla
- Status: verified
- Requirements: the owner's report that "the dataflow builder is having an issue to create the nodes" plus a second failing Curio session, investigated from two live screenshots and the on-disk agent transcripts; approval of memo dev/93 (2026-08-21) with §3.3 option (a) — grant `agent.researcher` the `package.install` tool — and edge case 39 resolved by extending the roster gate
- Evidence the design is grounded in (all live, none inferred): the Dataflow Builder's own words — "Since `curio.builtin/data-loading@1` is listed but was rejected by the validation system, I will use the simplified base IDs" — ending on `plan node 'gen_data' uses 'DATA_LOADING', which is not an available template for this project` after 33.1k tokens over four correction rounds; a read-only probe against the failing project measuring the asymmetry (`curio.builtin/data-loading@1` → plan mint REFUSE / node.create OK; `curio.postits/post-it-note@1`, the type of a node ON that canvas and the exact string the runtime printed in its own 12:08 removal preview → same); a removal-only plan SUCCEEDING at 12:08 because zero plan nodes means the availability loop never runs; the Researcher's transcript — "Since there is no installed notes template on your canvas, I will request the creation of a new notes package" — with TWO `node.kind.author` delegations in one run producing `curio.notes` then `curio.postits`, its first delegation reported `status: ok` beside "returned no parseable package draft"; and `.curio/users/guest/packages/curio.builtin@1/` holding ONLY `integrity.json` at 11:19:32 (2 templates available, both note surfaces) → complete at 11:35:59 (12 templates) → complete at 13:29:15, i.e. a store that OSCILLATES with request interleaving, which is why the bug read as unreproducible
- Design decisions/artifacts: memo `dev/93` (five defects D1–D5, amendments A1–A4 recording every deviation); `DEC-062` minted into the dev/03 table; `docs/AGENTS.md` gains "The template vocabulary agents plan in" and the Researcher's three-rung ladder
- Design-to-code decisions or deviations: **(1) Canonicalise at the PARSE boundary, not at each call site** — `content._parse_dataflow_plan_verbose` stores the canonical id, so the stored proposal, its pinned shape digest, and BOTH apply-time re-checks agree by construction; adding `@`-stripping per call site would have left the fifth site for the next person, which is exactly how the dev/90 A14 fix missed this path. **(2) Legacy aliases DERIVED, not tabulated** (deviation from §3.2, which specified a table + drift test): every `NodeType` member is its template id upper-snake-cased, so the RULE cannot go stale and it covers `spatial-join`, which never got an enum member; the drift test PARSES `constants.ts` and asserts every member round-trips. **(3) Apply canonicalises the COMPARISON, never the stored value** — a proposal minted before the change holds a raw versioned string whose digest was computed over exactly that string, so rewriting it would mark in-flight proposals stale on deploy day. **(4) A missing `integrity.json` is NOT unhealthy** (deviation from §3.1c): treating it so would re-seed on every request for any package shipping without one, reinstating the per-request destruction the change removes; the manifest gate already catches the observed corruption. **(5) The atomicity claim is stated NARROWER than the memo's wording, in code and tests**: POSIX `rename` cannot replace a non-empty directory, so a directory swap is two renames and the package is momentarily ABSENT between them — what is eliminated is the PARTIAL tree, the state that used to persist forever; readers needing zero gap must take the seed lock, as dev/92's `target_locks` did for promotes. **(6) Granting `package.install` was NOT enough**: `_mint_package_install` validated against `agent_catalog_overview`, which enumerated only the committed catalog, so an agent-authored store package was refused "not in the Nodes Catalog" — the rung would have been a door painted on a wall; the overview now enumerates catalog PLUS the user's store (store manifest wins a dirName collision), and `install_to_project` already being "add to lockfile, install to store if missing" meant the apply path needed no change. **(7) The roster gate widened to FOUR grants, not three** — two of the four `installedTemplates` readers held neither original grant, and `package.draft.apply` earns it on the merits since an authoring agent especially needs to see what already exists. **(8) The honest delegation status is fixed at the CALLER**, not in `content.make_delegation_part`, which already took a status and documented ok/failed — the defect was passing the child RUN status when nothing had been produced
- Files/modules: backend `common/file_locks.py` (new — the two-layer lock extracted from `projects/storage.py`, which now delegates), `packages/{seed,services}.py`, `agents/{services,content,builtin}.py`, prompt `researcher_notes_instruction.txt`; frontend `components/agents/attach/agentRunContext.ts` (the duplicate roster producer retired)
- Tests added/updated: `test_packages/test_seed.py` (+8 → 16: healthy store performs ZERO writes with `copytree`/`rmtree` monkeypatched to fail; the observed truncation self-heals; a file `integrity.json` names is restored; a package without `integrity.json` is left alone; built-in restored despite a stray tombstone; a failed swap keeps the previous tree; staging leftovers swept and never listed as a package; 8 seeder threads × 8 reader threads asserting no reader ever sees the package present-but-unreadable), `test_packages/test_available_templates.py` (+17 → 25+: the canonicaliser's three spellings and its deliberate non-conversions, the frontend-enum drift guard, `resolve_template`'s spellings/authorable-asymmetry/degraded-store/registry-failure, the not-enlisted listing, and the store-only package being proposable), `test_agents/test_routes.py` (new `TestPlanTemplateSpellings` (9) — versioned/legacy/mixed proposable and stored canonical, a plan may use a non-authorable template, unknown still refuses naming the spellings, versioned applies end to end, a pre-change proposal still applies, removal-only still applies; new `TestReuseLadder` (4) — the enlistable package named with its dirName, an enlisted one leaving that section, ENLIST-THEN-CREATE end to end, and no section without the grant; new `TestRosterGrantCoverage` (4) — the structural walk over `BUILTIN_AGENTS`), `test_agents/test_delegate_draft_mint.py` (+4, 1 updated: the duplicate-package regression, a rename mid-correction refused, terminal-vs-correctable classification, the verbose extractor's failure shapes), `test_agents/test_builtin.py` (the Researcher's tool list + ladder assertions), `test_projects/test_storage.py` (Windows-fallback lock tests repointed at the extracted module); frontend jest `attach/agentRunContext.test.ts` (the retired producer, plus a sibling fragment still composing)
- Verification evidence: `pytest utk_curio/backend/tests --ignore=test_frontend` → **1652 passed / 8 skipped**; frontend `agentRunContext` suite 10 passed, touched file `tsc`-clean. EVERY new mechanism A/B-verified rather than assumed: reverting the old swap fails the concurrency + failed-swap tests; restoring `force or is_builtin` fails the no-recopy tests; reverting parse-boundary canonicalisation fails 4 of 9 spelling tests; reverting the two apply comparisons fails exactly the pre-change-proposal test; `_DRAFT_CORRECTION_ROUNDS = 0` fails both loop tests; deleting the honest-status line fails the delegation-card assertion. The 141 `test_frontend/test_workflows.py` Playwright errors in a whole-suite run are PRE-EXISTING, established against a throwaway worktree at clean `53b33f37` (1620 passed + the same 141; the 8-test delta is this work's first commit), and that file passes standalone
- Commits: `aa6f5c95` (commit 2 — atomic/serialized/self-healing seeding), `fb30b658` (commit 6 — the reuse ladder), `307fd112` (commit 1 — canonicaliser + gate + degradation report), `2d70c6b2` (commit 3 — parse-boundary canonicalisation, one gate for plans and node.create), `9876ae9d` (commit 4 — one roster, gate widened), `f2c5f669` (commit 5 — draft correction rounds + honest delegation status), docs (this entry)
- Issues/regressions discovered en route (each pinned): the concurrency assertion's own checker was racy TWICE (iterdir-then-read, then an existence re-check a LATER swap had already satisfied) — it now pins the package directory's INODE across the read, a reminder that a concurrency assertion needs the same scrutiny as the code it guards; a negative assertion about a composed prompt collided with the INSTRUCTION naming the same section title, so it keys on the roster's own dirName suffix instead; `agents/services.py` had no module logger; `shutil.Error` (a partial copytree) is not an `OSError` and must not escape into a request handler; three `test_retention.py` "errors" in one local run were an artifact of `-p no:logging` removing the plugin that provides `caplog`, not a regression
- Follow-up work: readers taking the seed lock to close the swap's two-rename absence gap (a leaf lock, so no deadlock risk, best done when `available_templates` is next touched); **roster-aware delegate runs** — a delegate composes only preamble + instruction with `tools=[]` (DEC-046) and the frontend context rides the PARENT's request, so the Package Builder cannot know a suitable package already exists at the moment it is asked to author one, which is the deepest root of the duplication this memo fixed from both ends; dev/90 Follow-up D (Dataflow Builder → Researcher delegation) and dev/89 Follow-ups B/C unchanged

## BL-P5-20260822-36: The authoring delegate was told to check for duplicates with a tool it does not have (DEC-063, memo dev/94)

- Date / author: 2026-08-22 / Karla
- Status: verified
- Requirements: dev/93's stated follow-up ("roster-aware delegate runs"), investigated before implementing and found to be sharper than the note described; the owner approved memo dev/94 (2026-08-21) and its three-commit plan
- Evidence the design is grounded in: `package_build_instruction.txt:3` opened with "Reuse first. Before authoring anything, **read packages.catalog** … never a duplicate package"; the Package Builder holds that tool as a direct ATTACHMENT (`packages.catalog`, `packages.resolve`, `dataflow.read`, `package.draft.apply`) and as a DELEGATE runs with `tools: []` by DEC-046 construction (`delegation.run_delegate` composes preamble + instruction only, no tail instruction) — and the delegate path is the one packages are actually authored on. So its first rule was unexecutable, it had no way to know, and it did what a competent model does when told to check something it cannot check: it proceeded. dev/93's transcripts show the result — two `node.kind.author` delegations in ONE run producing `curio.notes` then `curio.postits` while a usable notes package sat in the user's store
- Why dev/93 did not already close it (stated because it looked closed): commit 6 gave the PARENT the enlist rung (the parent must still notice), commit 4 gave roster-granted agents the two-bucket roster (a delegate holds no grants), and commit 5 corrects drafts that FAIL — but a confident, well-formed, entirely duplicative draft never fails: it parses, it builds, it becomes a reviewed proposal, and the only thing between it and the store is the user noticing
- Design decisions/artifacts: memo `dev/94`; `DEC-063` minted into the dev/03 table (amends DEC-046's evidence posture, leaves its structure untouched); `docs/AGENTS.md` gains "And the delegate half" beside dev/93's template-vocabulary section
- Design-to-code decisions or deviations: **(1) Inputs, not tools** — giving delegates `packages.catalog` was considered and rejected: it needs the tail instruction and grants paragraph DEC-046 exists to withhold, to obtain facts the input path delivers with none of it. **(2) `installedInProject` is CARRIED, not filtered on** — enlisted means "extend or reuse it", store-only means "report it, the caller can enlist it" (dev/93 D4's middle rung); collapsing them would recreate the one-bucket mistake that caused this family of bugs. **(3) Canonical template ids ride each row**, because without them `mode: "extend"` is advice the model cannot follow — it would have to guess what it is extending. **(4) The payload bounds ITSELF** (40 packages / 8 templates / 200 description chars, truncation logged): `delegation._frame_inputs` applies no limit to the inputs body — only the child's REPLY is capped — so a large store would otherwise crowd the child's context. **(5) The reuse answer is a TAUGHT SCHEMA, not a heuristic** — `insteadOfAuthoring` → `{"reuseExisting": {dirName, reason}}`, the A8 lesson for the third time; a test pins that prose merely mentioning reuse is NOT a finding, because letting model wording drive control flow is what the typed-tail protocol exists to prevent. **(6) It is checked BEFORE the draft parse and returns immediately** — the doctrine is reuse-FIRST, and a reply naming a reuse target is declining to author, not failing to. **(7) It reports "ok" with the meaning in the SUMMARY, and no new status vocabulary** — `AgentDelegationEntry` renders `status === "failed" ? "failed" : "ok"` and `make_delegation_part` coerces to those two, so a third value would have been silently normalised to "failed"; DEC-062's honest-status rule ("produced NOTHING must not claim ok") is satisfied because this run produced the answer. **(8) Enforcement is a stated NON-GOAL** — the runtime cannot adjudicate "already satisfies the need"; that judgment is why an agent is there, and any later enforcement belongs on the review surface, argued on its own evidence
- Files/modules: `agents/services.py` (`_authoring_reuse_evidence`, the `_enriched_delegate_inputs` branch, `_extract_reuse_finding`, `_reuse_finding_text`, `_BUILD_REQUEST_CONTRACT["insteadOfAuthoring"]`, the early return in `_mint_package_draft_from_delegate`), prompt `package_build_instruction.txt`
- Tests added/updated: `test_delegate_draft_mint.py` +15 → 40 in the file. `TestAuthoringDelegateReuseEvidence` (8): a store package reported with dirName + canonical template ids; `installedInProject` distinguishing enlisted from store-only; the delegate's inputs carrying the evidence ALONGSIDE the dev/90 A8 contract; a parent-supplied view never overwritten; non-authoring capabilities untouched; the payload bounded on all three axes with truncation logged; a broken registry degrading to no evidence without raising; and the regression AT ITS SOURCE — a Researcher delegating `node.kind.author` while `curio.notes@1` sits in the store now sees `curio.notes@1` and `curio.notes/note-surface` in the child's framed task. `TestReuseInsteadOfAuthoring` (5): a reuse finding mints nothing and the card reports ok naming the package; the parent is told whether it must enlist first (BOTH branches); no correction rounds are spent (the redelegate callable asserted never called); an ordinary draft still wins when no reuse is claimed; a malformed or prose-only claim is not a finding. Plus 2 marker tests pinning the instruction on both evidence paths and the contract actually teaching the reuse reply
- Verification evidence: `pytest utk_curio/backend/tests --ignore=test_frontend` → **1667 passed / 8 skipped**. Both mechanisms A/B-verified rather than asserted: removing the enrichment branch fails exactly the two tests asserting the delegate receives it; removing the reuse-finding branch fails exactly the three behaviour tests, and the failure output reproduces the pre-fix text verbatim ("the authoring delegate produced no usable package draft … fix THAT and keep the same package id") — i.e. proof that without commit 2 we would have shipped facts the delegate could see, an instruction telling it to act on them, and a runtime that punished it for complying. Also verified against the live project (guest / `a9a1afc7`): the composed evidence lists `curio.notes@1` with `installedInProject: false` and its `note-surface` template, the exact fact missing when the duplicate was authored. The 141 `test_frontend/test_workflows.py` Playwright errors remain pre-existing (baselined against a clean worktree in BL-35)
- Commits: `21b2db3a` (commit 1 — the reuse evidence), `05bb5236` (commit 2 — the taught reuse reply, its outcome, and the instruction rewrite), docs (this entry)
- Issues/regressions discovered en route (each pinned): dev/93 commit 5's honest-status rule MIS-reported the good case — a delegate correctly answering "you already have this" minted nothing and therefore reported `failed`, and would have been sent into correction rounds to "fix" a correct answer; `delegation._frame_inputs` bounds nothing (only the reply is capped), so any injected payload must bound itself; the delegation status vocabulary is effectively two-valued end to end (backend coercion + frontend ternary), so a new outcome must ride the summary rather than the status
- Follow-up work: enforcement/advisory duplicate detection on the review surface (explicitly out of scope here — needs its own evidence); readers taking the seed lock to close the dev/93 swap's two-rename absence gap (unchanged); dev/90 Follow-up D (Dataflow Builder → Researcher delegation) and dev/89 Follow-ups B/C unchanged

## BL-P5-20260823-37: Follow-up B delivered as re-scoped (DEC-064, memo dev/92) — promote/invoke consistency, restart honesty, crash-loop quarantine; resident services descoped demand-driven

- Date / author: 2026-08-23 / Karla
- Status: verified
- Requirements: the owner's approval of memo dev/92 (2026-08-21, Option 3) — close dev/89 Follow-up B's real remainder after dev/91's on-demand model structurally absorbed start/hot-load, readiness, rollback, version routing, and the activating states (the memo's §0 table), and record the resident-services descope with re-open conditions instead of leaving it implied
- Design decisions/artifacts: memo `dev/92` (§0 code-audit evidence: promotes lock, invocations didn't, and the installer replace is rmtree-then-move; pip's InstallReport discarded at both authorities; the ledger records crash loops but never acts); `DEC-064` minted into the dev/03 table; `docs/EXTENDING.md` §8 updated; memo flipped to implemented
- Design-to-code decisions or deviations: **(1)** `target_locks.py` is a THIN wrapper over dev/93's `common/file_locks.keyed_thread_lock` — the first cut duplicated the dict+guard pattern and was replaced before commit after the dev/93 session's cross-session correction (no second keyed-lock implementation; that was a dev/93 checklist item and this memo's §10 rule). Thread-scope on purpose: promotion's original semantics, cross-process races stay correct via the base-digest check. **(2)** invoke_handler locks ONLY its read phase (microseconds); workers, semaphore waits, and wall-clocks run outside. **(3)** restart honesty consumes pip's own installed-list — no module-intersection heuristics — and says "recommended", the report's exact strength; the agent-chat surface is the backend-owned result turn (zero frontend), the catalog surface is one sentence composer + a non-error notice banner. **(4)** The breaker consumes the ledger's status taxonomy (one classification, two consumers); handler-error replies never count; refused payloads touch nothing; half-open keeps the counter; pin-change resets before the gate can refuse; in-process state documented as the correct restart semantic. **(5)** DEC/ledger ids shifted during the memo's wait (dev/93/94 landed on this branch meanwhile): planned DEC-062 → minted DEC-064, ledger seq 37
- Files/modules: backend `packages/target_locks.py` (new), `packages/{build_promotion,backend_runtime,services}.py`, `agents/services.py`; frontend `services/packageRestartCopy.ts` (new), `api/packagesApi.ts`, `components/packages/publishing/NodeCatalogDrawer.{tsx,module.css}`
- Tests added/updated: backend — lock identity across all three consumers pinned to `keyed_thread_lock('package-target', …)`, the interleaving regression (invocation parks while the store is torn open, completes against the NEW state, no transient 404/409), promotion restart-honesty trio (installed→journal persisted; skipped-only silent; dep-less never calls pip), the agent-lane apply pair (payload + result-turn text; silence twin), catalog-response pair, and 5 quarantine cases (threshold with ledger row and no worker spent, handler-errors never count, per-handler isolation via real timeout kills, reinstall reset, fake-clock half-open) under a module-wide breaker reset; frontend — 2 sentence-composer cases
- Verification evidence: backend `pytest tests/test_agents/ tests/test_packages/` → 1345 passed (4 skipped); frontend `npx jest` full → 958 passed / 87 suites; touched files `tsc`-clean. (Correction per the BL-31 append-only rule: dev/92 commit 1's message cites 1302 — the measured figure that run was 1333; the suites had grown with dev/93/94.)
- Commits: `e783dfe4` (B-1 lock), `0f339338` (B-2 backend), `fe49337d` (B-3 + B-2 frontend), docs (this entry)
- Issues/regressions discovered en route: the draft-manifest python deps are OVERWRITTEN by the packager's report-derived dependencies — declared deps must ride the REQUEST-level `dependencies` field (a test-authoring trap now stated in the test); breaker state is module-global, so test cleanup must be module-wide, not class-scoped
- Follow-up work: dev/89 Follow-up C (`node.appearance.write` recolor) and dev/90 Follow-up D (Dataflow Builder → Researcher delegation) remain; the §0.1 Option-2 overlay hardening stays the recorded prerequisite for ever re-opening resident services; per-handler daily quotas ride the invocation ledger when demanded

## BL-P5-20260823-38: Follow-up D delivered, Follow-up C deferred (DEC-065, memo dev/95) — DFB → Researcher research delegation with runtime-gathered findings and reviewed note sequences

- Date / author: 2026-08-23 / Karla
- Status: verified
- Requirements: the owner's approval of memo dev/95 (2026-08-23, Option 3 + the explicit instruction to defer Follow-up C) — the reference recording's surface is the DFB chat, so a research question there must reach the notes scenario without breaking DEC-046's depth-1 tool-less delegation, and the recolor mutation must be deferred as a decision
- Design decisions/artifacts: memo `dev/95` (§0: the wiring gap is one tuple + one pinned prompt; the tool-less constraint's standing answer applied a FIFTH time; A16 built the multi-note mint's substrate; depth-1 bounds the slice honestly); `DEC-065` minted; `docs/AGENTS.md` four-times sentence updated to name the pattern's fifth application
- Design-to-code decisions or deviations: **(1)** the child picks the note template from runtime-supplied candidates (`presentation_templates` — A14 profile, installed+enlisted) and the mint RE-validates through the one vocabulary — the runtime never guesses which template is "the notes one". **(2)** Row titles ride as the proposal's `goal` — `node.create`'s lane has no separate title, matching the Researcher's own attachment runs. **(3)** A13 default colors fill by INDEX (first yellow, rest green) only when a row omits its color. **(4)** No-template and empty-notes paths report outcome "ok" (the ANSWER is the product — dev/93 D5's produced-nothing rule is satisfied), while contract-miss, no-grant, and all-rows-refused report "failed"; every degradation names the way out (A4), with the no-template text pointing at the Researcher's own attachment since the enlist/author ladder is structurally unreachable at depth 1. **(5)** The prompt byte-pin updated once, deliberately (02cad07d…→6000ae6c…), and dev/90's growth-forbidding test became the wired-and-pinned test — the D4 rule both times
- Files/modules: backend `agents/builtin.py` (DFB delegatesTo + node.create), `agents/services.py` (`_delegated_search_results`, `_NOTES_REPLY_CONTRACT`, enrichment branch, `_extract_notes_reply`, `_mint_notes_from_delegate`, both loop wirings), `packages/services.py` (`presentation_templates`); prompts `orchestration_instruction.txt` (step 7) + `researcher_notes_instruction.txt` (delegate posture); docs `docs/AGENTS.md`, dev/03 tables
- Tests added/updated: `test_notes_delegation.py` (19 — search bounds/honesty ×4, enrichment injection + model-keys-win + contract markers, presentation candidates ×2, reply parser ×2, degraded mint paths ×4, the FULL DFB E2E lane incl. out-of-order apply of the A16 row and the injection-resistance twin, prompt markers ×2); `test_builtin.py` roster pins updated deliberately ×2
- Verification evidence: backend `pytest tests/test_agents/ tests/test_packages/` → 1364 passed (4 skipped); frontend untouched (no frontend surface — the cards and the A16 queue ship as-is)
- Commits: `af88f1d1` (roster + enrichment), `ee9c3371` (mint + wiring), `9d5efc56` (prompts + pin), docs (this entry)
- Issues/regressions discovered en route: two commit-message verification figures were projections (commit 1 said 1356/measured 1354; dev/92 commit 1 said 1302/measured 1333) — corrected in later messages per the BL-31 append-only rule; the lesson is to paste the measured line, never pre-write it
- Follow-up work: Follow-up C stays deferred with DEC-065's re-open conditions; the dev/89/90/91/92/95 program has NO remaining open follow-ups beyond it — the rich review-card polish and the §0.1 Option-2 overlay hardening remain the recorded discretionary items

## BL-P5-20260824-39: The rich draft review card delivered (memo dev/96) — BL-33's recorded "deferred polish" gap closes; no new decision minted

- Date / author: 2026-08-24 / Karla
- Status: verified
- Requirements: the owner's approval of memo dev/96 (2026-08-24) — deliver the review surface DEC-059 already promised: the mint's proposed-text has said "It awaits the user's explicit review of the diff, dependencies, and preview" since dev/89 commit 8 while the card rendered a counts line; the provenance sat one seam away in the proposal mirror, never reaching the transcript part the card renders from. NO new DEC — this implements a decided surface; this entry records the closure of BL-33's follow-up line ("the rich diff/SBOM/preview-screenshot review card remains deferred polish")
- Design decisions/artifacts: memo `dev/96` (§0: the overpromise quoted; screenshots recorded digest-only with no byte storage anywhere → per-state VERDICTS are the honest maximum, images stay out of scope as their own storage/retention feature; A6 bounded-part discipline as the payload law); dev/90 memo's follow-up note annotated
- Design-to-code decisions or deviations: **(1)** one composer (`_draft_card_payload`) reading only the typed request/result already in scope at the mint — no new reads, the card renders and never re-derives; **(2)** every capped list carries its TRUE total (no-silent-caps: files 20, templates 10, dep rows 10, findings 10 sorted blocks→warns→notes BY KEY so a cap can never hide a block, preview reasons 3, preview templates 8, nodes 8) with the worst case pinned numerically (<20KB at full saturation); **(3)** the PART is the reload-safe render source, the mirror gets a `draftCard` copy for listing symmetry; **(4)** native `<details>/<summary>` sections collapsed to their counts — blocking findings and failed previews render OPEN (a failed preview cannot reach a card today; drift-tolerance renders honestly if one ever does); the A9 operator-skip line renders VERBATIM; **(5)** requested-node colors are the parse-normalized canonical hex (a test expected 'yellow' and learned the request normalizes at parse — one color truth); **(6)** additive: pre-dev/96 parts render exactly as before, jest-pinned, no migration
- Files/modules: backend `agents/services.py` (`_draft_card_payload`, mint wiring); frontend `api/agentsApi.ts` (typed `draft`), `components/agents/content/AgentReviewCard.{tsx,module.css}`
- Tests added/updated: backend `test_draft_review_card.py` (9 — cap/total matrix, block-first ordering, dep rows, absent-section omission, skip-honesty verbatim, per-template failed states, normalized-hex node rows, the numeric size ceiling, E2E part+persisted-turn assertion); frontend jest (7 — counts/expand, honest overflow, block-open+styled, skip verbatim, failed-open, node line, legacy-part regression)
- Verification evidence: backend `pytest tests/test_agents/ tests/test_packages/` → 1373 passed (4 skipped); frontend `npx jest` full → 965 passed / 87 suites; touched files `tsc`-clean (measured, pasted)
- Commits: `2e8a343a` (composer + mint), `4d89c2cd` (card sections), docs (this entry)
- Issues/regressions discovered en route: palette names normalize to hex at request PARSE (the composer test's 'yellow' expectation corrected to '#fef3c0'); `MAX_REQUESTED_NODES=16` bounds the size-ceiling fixture
- Follow-up work: the dev/89→96 program is COMPLETE — remaining recorded items are Follow-up C (deferred, DEC-065 re-open conditions), the §0.1 Option-2 dependency overlay (recorded prerequisite for resident services), and the discretionary reference preview runner (dev/90 note); preview screenshot STORAGE would be its own memo if ever demanded

## BL-P5-20260824-40: Backend dependency overlays delivered (DEC-066, memo dev/97) — the §0.1 Option-2 hardening lands; DEC-064's resident-services prerequisite is met

- Date / author: 2026-08-24 / Karla
- Status: verified
- Requirements: the owner's approval of memo dev/97 (2026-08-24, Option A probe posture) — take handler dependencies out of the shared interpreter using the seam dev/91 shipped for exactly this, without changing the review-only-at-build python posture (dev/89 §3.4) and while fixing two audit findings: the dep-blind probe that refused module-level imports of declared deps with an unfixable-looking error, and the uninstall that never performed dev/91 §6.7's promised cleanup
- Design decisions/artifacts: memo `dev/97` (§0: the seam evidence, the measured blast radius this kills, the probe defect, the residue leak, the bounded shadowing analysis; the Option A/B decision with Option B's rejection reasons recorded); `DEC-066` minted; dev/91 §0.1 annotated; `docs/EXTENDING.md` §8 updated
- Design-to-code decisions or deviations: **(1)** one pip primitive with NO skip logic — wipe-before-build replaces idempotence (pip --target over an existing tree is overwrite-ish); **(2)** one routing core, two manifest adapters (typed + raw) so promote, catalog, and the dev/96 card speak identical routing; **(3)** overlay build and worker auto-resolution both under the dev/92 target lock — B-1's consistency covers dev/97 for free; **(4)** the post-Apply probe reuses invoke_from_files verbatim (one probe engine, three callers) and its first test failure was the ASSERTION's phrasing, not the rollback; **(5)** restartRecommended narrowed to the host portion with a host-pip-must-not-run pin; **(6)** lazy-import advice fires ONLY for declared deps (an undeclared module's honest fix is declaring or dropping it); **(7)** the ledger survives uninstall by design — asserted through the real DELETE route
- Files/modules: backend `packages/{pip_runner,backend_runtime,build_promotion,build_pipeline,services,routes}.py`, `agents/services.py` (card home + contract rule), prompts `package_build_instruction.txt`; frontend `api/agentsApi.ts`, `components/agents/content/AgentReviewCard.tsx`
- Tests added/updated: runtime overlay quartet (REAL pip --target with a hand-built wheel installed offline via PIP_NO_INDEX/PIP_FIND_LINKS, host-interpreter-untouched assertion, auto-resolution, explicit-beats-auto, residue sweep with ledger survival, clean-slate noop); promotion quintet (routing matrix with must-not-run pins, journal overlay provenance, healthy/broken/overlay-fed REAL post-Apply probes with rollback); route sweep; catalog routing; composer home matrix + 3 jest home lines; lazy-import DOD quartet through the real agent lane
- Verification evidence: backend `pytest tests/test_agents/ tests/test_packages/` → 1389 passed (4 skipped); frontend `npx jest` full → 968 passed / 87 suites; touched files `tsc`-clean (measured, pasted)
- Commits: `5020f9d4` (primitive + homes + auto-resolution + real-wheel E2E), `09fe7e14` (routing + post-Apply probe + narrowing + sweep + card), `91d3b55b` (probe honesty + lazy-import contract), docs (this entry)
- Issues/regressions discovered en route: none new beyond the memo's §0 findings; one test assertion phrasing corrected against the already-correct rollback message
- Follow-up work: the dev/89→97 program leaves Follow-up C (deferred, DEC-065), the discretionary reference preview runner, and screenshot storage-if-demanded; DEC-064's overlay prerequisite for ever re-opening resident services is now MET — re-opening still requires a real resident-only need and its own memo

## BL-P5-20260824-41: The reference preview runner shipped (memo dev/98) — local deployments graduate from declared-unpreviewed to actually previewed; no new DEC

- Date / author: 2026-08-24 / Karla
- Status: verified
- Requirements: the owner's approval of memo dev/98 (2026-08-24) — ship the runner dev/89 §3.7 designed the socket for, satisfying the EXISTING plan/report contract with a real browser (no validator/contract/fixture/limits change anywhere), so `CURIO_BUILD_PREVIEW_POLICY=skip` becomes the fallback it was meant to be
- Design decisions/artifacts: memo `dev/98` (§0: the contract pinned by the test fake; the document's deliberate omission of React; playwright+chromium+React-18-UMD already in the deployment; the scrubbed-env packaging problem; the Linux RLIMIT_AS caveat recorded); `docs/EXTENDING.md` §8 rewritten runner-first; the dev/90 operator-note superseded. NO new DEC — dev/89 decided the socket, this ships the reference implementation behind it, zero changes under `app/`
- Design-to-code decisions or deviations: **(1)** engine = Playwright/Chromium (jsdom rejected structurally — no layout, no screenshots: it cannot satisfy the validator without lying); **(2)** packaging = a generated pinned wrapper (the CURIO_BUILD_ESBUILD precedent; a shebang script is unusable under the scrubbed PATH), every baked path verified at generation with the fix named, never a wrapper that lies later; **(3)** the state driver renders the captured hook via ReactDOM.flushSync with `nodeState={appearance: data.appearance}` — both A15 spellings exercised; dimensions/screenshots MEASURED, a failed mount reports its error and omits them, the existing validator refuses; **(4)** ReactFlow's UMD ships after all (`dist/umd/index.js` — the memo's §0 said absent; corrected during implementation) and is baked when present; **(5)** playwright's version via importlib.metadata (`playwright.__version__` does not exist — the first cut's bug); **(6)** the fake↔reference schema-agreement test pins ONE report schema over two runners (A15)
- Files/modules: `utk_curio/tools/{__init__,preview_runner,install_preview_runner}.py` (new — nothing under `app/`); tests `test_reference_preview_runner.py`
- Tests added/updated: 10 REAL-toolchain tests, env-guarded with honest skip reasons — generator bake/probe under `env -i PATH=/usr/bin:/bin`, missing-UMD/missing-browser refusals, runner_from_env integration, the five-states E2E (measured dims, PNG magic bytes), the throwing-state twin, the unregistered-key twin, the schema-agreement drive, and the FULL pipeline pair (real esbuild 0.28.2 compiling .tsx + real Chromium through run_build: ready-with-ok-preview + screenshots as digest+size only; wrong-key → failed at preview with artifact_digest None — unpreviewed never stages)
- Verification evidence: backend `pytest tests/test_agents/ tests/test_packages/` → 1399 passed (4 skipped); the browser suite runs in ~6s warm (measured, pasted)
- Commits: `9b32a88f` (runner + generator + direct E2E), `37f1b7bc` (full-stack pipeline pair), docs (this entry)
- Issues/regressions discovered en route: **the shared-index swallow** — a third session's 16 staged planning-doc files (including a memo ALSO numbered dev/98) were committed into f9b87118 by a plain `git commit`; split surgically (reset → recommit own 5 files as 9b32a88f → third party's staged state restored byte-identically; the live peer session verified its ten commits intact and the reflog clean). Protocol hardened with the peer: pathspec commits (`git commit -- <paths>`) whenever the index holds foreign work; check `git status` before every add/commit; check the memo-number space ON DISK (`ls dev/` — untracked and staged files included) before minting, because both of today's collisions were with uncommitted files. OPEN ITEM for the owner: the third-party `98-planning-document-reconciliation-memo.md` (staged, author no longer running) collides with this memo's number and must renumber to **dev/100** (dev/99 is taken by the untracked seed-reader-locking memo); neither live session will rewrite an absent author's in-flight work
- Follow-up work: the dev/89→98 program leaves Follow-up C (deferred, DEC-065) and screenshot storage-if-demanded; the Linux preview-limits tier decision if a Linux operator adopts the runner

## BL-P5-20260824-42: Package-seed reader locking shipped (memo dev/99) — the two-renames absence window is no longer observable by any production reader; no new DEC

- Date / author: 2026-08-24 / Karla
- Status: verified
- Requirements: close the dev/93 (`DEC-062`) recorded follow-up — readers that form a logical snapshot of the seed-managed package store must share the seeder's exclusive per-user lock, observing either the complete old tree or the complete replacement, never the interval between `_swap_in_package`'s two renames; amendment R2 accepted in full (composite over downgrade; batch plan resolution; writer hoist)
- Design decisions/artifacts: memo `dev/99` + amendments R1/R2 + the appended implementation record; `ADR-AG-007` (template knowledge and the lock stay in the packages domain — agents consume composites). NO new DEC: this closes a follow-up already recorded under `DEC-062`
- Design-to-code decisions or deviations: **(1)** `packages/locks.py::package_seed_lock` is the ONE owner of `.seed.lock` / `package-seed`; seeder and readers import it — the dev/93 private constants are gone; **(2)** the lock meets readers at one seam, `_locked_store_index`, whose result is parsed manifests (detached), so every transform runs unlocked; **(3)** R1.1 resolved by the `template_landscape` composite calling UNLOCKED cores — the non-reentrant lock is acquired exactly once on every path; the roster's two blocks and `_authoring_reuse_evidence` consume it; **(4)** R1.2: `resolve_templates` batch gate — a plan mint reads the store once regardless of node count; **(5)** R1.3: `_plan_seed` resolves every fixture-catalog read before the lock — `_seed_locked` is store work only; **(6)** §2 audit outcomes: resolver/routes/starters/libraries/build-deps/install-deciding probes snapshot under one hold each; `build_extension` + `backend_runtime` deliberately left alone (single non-seeded package under the target lock — recorded with the reason); **(7)** §7.8 shipped as a real AST structural audit, not "if practical"; **(8)** the non-reentrancy test pins the primitive and the docs rather than nesting for real — the first draft's live nest left a thread holding the keyed lock for every later test
- Files/modules: `app/packages/{locks (new),seed,services,resolver,routes,starters,libraries,build_deps}.py`; `app/agents/services.py` (consumer only); tests `test_packages/{test_locks (new),test_seed,test_available_templates}.py`, `test_agents/{test_routes,test_delegate_draft_mint}.py`
- Tests added/updated: swap-window regression parked between the two renames, parametrised over five production readers (blocked during the window, complete after); 8×8 forced-reseed stress through the production reader requiring ZERO absent reads (R1.4: inode comparison kept, tolerance inverted); lock contract (same helper object, path/namespace/key, exception release both layers, same-user waits / other user does not — events, no sleeps, Windows `_FakeMsvcrt` retry); one-acquisition pins for `resolve_for_project`, `lockfile_for_user`, `agent_resolve_report`, `resolve_dependencies`, the composite, and the roster; plan-mint cost invariant (12 nodes = 2 nodes in store walks); batch/single resolution equivalence per item; landscape ≡ the three readers; degradation signal survives the composite; the R1.3 catalog-reads-before-lock recorder; the §7.8 structural audit + the ADR-AG-007 pin (agents module never enumerates the store or names the lock)
- Verification evidence: full backend suite excluding `tests/test_frontend` (Playwright, BL-35 whole-suite fixture baseline) — step 2: 1741 passed / 8 skipped; step 3: 1763 / 8 skipped (+18 Playwright fixture errors, `test_share_link.py` green in isolation); step 4: 1762 / 8 skipped; step 5: 1763 passed / 8 skipped (measured, pasted). A/B at step 2: removing the single `with package_seed_lock` in `_locked_store_index` fails exactly the two window tests and nothing else
- Commits: `83d3edec` (memo as DRAFT + R1/R2), `ea802aa7` (step 1 cores + composite), `e4d27921` (step 2 lock), `47304bc9` (step 3 roster + batch mint), `2cf58202` (step 4 audit + `test_locks.py`), `74e99a97` (step 5 writer hoist), docs (this entry)
- Issues/regressions discovered en route: **(a)** `git commit -- <paths>` commits working-tree content, not the index — step 3's first attempt swept step 4's same-file hunks in; soft-reset and redone (the memo record and the session memory carry the technique); **(b)** the live-nesting test deadlock described in (8); **(c)** the Stage-3 index's "Concrete implementation remainder" line still says dev/99 is "proposed — implementation not started" — it sits inside a third party's UNSTAGED reconciliation edit of `3.1-Agents-Catalog-Build-Log.md` and is left for that author to update (this entry is the authority meanwhile)
- Follow-up work: none required by the memo. Optional, demand-gated: a reader/writer lock if profiling ever shows reader-reader serialisation on the short snapshot matters (memo §2 explicitly defers it)

## BL-P5-20260824-43: Project package lockfile authority (memo dev/101) — a canvas save can no longer clobber `dataflow.packages`; uninstall refuses truthfully while nodes use the package; load serves the effective lockfile; no new DEC

- Date / author: 2026-08-24 / Karla
- Status: verified
- Requirements: the owner's report — a Package-Builder-created package (`curio.postits@1`) could not be deleted from the drawer and did not appear in the palette; its node painted "Loading node…" forever. Investigation: the promotion journal proves Apply wrote the lockfile; a later canvas save overwrote it with `[]`; the backend backfilled the package from the canvas node while the frontend read the raw `[]`
- Design decisions/artifacts: memo `dev/101`. NO new DEC — extends the already-decided rule (backend-owned spec sections survive a client save: agents via `preserve_agent_state`, datasets via dev/81) to `dataflow.packages`
- Design-to-code decisions or deviations: **(1)** `spec_packages.preserve_project_packages` carries the on-disk EFFECTIVE lockfile (backfill included) forward on update, so a clobbered `[]`-with-nodes spec becomes explicit on its first save; **(2)** `uninstall_from_project` refuses (409, "N node(s) on this canvas use <dir> — delete them first") instead of a success that the backfill made a permanent no-op; **(3)** `load_project`/`load_shared_project` serve `sorted(get_project_lockfile)` in `dataflow.packages` when the key is a list — one read truth for palette, registry and drawer; absent key stays absent; **(4)** the drawer's lockfile re-sync now pulses `refreshPackageRegistry` when the set changed (`applyProjectLockfile`); **(5)** the backfill-on-empty rule itself is deliberately kept — every frontend-saved spec carries `packages: []` by default, so retiring it would need a migration of every user's specs; recorded as a possible later cleanup
- Files/modules: `app/packages/{spec_packages,services}.py`, `app/projects/services.py`; frontend `registry/projectPackagesStore.ts`, `components/packages/publishing/NodeCatalogDrawer.tsx`; tests `test_packages/test_lockfile.py`, `test_projects/test_services.py`, `src/tests/registry/projectPackagesStore.test.ts`
- Tests added/updated: 17 backend (preservation ×5, referencing_nodes ×3, the clobber regression + add-attempt + metadata-only, uninstall refusal ×3 + route 409, load normalisation ×2) + 4 jest
- Verification evidence: full backend suite excluding `tests/test_frontend` (Playwright, BL-35 baseline) — commit 1: 1774 passed / 8 skipped; commit 2: 1780 passed / 8 skipped; jest store suite 4/4; `tsc --noEmit` clean on the touched files
- Commits: `6f372de4` (commit 1), `3dcb9d07` (commit 2), frontend + docs (this entry)
- Issues/regressions discovered en route: the drawer already surfaced `body.error`, so the 409 text needed no frontend work; the reported project heals on open + next save with no manual edit
- Follow-up work: optional — retire backfill-on-empty behind a one-time spec migration if the implicit lockfile ever bites again

## BL-P5-20260825-44: The Researcher's reuse ladder made self-correcting (memo dev/105) — spelling-tolerant `package.install` with a grant-aware hint, parameter refusals free of the round budget, refused-vs-failed prompt semantics, roster says when nothing renders a note; DEC-067

- Date / author: 2026-08-25 / Karla
- Status: verified
- Requirements: the owner's report with screenshot — "the Researcher is still struggling to create the nodes with the research questions and answers"; the agent wrote a correct sourced answer, said the post-it template "cannot be used for content notes" and that `curio.notes` "is not currently available in the catalog", and answered in chat only. Session `c59aae2f…` / execution `b18c3461…`, model gemma4: `web.search` ok → `node.create` refused (3 ms) → `package.install` refused (7 ms) → prose; `MAX_TOOL_ROUNDS = 3` exhausted; run status `ok`
- Design decisions/artifacts: memo `dev/105`; `DEC-067` (extends DEC-063 from prompts to refusal texts and loop accounting). dev/93 is NOT reopened — its roster was correctly populated (`curio.notes@1` offered on the ENLIST rung with its dirName; `package.install` granted); the failure was the seams between the rungs
- Design-to-code decisions or deviations: **(D1)** `_resolve_catalog_dir_name` accepts dirName / bare manifest id / template id (through `canonical_template_id`, no second parser), refuses an ambiguous major naming every candidate, and pins the CANONICAL dirName; `_package_install_miss_hint` branches on `loop_ctx["granted"]`; **(D2)** `ParamRefusal` is a `str` subclass (every consumer still sees a string) applied at the two mints the field run hit (`_mint_package_install`, `_mint_node_create`) — NOT at every `return "refused"` — and both loops admit `MAX_REFUSED_ROUNDS = 2` before refusals count as rounds again; broken store/catalog/spec refusals stay unmarked by design; `final` is computed AFTER execution on the tool path; **(D3)** prompt splits REFUSED (correct or take the next rung, same run) from FAILED (stop), forbids absence claims the refusal did not make, forbids narrating request mechanics, names the exact dirName spelling on the ENLIST rung; **(S1 re-scoped on evidence)** the memo assumed the compute post-it was the only Available entry; the live list held the twelve built-in CODE templates (the post-it was already filtered as non-authorable) — so rows gain an additive `presentation` flag and a `research.notes.compose` run whose Available rows include no presentation template gets the "None of these renders a note" line; an entirely empty roster is stated, not omitted; **(found en route)** the roster call site's `except Exception: landscape = None` swallowed the cause silently — the dev/93 D2 shape — it now logs a warning; **(test harness)** both fake providers record the ONE mutable message list by reference, so `calls[i][-1]` is always the run's FINAL message — the new lanes and this memo's earlier assertions read the message history in order instead
- Files/modules: `app/agents/services.py` (resolver, hint, `ParamRefusal`/`_refuse_params`, `MAX_REFUSED_ROUNDS`, both loops, `_execution_record.refusedRounds`, `_available_templates_block(notes_agent=)`, `_NO_NOTE_TEMPLATE_LINE`, roster-unavailable warning), `app/agents/tools.py` (`package.install` contract description), `app/packages/services.py` (`presentation` row flag), `llm-prompts/researcher_notes_instruction.txt`; tests `test_agents/test_package_install_resolver.py` (new), `test_agents/test_reuse_ladder_field.py` (new — the field replay, two variants), `test_agents/test_routes.py::TestReuseLadder` (+7), `test_agents/test_builtin.py::TestResearcher`, `test_packages/test_available_templates.py`
- Tests added/updated: resolver unit lane (6, incl. lock-step with `canonical_template_id`); ladder route lanes: three spellings mint the same proposal, miss hint grant-aware, free corrections then a mint, refusal cap degrading to the dev/73 cutoff card, catalog-unavailable still counting, roster line present/absent/DFB-unaffected; prompt markers (6); row-shape pin; field replay: variant A (live store) ends in ONE `package.install` proposal pinned to `curio.notes@1`, zero nodes added, `refusedRounds == 1`; variant B (no note template anywhere) reaches the AUTHOR delegation at round 2 with `refusedRounds == 2` — the flow that sat at round 4 of 3 before commit 2
- Verification evidence: `test_agents` + `test_packages` — commit 1: 1451 passed / 4 skipped; commit 2: 1454 / 4; commit 3: 1454 / 4 (+ affected files 351 after a one-line test fix); commit 4: 1457 / 4
- Commits: `51b7b97a` (1, D1), `e122ad3d` (2, D2), `f1d274af` (3, D3/S1/S3), docs + field lane (this entry)
- Issues/regressions discovered en route: the silent roster swallow (logged now, behavior unchanged); the harness's by-reference `calls` recording (documented in both `_results` helpers; existing dev/93 assertions on `calls[i][-1]` are left as written — they happen to hold — flagged here for the next author)
- Follow-up work: widen `_refuse_params` to other mutate mints (plan write, template create, dataset install) when a lane shows the same failure — one line per site; the pre-existing empty Post-it compute node on the owner's canvas predates the run and is the user's to delete
- Amendment A1 (found by the live test, commit 5): with D1–D3 landed the Researcher proposed `Install package · Simple Notes` on the first turn, and Apply failed in the FRONTEND — `usePackageInstallReview.beginReview` resolved the dirName against `GET /api/packages/catalog` (the committed catalog only), so a store-only agent-authored package refused "no longer in the Nodes Catalog" before the store-aware backend apply ran; dev/93's E2E lane had proved the backend apply only. Fix: the hook loads `listInstalled()` + `catalog()` in parallel, store row first, probe set = store dirNames; 5 jest lanes; no backend change. Lesson: a frontend review gate is a SECOND gate and needs its own lane
- Amendment A2 (found by the second live test, commit 6): the ladder closed end to end — two `curio.notes/note-surface` notes landed with the right content — but WHITE and headed "Note". The `node.create` contract named only nodeType/content/goal (the prompt asked for title + color the contract never admitted — the DEC-067 shape once more), `_mint_node_create` dropped `params.title` and `_apply_node_create` never forwarded it to `_insert_node` (which already accepted it), and the Researcher's own attachment path had no A13 yellow/green default (dev/95's delegate path did). Fix: contract names title/goal/appearance; mint stores a bounded title + fills the A13 default ONLY for a `research.notes.compose` run on a `presentation` template with no color given (per-run `_note_creates` index; explicit colors win; other agents/templates byte-unchanged); apply forwards title; dev/95 rows map title→title; prompt names the exact params. Field variant C is the gate; suites 1460 / 4
- Amendment A3 (owner request, commit 7): Apply on the enlist card now PROCEEDS to the notes — as review cards below the install card, one node at a time — with no second message. The ENLIST request carries the A12 `notes` rows (the AUTHOR shape, one parser); the install apply enlists, then mints the rows as one pending A16 node.create sequence through the ordinary mint (titles + A13 colors), rides the cards on the applied turn and returns ordered `followUpProposals`; the frontend install review walks them one `await` at a time so each note lands (and the transcript refreshes) before the next card is applied — real events, no timers. Revision 1 (bulk server-side `createdNodes`) was rejected by the owner for its presentation. Honest degradation (no notes / no presentation template / failed row) is said in the turn text; a failing walk step leaves the remaining cards pending and applicable by hand. Field variant D is the gate; backend 1463 / 4, jest attach 241 / 241
- Amendment A4 (third live test, commit 8): the commit-7 flow ran correctly end to end after a PROJECT uninstall (`DELETE /api/packages/projects/<pid>/curio.notes@1`), but the notes painted "Loading node…" with PACKAGES 0 — the `package.install` apply body carried `installedPackage` without `requiresRegistryRefresh`, so the provider's bridge never pulsed the registry / project-packages store before the walk's node-created events. Fix: the install apply declares the lockfile change (`requiresRegistryRefresh: True`); the existing `package-nodes-created` branch fires with `nodes: []` and the handler already treats that as store-entry + registry pulse, nothing inserted. Before commit 7 the gap was masked by the notes arriving in a later turn. Backend 1463 / 4; jest attach 243 / 243

## BL-P5-20260825-45: Installing an orchestrator installs its required specialist; the Solve turn carries the install remedy it mints (memo dev/106) — `requiresAgents` closure, refuse-before-write, dependents-guarded uninstall, one reason line, mirror-driven Install row; DEC-068

- Date / author: 2026-08-25 / Karla
- Status: verified (backend + jest); owner live re-test of the screenshot flow pending
- Requirements: the owner's report with screenshot (`Screenshot 2026-08-25 at 3.24.17 PM.png`) — Dataflow Builder + Researcher installed; the plan applied; Solve reported "Solved 0 of 6 plan nodes" with six bare `<id> · failed` lines, "Finished in 0s"; "Retry 6 failed" produced the identical card. Session `71488964…`, solve executions `735a4ba5…`/`97f4bcb5…` in project `cb605bfd…`: 14 ms / 11 ms, `tools: []`, `usage: null`
- Design decisions/artifacts: memo `dev/106`; `DEC-068` (refines DEC-046/`REQ-ORCH-001`: the rule binds the orchestrator, not the user's explicit Install). `delegation.resolve` untouched; dev/48/52 semantics preserved and made VISIBLE rather than replaced
- Design-to-code decisions or deviations: **(D4, structural)** `requiresAgents` is an additive manifest field validated as a subset of `delegatesTo` (self/duplicate/non-id/not-a-list rejected); only the DFB declares one (`agent.node-content-builder`) — the other 14 delegates stay optional, no fan-out; `delegation.required_closure` walks transitively through `find_visible` (the run-time visibility rule, so install-time and run-time agree), cycle-safe, depth ≤ 8; `install_in_project` computes the closure BEFORE any write, 409s naming the missing ids with "nothing was installed", then materializes root + closure and writes the lockfile + defaults records ONCE; response gains `installed`/`required`; `uninstall_from_project` 409s naming dependents (dev/101 shape; no cascade); **(D1)** `_solve_events` checks the mint status: `proposed` → the part rides the Solve turn's content after the card; `refused` → the reason quotes the refusal and never says "awaits review"; **(D3)** ONE `reason:` card line for the batch (not six), `payload.reason` additive; the provider records `node_result.error` per node (`solveErrors`, cleared when the attachment's next batch starts, NOT on done) and the strip renders distinct reasons once; **(D2)** the strip's `installReview` reads the mirror for a pending `project.install` exactly as `planReview` does for plans — Install/Dismiss target `onApplyProposal`/`onDismissProposal` with the proposal id, no transcript part needed; a `project.install` apply calls `notifyAgentsPaletteRefresh`; **(deliberate reading)** no auto-add to My Imports — `_materialize_builtin` already puts the definition bytes in the user store; import is the dev/12 publishing step; **(test fixture)** the missing-specialist solve fixture now edits the lockfile directly (`_drop_from_lockfile`) because the API can no longer produce that state — it is a legacy project
- Files/modules: `app/agents/manifest.py` (`requires_agents`), `app/agents/builtin.py` (`BuiltinAgentSpec.requires_agents`, DFB declaration, `requiresAgents` serialization), `app/agents/delegation.py` (`required_closure`, `required_by`), `app/agents/services.py` (`install_in_project`, `uninstall_from_project`, `_manifest_to_card`/`_requires_agents_rows`, `_solve_events` not-installed branch + `_finish`); frontend `api/agentsApi.ts` (`AgentRequirement`, `AgentInstallResponse`, `AgentSolveResult.reason`), `components/agents/catalog/{agentListUtils.ts,AgentsCatalogDrawer.tsx,.module.css}`, `components/agents/attach/{AgentAttachmentsProvider.tsx,AgentBuilderStrip.tsx,AgentChatPanel.tsx,AgentDockOverlay.tsx}`; `docs/AGENTS.md`
- Tests added/updated: `test_manifest.py::TestRequiresAgents` (7), `test_builtin.py::TestRequiresAgentsRoster` (2) + DFB pin, `test_delegation.py::TestRequiredClosure` (5), `test_routes.py::TestProjectInstall` (+6: one-write closure, idempotent, dependency-first, unresolvable 409 writes nothing, uninstall guard, card disclosure), `test_routes.py::TestSolve` (missing-specialist test now asserts the TRANSCRIPT PART + one reason line; +Apply→Retry solves; +refused mint never claimed); jest: `agentListUtils.test.ts` (+5), `AgentsCatalogDrawer.test.tsx` (+3), `AgentBuilderStrip.test.tsx` (+3), `AgentAttachmentsProvider.test.tsx` (+2)
- Verification evidence: `test_agents` 863 passed after commit 3 (673 → 861 → 863 across commits 1–3); final gate `test_agents` + `test_packages` 1486 passed / 4 skipped; jest catalog+palette 173 / 173, attach 248 / 248; `tsc` error count unchanged from baseline (65 pre-existing, none in touched files)
- Commits: `9db3c497` (1, manifest + roster + closure helpers), `69db666c` (2, install closure + uninstall guard + cards), `e438c5a3` (3, Solve turn honesty), `d1abd249` (4, drawer disclosure), `f6b27079` (5, strip + provider), docs (this entry)
- Issues/regressions discovered en route: the existing missing-specialist test asserted exactly the backend string the UI discarded, so the invisible proposal was green in CI — the lane now asserts the transcript content shape `["card", "proposal"]`
- Follow-up work: none required; if a second built-in ever gains a server-invoked delegate, it declares `requiresAgents` and inherits the mechanism (roster invariant test enforces subset + visibility)

## BL-P5-20260827-46: The Dataflow Builder can repair the cycle it diagnoses (memo dev/112) — plan edges carry a kind, acyclicity is validated at mint and apply, edge-only plans mint, removed connections are named, every applied turn carries a Topology verdict; DEC-070

- Date / author: 2026-08-27 / Karla
- Status: verified (backend + jest); owner live re-test of the 2026-08-25 flow pending
- Requirements: the owner's report with `Screenshot 2026-08-26 at 4.21.26 PM.png` — "the agent correctly detected the cycle, even though it had created that invalid topology itself … it was unable to delete and recreate the necessary nodes and connections". Session `78fbbe35…` / project `cb605bfd…`: five "fix the cycle" rounds, each a correct diagnosis and a VALID applied plan `{nodes:[], edges:[vis→merge], removeEdges:[old]}` — the intended `Interaction` kind dropped by the grammar, `sourceHandle:"out"` hardcoded at apply, so the same data edge (and cycle) was recreated with a fresh id each round; filler note nodes added because edge-only plans were refused; "removed 0 nodes" after each edge removal
- Design decisions/artifacts: memo `dev/112`; `DEC-070`. Verdict recorded there: NOT the deferred `connection` attach target (entry-30 follow-up; its memo is dev/113) — a plan-contract gap, the DEC-063 failure shape applied to edges
- Design-to-code decisions or deviations: **(G1)** `edges[].kind ∈ {data, interaction}`, data byte-absent (dev/59 discipline), `type` accepted as the Trill alias, `toHandle` on an interaction edge refused; the preamble's rule (visualization vis-vega/autk-map/vis-simple ↔ data-pool) executable in `plan_topology.interaction_edge_errors` with a corrective error naming the offender and the fix. **(G2)** ONE helper `plan_topology.py`: NET graph over DATA edges (saved − removals − cascade + plan), `closing_plan_edges` blames a plan only for cycles IT closes — a user-drawn cycle the plan leaves alone is reported, never refused; mint refuses with the loop path and the fix; whole-plan apply re-checks BEFORE any in-place mutation (`_mark_stale` persists the spec) → 409 + stale; per-edge apply refuses the closing edge as a named row. **(G3)** edge-only plans valid; the grammar error now names the three options. **(G4)** `removedEdges` on the card (endpoint labels, kind) + `_removal_phrase` truthful for connections in summary, applied line, and result card. **(G5)** `_topology_clause` on every whole-plan applied turn; the instruction requires reading it before claiming a repair. Materialization: `_interaction_spec_edge` (`type: Interaction`, `in/out` both ends) on both apply paths; frontend `appliedEdgeToCanvasEdge` — ONE bridge materialization (bidirectional, arrows both ends, loadTrill parity) for bulk and per-edge; fan-in and merge slots ignore interaction edges (mint now matches `_plan_edge_context`)
- Files/modules: backend `app/agents/{plan_topology.py (new), content.py, services.py, tools.py}`, `llm-prompts/orchestration_instruction.txt` (sha re-pinned `dbc86373…`); frontend `utils/agentCanvasEvents.ts`, `api/agentsApi.ts`, `components/agents/attach/useAgentCanvasMutations.ts`, `components/agents/content/AgentReviewCard.tsx`; `docs/AGENTS.md`
- Tests added/updated: `test_plan_topology.py` (new, 18), `test_content.py::TestDataflowPlanEdgeKindGrammar` (8), `test_routes.py::TestPlanTopologyMint` (7: the owner's plan refused with the cycle named + nothing materialized; interaction→pool mints edge-only with kind; interaction→merge refused naming the fix; removal-only breaking a user cycle mints and names the connection; remove-and-readd-as-data still refused; untouched user cycle not blamed; existing interaction edges ignored by fan-in/cycles), `test_routes.py::TestPlanTopologyApply` (4: repair applies as Interaction shape + "removed 1 connection. Topology: acyclic."; user cycle reported; whole-plan drift → 409 with nothing mutated; per-edge closing edge refused by name while the interaction edge applies); jest `AgentReviewCard.test.tsx` (+1, group label updated), `useAgentCanvasMutations.test.tsx` (+1)
- Verification evidence: `pytest tests/test_agents` 918 → 925 → 929 across commits 1–3, 929 after commit 5; jest full 93 suites / 1066 tests; `tsc` unchanged (one pre-existing notice in the mutations test, present at HEAD)
- Commits: `6d5f89fb` (1, grammar + helper), `9e224e47` (2, mint rules), `ff2570ca` (3, apply paths), `1a287344` (4, frontend), `c4a30608` (5, contract copy + AGENTS.md), docs (this entry)
- Issues/regressions discovered en route: (a) the whole-plan apply's pre-existing merge-slot stale (`services.py` "has no free input slot") raises AFTER removals mutated the spec in place, and `_mark_stale` writes the spec — so that stale persists the removals; the new topology re-check was placed BEFORE mutation for this reason; the merge-slot case is left as found (recorded here, not fixed — separate lane); (b) the `dataflow.plan.write` description and instruction line 1 still said "additive / never touched" two memos after dev/59 made removals legal
- Follow-up work: owner live re-test; column/schema metadata for the Topology/verdict line when the runtime journal captures it (dev/67-6 seam); the merge-slot stale-after-mutation ordering (a); dev/113 (connection attach target) builds on this helper

## BL-P5-20260827-47: The `connection` attach target ships — Node Builder on an existing edge, inserting a reviewed node between its endpoints (memo dev/113); DEC-071

- Date / author: 2026-08-27 / Karla
- Status: verified (backend + jest); owner live re-test pending (drag Node Builder onto an edge, insert, confirm the badge moves to the downstream connection)
- Requirements: dev/15 §5 ("Node Builder attaches to the canvas or a selected connection"), deferred at dev/48 §3.1 for lack of UI and an apply semantics; owner decisions 2026-08-27 ("take your recommendations"): existing-edge target, Node Builder only, drop-on-edge, midpoint badge, `node.insert`, dev/112 first, legacy `llmRequest` callers untouched
- Design decisions/artifacts: memo `dev/113`; `DEC-071`; consumes DEC-040/047/049/051 and DEC-070 (dev/112)
- Design-to-code decisions or deviations: **(1)** roster: Node Builder `targets=("canvas","node","connection")`, `tools += node.insert` (`_ROSTER_GRANTS` includes it so the template roster rides the run); **(2)** `node.insert` never takes an edge id — `_connection_target_edge` reads it from the attachment record (least privilege; a `params.edgeId` is ignored, test-pinned); **(3)** mint composes the equivalent plan and applies dev/112's rules; **deviation from the memo:** inserting into an INTERACTION connection is refused plainly instead of "keeping the kind" — under the interaction rule a node inside vis ↔ pool would have to be a data-pool for one half and a visualization for the other, which no node is; **(4)** apply pins `baseEdgeDigest` (id, endpoints, handles, kind) + template + endpoints, refuses 409 + stale BEFORE any mutation (`_mark_stale` persists the spec — the dev/112 lesson), then removes the edge, `_insert_node` placed at the endpoints' midpoint, adds source → new (`in`) and new → target keeping the original `targetHandle` (merge slot), re-targets every connection attachment on the removed edge to the downstream edge with a revision bump, one write, applied turn with the Topology verdict; **(5)** cards carry a server `targetLabel` ("Transform → Pool Input Merge"); **(6)** frontend: `pickEdgeAtPoint` samples a cubic bezier between endpoint anchors (data: right-middle → left-middle; in/out: top/bottom), tolerance 14 flow units, nearest wins, nodes first; a module hover store (`setAgentDropHoverEdge`/`useSyncExternalStore`) thickens the hovered edge without edge-data churn; `EdgeAgentBadges` mirrors `NodeAgentBadges` through `EdgeLabelRenderer` in both edge components; `attachAgentOnDrop` pre-saves for any element target; `targetContext` composes `Target connection:` with edge kind/handles and endpoint snapshots (content SIZE, never content); the provider dispatches `appliedGraph` ahead of `createdNode` so a split paints as one graph delta (removal first)
- Files/modules: backend `app/agents/{builtin.py, tools.py, services.py}`, `llm-prompts/node_build_instruction.txt` (§7); frontend `utils/{agentsPaletteEvents.ts, agentDropAttach.ts}`, `components/MainCanvas.tsx`, `components/edges/{UniDirectionalEdge,BiDirectionalEdge}.tsx`, `components/agents/attach/{EdgeAgentBadges.tsx (new), AgentChatPanel.tsx, AgentAttachmentsProvider.tsx, agentRunContext.ts}`, `components/agents/content/AgentReviewCard.tsx`, `api/agentsApi.ts`; `docs/AGENTS.md`
- Tests added/updated: backend `test_routes.py::TestNodeInsert` (6: connection attach + non-edge id 400; mint names the connection and ignores `params.edgeId`; apply splits, keeps `in_0`, re-targets downstream, node at x=600, applied turn ends with the verdict; interaction connection refused plainly; drift on the connection → 409 + nothing mutated; canvas attachment refused with guidance), `test_builtin.py` (targets + tools pins), `test_tools.py` (registry pin); jest `agentDropTarget.test.ts` (+4: midpoint hit/miss, nearest, missing endpoints, hover store), `EdgeAgentBadges.test.tsx` (new, 2), `AgentReviewCard.test.tsx` (+1), `agentRunContext.test.ts` (+1), `AgentAttachmentsProvider.test.tsx` (+1 precedence)
- Verification evidence: `pytest tests/test_agents` 935 passed; jest 94 suites / 1075 tests green; `tsc` unchanged (pre-existing notices only — `MainCanvas.tsx` `projectId` null, the mutations/provider test spreads)
- Commits: `c9a2307c` (1, backend contract + mint + apply + re-target), `8dad35a7` (2, drop-on-edge + badge + labels), `819f5f48` (3, composer + card + provider precedence), `91aeb141` (4, docs — this entry)
- Issues/regressions discovered en route: (a) the shared working tree was switched to `feat/agentcatalog` (no "s") and reset by another session mid-implementation — my dev/113 edits landed on the wrong branch; recovered via a saved patch and a dedicated worktree (`../curio-feat-agentscatalog`); (b) `DEC-069` had been minted the same day by dev/110 — dev/112 renumbered to DEC-070 (`779a4c96`), this memo's proposal to DEC-071; (c) the provider's `createdNode`-before-`appliedGraph` order would have painted an insert without removing the edge
- Follow-up work: owner live re-test; an edge context-menu attach gesture if drop-on-edge proves fiddly at small zoom (data model unchanged); other agents on edges = a one-line `targets` change per agent when wanted

## BL-P5-20260908-48: A data-loading node's source is never the model's word — the runtime source-grounding gate, Node Builder → Dataset Finder discovery, and the review card's Source block (memo dev/114); DEC-072

- Date / author: 2026-09-08 / Karla
- Status: verified (backend + jest); owner live re-test pending (ask Node Builder for a node that loads Brazilian IBGE demographics with no catalog match: expect a candidates card or a request for a path — never `bras_ibge_data.csv`; confirm a NOAA-style verified row and check the card's Source block)
- Requirements: GitHub issue #298 (fabricated local filenames → FileNotFoundError); docs/06 two-lane contract and "review before apply"; the dev/67-4 §1 asymmetry ("the Node Builder writes fetch code around an unverified URL; first contact with reality is the user executing the node"); owner directive 2026-09-08 (port onto `imp/agentcatalog`, never merge feat back)
- Design decisions/artifacts: memo `dev/114` (+ Amendment A1, the port record); `DEC-072`; consumes DEC-006/009/046/047/053/063/067/070
- Design-to-code decisions or deviations: **(1)** ONE pure gate module `source_grounding.py`; every boundary calls `_gate_generated_content`; **(2)** `curio_dataset_path("<id>")` (main's portable loader) grounded by catalog id — the memo had assumed literal paths only; **(3)** URLs in the USER's text are not evidence (the DEC-047 hand-off prompt is model-suggested text); **(4)** 401/403 grounded as credential-gated; 400 names the request shape (the owner's Census case); **(5)** `#269`'s `CANDIDATES_INSTRUCTION` reused as the delegate reply schema — one schema, not two; **(6)** `_candidates_pending_review` makes "the user confirms first" structural; **(7)** Solve gates in `_record_outcome` (write) and before the review mint (propose); **deviations from the memo:** no `node.insert` on this branch (dev/113 is feat-only); plan-carried content already refused (dev/67-5) so no plan check; docs to `docs/AGENT-CATALOG.md`; six planned commits landed as five
- Files/modules: backend `app/agents/{source_grounding.py (new), services.py, builtin.py, tools.py, node_context.py}`, `llm-prompts/{node_build_instruction.txt, default_preamble.txt, discovery_instruction.txt}`; frontend `api/agentsApi.ts`, `components/agents/content/{verificationChip.tsx (new), AgentReviewCard.tsx(+css), AgentDatasetCandidatesCard.tsx, AgentChatCard.tsx}`, `components/agents/attach/AgentChatPanel.tsx`; `docs/AGENT-CATALOG.md`
- Tests added/updated: backend `test_source_grounding.py` (new, 24: scanner incl. function-body parse and f-string templates, catalog-id form, verdict rules, refusal text, bounds), `test_source_grounding_routes.py` (new, 23: the #298 regression with the free refusal round, shapefile guess, catalog rows carry path/loader, user path, discovery mint + same-turn refusal + next-turn grounded create without re-probe, 404 refused, user-text URL not evidence, guessed filename with external context, credential-gated, unavailable source ends in a request, synthetic labeled / unrequested refused / computation untouched, Solve fails the node and hands the child `sourceGrounding` then solves on retry, dev/73 mint inherits the gate, candidates-mint recognition/dropping/honesty), `test_node_context.py` (+1 thin ref), `test_tools.py` (+1 path/loader), `test_builtin.py` (pins + 3 wording tests); jest `AgentReviewCard.test.tsx` (+6), `AgentDatasetCandidatesCard.test.tsx` (+2), `AgentChatPanel.test.tsx` (+2), `AgentChatCard.test.tsx` (new, 2)
- Verification evidence: `pytest utk_curio/backend/tests/test_agents` 1045 passed; jest 199 suites / 2296 tests green (two unrelated suites flaked once under the full parallel run and passed alone and on the rerun); `tsc --noEmit` unchanged (pre-existing notices only)
- Commits (on `imp/agentcatalog`): `634ee9a3` (1, gate module + catalog.search path/loader + node_context), `eaaa079f` (2, backend mints/Solve + NB ↔ DF seam + roster), `b219af6a` (3, frontend), `fa4aa140` (4, contract copy), `4039f346` (5, docs — this entry lives in the untracked `plans/` tree)
- Issues/regressions discovered en route: (a) the shared tree was on `main` at start; a temporary worktree on `feat/agentscatalog` was created and, at the owner's request, removed — `main` had absorbed feat through dev/106 and refactored it (17-file merge conflicts), so the owner chose the port; (b) `compose_node_context` read the thin installed-dataset ref by the wrong keys (fixed here, commit 1); (c) run-loop `messages_work` aliasing in provider-call recordings (test-fixture lesson, Amendment A1 §8)
- Follow-up work: owner live re-test; F1 request-parameter validation for verified endpoints (the HTTP 400 class); F2 the legacy `llmRequest` chat callers on `main` (the reporter's actual path — the gate is a pure function ready to be called there); F3 provenance persisted on the spec node; F4 package-authored python templates through the gate; F5 `catalog.search` relevance for the delegate path

## BL-P5-20260908-49: Solve is the engineering loop — verified data-loading nodes over the ONE round loop, as detached re-attachable background jobs with `interrupted` reconciliation (memo dev/115); DEC-073 (DEC-021's single-process slice)

- Date / author: 2026-09-08 / Karla
- Status: verified (backend + jest) AND live re-tested 2026-09-08 against gemma4 on a `curio start` stack — Amendment A3's three prompts all pass as re-read in memo §A3.1 (Task 3's Census endpoint now requires an API key: the loop names it in one `source-missing` round instead of exposing the 400/HTML answer); the live run produced four field-fix commits (`cb0332e9`, `0b99b99e`, `421c95c6`)
- Requirements: dev/67-0 ("every Solve must be validated; no trivial-code shortcut"); dev/114 F1 (the owner's HTTP 400 after the static gate); owner directions 2026-09-08 — Option A over LangGraph, data-loading only, A1 (every offered data-loading node, trail on the card), A2 (the trigger is the user's Solve, never the mint or Apply), A3 (field scenarios)
- Design decisions/artifacts: memo `dev/115` (+ A1–A3); `DEC-073`; consumes DEC-006/021/046/048/050/053/054/063/067/072
- Design-to-code decisions or deviations: **(1)** `_verified_content_rounds` extracted from `_validate_events` — one loop, four callers; `start_from_current` runs the node's current code as round 0; the DEC-072 gate runs BEFORE every execution (a fabricated path is a failed round of kind `ungrounded-source`, never a sandbox run); a failed round hands the correction `previousAttempt` + `validationError` + `sourceGrounding` + `urlEvidence` (fresh probe of the URLs the candidate fetched, budgeted through the run's cache); **(2)** runner: `dataset_paths` + `user_key` on the `/exec` payload (the `curio_dataset_path("<id>")` loaders dev/114 made canonical used to fail under validation), `ExecutionTimeout` = the node's failure, `CURIO_VALIDATION_EXEC_TIMEOUT` default 300 (was a hardcoded 120), measured `durationMs`; **(3)** Solve write mode writes only PASS; exhaustion `failed` with "not fixed after N attempts — <kind>: <detail>" (+ the Dataset Finder remedy for an ungrounded source); a sandbox outage leaves the node `pending` with the reason; propose mode mints an executed review with the `validation.attempts` trail (PASS or FAIL — dev/67-7's labeled choice, kept); `verify: false` keeps the legacy write; **(4)** `agent_jobs.py` (the `build_jobs.py` shape): the generator runs in a daemon thread, the request only subscribes (replay + tail), one live job per attachment, per-user cap refused BEFORE any in-flight state persists, finished jobs replayable 15 min, `GET …/jobs/stream`, `liveJob` on cards; everything needing the request context (grounding base, dataset-path mapping) is resolved in the request thread and handed to the job — and a job-thread mint grounds against that base (the propose-mode reviews were refusing catalog loaders until this); **(5)** reconciliation: `phase == solving` and the execution not live in THIS process → `interrupted` on first read for action (listing, Solve, cancel), one transcript turn naming the no-replay rule, Retry = new execution with `retryOf`; **deviation from the memo:** no persisted heartbeat — liveness IS the lease in the single-process topology (a ticker thread writing the spec would race `_finish`); the 15-minute `solvingSince` guard stays the outer bound; a multi-worker deployment would mis-reconcile and is OQ-009; **(6)** per-node Solve (`POST …/solve-node`): a node with content lands as an already-executed `node.content.write` (DEC-006), an empty node is written on PASS, exhaustion mints nothing; no cancel in v1; **(7)** frontend: the Dataflow Builder declares `runtime.execution: background` (the field the contract validated and nothing read), running dot on badges (tokens only — the colour-literal guard caught a slip), strip states in words + `interrupted`/`Retry N interrupted`, ONE Solve stream handler for started and re-attached batches, re-attach once per execution on chat open, `NodeSolveRow`, the review card's collapsed "Verification · N attempts" trail and the data-loading `node.create` effect-line note
- Files/modules: backend `app/agents/{agent_jobs.py (new), services.py, routes.py, builtin.py, validation.py}`, `app/execution/runner.py`; frontend `api/agentsApi.ts`, `components/agents/attach/{AgentAttachmentsProvider.tsx, AgentBuilderStrip.tsx(+css), AgentChatPanel.tsx, AgentDockOverlay.tsx, AgentAvatarBadge.tsx(+css), NodeSolveRow.tsx (new)}`, `components/agents/content/AgentReviewCard.tsx(+css)`; `docs/AGENT-CATALOG.md`
- Tests added/updated: backend `test_verified_rounds.py` (new, 34: the loop, dataset paths, verified Solve, detached jobs, per-node Solve), `test_agent_jobs.py` (new, 9), `test_runner.py` (+6), `test_validation.py` (+1), `test_builtin.py` (+1 background pin), dev/63's disconnect and cancel tests adapted (the job finishes on its own; a live job stands in for "another worker"), dev/114's Solve grounding test adapted (a fake sandbox for the retry half); jest `AgentBuilderStrip` (+3), `AgentReviewCard` (+3), `AgentAvatarBadge` (+1), `AgentChatPanel` (+3), `agentsApi` (+2), `AgentAttachmentsProvider` (+3)
- Verification evidence: `pytest tests/test_agents tests/test_execution` 1115 passed; jest 199 suites / 2311 tests green; `tsc --noEmit` unchanged (pre-existing notices only); after the field fixes: `pytest tests/test_agents tests/test_execution tests/test_backend.py` 1150 passed / 2 skipped; jest 199 suites / 2312 tests; `tsc --noEmit` clean
- Commits (on `imp/agentcatalog`): `7592497b` (1, runner + the ONE loop), `c792442b` (2, verified Solve), `b575a9a9` (3, detached jobs + reconciliation + per-node Solve), `617d9834` (4, manifest + frontend), `ea56464f` (5, docs — this entry lives in the untracked `plans/` tree); field fixes from the live re-test: `cb0332e9` (display blocks mid-reply / broken terminal block), `0b99b99e` (runner presents the sandbox shared secret — `execution/sandbox_auth.py`), `421c95c6` (correction grounded in the endpoint's real answer: gate-verified URLs reach the correction, composed-request probes, `finalUrl`/`pageTitle`/`bodySample` evidence, `endpointEvidence` on attempts, terminal `source-missing` declines, content-prompt rules)
- Issues/regressions discovered en route: (a) the runner's `/exec` payload omitted `dataset_paths`/`user_key` (fixed); (b) the loop's grounding context did not read the dataflow mission text, so a plan goal saying "synthetic" never reached the gate (fixed: goal + mission are human intent); (c) job-thread mints re-listed the catalog without a user and refused catalog loaders (fixed: the mint grounds against the batch base); (d) the dev/63 disconnect semantics changed by design — the test now waits for the job; LIVE (2026-09-08): (e) the validation runner never sent `CURIO_SANDBOX_TOKEN` — every verified round was `infrastructure · 401` under `curio start`, invisible to suites that fake `_http_exec` (fixed, one shared helper); (f) a URL the gate verified by probe was not in the correction's `verifiedUrls` (fixed); (g) URL evidence probed only the base URL — the Census composed request redirects to a 'Missing Key' HTML page (fixed: composed-request probes + redirect/page-title evidence); (h) a builder's one-line decline was gated as ungrounded code and re-asked (fixed: `source-missing`, terminal); (i) gemma4 places display blocks mid-reply / closes with broken JSON (fixed in `extract_content`); (j) recorded, not built: F6 the 4-call egress budget starves the last candidate row after one redirect; F7 key-gated APIs need a secret input the sandbox injects
- Follow-up work: owner live re-test (A3); F1 widen verified Solve beyond data-loading (`verifyKinds`); F2 OQ-009 multi-instance owner / the LangGraph re-open criteria; F3 sandbox-side cancel of an in-flight execution; F4 output-schema evidence in verdicts (dev/67-7 follow-up); F5 mint-time execution as an opt-in for Node Builder proposals (explicitly NOT the default — A2)

## BL-P5-20260908-50: Connection keys — a data-loading node reaches a key-gated API by name (`curio_secret("<name>")`), never by a literal (memo dev/116); DEC-074

- Date / author: 2026-09-08 → 2026-09-09 / Karla
- Status: implemented, verified AND live re-tested 2026-09-09 with a real Census key saved through the UI — PASS on the fourth pass (executed review whose Source block names the key; no value anywhere); three harness fixes landed as `ab87e8fb` (placeholder-keyed probes, the loop's own egress budget, comment-only repeats not re-run)
- Requirements: dev/115 F7 (the harness names the missing Census key and nothing lets the user supply it safely); dev/50's T4 line ("credential profiles at run time" — the runtime half); owner request 2026-09-08 "lets plan the F7 api key input for data loading nodes", approved commit by commit
- Design decisions/artifacts: memo `dev/116`; `DEC-074`; consumes DEC-006/022/053/061/063/067/072/073; T4 note on DEC-022
- Design-to-code decisions or deviations: **(1)** store `users/connection_keys.py` — `.curio/users/<key>/connection-keys.json` 0600 in a 0700 dir, atomic replace under the spec-lock idiom, `ConnectionKeyRef` for everyone but `resolve()`, 409 on re-binding a name to another host without `replace`, temporary guests 403, the shared guest → the `guest` store; routes on their own blueprint `/api/users/me/connection-keys[/<name>]` + `/suggest-name` (memo said "beside the auth routes"); **(2)** `secret_names`/`SECRET_CALL_RE` is the ONE scanner; `/processPythonCode` and `run_through_node` carry a `secrets` key only when a saved name is used (byte-compatible otherwise); both sandbox modes inject ONE callable (`sandbox/util/secrets.make_curio_secret`) beside `curio_dataset_path`, the isolated child pops the values from the request first; stdout/stderr redacted at the sandbox boundary; the redactor moved to `utk_curio/common/redaction.py` (the sandbox must not import the backend); the sandbox's missing-key sentence names the key and where to save one but not the saved names (names on every request would be a leak of a different kind; the gate lists them); **(3)** agent path: `GroundingContext.secrets`/`secret_values`, `availableSecrets` + rule clause, `secret` ref kind and label, unknown-name and credential-literal refusals, credential-gated hint, `secrets_fn` per round on all three loop callers, keyed composed probes (`_keyed_probe`, redacted, never cached/verified) over `egress.fetch(headers=, params=)` (params join the URL before the policy check), `_decline_remedy` → `evidence.remedy`/`attempt.remedy`/batch result/solve-node `done.remedy` with a second kind `use-connection-key`; both prompt clauses; **(4)** frontend: `ConnectionKeysSection` as a collapsed `<details>` in AI Settings whose body mounts only while open (the modal's existing tests query one Save button and a text-box Model field), masked write-only Key field cleared on save, the `use` line shown after saving, 409 → "Replace the host binding", Remove asks first and names the consequence, shared-guest banner; `AddKeyAction` = the ONE remedy rendering on the Solve strip (one per host from `solveRemedies`), the per-node Solve row and the review card's attempt trail; cards reach the modal through a window event (`curio:connection-keys`) answered by `ConnectionKeysModalHost` on the dock overlay (the settings modal is mounted only by the projects/catalog pages otherwise); the Source block reads a `secret` ref as *Connection key · name · host*
- Files/modules: backend `users/{connection_keys.py, connection_keys_routes.py}`, `app/__init__.py`, `api/routes.py`, `execution/runner.py`, `agents/{validation.py, services.py, source_grounding.py, egress.py, verify.py}`; shared `utk_curio/common/{__init__.py, redaction.py}`; sandbox `app/{api.py, worker.py}`, `util/secrets.py`, `isolation/{runner.py, protocol.py, child.py}`; prompts `new_content_prompt.txt`, `node_build_instruction.txt`; frontend `api/{agentsApi.ts, connectionKeysApi.ts}`, `components/AiSettingsModal.tsx`, `components/connectionKeys/*`, `components/agents/attach/{AgentAttachmentsProvider, AgentChatPanel, AgentDockOverlay, AgentBuilderStrip, NodeSolveRow}.tsx`, `components/agents/content/AgentReviewCard.tsx(+css)`; docs `docs/{USAGE, AGENT-CATALOG, NODE-CATALOG}.md`
- Tests added/updated: backend `test_users/test_connection_keys.py` (new), `test_redaction.py` (new), `test_backend.py` (+3), `test_execution/test_runner.py` (+2), `test_agents/test_validation.py` (+1), `test_agents/test_source_grounding.py` (+6), `test_agents/test_verified_rounds.py` (+5), `test_agents/test_egress.py` (+2), `test_agents/test_verify.py` (+1); sandbox `test_worker_secrets.py` (new), `test_isolation_secrets.py` (new); jest `aiSettings` (+6), `AgentReviewCard` (+4), `AgentChatPanel` (+1), `AgentBuilderStrip` (+2), `connectionKeysApi` (new), `connectionKeysRequest` (new)
- Verification evidence: backend `test_agents` + `test_execution` + `test_backend` + `test_users` + `test_redaction` 1231 passed / 2 skipped; sandbox suite green apart from the pre-existing platform-bound `test_isolation_fallback…unsupported_platform` failure (fails identically without these changes on this Mac); jest 201 suites / 2330 tests; `tsc --noEmit` clean
- Commits (on `imp/agentcatalog`): `58115a82` (1, store + routes + redactor), `ecccb58a` (2, resolver + wire + sandbox injection + boundary redaction), `97d97ccf` (3, agent path), `84169e4d` (4, frontend), `e68509a8` (5, docs), `ab87e8fb` (live re-test fixes: `composed_requests` carries `curio_secret(...)` placeholders + `_placeholder_probe`, `_LOOP_EGRESS_CALLS`, `repeated-attempt` after the gate)
- Issues/regressions discovered en route: LIVE 2026-09-09 — (d) a `curio_secret(...)` request parameter was non-composable so the keyed probe never ran (fixed: placeholder composition, value swapped in only on the keyed path); (e) the run-wide egress budget of 4 starved every correction round of its evidence (fixed: the loop budgets for all its rounds; dev/115 F6's Dataset Finder half stays open); (f) gemma4 repeats a failed correction changing only comments (fixed: not run again, named `repeated-attempt`); (g) gemma4 may stop at the candidates card and never propose — DEC-047 by design, the driver confirms like the card; (a) a nested component for the section body would have remounted its inputs on every keystroke — rendered as a fragment value instead; (b) the modal's existing tests collided with the section's Save button and combobox — resolved by mounting the body only while open, not by loosening the tests; (c) `egress.fetch` reused the name `headers` for the response headers — renamed `resp_headers` when request headers were added
- Follow-up work: F1 T4 encrypted-at-rest store behind `ConnectionKeyStore` (+ migrate the LLM key column); F2 Streetvision's browser-posted Google Maps key → a connection key; F3 dev/115 F6 egress budget starvation — CLOSED 2026-09-09 (`_RUN_EGRESS_CALLS` = candidate cap × 2 for the run; `_LOOP_EGRESS_CALLS` for the loop); F4 editor-side hint when a user types a credential literal into node code — DELIVERED by dev/117 (`BL-P5-20260909-51`); owner live re-test: save a Census key, Solve the Task 3 node, expect an executed review whose Source block names the key and whose trail carries no value

## BL-P5-20260909-51: The node editor hints — never blocks — when a person types a credential literal, and offers the connection-key route (memo dev/117); no new DEC

- Date / author: 2026-09-09 / Karla
- Status: implemented and verified (jest + backend fixture parity + tsc)
- Requirements: dev/116 F4 (the last leak path for a key is the human's own editor); owner choice 2026-09-09 ("lets plan the editor hint first") over the Streetvision Maps key and the T4 store
- Design decisions/artifacts: memo `dev/117`; consumes DEC-072 (the gate covers agent-authored content — this covers the human's, without a gate) and DEC-074 (a key is reached by name); no new DEC (a UI hint inside DEC-074's posture, like dev/96)
- Design-to-code decisions or deviations: **(1)** `services/connectionKeys/credentialLiterals.ts` is the TS twin of the backend's regex form — a NAME and a LINE per finding, the value dropped, ≤ 5 findings, 200 KB scanned, URLs/paths/data files excluded as sources, the name must start a token; the SAME fixture strings are asserted on both sides (`credentialLiterals.test.ts` ↔ `test_source_grounding.py::test_shared_fixtures_with_the_editor_detector`) with `KEEP IN SYNC` comments; **(2)** `suggestName`/`hostOf` moved onto `connectionKeysRequest.ts` — one implementation for the settings section and the hint; the old copy's bug (a URL's last path segment became the name) went with it; **(3)** `CredentialHint` bar above Monaco: `role=status`, `aria-live=polite`, sentence labels, tokens only, its stylesheet added to the colour-literal guard as an explicit extra file (the rest of `components/editing` is legacy CSS with literals of its own); Save as connection key → `requestConnectionKeys({host, suggestedName})` from the code's first URL; Dismiss keyed on `findingsKey` (a moved literal re-shows — accepted); read-only → text only; **(4)** `CodeEditor.tsx`: `CREDENTIAL_SCAN_DEBOUNCE_MS = 300` on typing, immediate scan on `onExternalApply` (dataset drop, LLM apply, collab, provenance), best-effort `setModelMarkers` Warning squiggles skipped where Monaco lacks them, timer and markers cleared on unmount; nothing blocks Play/save/collab and the model's text is never touched; dismissal is per mount by design (a reload shows the hint while the key is still there)
- Files/modules: frontend `services/connectionKeys/credentialLiterals.ts` (new), `components/editing/{CredentialHint.tsx, CredentialHint.module.css}` (new), `components/editing/CodeEditor.tsx`, `components/connectionKeys/{connectionKeysRequest.ts, ConnectionKeysSection.tsx}`, `tests/styles/agentColourLiterals.test.ts`; backend `agents/source_grounding.py` (sync comment only); docs `docs/USAGE.md`, `docs/AGENT-CATALOG.md`
- Tests added/updated: jest `credentialLiterals.test.ts` (new, 6), `connectionKeysRequest.test.ts` (+1), `CodeEditor.test.tsx` (+7: debounce, external apply + markers, dismiss/re-show, Save → request payload, read-only, no-markers Monaco, status region + untouched model); backend `test_source_grounding.py` (+1 shared-fixture parity)
- Verification evidence: jest 202 suites / 2345 tests; backend `test_source_grounding.py` 42 passed; `tsc --noEmit` clean
- Commits (on `imp/agentcatalog`): `5e0177b3` (1, detector + shared fixtures + `suggestName` on the bus), `1cfd63b2` (2, the bar + editor wiring + guard scope + tests), + docs commit (3 — this entry lives in the untracked `plans/` tree)
- Issues/regressions discovered en route: (a) the dev/116 `suggestName` took a URL's last path segment (`https://api.census.gov/data` → `data`) — the UI tests had passed a suggested name explicitly and missed it; fixed with the move
- Follow-up work: F1 a "Replace with `curio_secret("<name>")`" action once a key with a matching host is saved; F2 the same hint in `GrammarEditor.tsx` (keyed data URLs in Vega/Autark specs); dev/116 F2 (Streetvision Maps key) and F1 (encrypted store) unchanged

## BL-P5-20260909-52: Solve verifies every executable node kind — topological waves, per-wave persistence, an honest `not-executable` outcome, a batch deadline, same-batch reuse (memo dev/118); DEC-075

- Date / author: 2026-09-09 / Karla
- Status: implemented, verified AND live re-tested 2026-09-09 (gemma4, the A3 Task 2 prompt): 4–5 waves, browser-rendered kinds written *not executable*, the solved loader's output reused; one hollow pass found and fixed (`71445ad2`: an empty upstream is a named blocker for validation runs and its dependent WAITS pending — memo §Live re-test)
- Requirements: dev/115 F1 ("widen verified Solve beyond data-loading"); dev/67-0's standing rule ("every Solve must be validated; no shortcut for trivial code"); owner 2026-09-09 "lets continue with the F1 from dev/115", approved commit by commit
- Design decisions/artifacts: memo `dev/118`; `DEC-075` (supersedes DEC-073's v1 scope note); consumes DEC-021/050/054/067/072/073/074; the survey report is the memo's Evidence line
- Design-to-code decisions or deviations: **(1)** `workflow_spec.is_executable_kind` (the `code` category) is the ONE predicate for the batch, the per-node Solve and validate-node; `run_through_node` refuses a non-code TARGET by name (`notExecutable`); `validate_candidate` verdict `not-executable`; per-node Solve answers without a round; validate-node proposes the generation labeled and the plan ledger reads `validated` so Simulation Mode proceeds (the frontend chip word is unchanged — deviation, recorded); **(2)** `_solve_waves` over the batch's own targets (data-flow edges only; a cycle → one last wave the runner refuses), `_persist_wave` + `_apply_contents` shared with `_finish` (spec lock, deleted/user-edit guards, a `persisted` set so a landed content is never re-guarded), per-wave `nodeRuns` + `solvingSince` refresh, the re-read spec for the next wave, `upstreamOutputs` on the legacy inputs and the loop's `extra_inputs` (+ the content-prompt clause), `solve_wave` on the stream; **(3)** `CURIO_SOLVE_BATCH_DEADLINE` (default 45 min) checked before every wave and dispatch → `pending` + reason + `batch_reason` once; `precondition` (slice bound, cycle) → `skipped` after ONE round (no correction), `nodeRuns = skipped` — deviation from the memo's "pending kept" (retrying the same slice re-hits the bound; the runner's message says to run the dataflow manually); **(4)** `prior_outputs` on the runner/validation/loop, `evidence.output` + `evidence.reusedNodes`, `attempt.reusedNodes` / `reuseRetried`, the vanished-artifact retry triggers on the TARGET's input-load failure with reuse in place (the memo's "blocker is a reused node" named the wrong node); `upstreamOutputs` never carries the artifact record; **(5)** frontend: `solve_wave` → `solveWave` (strip status line *wave 2 of 3 — N nodes*), `solveNotices` (pending/skipped reasons and written-not-executed, rendered once per text as a note, never as an error), pill `written — runs in the browser, not executed`, `isExecutableNodeType` (the frontend twin, KEEP IN SYNC) drives the review card's effect line, attempt rows read *not executable* and *reused N upstream result(s) (re-run whole once)*
- Files/modules: backend `execution/{workflow_spec.py, runner.py}`, `agents/{validation.py, services.py}`, `llm-prompts/new_content_prompt.txt`; frontend `utils/executableNodeKinds.ts` (new), `api/agentsApi.ts`, `components/agents/attach/{AgentAttachmentsProvider, AgentDockOverlay, AgentChatPanel, AgentBuilderStrip(+css)}.tsx`, `components/agents/content/AgentReviewCard.tsx`; docs `docs/{AGENT-CATALOG, USAGE}.md`, docs/09 note
- Tests added/updated: backend `test_runner.py` (+5), `test_validation.py` (+2), `test_verified_rounds.py` (+11: the loop's not-executable round, per-node Solve on a Vega node, wave order + `upstreamOutputs`, per-wave persistence observed mid-batch, the wave helper, the deadline, the slice bound, the env reader, three reuse cases; the v1 pins flipped), `test_routes.py` (+1 validate-node data-pool; `TestSolve._applied_plan` and `TestProposeModeSolve` stub the sandbox they now reach), `test_source_grounding_routes.py` (the sibling now runs in wave 2); jest `AgentAttachmentsProvider` (+1), `AgentBuilderStrip` (+2), `AgentReviewCard` (+2), `executableNodeKinds` (new)
- Verification evidence: backend `test_agents` + `test_execution` + `test_backend` 1201 passed / 2 skipped; jest full run green; `tsc --noEmit` clean
- Commits (on `imp/agentcatalog`): `1a957ead` (1, not-executable honesty), `fdeedfca` (2, waves + per-wave persistence + upstreamOutputs + widened gate), `2387a570` (3, batch deadline + skipped bounds), `29225ca2` (4, same-batch ancestor reuse), `5a1090a8` (5, frontend + docs), `71445ad2` (live fix: empty upstream = blocker, dependents wait); ledgers tracked as `tracking …` commits
- Issues/regressions discovered en route: LIVE (e) a dependent of empty (declined) loaders ran them as seed-only bodies, got `None`, and passed round 2 by coding around it — fixed as `strict_upstream` (`71445ad2`); a plain Run keeps Play's semantics; (a) every route-level Solve test that never stubbed the sandbox passed only because computation nodes bypassed verification — the shared plan helper now stubs it; (b) the memo's retry wording named the reused node as the blocker; a vanished artifact fails the TARGET's input load; (c) a `precondition` refusal used to be corrected twice and then exhausted as `repeated-attempt` — the loop now stops after one round; (d) `spatial-join` (python, editor none) is missing from `NAMESPACED_TO_LEGACY` — passive to the runner and the e2e runner alike (F5)
- Follow-up work: owner live re-test; F1 cross-batch reuse from the runtime journal once artifact liveness is tracked; F2 sandbox-handler package kinds as executable targets; F3 a schema/column channel to corrections; F4 Simulation Mode and the batch sharing the wave scheduler; F5 map `spatial-join` and package Python kinds in `NAMESPACED_TO_LEGACY`; F6 a plan-chip word for a not-executable plan node (today `validated`)

## BL-P5-20260909-53: a node kind's executability is read from its template through the roster — never from a hand-kept list; the live versioned-id refusal fixed (memo dev/119); DEC-076

- Date / author: 2026-09-09 / Karla
- Status: implemented and verified 2026-09-09 (owner live re-test of the palette-dragged case pending — the hotfix was minted from the owner's live report)
- Requirements: dev/118 F5 ("map `spatial-join` and package Python kinds"); owner 2026-09-09 "plan for the built-in spatial-join node"; the survey found the premise half wrong and a LIVE defect — a palette-dragged Data Loading node's versioned id (`curio.builtin/data-loading@1`) classified passive, so **Solve this node** refused the user's real code
- Design decisions/artifacts: memo `dev/119`; `DEC-076`; consumes DEC-061/062/072/073/075; DEC-075 carries a source-of-truth note
- Design-to-code decisions or deviations: **(1)** `normalize_type` strips `@<major>` before the legacy lookup (the hotfix; `raw_type` keeps the wire id for the sandbox). **(2)** every roster row carries `engine`, `editor`, `hasCode`, `backendHandler`, `executable`; `packages_services.template_is_executable` is the ONE derivation and `roster_templates(user_key, project_id)` the ONE snapshot (`{canonical_id: {executable, engine}}`, None when the store is unreachable). **(3)** `parse_workflow_dict(templates=)` / `is_executable_kind(kind, templates)` classify from the snapshot; a roster row known and not executable is never `code` whatever the legacy name said; unknown or no roster → the legacy tables, documented as the offline fallback. **(4)** `NodeSpec.engine` routes `/exec` vs `/execJs`; `run_through_node(templates=)`, `validate_candidate(templates=)`, the loop (its existing `available` roster), the batch (one snapshot) and the per-node Solve (one per request) all thread it. **(5)** the reason reads *has no code the sandbox could run — it works in the browser or through its own service; Play the dataflow to see it*. **(6)** `node.create` mints `pins.executable` from the roster row (display, never a revision pin); `AgentReviewCard.nodeKindExecutable` reads it, falls back to the registry descriptor's `hasCode` for older parts and says nothing when unknown; `utils/executableNodeKinds.ts` deleted. **(7)** a `backendHandler` template is NOT sandbox-executable (dev/91's path; dev/118 F2 unchanged)
- Files/modules: backend `execution/{workflow_spec.py, runner.py}`, `agents/{validation.py, services.py}`, `packages/services.py`; frontend `api/agentsApi.ts` (pins type), `components/agents/content/AgentReviewCard.tsx`, `components/agents/attach/AgentBuilderStrip.tsx`, `utils/executableNodeKinds.ts` (deleted); docs `docs/AGENT-CATALOG.md`, `docs/USAGE.md`, `docs/AUTHORING-NODES.md`, `docs/EXTENDING.md`
- Tests added/updated: backend `test_runner.py` (`TestDev119VersionedIds`, `TestDev119RosterClassification`: roster classification + legacy fallback, the roster overriding a legacy code NAME, a package Python kind on `/exec` and a JS kind on `/execJs`, spatial-join refused with the service wording), `test_available_templates.py` (`TestDev119ExecutableFlag`: flags follow the manifest not the name, handler templates not executable, the snapshot shape + None on failure; key-set pin widened), `test_verified_rounds.py` (per-node Solve on a versioned id passes; `data-summary@1` verified; a roster-only `custom-js@1` verified on `/execJs`; `spatial-join@1` not executable with the new reason), `test_routes.py` (`pins.executable` on the minted proposal; wording pins); jest `AgentReviewCard.test.tsx` (roster flag true/false/unknown, the fallback helper), `AgentBuilderStrip.test.tsx`, `AgentAttachmentsProvider.test.tsx` (words); `executableNodeKinds.test.ts` deleted
- Verification evidence: backend 3107 passed / 5 skipped without the Playwright directory (`test_broken_library_stub…empty_pythonpath_entry` fails on the untouched tree too — environmental); jest full run green; `tsc --noEmit` clean
- Commits (on `imp/agentcatalog`): `c43eedaa` (1, hotfix), `33308182` (2, roster executability), `92901dda` (3, frontend + docs)
- Issues/regressions discovered en route: (a) the roster is project-scoped — roster tests must `_lockfile_add` the package; (b) the whole-tree pytest run includes the Playwright `test_frontend/` suite, which boots its own stack on :5002 and errors while a live `curio start` holds the port; a directory run after closing the stack showed two unrelated pre-existing e2e failures (a broken-library toast count and a strict-mode `Close` locator with two toasts open) — postponed by the owner, not investigated
- Follow-up work: F1 `sandbox checkIOType` matches legacy ids only (dead for namespaced ids) — retire or canonicalise; F2 `tests/test_frontend/workflow_spec.py` is a drifted copy of the runner's classifier — import the app module; F3 the two e2e failures above; dev/118 F2 (handler kinds as executable targets) unchanged

## BL-P5-20260909-54: the sandbox's name-keyed I/O check retired; the e2e runner-spec shim restored and test-pinned (memo dev/120); no new DEC

- Date / author: 2026-09-09 / Karla
- Status: implemented and verified 2026-09-09 (owner smoke of `test_alive.py` with the fixture-booted stack pending)
- Requirements: dev/119 F1 (`checkIOType` matches legacy ids only — dead for namespaced ids) and F2 (`tests/test_frontend/workflow_spec.py` is a drifted copy); owner 2026-09-09 "tackle the two recorded findings"; owner decision 2026-09-09: keep `checkIOType` seeded as a no-op
- Design decisions/artifacts: memo `dev/120`; no new DEC — enforces DEC-062 (one vocabulary) and DEC-076 (the template decides; legacy tables are the offline fallback); both rows carry a dev/120 note
- Design-to-code decisions or deviations: **(1)** the e2e module is dev/67-7's shim again (12 re-exports, defines nothing — AST-pinned; identity of ten names pinned) — the `619ab203` merge had replaced it with main's full copy; **(2)** `CONSTANTS`/`FLOW_SWITCH` removed from `PY_CODE_TYPES`/`CODE_EDITOR_TYPES` — every legacy code kind must have a namespaced spelling in `NAMESPACED_TO_LEGACY` (test-pinned), and the tables are commented as the offline fallback that must not grow (`DATA_SUMMARY` deliberately NOT added); **(3)** the five validators deleted; `checkIOType(data, nodeType, input=True) -> None` stays and stays seeded on both paths (owner decision — the #158 contract whole); the worker's three calls and synthetic payloads removed; `node_type` still tags artifacts and logs; **(4)** the e2e baseline replay no longer runs the check the product never ran; **(5)** alternatives rejected: canonicalise-and-re-enable (turns on year-dormant constraints, keeps a second port vocabulary); deferred: run-time port validation from declared ports (own memo)
- Files/modules: `backend/app/execution/workflow_spec.py`, `backend/tests/test_frontend/{workflow_spec.py, utils.py}`, `backend/tests/test_execution/test_workflow_spec_single_source.py` (new), `backend/tests/test_sandbox_namespace.py`, `sandbox/util/parsers.py`, `sandbox/app/worker.py`, `sandbox/python_wrapper.txt` (comment), `sandbox/tests/test_worker_io_check_retired.py` (new), `docs/ARCHITECTURE.md`
- Tests added/updated: shim identity (10 names), shim defines nothing, version strip through the shim, no phantom in `CODE_TYPES`; seeded `checkIOType` is the no-op (namespace test); a Data Loading node returning a dict and a Data Export node returning a value execute under `@1`, unversioned and legacy spellings; the retired validators are absent
- Verification evidence: backend `test_execution`+`test_agents`+`test_backend`+`test_packages` 2084 passed / 5 skipped (environmental stub test aside); `sandbox/tests` + namespace test 334 passed / 44 skipped (macOS isolation-fallback test aside); `tests/test_frontend --collect-only` 428 collected without a stack
- Commits (on `imp/agentcatalog`): `0b3ebc11` (1, shim + phantoms), `b534f667` (2, validators retired, name kept as no-op)
- Issues/regressions discovered en route: `detect_kind` reports a dict as `dict`, not `json` (test corrected); the legacy `python_wrapper.txt` (unused since the in-process worker) claimed the check "halts the pipeline" — comment corrected, file kept for reference
- Follow-up work: F1 run-time port validation derived from the template's declared ports, if demand (needs the `SupportedType` ↔ `detect_kind` mapping first); F2 a docs pass over `docs/ARCHITECTURE.md`'s wrapper section, which predates the in-process worker beyond the one sentence fixed

## BL-P5-20260909-55: the example-derived agent validation harness — 31 reviewed prompt fixtures, a semantic comparator, and four tiers (memo dev/121); `DEC-077`

- Date / author: 2026-09-09 / Karla
- Status: implemented on `imp/agentcatalog` 2026-09-09; the deterministic tiers are green, the browser tier is written and collects but needs a dedicated stack (the only stack on this machine is the owner's live one, and the e2e fixtures reset the database), and all 31 prompts are committed as `review.status = pending-owner-review`
- Requirements: the owner's 2026-09-09 brief — *"an evaluation harness that measures whether the Dataflow Builder and its delegated agents can reconstruct Curio's existing example dataflows accurately from natural-language prompts"*, with the explicit constraints: no second agent runtime/parser/classifier/installer; semantic equivalence not byte equality; unsupported constructs as explicit capability failures; both provisioned and resolution dependency modes; four test tiers with live-model runs opt-in and report-only; and a fine-tuning follow-up that stays capability-gated
- Design decisions/artifacts: memo `dev/121`; `DEC-077`; consumes DEC-048, DEC-051, DEC-055, DEC-058, DEC-062, DEC-063, DEC-072, DEC-073, DEC-075, DEC-076, dev/81 (dataset refs), dev/101 (lockfile authority)
- Design-to-code decisions or deviations: **(1)** node order needed a canonical LABELING, not attribute sorting — 14 of the 31 examples failed order-independence because attribute-identical nodes (example 01's three transforms) took their indices from file order; colour refinement plus individualization now makes canonical equality mean isomorphism, exact for all 31. **(2)** `has_content` left node identity (`compare=False`): a plan proposes placeholders and Solve fills only what the sandbox can run, so an empty grammar node is a stage of the workflow, not a wrong node — it is reported as `content_missing` with no score effect. **(3)** A fixture's `outputKind` is UNMEASURED today: the Solve payload records each attempt's round kind (`executed`, `ungrounded-source`, `upstream-blocker`) and no artifact data type, and reading the round kind as a data type produced four false intent mismatches. **(4)** Structurally interchangeable nodes get a bounded, monotone word-based tie-break AFTER the structural match (their swap provably cannot change a structural finding), because no structural matcher can tell automorphic branches apart and the fixture's words are the only distinguisher. **(5)** A proposal's target is read from its PINS, not a params echo. **(6)** pip is stubbed in the deterministic tier and only pip — the install service, catalog copy, store, lockfile and `importErrors` are all real. **(7)** Fabrication scoring stands but is unit-tested rather than integration-tested, because through the real pipeline the runtime's own gates fire first: an invented template and an edge into a Data Loading node are refused at plan validation, and an invented catalog id is refused by the DEC-072 gate — so the harness records a refusal, which is a different finding from a badly-built graph.
- Files/modules: new `backend/app/agents/evaluation/{__init__,fixtures,canonical,dependencies,compare,scoring,attempt,oracle,report,export,live}.py`; new `docs/schemas/example-prompt-fixture.v1.json`; new `docs/examples/prompts/**` (31 fixtures + README); new `utk_curio/tools/agent_eval.py`; new `scripts/example_fixture_skeleton.py`; new tests `backend/tests/test_agents/{test_reconstruction_canonical,test_example_fixtures,test_reconstruction_scoring,test_example_reconstruction,test_reconstruction_export,test_live_eval}.py` and `backend/tests/test_agents/_reconstruction_driver.py`; new `backend/tests/test_frontend/test_example_reconstruction_e2e.py`; edited `backend/tests/conftest.py` (`--live-eval` + the `live_eval` marker), `backend/tests/test_frontend/fixtures.py` (three shared-session classes); docs `docs/AGENT-CATALOG.md` (new part 7), `docs/CONTRIBUTING.md`
- Tests added/updated: 135 canonicalization/labeling/role/dependency assertions incl. every example order-free and exactly labeled and the eight interaction-edge fixtures named; 506 fixture assertions (completeness both ways, schema refusals, digest drift with a re-review message, expected-graph recomputation, required-vs-declared parity, catalog existence, seven leak prohibitions each proven to fire, and two guards keeping fixtures out of the dataflow scanners); 43 comparator/scoring/report assertions (equivalence, each failure category, fabrication capping, not-measured exclusions, weights, redaction of a credential-carrying transcript); 57 integration assertions (23 fixtures reaching 1.0 through the real path, 8 declaring their gap, seven mutations, both dependency modes, honest Solve outcomes); 25 export/live-refusal/CLI assertions; 4 browser tests (collect-verified)
- Verification evidence: `tests/test_agents` 1925 passed; `test_agents`+`test_execution`+`test_packages`+`test_projects`+`test_datasets` 3579 passed / 3 skipped with one pre-existing environmental failure (`test_broken_library_stub…empty_pythonpath_entry`, confirmed failing at `65f91e54` in a throwaway worktree); `tests/test_frontend` collects 464 (435 selected with `--with-examples`)
- Commits (on `imp/agentcatalog`): `37533fce` (1, fixture contract + canonical form), `a6e4bb18` (2, the 31 fixtures), `a0cb7e8a` (3, comparator + scoring + report), `463da635` (4, oracle + driver + deterministic suite), `292547ba` (5, browser tier), `d34f766c` (6, live runner + export), plus commit 7 (docs + ledgers)
- Issues/regressions discovered en route: **(a) a real product finding** — Solve's grounding evidence is built from the dataflow's task and its target nodes' GOALS (`_solve_grounding_base`), never the chat session, so two documented DEC-072 routes are unreachable on the Solve path: a path the user typed in conversation, and a request for synthetic data. Half the legacy fixtures and every PBF example hit it. The oracle works with it (the source rides the node's intent, which is what a competent planner does), and it is recorded as a follow-up rather than worked around silently. **(b)** commit 2 changed the default expected-block refs to the readable role-derived form and left commit 1's round-trip test asserting `n0`/`n1`, so `tests/test_agents` ran with one failure between commits 2 and 3 — fixed in commit 3 and noted here per tracking rule 11. **(c)** the install lanes belong to the agents that hold them (`dataset.install` → Dataset Finder, `package.install` → Researcher); a first cut drove them through the Dataflow Builder, which holds neither. **(d)** a fake sandbox must answer with a plausible data type, because the runtime checks the produced type against the DOWNSTREAM node's declared input ports. **(e)** `git stash -u` was used once on the shared tree to check whether a failure pre-dated the work — the wrong tool with a 354 MB untracked directory present; a throwaway `git worktree` answered the same question safely and is the method to use.
- Issues found by the owner's first real run (gemma4, score 0.64): **(f)** the run finished with a score and an EMPTY canvas. The record proved the order — plan applied, six of six nodes solved, a real graph scored — and the spec was rewritten six seconds after the run ended carrying `nodeProvenance`, a key only the frontend emits. A canvas save is a whole-spec PUT of the browser's live nodes with no revision basis, so the project the panel invited the owner to open mid-run held an empty graph and its next save wrote that emptiness over the applied plan. Fixed per deviation (10), plus the panel no longer offering the project until the run is terminal; the general hazard is F7. **(g)** The transcript stopped at production's Solve card, so the conversation never said what the run was or how it scored. Fixed per deviation (11).
- Quality/evaluation boundary (tracking rule 17): the `DEC-055` report-only evaluator boundary was NOT bypassed. No agent scores a reconstruction; the comparator is deterministic code, the Generated Content Evaluator is not invoked anywhere in the harness, and an advisory evaluator column is recorded as follow-up F4 rather than built. The live tier writes reports (`isReleaseGate: false`) and is excluded from CI by marker.
- Follow-up work: F1 the AI Settings Model Training panel, gated on its own memo (provider capability detection, dataset consent/licensing, redaction, cost disclosure, job status/cancellation, trained-model versioning, the held-out evaluation gate, activation, rollback; reconciled with DEC-058, and a model never judges itself); F2 port dev/112's `edges[].kind` + `plan_topology.py` to this branch, after which the eight T2 fixtures score without a fixture edit; F3 Solve-path source evidence — let a path or synthetic-data request the user typed in the chat count when Solve fills a node, or document the intent as the only route; F4 column/schema and output-kind assertions once the runtime journal carries them (dev/115 F4 / dev/118 F3); F5 an advisory Generated Content Evaluator column beside the deterministic score; F6 persisted provenance scoring once dev/114 F3 lands; F7 `--parallel` for live runs under the per-user job backpressure; F8 owner review of the 31 prompts (the export refuses until then); F9 run the browser tier on a dedicated stack

## BL-P5-20260909-56: Model training in AI Settings — the probe, the set, the consent, the job and the gate (memo dev/122); `DEC-078`

- Date / author: 2026-09-09 / Karla
- Status: implemented on `imp/agentcatalog` 2026-09-09; every tier green offline. A real fine-tune is owner-gated (money, hours, and an endpoint that offers the feature), and the 31 prompt fixtures remain `pending-owner-review`, so an actual training run cannot happen until the owner approves prompts and runs it.
- Requirements: dev/121 F1 and its nine preconditions, on the owner's instruction of 2026-09-09 (*"go ahead with the finetuning related follow ups, as always memo first"*). dev/121 §3.9 ended *"Until that memo is approved and implemented, AI Settings gets no panel"*; the memo maps each precondition to the section that discharges it.
- Design decisions/artifacts: memo `dev/122`; `DEC-078`; consumes DEC-077, DEC-055, DEC-058, DEC-074, DEC-062, DEC-072, dev/87 §4 (the audit-before-export rule), dev/90 A2, dev/11:86.
- Design-to-code decisions or deviations: **(1)** capability is probed and recorded rather than tabulated, with four distinct refusals (no such feature / key without scope / unreachable / no key yet), and a grep test forbids a provider→capability table. **(2)** An unrecognised provider job status stays `running` rather than being called terminal — acting on a status we do not understand is worse than waiting. **(3)** No worker, queue or lease: the provider owns the job and Curio records its last word with the time it was read, so `OQ-009` is not pre-empted (grep-tested), which also rules out `agent_jobs` (attachment-keyed, Solve-kinded, no cancel primitive) and `build_jobs` (a local worker). **(4)** The training target is produced by the evaluation oracle and re-checked through the runtime's own two-step recognition (`extract_plan_attempt` then `parse_dataflow_plan_verbose`); the first cut passed the whole reply to the verbose parser, which takes the plan OBJECT, and twelve fixtures "failed to parse" for a reason that was the harness's. **(5)** The consent record is fsynced BEFORE the upload, and the test proves the ordering by its failure mode. **(6)** A trained model is recorded in a SIBLING of `model-suggestions.json`, because that file's contract is "only ever a recording of what an endpoint said about itself". **(7)** The gate is not a threshold — a mean score of 0.0 satisfies it — because no pass mark is justifiable and a platform-set one would be Curio judging models for the user. **(8)** Evaluating a model that is not active means pointing the account at it for the duration: `agent_eval --model` owns that switch and restores it in a `finally`, and `service.account_model_check` reports an account left on a trained model with no activation record so an interrupted run is visible rather than silent. **(9)** `services.roster_block` is a new public wrapper over `_available_templates_block` so a training example's system turn is composed by the runtime's own formatter (`DEC-062`).
- Files/modules: `app/agents/providers.py` (+`FineTuningCapabilities`, `FineTuningJob`, `FineTuningUnavailable`, probe + four primitives), `app/agents/testing_provider.py` (the scripted lane), `app/agents/model_catalog.py` (+capability recording, +`trained-models.json`), new `app/agents/training/{__init__,dataset,consent,records,gate,service}.py`, `app/agents/services.py` (+`roster_block`), `app/agents/routes.py` (+8 routes), `app/agents/evaluation/live.py` (snake_case `/me` fix), `utk_curio/tools/agent_eval.py` (+`--model`, `--gate-for`), `docs/schemas/example-prompt-fixture.v1.json` (+optional `consent`), all 31 fixtures (+`consent` declarations), frontend `api/trainingApi.ts`, `components/training/ModelTrainingSection.{tsx,module.css}`, `components/AiSettingsModal.tsx`; docs `docs/AGENT-CATALOG.md` (part 7's training subsection), `docs/CONTRIBUTING.md`
- Tests added/updated: `test_fine_tuning_provider.py` (45), `test_training_dataset.py` (66), `test_training_routes.py` (27), `test_training_gate.py` (26), `src/tests/training/modelTrainingSection.test.tsx` (13); plus corrections to `test_reconstruction_export.py`'s `/me` fake and `test_example_fixtures.py`'s corpus (unchanged count, new consent assertions)
- Verification evidence: `tests/test_agents` 2089 passed; `test_agents`+`test_execution`+`test_packages`+`test_projects` 3278 passed / 3 skipped with the one pre-existing environmental failure (`test_broken_library_stub…empty_pythonpath_entry`, confirmed failing at `65f91e54`); full jest 2364 passed across 203 suites; `tsc --noEmit` clean
- Commits (on `imp/agentcatalog`): `9473e87a` (1, provider contract), `fa9cb604` (2, training set + consent + fixture declarations), `087f4231` (3, records/service/routes), `c3b06458` (4, gate/activation/rollback + eval flags), `2f621127` (5, the panel), plus commit 6 (docs + ledgers)
- Issues/regressions discovered en route: **(a)** the dev/121 live runner read `llmApiType`/`llmBaseUrl`/`llmModel` from `GET /api/auth/me`, which answers in snake_case — a live evaluation report would have carried an empty provider record, and the test's fake had been written to match the bug rather than the API. Fixed both sides. **(b)** `status()` returned early for a terminal job without computing the gate, so a succeeded job showed no gate information. **(c)** The verbose plan parser takes the plan object, not a reply (see deviation 4). **(d)** `_catalog_manifests`/`package_id` are the packages domain's real names; the first cut guessed `parse_manifest`/`manifest.id`.
- Quality/evaluation boundary (tracking rule 17): the `DEC-055` report-only evaluator boundary was NOT bypassed. No agent and no model participates in the gate or the activation path — a grep test asserts the training package contains no completion call, no delegate and no reference to the generated-content evaluator — and the gate record carries `comparator: "deterministic"` so a reader never has to wonder whether a model graded itself. `DEC-028`'s self-certification firewall and dev/11's *"a candidate cannot judge/approve itself"* stand unchanged. Neither `OQ-009` nor `OQ-010` is closed by this work.
- Follow-up work: F1 Gemini `tunedModels` as a second job surface if demand appears; F2 user-authored training data (`consent.dataContent: "user-content"`) with its own consent surface, licence capture and redaction over node code — the one that needs real care; F3 a second corpus built from a user's own dataflows, gated on F2; F4 evaluate the base model too so a report shows both without Curio drawing a conclusion; F5 pass the validation split to endpoints that accept a `validation_file`; F6 owner approval of the 31 prompts, without which no training run can start; F7 an owner-run live fine-tune against a real endpoint

## BL-P5-20260909-57: Evaluation mode — an evaluation run through the product, with the account's own model (memo dev/123); `DEC-079`

- Date / author: 2026-09-09 / Karla
- Status: implemented on `imp/agentcatalog` 2026-09-09; every tier green offline. The owner then ran it for real (gemma4 at `sage200`, `01-vega-lite-chained-transforms`, 0.64, plan applied and six of six nodes solved) — F1 discharged for the first time, and it exposed two defects the deterministic mirror could not, both fixed here and one generalised by `dev/124`.
- Requirements: the owner's correction of 2026-09-09 — *"production evaluations run through Curio's real UI using the LLM provider and model configured in AI Settings. Preserve the existing scripted-provider path as the deterministic automated-testing layer"*, with an audit-and-adapt mandate (no discarding, no duplication, extend in place), plus two follow-on instructions: *"The Panel should detect if a model has been setup through the app start command"* and *"it should be a way to approve when selecting it in the evaluation mode panel"*.
- Design decisions/artifacts: memo `dev/123`; `DEC-079`; consumes DEC-077, DEC-048, DEC-068, DEC-073, DEC-040, DEC-053, RISK-SECRET-001. Supersedes only dev/121 §3.7's delivery surface.
- Design-to-code decisions or deviations: **(1)** the fixed user policy existed TWICE (`evaluation/live.py`, the deterministic test driver) — extracted into `evaluation/policy.py`, both re-pointed, with a test asserting no second copy of the decision exists. **(2)** The automated approval is a project-scoped, run-scoped, kind-limited, per-apply-recorded grant (`evaluation/authorization.py`), refused in any unmarked project; nothing global changed. **(3)** The run is an `agent_jobs` job of a new third kind rather than a parallel runner, so no second liveness model appears and `OQ-009` stays untouched. **(4)** Phases are forward-only on the record, so "where did it get to" has one answer. **(5)** Readiness distinguishes an account-configured model from a deployment-configured one, because the launcher's `--llm-*` flags are a real configuration. **(6)** Prompt review became a route and a panel control (`evaluation/review.py`), validating the whole fixture against the schema before writing and asserting that only the review block moved. **(7)** The run's phases execute inside a pushed request context carrying the user — see the findings. **(8)** Cancel is a sentinel FILE, not a field in the run's own record — see the findings. **(9)** The evaluation project is kept and named for the fixture and run, because the graph is the evidence. **(10)** The marker also answers who may replace the run's graph: a client save is refused while the run is writing, and refused whenever it would leave a run-built graph with no nodes at all — node lists are NOT made a backend-owned section, because a person's canvas is the authority for their own graph, so editing a finished evaluation project stays untouched. **(11)** The run's verdict is appended to the agent's transcript, never before the model answers (the session IS the model's context) and never carrying a piece of the reference (those turns become context for any later conversation in the kept project).
- Files/modules: new `app/agents/evaluation/{policy,records,authorization,review,service,training_host}.py`; edited `app/agents/evaluation/live.py` (policy delegation), `app/agents/agent_jobs.py` (third job kind), `app/agents/routes.py` (+8 routes); new frontend `api/evaluationApi.ts`, `components/evaluation/EvaluationModeSection.{tsx,module.css}`, edited `components/AiSettingsModal.tsx`; new tests `tests/test_agents/{test_evaluation_policy,test_evaluation_service}.py`, `src/tests/evaluation/evaluationModeSection.test.tsx`, `tests/test_frontend/test_example_reconstruction_e2e.py` (+`TestEvaluationModeRunsFromAiSettings`, collecting — unexecuted for the same reason dev/121's four are); adapted `tests/test_agents/_reconstruction_driver.py`, `test_reconstruction_export.py`, `test_training_dataset.py`; docs `docs/AGENT-CATALOG.md`, `docs/CONTRIBUTING.md`
- Tests added/updated: 15 policy assertions (each decision, pins-not-params, both callers delegating, no second copy), 32 service assertions through the real routes (readiness ×3, fixtures with no expected graph, approve/withdraw/refusals, a nine-phase run to 1.0, the caller's project unmodified, the marked and kept project, the required closure, the leak check, every apply authorized and recorded, the record complete and key-free, the score matching the shared scorer, a declined proposal, a refusing model, a provider failure, one-at-a-time, a mid-run cancel, bad ids, the listing, an unconfigured account, and the five properties bounding the grant), 12 panel assertions
- Verification evidence: `tests/test_agents` 2137 passed / 1 deselected; full jest 2376 passed across 204 suites; `tsc --noEmit` clean
- Commits (on `imp/agentcatalog`): `9b2d51bb` (1, one shared policy + the test adaptations), `94ab1854` (2–4, service/records/authorization/review/routes), `8c0d8bb5` (5, the panel), `ac03d06f` (6, docs), `ec02d2b8` (7, the browser case), `1187ac4b` (the owner's first prompt approval, made through the panel), `6e98a205` (8, the two defects the owner's first real run exposed), plus this tracking entry
- Issues/regressions discovered en route: **(a)** the production paths assume a REQUEST context — `services._catalog_grounding_refs` reads `g.user` when one is available and answers "no catalog" when it is not, so driving `run_attachment` from a job thread silently lost the Data Catalog and every `curio_dataset_path` loader was refused as ungrounded; the run completed with a low score and no error. Solve's workers dodge it by building their grounding base in the request thread, so the service pushes a request context carrying the user rather than changing dev/114's gate to accommodate a caller. **(b)** Cancel was a lost update: the run rewrites its whole record each phase, so a cancel written by another request was clobbered between its read and its write — the flag never reached the run and the event vanished. A cancel is now a sentinel file, so the two writers never share one. **(c)** `install_in_project` answers `{"agents": <lockfile of coordinate strings>, "installed": [coords]}`; the first cut called `.get()` on a string. **(d)** A fixture had been hand-edited to `review.status: "papproved"`, which the schema rejects and which broke every suite at collection — corrected to `approved` with the reviewer fields left as found, and the real lesson (review belongs in the interface) became scope. **(e)** Two tests had encoded "the whole corpus is unreviewed" as an invariant, which the first approval falsified; both now assert the behaviour per split, and the missing half — that an approved prompt makes its split exportable — was added.
- Quality/evaluation boundary (tracking rule 17): the `DEC-055` report-only evaluator boundary was NOT bypassed. The comparison is deterministic code (`attempt.score_attempt`); no agent and no model participates in scoring or in the automated approval; the Dataflow Builder's `review_policy` is still `review-before-apply` (test-pinned) and the grant is a property of the evaluation project, checked per apply. Neither `OQ-009` nor `OQ-010` was closed by this work.
- Follow-up work: F1 an owner-run live evaluation against the configured gemma4 endpoint (the acceptance scenario; a live failure the deterministic mirror did not predict becomes a fixture, per dev/115 A3); F2 a comparison detail view on the canvas rather than only a table; F3 batch runs over a whole tier once one run's real cost and duration are known; F4 evaluating a user's own dataflow against a prompt they write, which needs a fixture-authoring surface; F5 a two-run comparison view; F6 fine-tuning remains dev/122's, untouched by this phase; F7 a client save carries no revision basis, so any canvas that loaded a project before a server-side write erases it on the next save — closed for evaluation projects only, the general fix (optimistic concurrency on every save path) wants its own memo

## BL-P5-20260910-58: a save may not delete what the client never saw — every spec write counted, the basis declared, loss refused (memo dev/124); no new DEC

- Status: implemented on `imp/agentcatalog` 2026-09-10. Closes `dev/123` F7 and retires the evaluation-only clause `dev/123` shipped one phase earlier.
- Requirements: the owner's instruction of 2026-09-10 — *"implement the follow up F7"* — against the finding that produced it: a finished evaluation run with a score and an empty canvas, because a canvas opened before the run's writes saved its empty graph over them.
- Design decisions/artifacts: memo `dev/124`; **no new `DEC`** — this is the general form of a rule `DEC-079` already states, so the special case is deleted rather than joined by a second one. Consumes DEC-040 (graph-backed state), DEC-075 (Solve's per-wave writes are the commonest background writer), DEC-079.
- Design-to-code decisions or deviations: **(1)** the counter is bumped in `storage.write_spec`, the ONE place a spec reaches disk, so a writer inherits it without knowing it exists — the `spec_revision` column could not serve, because `upsert_project` runs for a client save and nothing else and therefore stays equal across exactly the background writes a client must notice. **(2)** It lives in a one-line file beside the spec, not a key inside it: a client sends the whole document back, so a number inside it is a number the client can reset, and the first cut proved it by resetting the count on every save. **(3)** The bump precedes the spec write, so a crash between them leaves the counter ahead — a needless reload rather than a silent overwrite. **(4)** The check refuses LOSS, not divergence: refusing every stale save would reject correct saves several times an hour, because a canvas hears about an apply through a live event without reloading and an install does not touch the graph at all. **(5)** Content counts as lost only when BLANKED — replacing code with other code is what editing looks like. **(6)** No basis means no opinion and no check, which is what keeps scripts, tests and the internal dataset-uninstall caller working; the consequence is stated in the memo's §11 C rather than hidden. **(7)** Because staleness alone never refuses, the client needs the revision at only its two sync points rather than plumbed through every writing endpoint. **(8)** Every endpoint that reports `spec_revision` now reports this counter, so the field means one thing across the API. **(9)** No `Reload` button in the toast: `showToast` takes a message and a variant, and the sentence already says what to do.
- Files/modules: new `app/projects/concurrency.py`; edited `app/projects/{storage,services,schemas,routes}.py`, `app/agents/evaluation/authorization.py` (graph clause deleted, `assert_run_is_not_writing` remains); frontend `api/projectsApi.ts`, `hook/useWorkflowOperations.ts`; new tests `tests/test_projects/test_save_concurrency.py`, `src/tests/hook/useWorkflowOperations.saveBasis.test.ts`; extended `tests/test_projects/{test_storage,test_routes}.py`, `tests/test_agents/test_evaluation_service.py`; docs `docs/AGENT-CATALOG.md`, `docs/CONTRIBUTING.md`
- Tests added/updated: 14 assertions on the pure rule (each loss shape, each edit shape, no basis, current basis, malformed specs), 4 on the counter (every write bumps it whoever wrote it, a client payload cannot reset it, a pre-dev/124 project reads as no basis, an unreadable counter reads as zero), 7 through the routes (the defect replayed in order and refused with nothing written, the same save with a fresh basis accepted, a stale save that only adds accepted, blanking Solve's code refused, no basis unchecked, a malformed basis unchecked, the reported revision moving on a background write), 5 in jest (the basis captured on load, advanced by each save's own response, established by a create, omitted when never synced, and the refusal reaching the person who saved), plus dev/123's reproduction rewritten to the real sequence with its recovery asserted
- Verification evidence: `tests/test_projects` 304 passed; `tests/test_agents` 2146 passed; `tests/test_datasets` 465 passed; `tests/test_packages` green but for the pre-existing unrelated `test_teardown_preserves_an_empty_pythonpath_entry`; jest 2382 passed across 205 suites; `tsc --noEmit` clean
- Commits (on `imp/agentcatalog`): `91d83ba0` (memo), `71f49d9a` (1, the counter), `8214eaca` (2, the rule), `0b93c8f0` (3, the check on save + the retirement), `ebcaa85a` (4, the client's basis), plus this tracking entry
- Issues/regressions discovered en route: **(a)** the first cut bumped the incoming payload's counter, so every client save reset it to 1 — three existing project tests failed immediately, which is what a counter in a client-supplied document earns. **(b)** The project detail is nested under `"project"` in the `GET` response, not at the top level; two tests read it wrong before the shape was checked. **(c)** dev/123's reproduction had never actually replayed the sequence — it asserted the refusal against a project that was never stale — so moving it to the new contract also made it a truer test.
- Quality/evaluation boundary (tracking rule 17): nothing here touches agent authority. No review gate changed, no agent permission widened, and no model can write a spec. The evaluation module lost a rule and gained none.
- Follow-up work: none proposed. The memo's §11 D records what this deliberately does not close — a save on a CURRENT basis still overwrites node content freely, which is editing, and two clients whose saves both add remain last-writer-wins on positions. Neither loses work a client never saw.

## BL-P5-20260910-59: the interaction-edge capability reaches `imp/agentcatalog` — plan edges carry a kind, the graph stays a DAG, and the eight declared capability gaps close (memo dev/125); no new DEC

- Status: implemented on `imp/agentcatalog` 2026-09-10. Closes `dev/121` **F2**. Ports `dev/112` (`DEC-070`), which shipped on `feat/agentscatalog` only and is never merged back.
- Requirements: the owner's instruction — *"implement the deferred follow-up: interaction-edge capability"* — plus a re-investigation of `~/Desktop/Screenshot 2026-08-26 at 4.21.26 PM.png` (session `78fbbe35`), the SAME evidence `dev/112` was written from. Verdict re-confirmed: not the `connection` attach target (BL-P5 entry 30 / `dev/113`, `DEC-071`), and the agent was never "unable to delete" — it deleted and recreated the same data edge five times because the `Interaction` kind it asked for was silently dropped by the grammar.
- Design decisions/artifacts: memo `dev/125`; **no new `DEC`** — `DEC-070` already states this posture and is already in the dev/03 table, which the shared plans tree carries even where the code does not. Consumes DEC-049 (removals reviewed by name, digest-pinned), DEC-051 (topology validated before anything materializes), DEC-063 (an instruction must be executable on every path its agent runs on), DEC-076 (executability read from the roster, not a hand-kept list), DEC-077 (a gap is NAMED, never hidden by a lowered expectation).
- Design-to-code decisions or deviations: **(1)** `dev/112` commits 1, 4 and 5 applied unchanged; commits 2 and 3 (`services.py` mint and apply) were hand-ported across 654 commits of divergence — the only conflict was the import line. **(2)** *Deviation from `dev/112` §6*: the plan grammar's duplicate-edge key is now `(from, to, KIND)`, not `(from, to)`. `dev/112` decided a pair was a duplicate whatever its kind; the shipped corpus disproves it — `docs/examples/dataflows/Interaction_Vega.json` and the three other linked-view examples carry BOTH a data edge and an `Interaction` edge between the same data-pool and visualization, which is what a linked view IS. The old key made those graphs unproposable and all eight fixtures scored 0 with the whole plan refused. **(3)** *Deviation, same class*: `_INTERACTION_CAPABLE` gained `autk-grammar`. `dev/112` transcribed the preamble's list (VIS_VEGA, AUTK_MAP, VIS_SIMPLE, DATA_POOL); `autk-map` exists in NO manifest on this branch, while every shipped Autark interaction wires into `curio.builtin/autk-grammar`. This is the `DEC-076` drift exactly, caught by the corpus rather than by review; the reason is written at the definition and recorded as `dev/125` F1. **(4)** *Addition beyond `dev/112`*: `dataflow.read`'s edge projection now carries `kind`. The instruction tells the builder to re-read the graph and confirm the topology before claiming a repair; without the kind in the projection that read-back was not executable — the same `DEC-063` defect one layer up. **(5)** Two harness defects surfaced only because the gap closed and fixture 08 reached Solve for the first time: the fake sandbox's `code_to_kind` matched the declared content by EQUALITY, but the runtime wraps and indents every dispatched node, so the lookup had never once fired and every node fell through to a word heuristic; and that heuristic read the word "raster" out of a JS comment, so the Autark grammar downstream refused the type and Solve burned three correction rounds on content that was never wrong. Fixed by matching whitespace-insensitively and by declaring `analysis1`'s real `outputKind` (`list`, what the example's JS returns). **(6)** One fixture file changed: fixture 08's `intents` gained `{"ref": "analysis1", "outputKind": "list"}` — an ADDED, scored, truthful assertion using a field the fixture schema defines for exactly this. No fixture's `expected` graph was touched, and the eight still declare `interaction-edge` in `capability.needs` as the record of what this branch once could not do.
- Files/modules: new `app/agents/plan_topology.py`; edited `app/agents/{content,services,tools}.py`, `app/agents/evaluation/{oracle,attempt}.py`, `llm-prompts/orchestration_instruction.txt`; frontend `components/agents/attach/useAgentCanvasMutations.ts` (one `appliedEdgeToCanvasEdge` for both bridge paths), `components/agents/content/AgentReviewCard.tsx`, `api/agentsApi.ts`, `utils/agentCanvasEvents.ts`; new tests `tests/test_agents/test_plan_topology.py`; extended `tests/test_agents/{test_content,test_routes,test_tools,test_builtin,test_example_reconstruction,test_training_dataset,test_training_routes}.py`, `src/tests/attach/useAgentCanvasMutations.test.tsx`, `src/tests/content/AgentReviewCard.test.tsx`, `tests/test_agents/_reconstruction_driver.py`; fixture `docs/examples/prompts/08-autark-spatial-join-regression.prompt.json`; docs `docs/AGENT-CATALOG.md`
- Tests added/updated: grammar (kind default/explicit/invalid, `toHandle` on an interaction edge refused, edge-only plans valid, byte-identity of an additive data-only plan); the pure helper in isolation; `TestPlanTopologyMint` and the apply half through the routes (the owner's exact plan refused with the cycle named, the same plan as an interaction edge minted and materialized as `type: Interaction` with `in/out` handles, an interaction edge into `merge-flow` refused, a removal-only plan that breaks a user-drawn cycle minted, an untouched cycle reported not blamed, apply-time drift → 409 + stale, per-edge re-check, truthful removal counts, `removedEdges` labels); `dataflow.read` names interaction edges and leaves data edges bare; the frontend materialization; and three tests REPINNED from the gap to its closure — the eight are representable, they are no longer excluded from training, and the gap MACHINERY keeps its own test against a synthetic unexpressible kind so the next one is named rather than discovered prompt by prompt
- Verification evidence: `tests/test_agents` 2187 passed (2146 before); `tests/test_projects` + `tests/test_datasets` 769 passed; `tests/test_packages` 864 passed / 3 skipped, green but for the pre-existing unrelated `test_teardown_preserves_an_empty_pythonpath_entry` (recorded by `dev/121` and `dev/124` before this work); jest 2384 passed across 205 suites; `tsc --noEmit` clean. **The load-bearing evidence: all eight `capability-gap:interaction-edge` fixtures now reconstruct to exactly 1.0 through the production path, with no fixture's expected graph edited.**
- Commits (on `imp/agentcatalog`): pending — the working tree holds the change unstaged at the owner's standing instruction
- Issues/regressions discovered en route: the five in the deviations above. The two harness defects (5) are the more interesting pair: both had been dead code paths, invisible for as long as the fixtures that would exercise them were blocked at the oracle. Closing a capability gap is also how you find out what the gap was hiding.
- Quality/evaluation boundary (tracking rule 17): no agent authority changed. Every new refusal is a REFUSAL — the plan contract accepts strictly less than before in one respect (a cycle) and strictly more in another (kinds, edge-only plans), and no agent gained a path to write anything it could not write before. The comparator remains deterministic code; `DEC-055` and `DEC-028` untouched.
- Follow-up work: `dev/125` F1 — if a template ever declares interaction capability, `_INTERACTION_CAPABLE` reads it from the roster and stops being a list. F2 — `dev/113` (`node.insert`) is also absent from this branch; not needed for this repair, deliberately not ported, re-open on demand with its own memo. F3 — the cascade computation is still written twice (mint and apply), worth unifying when either is next touched.

## BL-P5-20260910-60: the Dataset Finder is a declared dependency, attached to the data-loading node it resolves — and resolution asks it instead of guessing (memo dev/126); `DEC-080`

- Status: implemented on `imp/agentcatalog` 2026-09-10. Origin: the owner's report — *"The dataset finder must always be initiated when resolving a data-loading node, and it should be attached directly to that node… The flow currently occurring in this project: `http://localhost:8080/dataflow/667a4b6c-…` must not happen. The dataset finder should be treated as a DFB agent dependency, similar to how the node content builder operates. Additionally, the node builder should always be attached to a node when a dataflow plan is being applied."*
- Requirements: reproduced from disk before writing a line. Project `667a4b6c` has zero nodes, three agents in its lockfile, one canvas attachment, and an applied `project.install` proposal for `agent.dataset-finder@1.0.0`; its five-turn session shows the goal, then *"Since I do not have the dataset IDs yet, I will start by delegating the discovery process"* with the install proposal in place of a plan, then the applied install, then the user re-typing the request, then an empty reply. Four causes, each read in the code at `573451df`: the dependency was undeclared (`builtin.py:285`), the orchestration instruction never said to plan first (`orchestration_instruction.txt:19` pointed at an attachment that cannot exist before an apply), only the per-node apply attached an agent (`services.py:1910` vs `_apply_dataflow_plan`), and nothing initiated discovery when a data-loading node was resolved (`services.py:6296`, whose remedy sentence existed in three copies telling the USER to attach the Dataset Finder by hand).
- Design decisions/artifacts: memo `dev/126`; **`DEC-080`** (repair where the user acts on the dependent; the node-attached source resolution). Consumes DEC-068, DEC-072, DEC-063, DEC-006, DEC-047, DEC-040, DEC-053, REQ-ORCH-001.
- Design-to-code decisions or deviations: **(1)** the plan-node agent list holds NO data-loading predicate of its own — which of the two agents attaches to a given node is the manifests' `compatibleTargets[].requires`, read through one predicate extracted from `attach_agent`, so the drawer and the automatic attach cannot disagree. **(2)** *Deviation from the memo's §D*: discovery is initiated in TWO stages, not one. The memo's single pre-round check would have fired on every project with a non-empty Data Catalog — which is every shipped install — so the catalog grounds a FIRST attempt (the child is handed its rows and the DEC-072 gate enforces them) and the round's own refusal or decline is what initiates discovery when it does not fit. The strict reading is one predicate away and is recorded as F3. **(3)** grounding evidence is ranked (node-specific beats a pending card; project-wide loses to one), which the memo did not state. **(4)** "Awaiting your selection" is said ONLY when something is selectable: discovery that finds nothing leaves the node its own ungrounded-source/source-missing outcome with the discovery attempt recorded — this preserved the two harness tests whose contract is that a fabricated dataset id never scores as a success. **(5)** the Data Catalog rows are listed at the Solve entry point, not inside the job — dev/123's field finding (production paths assume a request context) applied without relearning it. **(6)** the memo's `mark_skipped` wrote `status: resolved`, which would have suppressed the post-failure stage forever; it writes an informational `not-needed` instead.
- Files/modules: new `app/agents/dataset_resolution.py` (the state machine, the selection record, the initiation) and frontend `components/agents/attach/OpenDatasetFinderAction.tsx`; edited `app/agents/{builtin,services,routes,attachments}.py`, `llm-prompts/{orchestration,node_build,discovery}_instruction*.txt`, frontend `api/agentsApi.ts`, `components/agents/attach/{AgentAttachmentsProvider,AgentBuilderStrip,AgentChatPanel,AgentDockOverlay,NodeSolveRow}.tsx`, `components/agents/content/AgentDatasetCandidatesCard.{tsx,module.css}`; docs `AGENT-CATALOG.md`, `USAGE.md`.
- Tests added/updated: 8 on the attach helper + 4 on the shared compatibility predicate; 21 on the state machine, the pick resolver's five refusals (including a smuggled URL), the selection outcomes and the delegation-home table; 10 route tests end-to-end (a loader the project cannot ground ends pending with candidates in its own Dataset Finder chat and the attempts kept; a second Solve attempts nothing; a goal naming a path skips discovery and records why; discovery that finds nothing keeps the node's failure; a recorded selection resolves the node with `confirmedSource` handed over; the endpoint's five cases); the regression test named for session `7c300d0d` (no install proposal for a required agent, the disclosure turn present) and its complement (a preferred delegate still reaching the reviewed lane); 6 jest tests. Six existing tests were re-framed with their reasons in the commit messages — three legacy-lockfile Solve tests plus one apply test now use a blocker standing in for the one state the repair cannot fix, and two found their proposal at the NODE's agent because dev/73's rule finally has one.
- Verification evidence: `tests/test_agents` 2236 passed (2189 before); `tests/test_projects` + `tests/test_datasets` 769 passed; jest 2390 across 205 suites; `tsc --noEmit` clean.
- Commits (on `imp/agentcatalog`): `32235ebd` (memo), `3506446e` (1, shared logic), `650536d8` (2, `DEC-080` + the declaration), `e9a6c2ac` (3, both apply paths), `943ad114` (4, `dataset_resolution.py`), `3599b35c` (5, resolution initiates discovery), `4a30196f` (6, the endpoint + the surfaces), `68fa2fd9` (7, the three instructions + the moved byte pin), `1156e62b` (8, docs + ledgers, this entry included) and `6b6d27f9` (9, the ledgers repointed after the trailer strip). Renumbered 2026-09-10 at the owner's request: every commit of dev/125 and dev/126 was rewritten message-only to drop the `Co-Authored-By` trailer, trees byte-identical, so an earlier note quoting `a6437932`…`e3ad9192` names these same commits.
- Issues/regressions discovered en route: the six deviations above. The load-bearing one is (2): a rule written as "always, unless the node's source is already grounded" means something very different once you notice that every install ships a non-empty Data Catalog — the answer was to keep both stages rather than to weaken either.
- Quality/evaluation boundary (tracking rule 17): no agent gained authority. Every install is either a click or a DECLARED closure of one; no model chooses an install; the selection endpoint accepts identifiers the runtime itself proposed and nothing else; a node that cannot be resolved now WAITS instead of failing, which removes an outcome rather than adding a power. The evaluation harness is untouched and its two fabrication tests still hold.
- Follow-up work: `dev/126` F1 (`node.create`/`node.insert` attach parity), F2 (routing the Dataset Finder's hand-off prompt to the node's own Node Builder — `suggestedPrompts` carries no target), F3 (the strict "always initiate" variant, one predicate), F4 (the owner's live re-test in `667a4b6c`), F5 (whether a preferred delegate should ever be repaired — it must not, without a new decision).

## BL-P5-20260910-61: when Solve cannot fix a node — the whole attempt trail in the transcript, an error named precisely, a real retry budget, and the inputs' columns handed over (memo dev/127); no new DEC

- Status: implemented on `imp/agentcatalog` 2026-09-10. Origin: the owner's screenshot of dataflow `623b6620` — *"The agent is still unable to correctly solve the data flow. It displayed an error, but this diagnosis is too vague. It should also show the code it attempted to execute alongside the error message. Additionally, the agent should have more retry attempts before reaching the 5-minute timeout."* — plus *"It is important to clearly display all attempts to fix in the chat transcript."*
- Requirements: read from disk before writing a line. That project is a dev/126 project (seven plan nodes, a Node Builder on each, a Dataset Finder on both loaders, the applied card reading "agents attached: Dataset Finder ×2 · Node Builder ×7"), so dev/126 works in the field and what failed is the repair loop. Five defects in five card lines: three fixed-width slices of raw tracebacks (`raw[-200:]`, `[-160:]`, `[-100:]`) that produced `das/core/generic.py`, `ic.py", line 1776` and `— de`; an attempt row carrying `contentSha256` and never the code, on a card shape whose lines are capped and could not carry it; a hard `_VALIDATE_CORRECTION_ROUNDS = 2` spent in 25 s against a 300 s sandbox timeout and a 45-minute batch deadline; one cause reported as three unrelated failures with only the last quoted; and — the reason the rounds were wasted — `_upstream_outputs_for` handing a child a TYPE and nothing else, and only for upstreams in `wave_outputs`, which excludes the `merge-flow` that fed the failing node, so it received an EMPTY list and guessed a join key three times.
- Design decisions/artifacts: memo `dev/127`; **no new `DEC`** — this is `DEC-063` applied for the eighth time (an input the agent needs must be supplied on the path it runs on), inside `DEC-073`'s loop, under `DEC-075`'s honesty rules. Consumes DEC-072, DEC-006, DEC-040, REQ-SEC-002.
- Design-to-code decisions or deviations: **(1)** the memo promised an unconnected merge slot would be STATED (`arg[1]: not connected`); it is not — a merge's expected arity is not declared per node, so a gap is invisible to the walk. Recorded as a follow-up rather than faked. **(2)** the memo said a repeat stops the loop immediately; implemented as dev/116 intended plus a limit — the FIRST repeat is reported to the model, the SECOND ends the loop. **(3)** the part's rows carry no `selfRaised` flag: the sentence "raised by the code's own check, not by the library" is part of the error text, so there is no second field to keep in sync. **(4)** frames render as `path:line in func`, not in Python's own quoting, which is what a slice through a traceback leaves behind. **(5)** the suite pins `CURIO_SOLVE_CORRECTION_ROUNDS=2` in an autouse fixture: every pre-dev/127 test scripts a fixed number of provider replies and asserts on three attempts, and raising the default made one of them PASS on a reply nobody wrote. **(6)** the artifact preview is bounded twice — the sandbox's `maxRows` and the backend's 512 KB streaming cap — because `load_artifact_as_dict`'s own comment records a MemoryError from an unbounded read; an oversized preview is reported as no schema.
- Files/modules: new `app/agents/failure_text.py`, `app/agents/upstream_schema.py`, frontend `components/agents/content/AgentSolveAttemptsCard.{tsx,module.css}`; edited `app/agents/{services,content}.py`, `app/execution/runner.py` (`load_artifact_preview`), frontend `api/agentsApi.ts`, `components/agents/attach/{AgentChatPanel,NodeSolveRow}.tsx`, `components/agents/content/AgentReviewCard.tsx`, `tests/test_agents/conftest.py`; docs `AGENT-CATALOG.md`, `USAGE.md`.
- Tests added/updated: 20 on the failure text, built on the owner's three real tracebacks (the partial-line test is the one that would have caught `— de`); 6 on the attempt trail (code recorded per failed round, no copy on a pass, each bound named, a second repeat stopping the loop with the sandbox called once); 6 on the budget (the shipped default, env parsing and fallback, six attempts when the clock allows, a 100 s budget with 40 s rounds stopping the fourth, a clock that jumps mid-round still recording that round, a 500-round env clamped); 13 on the schema and the walk (the report's own two frames, a merge summarized in order, every bound, the walk where dev/118 returned nothing, argIndex, a schema_fn that raises, the depth bound, and the artifact never leaking into a row); 6 on the bounded preview; 4 route tests for the part (durable across a reload, none for a solved node, one for an awaiting-source node); 9 jest tests for the card and the panel.
- Verification evidence: `tests/test_agents` + `tests/test_execution` 2336 passed (2272 before this phase's commit 4, 2236 before the phase); `tests/test_projects` 304; jest 2400 across 206 suites; `tsc --noEmit` clean.
- Commits (on `imp/agentcatalog`): `7d26f591` (memo), `a7817740` (1, one reading of a failed round), `62acd01c` (2, the code and the bound), `3e0b387e` (3, the budget), `e7a7f847` (4, the transcript part), `3f36f3bc` (5, the inputs' columns), `4b836c0b` (6, the frontend), and this tracking commit (7, docs).
- Issues/regressions discovered en route: the six deviations above. The load-bearing discovery is (D5): the owner asked for more retries, and more retries alone would have bought more guesses — the loop was missing the one input that makes a correction possible. The second-order one is (5): a test suite that pins a budget is admitting the old assertions were written against a cap, which is exactly the confusion the failure sentence was creating for the user.
- Quality/evaluation boundary (tracking rule 17): no agent gained authority. Nothing new is written to a spec, no review gate moved, and the new part is runtime-minted and unparseable from a model tail, so a model cannot fabricate a trail. The schema is READ-ONLY and bounded; the artifact itself never enters a prompt. The evaluation harness is untouched.
- Follow-up work: `dev/127` F1 (per-agent budgets instead of env-only), F2 (retiring the preamble's `arg`-is-a-list paragraph now that the runtime states it per node), F3 (the `data-transformation` node's `gdf.metadata` pattern), F4 (whether a passing round's code is worth recording for a diff), F5 (the owner's live re-run of `623b6620`), and the new F6 (an unconnected merge slot cannot be named until arity is declared).

## BL-P5-20260910-63: `arg` from a merge is a list — one slot authority, one contract, one refusal before the sandbox (memo dev/128); no new DEC

- Status: implemented on `imp/agentcatalog` 2026-09-10. Origin: two owner reports the same day. First: *"The merge node always outputs a list called `arg`, where each item in this list corresponds to the linked nodes in the order of their connections to the input handles of the merge node. Your attempts always used `arg` alone; when I changed it to `arg[0]`, it worked correctly."* Second, mid-phase: *"I attempted to recreate the same dataflow, but it generated a node with an error. This must never happen; the dataflow must be completely validated before inserting into a node."* Plus a budget instruction: *"also change the fix attempts to 10 and 15 mins at max"*.
- Requirements: read from disk. Report one — dataflow `623b6620`, node `08b108a1` (fed by a two-input merge) now begins `gdf = arg[0]`, the owner's edit, and its runtime journal records that version running clean (ok, geodataframe, 101 ms). Report two — dataflow `00708324`: the Solve card says `ed1a326f · solved · pass after 1 round` while the runtime journal for the same node says `status: error`, `validation: false`, `KeyError: 'tract_id'`, and the generated code's own comment says *"arg[0] is the Population Data / arg[1] is the Chicago Boundaries"* against a spec whose edges read Boundaries → `in_0`, Population → `in_1`.
- Design decisions/artifacts: memo `dev/128`; **no new `DEC`** — `DEC-063`'s ninth application, in the `DEC-072` gate's shape. Consumes DEC-067 (a refusal is a free correction round), DEC-073, DEC-075, dev/67-3 (explicit handles), dev/127 (the trail, the budget, the schemas).
- Design-to-code decisions or deviations: **(1)** the order had to be fixed BEFORE the contract: both spec parsers dropped `targetHandle`, the merge sort read `in_N` from the edge *id*, and an agent-applied edge has a UUID id — so validation ordered a plan-created merge lexicographically while Play ordered it by handle (`mergeFlowUtils`). Enforcing a contract built on that order would have made the runtime confidently wrong. `merge_slot_index` reads the handle first with the id suffix as the canvas-legacy fallback, and dev/127's `argIndex` is corrected by the same change. **(2)** slots name `upstreamNodeId`/`upstreamNodeType`: the child's own inputs already carry `nodeId`/`nodeType` for the node being generated, and the collision broke two suites as soon as the contract rode a prompt (a dev/126 harness matched the wrong node's frames; a dev/115 fixture mis-routed a request so a node "solved" without its code running). **(3)** the budget instruction is stated in ATTEMPTS (`CURIO_SOLVE_MAX_ATTEMPTS`, default 10) rather than corrections-after-the-first, with `CURIO_SOLVE_NODE_BUDGET` at 900 s and a ceiling of 20; dev/127's §12 records the supersession.
- Files/modules: new `app/agents/input_contract.py`; edited `app/execution/workflow_spec.py` (`merge_slot_index` + both edge projections), `app/agents/services.py` (the contract composed, handed to every round, and the gate beside the source gate; the widened budget), `llm-prompts/new_content_prompt.txt`, `tests/test_agents/conftest.py`; docs `AGENT-CATALOG.md`, `USAGE.md`; frontend copy for the `rounds` bound.
- Tests added/updated: 6 on the slot authority (the owner's UUID edges whose lexicographic order INVERTS their handles; the canvas legacy encoding; a handle contradicting the id; the handle surviving both projections; the reader's seven cases; and the runner's own assembly delivering slot order through a fake sandbox); 21 on the contract (shape for a multi-input merge, a one-input merge, a code upstream, no upstream, a pool in between, `describe`; schema enrichment and its absence; the owner's code refused with the slots and columns named; seven legitimate list uses accepted; a single shape judging nothing; a rebound name; a name bound to a slot; a syntax error; code without `arg`; and four loop tests — refused without reaching the sandbox, the correction told exactly what was wrong, the contract on every round, a single-upstream node never gated, a loader given no contract); 6 on the widened budget.
- Verification evidence: `tests/test_agents` + `tests/test_execution` 2363 passed (2336 before); `tests/test_projects` 304; jest 2400 across 206 suites; `tsc --noEmit` clean.
- Commits (on `imp/agentcatalog`): `b6835da6` (memo), `4e9dd45c` (1, the owner's budget), `35a1e46a` (2, the slot authority), `d717ba31` (3, `input_contract.py`), `fef64abb` (4, handed over and enforced), and this tracking commit (5, docs).
- Issues/regressions discovered en route: the three deviations above. The load-bearing one is (1): the owner asked why the agent wrote `arg` instead of `arg[0]`, and the answer under it was that the runtime itself did not know which slot was which — dev/127 had handed the model an authoritative-looking mapping that was inverted, and the model repeated it in a comment. A contract is only worth enforcing when the fact behind it is right.
- Quality/evaluation boundary (tracking rule 17): no agent authority changed. The new refusal is a REFUSAL — the runtime never edits a candidate; nothing new is written to a spec; the contract is read-only graph fact plus dev/127's bounded schemas. The evaluation harness is untouched.
- Follow-up work: `dev/128` F1 (a code upstream returning a tuple also makes `arg` a list the runtime cannot see statically — teach it from the recorded artifact's `outputs` dataType once the upstream has run), F2 (a JavaScript candidate is not gated — Python AST only), F3 (dev/127 F6, an unconnected merge slot, still open and visible here as a shorter `length`), F4 (owner live re-run of both dataflows).
