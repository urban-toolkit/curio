"""Presentation-layer schemas for the dataset catalog HTTP surface.

* :mod:`catalog_item` — the ``CatalogItem`` TypedDict documenting the catalog
  row contract produced by ``domain.catalog_item.base_item``;
* :mod:`requests` — parsers that turn raw request params into normalized
  service inputs (live-output refs).
"""
