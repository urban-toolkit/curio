"""Dataset catalog application service layer.

The catalog service facade and its mixins live here. External callers should
import the public API from ``utk_curio.backend.app.datasets.service`` (the single
facade), which re-exports the implementations in this subpackage.
"""
