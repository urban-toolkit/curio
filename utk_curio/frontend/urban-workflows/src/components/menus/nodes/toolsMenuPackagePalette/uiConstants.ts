/** Tooltip placement shared by built-in tools and package-kind rows in the dock. */
export type ToolsMenuTooltipSide = "left" | "right";

export const OVERLAY_TRIGGER_DELAY_PROPS = {
    show: 120,
    hide: 10,
};
